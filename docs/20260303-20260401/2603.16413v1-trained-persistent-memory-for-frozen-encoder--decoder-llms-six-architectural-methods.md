---
title: "Trained Persistent Memory for Frozen Encoder--Decoder LLMs: Six Architectural Methods"
title_zh: 针对冻结编码器-解码器大语言模型的训练式持久内存：六种架构方法
authors: Hong Jeong
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16413v1"
tags: ["query:parametric-memory"]
score: 9.0
evidence: 大模型连续潜空间中的持久记忆
tldr: 本研究针对冻结的编码器-解码器大语言模型缺乏状态持久性的问题，提出了一种在连续潜空间中实现持久化存储的方法。通过在Flan-T5-XL模型中引入小型可训练适配器，研究了六种不同的架构方法。该系统允许在推理阶段无需梯度即可积累信息，实现对话式学习。实验证明，在增加存储容量后，模型能有效召回历史信息，为构建大规模可扩展的神经存储系统奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.16413v1/fig-001.webp\", \"caption\": \"Table 3: Memory recall rate (%) by lag bucket on LoCoMo (smoothed, non-increasing isotonic fit) at two capacity scales. The baseline is identically zero by construction. n gives the number of QA pairs per bucket. Higher→ stronger memory retention.\", \"page\": 13, \"index\": 1, \"width\": 552, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2603.16413v1/fig-002.webp\", \"caption\": \"Figure 9: Forgetting curves on LoCoMo at two memory-capacity scales. Each point is the memory recall rate from Eq. (40), smoothed by a weighted non-increasing isotonic fit. Left (1×): three methods (M.1, M.3, M.5) collapse; M.2 XAttn and M.6 Slot dominate. Right (10×): all six methods produce non-trivial curves; M.4 Hebbian is strongest at long lag. Higher and flatter curves indicate stronger memory.\", \"page\": 12, \"index\": 2, \"width\": 494, \"height\": 780}]"
motivation: 旨在解决冻结的编码器-解码器模型在推理后会丢弃潜变量表示、无法跨会话保留信息的局限性。
method: 提出了六种基于可训练适配器的架构方法，通过在连续潜空间中进行可微的读写操作来实现持久化内存。
result: 在LoCoMo遗忘曲线评估中，所有六种方法在10倍容量下均表现出正向的记忆召回能力，而基准模型得分归零。
conclusion: 该研究验证了在冻结LLM中实现持久化潜空间存储的可行性，并指出存储容量是影响性能的关键设计参数。
---

## 摘要
冻结的编码器-解码器语言模型是无状态的：其潜在表示在每次前向传播后都会被丢弃，因此信息无法在会话之间持久存在。本文提出了一项概念验证性初步研究，证明在冻结大语言模型（LLM）的连续潜在空间中实现持久内存是可行的——即使在严苛的资源限制下（单个冻结的 Flan-T5-XL 骨干网络、小型可训练适配器、单一数据集）。我们实现了涵盖三个注入点和四种写入机制的六种架构方法；与文本级内存系统不同，其每一次写入和读取都是针对稠密向量的可微操作。在仅对适配器进行训练后，内存库在推理阶段无需梯度即可持续累积，从而实现了对话式学习。在 LoCoMo 数据集上进行的两种容量规模（1倍和10倍）遗忘曲线评估中，无状态基准模型的得分恰好为零；在10倍规模下，所有六种训练后的适配器均产生了正向的记忆召回曲线；在1倍规模下，有三种方法失效，这表明容量是一个关键的设计参数。由于内存库是一个紧凑的数值数组，因此可以在不改变骨干网络的情况下扩展到任意大的容量。我们认为，使用更大的模型、更多的数据以及数量级更大的内存进行全端到端训练将产生显著更强的结果；本初步研究为这类尝试奠定了可行性基准和所需的设计空间分类学。

## Abstract
Frozen encoder--decoder language models are stateless: the latent representation is discarded after every forward pass, so no information persists across sessions. This paper presents a \textbf{proof-of-concept pilot study} showing that persistent memory in the \emph{continuous latent space} of a frozen LLM is feasible -- even under severe resource constraints (a single frozen Flan-T5-XL backbone, small trainable adapters, a single dataset). We implement six architectural methods spanning three injection points and four write mechanisms; unlike text-level memory systems, every write and read is a differentiable operation on dense vectors. After training only the adapter, the memory bank continues to accumulate at inference time without gradients, enabling \emph{conversational learning}. Under a forgetting-curve evaluation on LoCoMo at two capacity scales (1$\times$ and 10$\times$), the stateless baseline scores exactly zero; at 10$\times$ all six trained adapters produce positive memory-recall curves; at 1$\times$ three methods collapse, revealing capacity as a critical design parameter. Because the memory bank is a compact numerical array, it can be scaled to arbitrarily large capacity without altering the backbone. We argue that full end-to-end training with larger models, larger data, and orders-of-magnitude larger memory will yield substantially stronger results; this pilot study establishes the feasibility baseline and design-space taxonomy that such efforts require.

---

## 论文详细总结（自动生成）

这是一份关于论文《Trained Persistent Memory for Frozen Encoder–Decoder LLMs: Six Architectural Methods》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：预训练的编码器-解码器（Encoder-Decoder）大语言模型（如 T5）本质上是**无状态的**。在前向传播结束后，中间的潜空间表示（Latent Representation）会被丢弃，导致模型无法跨会话保留信息（例如，无法记住用户在几轮对话前提到过的个人偏好）。
*   **研究动机**：现有的长期记忆方案（如 MemGPT）多在**文本层面**操作（存储、检索自然语言字符串），而本研究旨在探索在**潜空间层面**实现持久化内存。通过在冻结的模型中加入微型可训练适配器，使模型能够直接在连续向量空间中进行可微的读写操作，从而实现“对话式学习”。

### 2. 方法论：核心思想与技术细节
论文提出了六种架构方法，通过在三个不同的注入点和四种写入机制中进行组合，构建了持久化内存库 $P_t$：

*   **核心思想**：保持骨干模型（Flan-T5-XL）完全冻结，仅训练一个微小的适配器 $\theta_{Mem}$。内存库 $P_t$ 在推理阶段通过非梯度更新（Write）持续积累信息，并通过读取（Read）操作注入到解码器中。
*   **六种架构方法**：
    1.  **M.1 前缀法 (Prefix)**：将内存压缩为 soft tokens，作为编码器输入的前缀。
    2.  **M.2 并行交叉注意力 (XAttn)**：在解码器层中插入并行的注意力层来读取内存。
    3.  **M.3 KV 扩展 (KV Extension)**：将内存投影为额外的 Key-Value 对，拼接在解码器交叉注意力中。
    4.  **M.4 赫布/结合记忆 (Hebbian)**：使用外积（Outer-product）写入规则积累关联矩阵，模拟语义记忆。
    5.  **M.5 上下文门控 (Gated)**：在解码器分支中使用内容相关的门控机制控制内存流入。
    6.  **M.6 槽位存储 (Slot)**：采用类似神经图灵机的稀疏槽位寻址写入机制。
*   **学习阶段**：
    *   **Type 1 (监督学习)**：离线训练适配器参数，学习如何读写。
    *   **Type 2 (对话式学习)**：推理时适配器冻结，但内存库 $P_t$ 随对话实时更新。

### 3. 实验设计与 Benchmark
*   **数据集**：主要使用 **LoCoMo**（长对话记忆基准），该数据集包含显式的问答监督和证据轮次标注。
*   **评估指标**：引入了**“遗忘曲线”（Forgetting Curve）**。通过计算“内存召回率”（Memory Recall Rate），衡量内存对 F1 分数提升的贡献（归一化到 0-100% 尺度）。
*   **对比基准**：
    *   **Stateless Baseline**：完全无状态的原始模型（得分恒为 0）。
    *   **容量对比**：测试了 1×（64 槽位）和 10×（640 槽位）两种内存容量规模。
*   **原则**：所有方法仅接收当前轮次的输入 $x_t$，不提供历史文本，强制模型依赖持久内存 $P$。

### 4. 资源与算力
*   **硬件**：使用了**单张 NVIDIA GPU**（具体型号未指明，但提到在单卡上完成）。
*   **模型规模**：骨干网络为 **Flan-T5-XL (3B 参数)**，采用 bfloat16 精度。
*   **训练细节**：使用 AdamW 优化器，训练 10 个 epoch，有效 Batch Size 为 16。适配器参数量极小（约 1M 到 21M，仅占骨干模型的 0.1% - 0.6%）。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 种方法在 2 种容量下进行了完整的遗忘曲线测试，并进行了“累积知识曲线”分析和“适配器干扰（Adapter Tax）”分析。
*   **充分性评价**：作为一项**初步研究（Pilot Study）**，实验设计非常严谨且具有针对性。它通过控制变量（相同的骨干、相同的输入）公平地对比了不同架构的优劣。虽然只使用了一个主要数据集，但通过对证据滞后（Lag）的分桶分析，深入揭示了内存失效的动态过程。

### 6. 主要结论与发现
*   **可行性验证**：证明了在冻结 LLM 的潜空间中安装持久内存是可行的。
*   **容量是关键**：在 1× 容量下，三种方法（M.1, M.3, M.5）发生崩溃；但在 10× 容量下，所有六种方法均表现出正向的记忆能力。
*   **表现最优者**：在低容量下，**M.2 (XAttn)** 和 **M.6 (Slot)** 表现最好；在高容量下，**M.4 (Hebbian)** 在长期记忆保持上领先。
*   **知识累积**：最强的方法在 30 个会话中表现出稳定的知识增长（$\Delta K$ 达 9.7%）。

### 7. 优点与亮点
*   **架构分类学**：系统性地定义了潜空间内存的注入点和写入机制，为后续研究提供了清晰的路线图。
*   **评估创新**：提出的“头顶空间归一化遗忘曲线”比传统的单一 F1 指标更能客观反映内存系统的效能。
*   **高效性**：内存库是紧凑的数值数组，扩展容量不会增加骨干网络的推理计算成本。

### 8. 不足与局限
*   **绝对性能有限**：由于骨干网络 100% 冻结且适配器极小，绝对召回率（约 12%）仍有提升空间。
*   **模型局限**：实验仅限于编码器-解码器架构，尚未在目前主流的 Decoder-only（如 Llama）模型上验证。
*   **任务单一**：主要集中在对话 QA 任务，未测试更复杂的推理或长文本生成任务。
*   **规模限制**：作为 Pilot Study，未进行大规模端到端训练（Unfreezing backbone），这限制了模型学习更复杂存储策略的能力。

（完）
