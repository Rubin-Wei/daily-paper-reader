---
title: "Adaptive Loops and Memory in Transformers: Think Harder or Know More?"
title_zh: Transformer 中的自适应循环与存储：思考更深还是知识更多？
authors: "Markus Frey, Behzad Shomali, Ali Hamza Bashir, David Berghaus, Joachim Koehler, Mehdi Ali"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08391v3"
tags: ["query:pm"]
score: 9.0
evidence: Transformer中用于存储容量的门控记忆库与权重
tldr: 本研究探讨了Transformer模型中自适应循环与记忆库的结合。针对循环模型存储容量不足的问题，作者提出了带有学习停止机制的逐层自适应循环和门控记忆库。实验表明，循环机制显著提升了数学推理能力，而记忆库则增强了常识任务表现。该组合模型在相同计算量下优于更深层的基准模型，并展现出层级化的功能分工，为提升模型推理效率提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08391v3/fig-001.webp\", \"caption\": \"Table 3: Full results at the final checkpoint. Best result within each group is bolded. Base = standard transformer; LN = Loop-N ; L3IF = iso-FLOP for Loop-3; M-BIP = memory iso-parameter baseline; Mg0 = memory model with gate init g0; M-BIF = memory iso-FLOP baseline.\", \"page\": 11, \"index\": 1, \"width\": 706, \"height\": 708}, {\"url\": \"assets/figures/arxiv/2603.08391v3/fig-002.webp\", \"caption\": \"Figure 1: Architecture overview. Left: A standard transformer passes hidden states through L unique blocks. Center: Our loop model allows each block to iterate up to N times, with a learned halting mechanism that produces a weighted combination of intermediate states. Per-step scales ζ(sn) are initialized near zero for training stability. Right: The combined model additionally retrieves from local (per-layer) and global (shared) memory banks, gated by learned input-dependent scalars.\", \"page\": 2, \"index\": 2, \"width\": 794, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.08391v3/fig-003.webp\", \"caption\": \"Figure 4: Memory gate activations across layers and training. Left: Local memory gate values show high variance across layers while later layers tend to have higher gate activations, and the spread increases over training. Right: Global memory gate values increase during training but converge to a more uniform profile across layers, with activations rising up to approximately layer 5 and then plateauing.\", \"page\": 10, \"index\": 3, \"width\": 821, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2603.08391v3/fig-004.webp\", \"caption\": \"Table 2: Model configurations. All models use the same base transformer architecture. Loop parameters include per-step scales and halting router weights. Memory parameters include local/global key-value banks and gating networks. Iso-parameter baselines add extra FFN capacity to match the corresponding model’s parameter count. Iso-FLOP baselines use 36 layers to approximate the forward-pass cost of 3-loop models.\", \"page\": 10, \"index\": 4, \"width\": 727, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2603.08391v3/fig-005.webp\", \"caption\": \"Table 5: Results at the mid checkpoint (step 20000).\", \"page\": 13, \"index\": 5, \"width\": 706, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2603.08391v3/fig-006.webp\", \"caption\": \"Table 1: Summary of results, averaged across benchmarks within each group. CS = commonsense; BPB = bits per byte (lower is better, see Appendix A.2 for details). Best result per column within each model group is bolded. Full per-benchmark breakdowns are in Appendix A.4.\", \"page\": 4, \"index\": 6, \"width\": 656, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2603.08391v3/fig-007.webp\", \"caption\": \"Table 4: Results at the early checkpoint (step 5000).\", \"page\": 12, \"index\": 7, \"width\": 706, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2603.08391v3/fig-008.webp\", \"caption\": \"Figure 3: Expected loop iterations vs. validation cross-entropy for all configurations. Each point represents one evaluation during training; curves are colored by model configuration. Across all looped models, the expected number of iterations begins to increase rapidly once the cross-entropy drops below approximately 3.27± 0.59. This phase transition is consistent across Loop-3, Loop-5, and Loop-7 configurations, suggesting it depends on the model’s language competence rather than the maximum number of allowed iterations.\", \"page\": 9, \"index\": 8, \"width\": 818, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2603.08391v3/fig-009.webp\", \"caption\": \"Figure 2: Expected number of loop iterations per layer over training. Left: Each curve represents one layer. Early layers (lighter colors) consistently use fewer iterations than later layers (darker colors). Middle: Expected steps at the end of training. Right: All models show a characteristic transition which occurs at approximately the same cross-entropy value across configurations (see Figure 3 in the Appendix for all configurations).\", \"page\": 5, \"index\": 9, \"width\": 817, \"height\": 244}]"
motivation: 循环Transformer虽然参数效率高，但相比深层模型缺乏存储容量，难以在推理深度和知识存储之间取得平衡。
method: 提出一种结合了逐层自适应循环（通过学习停止机制迭代隐藏状态）和门控记忆库（提供额外学习存储）的新型架构。
result: 循环机制主要增强数学推理，记忆库则改善常识任务，两者结合在同等计算量下性能优于层数多三倍的基准模型。
conclusion: 自适应循环与记忆库的结合能有效平衡模型的计算深度与存储能力，通过层级分工实现更高效的推理与知识处理。
---

## 摘要
思维链（CoT）提示使语言模型能够进行推理，但需要将中间步骤显式地表达出来。循环 Transformer 提供了一种替代方案，通过在隐藏状态内迭代优化表示。这种参数效率是有代价的，因为循环模型缺乏深层模型（每层使用独立权重）所具备的存储容量。在这项工作中，我们研究了同时具备自适应逐层循环（每个 Transformer 块通过学习到的停机机制来学习迭代其隐藏状态）和门控存储库（提供额外的学习存储空间）的 Transformer 模型。我们发现，与参数量和 FLOPs 匹配的模型相比，循环主要有利于数学推理，而存储库则有助于恢复在常识任务上的性能。结合这两种机制所产生的模型在数学基准测试中优于层数多出三倍的等 FLOPs 基准模型。对模型内部的分析揭示了层的专业化：早期层学习进行极少的循环并谨慎地访问存储，而后期层则更频繁地进行这两项操作。

## Abstract
Chain-of-thought (CoT) prompting enables reasoning in language models but requires explicit verbalization of intermediate steps. Looped transformers offer an alternative by iteratively refining representations within hidden states. This parameter efficiency comes at a cost, as looped models lack the storage capacity of deeper models which use unique weights per layer. In this work, we investigate transformer models that feature both adaptive per-layer looping, where each transformer block learns to iterate its hidden state via a learned halting mechanism, and gated memory banks, that provide additional learned storage. We find that looping primarily benefits mathematical reasoning, while memory banks help recover performance on commonsense tasks compared to parameter and FLOP matched models. Combining both mechanisms yields a model that outperforms an iso-FLOP baseline, with three times the number of layers, across math benchmarks. Analysis of model internals reveals layer specialization: early layers learn to loop minimally and access memory sparingly, while later layers do both more heavily.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Loops and Memory in Transformers: Think Harder or Know More?》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了 Transformer 架构在**参数效率**与**存储容量**之间的权衡。
*   **背景**：循环 Transformer（Looped Transformers）通过多次迭代同一个层来增加“有效深度”，从而在不增加参数的情况下提升推理能力（隐式思维）。
*   **核心问题**：循环模型虽然擅长逻辑操纵，但由于权重共享，其存储事实性知识的容量远低于具有独立权重的深层模型。
*   **研究目标**：通过引入“自适应循环”和“门控记忆库”，探索模型是否能同时实现“思考更深”（通过循环处理复杂逻辑）和“知道更多”（通过记忆库存储知识）。

### 2. 方法论：核心思想与关键技术
作者提出了一种增强型架构，结合了以下两个核心机制：
*   **自适应逐层循环（Adaptive Looping）**：
    *   借鉴 PonderNet，为每个 Transformer 块引入一个**学习停机机制（Halting Mechanism）**。模型根据当前隐藏状态预测停止概率，最终输出是各次迭代结果的加权组合。
    *   引入**可学习的循环缩放参数**（初始化接近零），确保训练初期模型表现为标准 Transformer，随后逐渐学会何时增加迭代次数。
*   **门控记忆库（Gated Memory Banks）**：
    *   **局部记忆（Local Memory）**：每层拥有独立的静态学习参数作为 Key-Value 对，存储层特定的知识。
    *   **全局记忆（Global Memory）**：所有层共享的存储库，用于跨层访问通用信息。
    *   **门控集成**：使用输入相关的门控机制（Sigmoid 激活）来决定从记忆库中提取多少信息，避免强制使用记忆干扰简单任务。

### 3. 实验设计
*   **数据集与场景**：模型在 **FineWeb-Edu** 数据集（去重版）上预训练，消耗约 140 亿（14B）个 Token。
*   **Benchmark**：
    *   **常识任务**：ARC (Challenge/Easy), HellaSwag, PIQA, Winogrande 等（评估准确率和 BPB）。
    *   **数学任务**：Algebra, Geometry, Precalculus 等（评估 BPB，即每字节比特数，数值越低越好）。
*   **对比方法（Baselines）**：
    *   **IsoPar（等参数基准）**：通过增加 FFN 宽度使参数量与实验模型对齐。
    *   **IsoFLOP（等计算量基准）**：使用 36 层深层模型，其前向传播成本与循环 3 次的模型相当。

### 4. 资源与算力
*   **模型规模**：基础模型约 **200M 参数**（12 层，768 维度）。
*   **训练细节**：使用 AdamW 优化器，峰值学习率 0.003，Batch Size 约 360K tokens。
*   **算力说明**：论文**未明确提及具体的 GPU 型号和数量**，但提到使用了其开发的 `Modalities` PyTorch 原生框架进行大规模训练。

### 5. 实验数量与充分性
*   **实验组数**：作者对比了不同最大循环次数（$N_{max} \in \{3, 5, 7\}$）、三种不同的记忆门控初始化策略（闭合、平衡、开启），以及多种基准对照组。
*   **充分性**：实验涵盖了从训练动态分析（循环次数随训练进度的变化）到下游任务评估的多个维度。通过等参数和等 FLOPs 的双重对比，确保了结论的客观性，排除了“性能提升仅来自参数增加”的干扰。

### 6. 主要结论与发现
*   **功能分化**：**循环机制**显著提升了数学推理（算法化计算），而**记忆库**则有效弥补了常识任务中的性能损失。
*   **超越深层模型**：在数学基准测试中，结合了循环和记忆的模型优于层数多出三倍的等 FLOPs 深层模型。
*   **层级专业化**：模型内部出现了自发的专业化分工——**早期层**倾向于极少循环且少用记忆；**后期层**则大量循环并频繁访问记忆。
*   **相位转变**：模型并非从一开始就使用循环，而是在验证集交叉熵降至约 3.27 左右（即具备一定语言能力后）才开始爆发式地利用循环机制。

### 7. 优点
*   **架构创新**：巧妙地将“动态计算深度”与“静态参数存储”结合，解决了循环 Transformer 的固有缺陷。
*   **效率极高**：证明了在特定任务（如数学）上，通过循环复用参数比单纯堆叠层数更有效。
*   **深入的内部透视**：通过对停机概率和门控值的可视化，揭示了模型如何自发学习“思考”与“检索”的策略。

### 8. 不足与局限
*   **规模限制**：实验主要在 200M 参数的小规模模型上完成，该结论是否能推广到百亿（10B+）参数量级尚不确定。
*   **评估指标偏差**：数学任务主要使用 BPB 指标而非最终答案的准确率，这可能无法完全反映模型解决复杂多步问题的真实成功率。
*   **效率权衡不完整**：虽然对比了等 FLOPs 基准，但未提供关于增加循环/记忆与增加宽度/深度在连续计算预算下的完整帕累托前沿分析。

（完）
