---
title: Pretraining and Benchmarking Modern Encoders for Latvian
title_zh: 拉脱维亚语现代编码器的预训练与基准测试
authors: Arturs Znotins
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15005v1"
tags: ["query:pretrain"]
score: 9.0
evidence: 预训练拉脱维亚语专用编码器，包括长上下文变体
tldr: 针对拉脱维亚语这一低资源语言，本研究预训练了一系列基于RoBERTa、DeBERTaV3和ModernBERT架构的现代编码器模型。通过在多样化的语言基准测试中进行评估，研究证明了这些模型在性能上优于现有的多语言和单语言基准，特别是lv-deberta-base模型表现最为出色。该研究填补了拉脱维亚语NLP资源的空白，并开源了所有模型和评估资源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.15005v1/fig-001.webp\", \"caption\": \"Table 2: Model size (millions of parameters) and vocabulary size (thousands).\", \"page\": 5, \"index\": 1, \"width\": 375, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2603.15005v1/fig-002.webp\", \"caption\": \"Table 1: Overview of curated Latvian text corpora. Word counts are in millions.\", \"page\": 3, \"index\": 2, \"width\": 854, \"height\": 465}, {\"url\": \"assets/figures/arxiv/2603.15005v1/fig-003.webp\", \"caption\": \"Table 3: Results for sentiment classification (LTEC), linguistic acceptability (ScaLA), named entity recognition (FSNER), question answering (WikiQA), and commonsense reasoning (COPA). MCC denotes Matthews correlation coefficient; MF1 macro-F1; mF1 micro-F1; and EM exact match. † indicates scores excluding the MISC class. ⋄ marks few-shot prompting for LLMs, while all other LLM results are zero-shot. The best overall result in each column is highlighted in bold, and the best result within each model group is underlined.\", \"page\": 6, \"index\": 3, \"width\": 923, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2603.15005v1/fig-004.webp\", \"caption\": \"Table 4: Universal Dependencies results on the Latvian UD treebank (UD v2.16) under joint multi-task fine-tuning. Evaluation is performed using the standard CoNLL-U scoring script.\", \"page\": 7, \"index\": 4, \"width\": 937, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2603.15005v1/fig-005.webp\", \"caption\": \"Table 5: WSD results. Match Acc. denotes binary accuracy for context–sense pairs, and Sense Acc. denotes top-1 sense selection accuracy.\", \"page\": 7, \"index\": 5, \"width\": 454, \"height\": 519}]"
motivation: 拉脱维亚语在现有预训练语料中代表性不足，且缺乏基于现代架构的高性能单语言编码器。
method: 采用RoBERTa、DeBERTaV3和ModernBERT架构，预训练了一系列包含长文本支持的拉脱维亚语专用编码器。
result: lv-deberta-base模型以1.11亿参数实现了最强性能，超越了更大规模的多语言基准和先前的单语言模型。
conclusion: 本研究发布的模型和评估资源显著提升了拉脱维亚语NLP的处理能力，为相关研究和应用提供了重要支持。
---

## 摘要
仅编码器（Encoder-only）Transformer 在实际 NLP 任务中仍然至关重要。尽管多语言模型的最新进展提升了跨语言能力，但拉脱维亚语等低资源语言在预训练语料库中的代表性仍然不足，且目前现有的拉脱维亚语单语编码器较少。我们通过预训练一套基于 RoBERTa、DeBERTaV3 和 ModernBERT 架构的拉脱维亚语专用编码器（包括长上下文变体）来填补这一空白，并在多种拉脱维亚语诊断和语言基准测试中对其进行了评估。我们的模型在受益于最新架构和效率进步的同时，与现有的单语和多语言编码器相比具有竞争力。我们表现最好的模型 lv-deberta-base（1.11 亿参数）实现了最强的综合性能，优于更大规模的多语言基准模型和先前的拉脱维亚语专用编码器。我们发布了所有预训练模型和评估资源，以支持拉脱维亚语 NLP 的进一步研究和实际应用。

## Abstract
Encoder-only transformers remain essential for practical NLP tasks. While recent advances in multilingual models have improved cross-lingual capabilities, low-resource languages such as Latvian remain underrepresented in pretraining corpora, and few monolingual Latvian encoders currently exist. We address this gap by pretraining a suite of Latvian-specific encoders based on RoBERTa, DeBERTaV3, and ModernBERT architectures, including long-context variants, and evaluating them across a diverse set of Latvian diagnostic and linguistic benchmarks. Our models are competitive with existing monolingual and multilingual encoders while benefiting from recent architectural and efficiency advances. Our best model, lv-deberta-base (111M parameters), achieves the strongest overall performance, outperforming larger multilingual baselines and prior Latvian-specific encoders. We release all pretrained models and evaluation resources to support further research and practical applications in Latvian NLP.

---

## 论文详细总结（自动生成）

这篇论文对拉脱维亚语（Latvian）的自然语言处理（NLP）做出了重要贡献，通过预训练现代编码器模型并建立统一的基准测试，显著提升了该低资源语言的表示能力。以下是对论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：拉脱维亚语作为一种低资源语言，在主流多语言模型（如 mBERT, XLM-R）中因语料占比极小而导致性能被稀释（容量-覆盖权衡）。同时，现有的拉脱维亚语单语模型大多基于较旧的 BERT 架构，缺乏对现代架构（如 DeBERTaV3, ModernBERT）和长文本处理能力的支持。
*   **研究背景**：尽管当前 NLP 重点转向了生成式解码器模型（LLMs），但仅编码器（Encoder-only）模型在分类、序列标注、检索增强生成（RAG）及高吞吐量任务中仍具有不可替代的效率和性能优势。

### 2. 方法论：核心思想与技术细节
作者预训练了一系列基于三种现代架构的拉脱维亚语专用编码器：
*   **RoBERTa**：采用优化的预训练配方，使用 30% 的跨度掩码（Span-based masking）。
*   **DeBERTaV3**：核心在于**解耦注意力机制（Disentangled Attention）**和**替换令牌检测（RTD）**目标，提高了样本利用效率。
*   **ModernBERT**：侧重于高吞吐量和长上下文支持。采用了 **FlashAttention-2**、**RoPE 相对位置编码**和 **Unpadding** 技术。
    *   **训练阶段**：分为三个阶段：稳定阶段（1024 长度）、上下文扩展阶段（扩展至 8192 长度）和冷却阶段（降低学习率和掩码率）。
*   **分词器**：使用字节级 WordPiece 分词器，词表大小为 32,768。
*   **语料处理**：整合了 FineWeb2、HPLT-v2、新闻、法律、维基百科等共 **64.3 亿词（约 100B tokens）**。使用了 MinHash LSH 进行去重，并利用 KenLM 语言模型进行质量过滤。

### 3. 实验设计
研究采用了多维度的评估体系：
*   **轻量级诊断任务（EuroEval 风格）**：包括情感分类（LTEC）、语言可接受性（ScaLA）、命名实体识别（FSNER）、阅读理解（WikiQA）和常识推理（COPA）。
*   **形态句法建模（Universal Dependencies）**：在拉脱维亚语 UD 树库上进行多任务微调，评估词性标注、词干提取和依存句法分析。
*   **语义表示（词义消歧 WSD）**：作者**新发布了一个手动标注的拉脱维亚语 WSD 数据集**，基于 Latvian WordNet，采用 GlossBERT 方法进行评估。
*   **对比基准（Baselines）**：
    *   **多语言模型**：mDeBERTaV3-base, XLM-R (Base/Large), mmBERT, mBERT。
    *   **现有单语模型**：HPLT-BERT, LitLat BERT, LVBERT。
    *   **大语言模型（LLM）**：GPT-5, Gemma-3-27B（用于零样本/少样本对比）。

### 4. 资源与算力
*   **硬件**：使用单台 **DGX 机器**，配备 **8 块 NVIDIA GPU（每块 141GB 显存，推测为 H100 或 H200）**。
*   **训练量**：所有模型均在 **100B tokens** 上进行预训练，以确保公平比较。
*   **效率**：ModernBERT 凭借架构优化，训练速度比传统模型快约 2 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础语法到高级语义的 10 余项任务。
*   **公平性**：作者发现 EuroEval 默认的超参数在拉脱维亚语上不稳定，因此进行了**详尽的超参数搜索**（针对学习率和随机种子），每组实验运行 5 次取平均值，确保了对比的客观性。
*   **消融/变体**：测试了 ModernBERT 的不同规模（Mini, Base, Large）以及长短上下文的效果。

### 6. 主要结论与发现
*   **单语优势**：单语预训练模型在几乎所有任务上都显著优于参数量更大的多语言模型。
*   **最佳模型**：**`lv-deberta-base` (111M 参数)** 表现最为出色，在常识推理（COPA）和词义消歧（WSD）上取得了突破性进展，性能甚至超过了 5.6 亿参数的 XLM-R Large。
*   **ModernBERT 表现**：虽然在吞吐量和长文本上有优势，但在短文本诊断任务上略逊于 DeBERTaV3，与 RoBERTa 持平。
*   **与 LLM 对比**：在大多数 NLU 任务中，微调后的编码器优于 GPT-5 的零样本/少样本表现；但在 COPA 常识推理中，LLM 仍具有压倒性优势。

### 7. 优点与亮点
*   **填补空白**：为拉脱维亚语提供了首个支持 8192 长度的长上下文编码器。
*   **资源贡献**：不仅发布了模型，还开源了统一的评估基准和全新的 WSD 数据集，极大促进了该语言的学术研究。
*   **架构前瞻性**：引入了 ModernBERT 等最新架构，并证明了其在低资源语言上的有效性。

### 8. 不足与局限
*   **单语局限**：模型仅在拉脱维亚语上训练，缺乏跨语言迁移能力，在处理代码混用（Code-switching）场景时可能不如多语言模型。
*   **语料偏差**：预训练语料主要来自正式文本（新闻、法律），对口语、方言或非正式社交媒体语言的覆盖可能不足。
*   **评估维度**：实验主要集中在监督学习任务，未深入探讨模型在无监督检索、文本嵌入质量或特定专业领域（如医疗、金融）的鲁棒性。

（完）
