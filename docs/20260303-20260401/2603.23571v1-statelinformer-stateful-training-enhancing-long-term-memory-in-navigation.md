---
title: "StateLinFormer: Stateful Training Enhancing Long-term Memory in Navigation"
title_zh: StateLinFormer：通过有状态训练增强导航中的长期记忆
authors: "Zhiyuan Chen, Yuxuan Zhong, Fan Wang, Bo Yu, Pengtao Shao, Shaoshan Liu, Ning Ding"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23571v1"
tags: ["query:am"]
score: 9.0
evidence: 用于导航长期记忆的有状态记忆机制
tldr: 针对导航智能中长期记忆不足的问题，本文提出 StateLinFormer。该模型采用线性注意力机制，并通过有状态训练在连续训练段间保留循环记忆状态，而非在每个批次边界重新初始化。这种方法模拟了无限长序列的学习，显著提升了模型在长时程任务中的记忆保留和上下文学习能力，在 MAZE 和 ProcTHOR 环境中表现优于传统 Transformer。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.23571v1/fig-001.webp\", \"caption\": \"Fig. 2. Relative Standard Deviation (RSD) of Memory Norms: Stateful vs. Stateless Training.\", \"page\": 6, \"index\": 1, \"width\": 487, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2603.23571v1/fig-002.webp\", \"caption\": \"Fig. 3. Comparison of Success Rates between Stateful and Stateless Training across Increasing Context Lengths\", \"page\": 6, \"index\": 2, \"width\": 483, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2603.23571v1/fig-003.webp\", \"caption\": \"Fig. 1. The framework of StateLinFormer. Top: Conventional stateless training with memory reset each batch. Bottom: StateLinFormer, which maintains persistent memory across batches to support long-horizon adaptation.\", \"page\": 5, \"index\": 3, \"width\": 1002, \"height\": 535}, {\"url\": \"assets/figures/arxiv/2603.23571v1/fig-004.webp\", \"caption\": \"Fig. 4. The model structure of StateLinformer\", \"page\": 9, \"index\": 4, \"width\": 471, \"height\": 581}]"
motivation: 现有的导航模型受限于固定上下文窗口或缺乏灵活性，难以在长时间交互中维持有效的长期记忆。
method: 提出一种基于线性注意力的有状态训练机制，通过在训练段之间传递并保留记忆状态来模拟无限长序列的学习。
result: 在 MAZE 和 ProcTHOR 环境下的实验表明，StateLinFormer 在长时程记忆保留和上下文依赖适应方面显著优于基准模型。
conclusion: 有状态训练能有效增强导航模型的长期记忆和上下文学习能力，使其在复杂长序列任务中更具优势。
---

## 摘要
有效的导航智能依赖于长期记忆，以支持即时泛化和持续适应。然而，现有方法面临困境：模块化系统依赖显式建图但缺乏灵活性，而基于 Transformer 的端到端模型受限于固定上下文窗口，限制了在长时间交互中的持久记忆。我们提出了 StateLinFormer，这是一种采用有状态记忆机制训练的线性注意力导航模型，该机制在连续的训练片段中保留循环记忆状态，而不是在每个批次边界重新初始化。这种训练范式有效地近似了在无限长序列上的学习，使模型能够实现长时程记忆保留。在 MAZE 和 ProcTHOR 环境中的实验表明，StateLinFormer 的性能显著优于其无状态线性注意力对应模型以及具有固定上下文窗口的标准 Transformer 基准模型。值得注意的是，随着交互长度的增加，持久的有状态训练显著提高了上下文相关的适应能力，这表明模型在导航任务中的上下文学习（ICL）能力得到了增强。

## Abstract
Effective navigation intelligence relies on long-term memory to support both immediate generalization and sustained adaptation. However, existing approaches face a dilemma: modular systems rely on explicit mapping but lack flexibility, while Transformer-based end-to-end models are constrained by fixed context windows, limiting persistent memory across extended interactions. We introduce StateLinFormer, a linear-attention navigation model trained with a stateful memory mechanism that preserves recurrent memory states across consecutive training segments instead of reinitializing them at each batch boundary. This training paradigm effectively approximates learning on infinitely long sequences, enabling the model to achieve long-horizon memory retention. Experiments across both MAZE and ProcTHOR environments demonstrate that StateLinFormer significantly outperforms its stateless linear-attention counterpart and standard Transformer baselines with fixed context windows. Notably, as interaction length increases, persistent stateful training substantially improves context-dependent adaptation, suggesting an enhancement in the model's In-Context Learning (ICL) capabilities for navigation tasks.

---

## 论文详细总结（自动生成）

### StateLinFormer：通过有状态训练增强导航中的长期记忆

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能（Embodied AI）中的导航任务要求智能体具备强大的**长期记忆**，以在持续交互中构建空间表示并做出一致决策。现有方法存在两难困境：
*   **模块化系统（如 SLAM）**：依赖显式建图，虽有精确几何信息但缺乏语义灵活性，且难以跨环境泛化。
*   **端到端 Transformer 模型**：虽然泛化能力强，但受限于**固定上下文窗口**，导致在长距离任务中无法保留持久记忆，且计算复杂度随序列长度呈平方级增长。
*   **核心痛点**：现有的序列建模通常采用“无状态训练”（Stateless Training），即在每个训练批次（Batch）开始时重置记忆状态。这导致模型在训练时只学习了如何从零开始处理短序列，而无法在推理时有效利用持续演进的长时程记忆状态。

#### 2. 方法论：核心思想与技术细节
论文提出了 **StateLinFormer**，其核心在于将**线性注意力架构**与**有状态训练（Stateful Training）**相结合：
*   **核心思想**：在训练过程中，不再于每个 Batch 边界重置隐藏状态，而是将上一个 Batch 的末尾状态 $M_{T}^{(b)}$ 传递给下一个 Batch 作为初始状态 $M_{0}^{(b+1)}$。这种方式在截断梯度的同时，模拟了在无限长序列上的前向状态暴露。
*   **架构细节**：
    *   **解码器**：采用线性注意力机制（具体为 **RWKV-7** 架构），其记忆更新公式为 $M_t = M_{t-1} + \phi(k_t)v_t^\top$，其中 $M_t$ 是固定大小的记忆矩阵。
    *   **编码器**：使用预训练的 SigLIP 提取视觉特征，并结合目标指令（Goal Encoder）生成统一的观测表示 $o_t$。
    *   **动作预测**：将当前观测 $o_t$ 与上一动作 $a_{t-1}$ 拼接，输入线性注意力解码器，通过 MLP 输出动作分布。
*   **优化视角**：有状态训练改变了优化目标，使模型参数在自身循环动力学诱导的平稳记忆分布 $d_\theta$ 下进行优化，而非集中在零初始化状态附近。

#### 3. 实验设计
论文引入了**持续对象导航（CON）**基准测试，要求智能体在同一环境中连续完成一系列目标任务，而不重置环境或状态。
*   **实验场景**：
    1.  **MAZE**：15x15 的随机网格世界，用于测试基础的空间推理。
    2.  **ProcTHOR**：基于 AI2-THOR 的逼真 3D 室内环境，具有复杂的语义和视觉特征。
*   **对比方法（Baselines）**：
    *   **Stateless Linear Attention**：相同架构但采用传统批次重置训练（消融实验）。
    *   **SPOC-10M**：采用标准 Transformer 解码器的变体，在相同数据量下训练。
    *   **SPOC-Pretrained**：在 40M 帧大规模数据上预训练的原始 Transformer 模型（作为高性能基准）。

#### 4. 资源与算力
论文明确说明了训练配置：
*   **硬件**：使用了 **8 张 NVIDIA A800 GPU**。
*   **训练参数**：Batch Size 为 4，共训练 10 个 Epoch。
*   **模型规模**：约 **2 亿（0.2B）参数**，包含 18 层线性注意力层，隐藏维度为 512。
*   **数据规模**：MAZE 数据集包含 1 亿步（100M steps），ProcTHOR 数据集包含 1000 万步（10M steps）。

#### 5. 实验数量与充分性
*   **实验组数**：在两个不同复杂度的环境（MAZE 和 ProcTHOR）中进行了对比，并针对上下文长度（Context Length）进行了性能趋势分析。
*   **充分性评价**：实验设计较为充分。通过与同参数量的 Transformer 以及更大规模预训练的模型对比，证明了“有状态训练”而非“架构本身”是性能提升的关键。
*   **客观性**：通过引入 RSD（相对标准差）分析记忆模长的稳定性，从统计学角度验证了有状态训练能诱导出更稳定的记忆分布。

#### 6. 主要结论与发现
*   **性能超越**：StateLinFormer 在成功率和到达目标步数上均显著优于无状态版本。在 ProcTHOR 中，仅用 10M 数据训练的 StateLinFormer 甚至超过了用 40M 数据训练的 SPOC-Pretrained。
*   **涌现的上下文学习（ICL）**：随着交互步数的增加，StateLinFormer 的成功率持续上升，而无状态模型则表现平平甚至下降。这表明有状态训练增强了模型在不更新参数的情况下，通过历史信息进行在线适应的能力。
*   **记忆稳定性**：有状态训练产生的记忆状态分布更稳定（RSD 更低），避免了推理时因序列过长导致的状态漂移。

#### 7. 优点
*   **训练-推理一致性**：解决了传统模型在训练时“见短”而在推理时“用长”的分布偏移问题。
*   **计算效率**：线性注意力保证了在处理无限长交互流时，每一步的计算和存储开销是恒定的。
*   **ICL 能力增强**：无需显式设计复杂的记忆检索机制，仅通过优化协议的改变就诱导出了强大的在线适应能力。

#### 8. 不足与局限
*   **理论保证缺失**：虽然从优化角度给出了直观解释，但缺乏关于循环动力学收敛性和稳定性的严格数学证明。
*   **任务覆盖面**：目前仅在导航任务（MAZE/ProcTHOR）中验证，尚未在视觉语言导航（VLN）或更复杂的机械臂操作任务中测试。
*   **真实世界验证**：所有实验均在模拟器中完成，未在真实物理机器人上进行 Sim-to-Real 的验证，可能存在环境噪声适应性问题。

（完）
