---
title: Inference-time Trajectory Optimization for Manga Image Editing
title_zh: 漫画图像编辑的推理时轨迹优化
authors: Ryosuke Furuta
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27790v1"
tags: ["query:ttt"]
score: 9.0
evidence: 图像编辑的推理时自适应方法
tldr: 针对预训练图像编辑模型在处理漫画图像时表现不佳的问题，本文提出了一种推理时轨迹优化方法。该方法无需大规模重训或微调，仅利用输入图像本身，在推理阶段微调生成轨迹，使模型在空提示词下能更准确地重建原图。实验证明，该方法在保持极低计算开销的同时，显著提升了漫画编辑的质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27790v1/fig-001.webp\", \"caption\": \"Table 2: Quantitative evaluation of text removal\", \"page\": 6, \"index\": 1, \"width\": 718, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.27790v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative comparison for text removal. ©Nagano Noriko ©Omi Ayuko\", \"page\": 6, \"index\": 2, \"width\": 1058, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2603.27790v1/fig-003.webp\", \"caption\": \"Figure 2: Examples of manga image editing. In each pair, the left image is the input and the right image is the ground truth. ©Kato Masaki ©Yabuno Tenya, Watanabe Tatsuya\", \"page\": 2, \"index\": 3, \"width\": 506, \"height\": 612}, {\"url\": \"assets/figures/arxiv/2603.27790v1/fig-004.webp\", \"caption\": \"Table 6: Evaluation on various models for text removal\", \"page\": 8, \"index\": 4, \"width\": 622, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2603.27790v1/fig-005.webp\", \"caption\": \"Figure 5: Qualitative comparison for screentone synthesis. ©Saki Kaori ©Kurita Riku\", \"page\": 8, \"index\": 5, \"width\": 1058, \"height\": 496}]"
motivation: 现有的图像编辑模型多基于自然图像训练，在处理漫画时效果不佳，且受限于计算成本和版权问题难以进行大规模微调。
method: 提出一种推理时自适应方法，通过在推理阶段微调生成轨迹，确保输入图像在空提示词下能被忠实重建。
result: 实验结果显示，该方法在漫画编辑任务上一致优于现有基准模型，且计算开销几乎可以忽略不计。
conclusion: 该研究为特定领域（如漫画）的图像编辑提供了一种高效且无需额外训练数据的推理时优化方案。
---

## 摘要
我们提出了一种推理时自适应方法，仅利用输入图像本身，使预训练的图像编辑模型能够适配每一张输入的漫画图像。尽管预训练图像编辑领域近期取得了进展，但由于此类模型主要在自然图像数据上进行训练，其在漫画上的表现往往不尽如人意。然而，由于计算成本和版权限制，在漫画数据上重新训练或微调大规模模型通常是不切实际的。为解决这一问题，我们的方法在推理阶段对生成轨迹进行微调，使得输入图像在空提示词下能够被更忠实地重建。实验结果表明，我们的方法在仅产生微小计算开销的情况下，性能持续优于现有的基准方法。

## Abstract
We present an inference-time adaptation method that tailors a pretrained image editing model to each input manga image using only the input image itself. Despite recent progress in pretrained image editing, such models often underperform on manga because they are trained predominantly on natural-image data. Re-training or fine-tuning large-scale models on manga is, however, generally impractical due to both computational cost and copyright constraints. To address this issue, our method slightly corrects the generation trajectory at inference time so that the input image can be reconstructed more faithfully under an empty prompt. Experimental results show that our method consistently outperforms existing baselines while incurring only negligible computational overhead.

---

## 论文详细总结（自动生成）

以下是对论文《Inference-time Trajectory Optimization for Manga Image Editing》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：现有的图像编辑模型（如 Qwen Image Edit, FLUX.1 Kontext）虽然在自然图像上表现优异，但在处理漫画（Manga）图像时效果较差。这主要是因为训练数据中漫画占比极少，导致模型存在严重的“域偏移”（Domain Shift）。
*   **核心挑战**：
    1.  **成本高昂**：在海量漫画数据上重新训练或微调大规模模型需要巨大的计算资源。
    2.  **版权限制**：漫画作品受版权保护，收集大规模数据集进行训练存在法律风险。
*   **核心问题**：如何在不更新模型参数、不使用额外训练数据的前提下，让预训练模型适配特定的漫画输入？

### 2. 方法论：推理时轨迹优化
*   **核心思想**：利用扩散/流模型（Diffusion/Flow models）“先生成全局结构，后生成局部细节”的特性。在推理的前几个步骤中，微调生成轨迹，使其在“空提示词”（Empty Prompt）下能够忠实地重建输入图像，从而为后续的编辑任务提供准确的结构锚点。
*   **关键技术细节**：
    1.  **轨迹修正**：在生成的前 $M$ 步（通常 $M < 10$），模型同时计算“编辑提示词”和“空提示词”下的速度向量。
    2.  **目标计算**：定义一个修正目标 $Z^*_{t_i}$，它是通过最小化空提示词下的单步重建误差得到的：$Z^*_{t_i} = X_{in} - (t_N - t_i)v_\theta(Z_{t_i}, t_i, \emptyset, X_{in})$。
    3.  **插值更新**：将当前的潜在状态 $Z_{t_i}$ 向修正目标靠近：$Z_{t_i} \leftarrow (1 - \alpha)Z_{t_i} + \alpha Z^*_{t_i}$，其中 $\alpha$ 是修正强度。
*   **数学解释**：论文证明了这种轨迹修正等同于在每个时间步对“空提示词重建误差”进行梯度下降，且无需计算复杂的雅可比矩阵（Jacobian-skipping），因此效率极高。

### 3. 实验设计
*   **应用场景**：
    1.  **无掩码文字移除（Mask-free Text Removal）**：直接输入带文字的漫画，要求模型自动识别并移除文字。
    2.  **网点合成（Screentone Synthesis）**：将线稿转换为带有专业网点效果的完成稿。
*   **数据集**：使用 **Manga109s** 数据集构建了配对数据（文字移除 3,204 对，网点合成 1,355 对）。
*   **对比方法（Benchmark）**：
    *   **基础模型**：Qwen Image Edit 2509/2511, FLUX.1 Kontext, FLUX.2 Klein。
    *   **通用编辑方法**：Noise Inversion, FlowChef, FlowEdit。
    *   **任务特定模型**：SickZil-Machine（文字移除专用）、Sketch2Manga（网点合成专用）。
*   **评价指标**：PSNR, SSIM, LPIPS（文字移除）；FID, CMMD（网点合成）。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA H200 GPU**。
*   **计算开销**：
    *   **推理时间**：基准模型（Baseline）平均每张图耗时 32.21 秒，本文方法耗时 32.23 秒。
    *   **额外开销**：增加的计算时间仅为 0.02 秒（约 0.06%），几乎可以忽略不计。
*   **训练时长**：该方法为 **Training-free**（无需训练），因此无训练算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对两个核心任务进行了大规模定量评估（数千张图像）。
    *   进行了详尽的**消融实验**，包括修正步数 $M$、修正强度 $\alpha$、提示词选择（空提示词 vs 编辑提示词）以及路径选择（优化路径 vs 直线路径）的影响。
    *   **跨模型验证**：在 Qwen 和 FLUX 系列等多个主流 Rectified Flow 模型上验证了通用性。
*   **评价**：实验设计非常充分且客观。通过与任务特定（Task-specific）的 SOTA 模型对比，证明了通用模型在经过推理时优化后，甚至能在局部细节处理上超越专用模型。

### 6. 主要结论与发现
1.  **有效性**：该方法显著提升了预训练模型在漫画领域的表现，特别是在保持原图结构和风格一致性方面。
2.  **空提示词的重要性**：使用空提示词作为修正锚点比使用编辑提示词更有效，因为它能分离“结构保持”和“内容编辑”两个目标，避免冲突。
3.  **极低成本**：无需微调参数即可实现领域自适应，是解决版权和算力限制的理想方案。

### 7. 优点
*   **即插即用**：无需更新模型权重，适用于任何基于 Rectified Flow 的图像编辑模型。
*   **效率极高**：通过并行化处理（Batching），几乎不增加推理延迟。
*   **结构保持力强**：有效解决了扩散模型在编辑时容易改变非编辑区域（如人物脸部变形）的顽疾。
*   **无需掩码**：在文字移除任务中不需要预先标注文字区域，实现了全自动处理。

### 8. 不足与局限
*   **模型限制**：该方法目前专门针对 **Rectified Flow** 架构设计，虽然这是当前主流，但对于传统的 DDPM 或其他生成架构的适用性尚未探讨。
*   **任务局限**：主要适用于“结构保持类”编辑任务（如文字移除、上色、网点），对于需要大幅度改变全局结构的编辑任务（如改变人物姿势）可能效果有限。
*   **超参数敏感性**：修正步数 $M$ 和强度 $\alpha$ 仍需根据具体模型和任务进行微调，缺乏一种完全自动化的参数确定机制。

（完）
