---
title: "AgentRM: An OS-Inspired Resource Manager for LLM Agent Systems"
title_zh: AgentRM：一种受操作系统启发的 LLM 智能体系统资源管理器
authors: Jianshu She
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13110v1"
tags: ["query:am"]
score: 9.0
evidence: 针对大模型智能体内存与上下文退化的资源管理器
tldr: 针对大语言模型智能体系统存在的调度失效和上下文退化问题，本文通过分析 4 万多个 GitHub 问题，借鉴操作系统设计理念提出了 AgentRM。该中间件包含多级反馈队列调度器和三层上下文生命周期管理器，有效解决了僵尸进程、速率限制和智能体“失忆”等挑战，在显著提升系统响应速度和吞吐量的同时，确保了关键信息的完整保留。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.13110v1/fig-001.webp\", \"caption\": \"Fig. 3. Comprehensive scheduling performance comparison across all test scenarios.\", \"page\": 5, \"index\": 1, \"width\": 478, \"height\": 313}]"
motivation: 现有智能体框架面临调度阻塞、僵尸进程堆积及上下文增长导致的“失忆”等严重影响用户体验的资源管理难题。
method: 提出受操作系统启发的 AgentRM 中间件，采用多级反馈队列调度、速率限制感知控制及三层上下文生命周期管理机制。
result: "该系统将 P95 延迟降低了 86%，吞吐量提升了 168%，并实现了 100% 的关键信息保留率且彻底消除了僵尸智能体。"
conclusion: 借鉴成熟的操作系统资源管理策略是解决大模型智能体系统性能瓶颈和内存管理问题的有效途径。
---

## 摘要
大语言模型（LLM）智能体系统在各个领域得到了快速应用，但仍面临严重的用户体验问题，限制了其实际部署。通过对六个主要智能体框架（OpenClaw、AutoGen、CrewAI、LangGraph、Codex、Claude Code）的 40,000 多个 GitHub issue 进行实证分析，我们发现了两个根本性的资源管理挑战：(1) 调度失败导致系统因阻塞、僵尸进程和速率限制级联而产生无响应；(2) 上下文退化导致智能体因内存无限增长和保留策略不佳而产生“健忘症”。借鉴数十年的操作系统研究经验，我们提出了 AgentRM，这是一种将智能体资源类比为操作系统资源的中间件资源管理器。AgentRM 采用多级反馈队列（MLFQ）调度器，具备僵尸进程回收和速率限制感知的准入控制功能，并结合了实现自适应压缩和休眠机制的三层上下文生命周期管理器。我们的评估结果显示了显著的改进：AgentRM-MLFQ 将 P95 延迟降低了 86%，减少了 96% 的通道浪费，并将吞吐量提高了 168%，同时消除了僵尸智能体（0 对比基准值的 29 个）。与现有方法相比，AgentRM-CLM 实现了 100% 的关键信息保留和 95% 的质量得分（现有方法为 65.1% 的保留率和 87% 的质量得分），尽管其压缩成本更高（34,330 对比 17,212 个 token）。

## Abstract
Large Language Model (LLM) agent systems have experienced rapid adoption across diverse domains, yet they suffer from critical user experience problems that limit their practical deployment. Through an empirical analysis of over 40,000 GitHub issues from six major agent frameworks (OpenClaw, AutoGen, CrewAI, LangGraph, Codex, Claude Code), we identify two fundamental resource management challenges: (1) scheduling failures leading to system unresponsiveness due to blocking, zombie processes, and rate limit cascades, and (2) context degradation causing agent "amnesia" from unbounded memory growth and poor retention policies. Drawing inspiration from decades of operating systems research, we present AgentRM, a middleware resource manager that treats agent resources analogously to OS resources. AgentRM employs a Multi-Level Feedback Queue (MLFQ) scheduler with zombie reaping and rate-limit-aware admission control, coupled with a three-tier Context Lifecycle Manager that implements adaptive compaction and hibernation mechanisms. Our evaluation demonstrates significant improvements: AgentRM-MLFQ reduces P95 latency by 86%, decreases lane waste by 96%, and increases throughput by 168% while eliminating zombie agents (0 vs. 29 baseline). AgentRM-CLM achieves 100% key information retention with 95% quality score compared to 65.1% retention and 87% quality for existing approaches, albeit with higher compaction costs (34,330 vs. 17,212 tokens).

---

## 论文详细总结（自动生成）

以下是对论文《AgentRM: An OS-Inspired Resource Manager for LLM Agent Systems》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 智能体（Agent）系统在实际部署中面临严重的资源管理瓶颈，导致用户体验不佳。
*   **研究背景**：作者通过对 OpenClaw、AutoGen、LangGraph 等 6 个主流框架的 40,000 多个 GitHub Issue 进行实证分析，总结出两大痛点：
    *   **调度失效**：由于缺乏有效的任务调度，系统常出现高优先级任务被阻塞、已完成任务占用资源不释放（僵尸进程）以及 API 速率限制导致的级联故障。
    *   **上下文退化**：长对话导致上下文窗口溢出，现有系统要么简单截断导致智能体“失忆”，要么因内存溢出崩溃。
*   **核心思想**：将智能体资源（执行通道、API 配额、上下文窗口）类比为操作系统资源（CPU、I/O、内存），借鉴成熟的 OS 管理技术来优化智能体系统。

### 2. 方法论：AgentRM 架构
AgentRM 作为智能体网关与模型 API 之间的中间件，包含两个核心组件：
*   **智能体调度器（Agent Scheduler）**：
    *   **多级反馈队列 (MLFQ)**：设置三级队列（交互级、子智能体级、后台级），根据执行时间和资源消耗动态调整优先级，防止长任务阻塞交互任务。
    *   **僵尸回收器 (Zombie Reaper)**：每 5 秒扫描一次，识别占用通道超过 30 秒且无进展的“僵尸”任务，通过概率恢复机制（50% 成功率模拟瞬时故障恢复）或强制终止来释放资源。
    *   **速率限制感知**：采用令牌桶算法和 AIMD（加法增加乘法减少）退避机制管理 API 调用频率。
*   **上下文生命周期管理器 (CLM)**：
    *   **三层存储架构**：Tier 0（活跃上下文，类似 L1 缓存）、Tier 1（压缩摘要，类似 RAM）、Tier 2（完整记录，类似磁盘）。
    *   **自适应压缩算法**：基于公式 $v(m) = \alpha \cdot \text{recency} + \beta \cdot \text{importance} + \gamma \cdot \text{key\_info}$ 计算消息价值，对低价值信息进行压缩而非直接丢弃。
    *   **休眠机制**：对非活跃会话进行序列化持久化，支持状态恢复。

### 3. 实验设计
*   **实验场景**：设计了五种模拟真实负载的场景：常规负载、高负载（280 轮次）、突发流量、故障环境（30% 挂起率）以及级联速率限制环境。
*   **对比基准 (Benchmarks)**：
    *   **调度对比**：FIFO（先进先出）、Round Robin（轮询）、Priority Queue（优先级队列）。
    *   **上下文管理对比**：无管理、FIFO 截断、滑动窗口、MemGPT 模式。
*   **评估指标**：P95 延迟、吞吐量、僵尸进程数量、通道浪费时间、关键信息保留率、上下文质量得分。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）。
*   **实现细节**：提到使用 SQLite 处理 Tier 1 存储，JSONL 处理 Tier 2 存储，并使用一个“小型语言模型（Small Language Model）”负责生成摘要和进行语义分析。由于 AgentRM 是中间件系统，其核心开销在于管理逻辑和额外的摘要调用，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：针对调度部分进行了 5 组不同压力和故障率的对比实验；针对上下文部分进行了 4 组不同长度（50/100/200 轮次及多主题）的实验。
*   **充分性评价**：实验设计较为充分，涵盖了从正常运行到极端故障的多种边界情况。通过平均值和 P95 指标展示了系统在长尾延迟上的表现，客观地反映了 OS 策略在处理“僵尸进程”和“资源浪费”上的优势。

### 6. 主要结论与发现
*   **调度优化**：AgentRM-MLFQ 在高负载下将 P95 延迟降低了 **86%**，吞吐量提升了 **168%**，并将通道浪费减少了 **96%**。
*   **僵尸消除**：在基准方法产生 29 个僵尸智能体的场景下，AgentRM 成功将其降至 0 或极低水平。
*   **上下文保留**：AgentRM-CLM 实现了 **100%** 的关键信息保留率，而现有最佳方法仅为 65.1%；上下文质量得分稳定在 0.95（基准为 0.87）。
*   **成本权衡**：高质量的上下文管理代价是更高的 Token 消耗（压缩成本约为基准方法的 2 倍），但换取了极高的任务连续性。

### 7. 优点
*   **实证驱动**：基于 4 万多个真实 GitHub Issue 总结问题，研究动机极具说服力。
*   **跨界借鉴**：将经典的 OS 调度和内存管理理论（如 MLFQ、虚拟内存分层、僵尸进程回收）完美移植到智能体领域，方案优雅且易于理解。
*   **透明性**：作为中间件实现，无需修改现有智能体框架的代码即可接入。

### 8. 不足与局限
*   **实验环境**：主要基于模拟负载而非真实的大规模生产环境部署，可能无法完全捕捉到极其复杂的分布式边缘案例。
*   **模型依赖**：上下文压缩的质量高度依赖于负责摘要的“小模型”，若该模型表现不佳，可能导致 Tier 1 摘要失真。
*   **成本开销**：智能压缩带来的额外 Token 成本在某些对价格极其敏感的应用中可能成为阻碍。
*   **权重调优**：价值评分函数中的权重（$\alpha, \beta, \gamma$）可能需要针对不同领域进行手动微调，缺乏完全自动化的超参数进化机制。

（完）
