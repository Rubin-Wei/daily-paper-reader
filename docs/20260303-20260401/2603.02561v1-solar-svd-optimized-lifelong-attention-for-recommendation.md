---
title: "SOLAR: SVD-Optimized Lifelong Attention for Recommendation"
title_zh: SOLAR：面向推荐系统的 SVD 优化终身注意力机制
authors: "Chenghao Zhang, Chao Feng, Yuanhao Pu, Xunyong Yang, Wenhui Yu, Xiang Li, Yongqi Liu, Lantao Hu, Kaiqiao Zhan, Han Li, Kun Gai"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02561v1"
tags: ["query:lcp"]
score: 8.0
evidence: Transformer中的长上下文建模
tldr: 针对推荐系统中长序列建模的高计算复杂度问题，本文提出SOLAR框架。利用推荐数据天然的低秩特性，设计了SVD-Attention机制，在保留Softmax算子的同时将复杂度从O(N^2 d)降至O(Ndr)。该方法支持万级长度的行为序列建模，在快手线上环境显著提升了视频播放量等核心业务指标，实现了性能与效果的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.02561v1/fig-001.webp\", \"caption\": \"Figure 2. Overview of Attention Complexity. Softmax attention explicitly forms the dense attention matrix QK⊤ ∈ RN×N and then multiplies by V , resulting in O(N2d) time complexity. Linear attention leverages associativity to rewrite the computation as Q (K⊤V ), avoiding the N ×N matrix and reducing complexity to O(Nd2). SVD-Attention applies a rank-r SVD (r ≪ d) to obtain a low-rank factorization. After reassembling the decomposed UΣV , the computation of U can theoretically be eliminated, as U⊤U = I , in low rank representations, which maintains the order of computation as in traditional attention, yielding an overall complexity of O(Ndr).\", \"page\": 3, \"index\": 1, \"width\": 1017, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2603.02561v1/fig-002.webp\", \"caption\": \"Figure 4. Forward latency of the attention module on CPU under single-thread execution. The x-axis varies the history length N and the y-axis is the time in milliseconds. Candidate size m and embedding dimension d are held fixed.\", \"page\": 8, \"index\": 2, \"width\": 454, \"height\": 281}]"
motivation: 传统注意力机制在处理长序列时计算成本过高，而现有线性注意力方法往往会损失Softmax特性并改变注意力分布。
method: 提出基于奇异值分解的SVD-Attention，利用低秩矩阵实现理论上无损且保留Softmax的复杂度优化。
result: "在快手线上推荐场景中，SOLAR成功支持了万级长度序列建模，并带来了0.68%的视频播放量增长及多项业务指标提升。"
conclusion: SOLAR证明了结合低秩分解与注意力机制可以有效解决超长序列推荐的性能瓶颈，具有极高的工业应用价值。
---

## 摘要
注意力机制仍然是 Transformer 中的核心算子，因为它提供了极具表现力的全局信用分配，但其随序列长度 $N$ 呈 $O(N^2 d)$ 的时间和内存开销使得长上下文建模变得昂贵，且通常迫使人们采用截断或其他启发式方法。线性注意力通过核特征映射重排计算，将复杂度降低到 $O(N d^2)$，但这种重构舍弃了 softmax 机制并改变了注意力分数的分布。在推荐系统中，矩阵的低秩结构并非罕见情况，而是其表示学习中默认的归纳偏置，在用户行为序列建模中尤为明显。利用这一结构，我们引入了 SVD-Attention，它在理论上对低秩矩阵是无损的，在保留 softmax 的同时将注意力复杂度从 $O(N^2 d)$ 降低到 $O(Ndr)$。基于 SVD-Attention，我们提出了 SOLAR（面向推荐的 SVD 优化终身注意力机制），这是一个序列建模框架，支持万级规模的行为序列和数千个候选物品的级联过程，且无需任何过滤。在快手的在线推荐场景中，SOLAR 带来了 0.68% 的视频播放量（Video Views）增益以及其他业务指标的提升。

## Abstract
Attention mechanism remains the defining operator in Transformers since it provides expressive global credit assignment, yet its $O(N^2 d)$ time and memory cost in sequence length $N$ makes long-context modeling expensive and often forces truncation or other heuristics. Linear attention reduces complexity to $O(N d^2)$ by reordering computation through kernel feature maps, but this reformulation drops the softmax mechanism and shifts the attention score distribution. In recommender systems, low-rank structure in matrices is not a rare case, but rather the default inductive bias in its representation learning, particularly explicit in the user behavior sequence modeling. Leveraging this structure, we introduce SVD-Attention, which is theoretically lossless on low-rank matrices and preserves softmax while reducing attention complexity from $O(N^2 d)$ to $O(Ndr)$. With SVD-Attention, we propose SOLAR, SVD-Optimized Lifelong Attention for Recommendation, a sequence modeling framework that supports behavior sequences of ten-thousand scale and candidate sets of several thousand items in cascading process without any filtering. In Kuaishou's online recommendation scenario, SOLAR delivers a 0.68\% Video Views gain together with additional business metrics improvements.