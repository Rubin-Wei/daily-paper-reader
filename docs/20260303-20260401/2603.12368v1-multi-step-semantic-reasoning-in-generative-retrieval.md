---
title: Multi-Step Semantic Reasoning in Generative Retrieval
title_zh: 生成式检索中的多步语义推理
authors: "Steven Dong, Yubao Tang, Maarten de Rijke"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12368v1"
tags: ["query:pm"]
score: 9.0
evidence: 生成式检索模型将语料库编码在模型参数中
tldr: 生成式检索（GR）在处理涉及数值和复杂语义推理的查询（如财务报告）时表现不佳。本文提出 ReasonGR 框架，通过结构化提示策略（结合任务指令与逐步推理引导）和推理增强适配模块，提升了模型的多步语义推理能力。在 FinQA 数据集上的实验证明，该方法显著提高了检索的准确性和一致性，为复杂场景下的生成式检索提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.12368v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 1900, \"height\": 200}]"
motivation: 现有的生成式检索模型在处理需要数值计算和多步语义推理的复杂查询时，由于推理能力有限导致检索精度不足。
method: 提出了 ReasonGR 框架，通过结合任务特定指令与逐步推理引导的结构化提示，并集成推理增强适配模块来优化参数学习。
result: 在包含复杂财务文档的 FinQA 数据集上，ReasonGR 显著提升了检索的准确率和结果的一致性。
conclusion: 该研究证明了通过增强多步推理能力可以有效提升生成式检索在推理密集型任务中的实用性。
---

## 摘要
生成式检索（GR）模型将语料库编码在模型参数中，并直接为给定查询生成相关的文档标识符。尽管这种范式在检索任务中展现出潜力，但由于推理能力有限，现有的 GR 模型在处理数值背景下的复杂查询（例如涉及财务报告语义推理的查询）时表现不佳。这一局限性导致检索准确率不理想，并阻碍了其实际应用。我们提出了 ReasonGR，这是一个旨在增强 GR 中数值背景下多步语义推理的框架。ReasonGR 采用了一种结构化提示策略，将特定任务指令与逐步推理引导相结合，以更好地处理复杂的检索查询。此外，它集成了一个专注于推理的自适应模块，以改进推理相关参数的学习。在包含复杂文档财务查询的 FinQA 数据集上进行的实验表明，ReasonGR 提高了检索的准确性和一致性，显示了其在推理密集型检索场景中推进 GR 模型的潜力。

## Abstract
Generative retrieval (GR) models encode a corpus within model parameters and generate relevant document identifiers directly for a given query. While this paradigm shows promise in retrieval tasks, existing GR models struggle with complex queries in numerical contexts, such as those involving semantic reasoning over financial reports, due to limited reasoning capabilities. This limitation leads to suboptimal retrieval accuracy and hinders practical applicability. We propose ReasonGR, a framework designed to enhance multi-step semantic reasoning in numerical contexts within GR. ReasonGR employs a structured prompting strategy combining task-specific instructions with stepwise reasoning guidance to better address complex retrieval queries. Additionally, it integrates a reasoning-focused adaptation module to improve the learning of reasoning-related parameters. Experiments on the FinQA dataset, which contains financial queries over complex documents, demonstrate that ReasonGR improves retrieval accuracy and consistency, indicating its potential for advancing GR models in reasoning-intensive retrieval scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multi-Step Semantic Reasoning in Generative Retrieval》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
生成式检索（Generative Retrieval, GR）是近年来信息检索领域的新兴范式，它通过模型参数直接生成文档标识符（DocID），省去了传统检索的外部索引。然而，现有的 GR 模型在处理**数值背景下的多步语义推理**（如分析复杂的财务报告）时表现乏力。
*   **痛点**：GR 模型通常将检索视为“黑盒”操作，缺乏显式的推理步骤；且传统的最大似然估计（MLE）训练主要关注表面文本匹配，无法培养逻辑推理能力。
*   **目标**：提升 GR 模型在处理复杂、涉及数值计算和多步逻辑推导的查询时的检索准确性。

### 2. 方法论：ReasonGR 框架
ReasonGR 旨在通过结构化引导和参数高效微调来增强模型的推理能力：
*   **核心思想**：将“思维链（CoT）”引入检索过程，让模型在生成 DocID 之前先生成推理轨迹。
*   **关键技术细节**：
    *   **结构化提示策略**：结合“任务特定指令”（如“根据问题预测文档 ID”）与“CoT 引导语”（如“让我们一步步思考”），引导模型进行逻辑推导。
    *   **推理适配模块（LoRA/QLoRA）**：在 Transformer 骨干网络中注入低秩适配器（LoRA），仅训练少量新增参数，并使用 4-bit 量化（QLoRA）降低显存占用。
    *   **训练任务设计**：包含语料库记忆（文档到 DocID 的映射）和相关性学习（查询到推理轨迹再到 DocID 的映射）。
    *   **自适应惩罚缩放损失（Adaptive Penalty Scaling Loss）**：在交叉熵损失基础上，引入精确匹配（EM）、部分匹配（PM）等评估信号作为惩罚因子，对错误预测进行细粒度监督。

### 3. 实验设计
*   **数据集**：**FinQA**。这是一个极具挑战性的财务问答数据集，包含大量需要对财务报表和表格进行数值推理的查询。
*   **基准方法（Baselines）**：
    *   **BM25**：传统的稀疏检索方法。
    *   **DSI (Differentiable Search Index)**：经典的生成式检索模型。
*   **评估指标**：精确匹配（EM）、部分匹配（PM）、集合匹配（SM）和结构得分（S-Score）。

### 4. 资源与算力
*   **硬件**：使用了 **1 张 NVIDIA H100 GPU**。
*   **模型骨干**：FLAN-T5 base。
*   **训练时长**：
    *   ReasonGR (Zero) 变体：约 3.6 小时（40 epochs）。
    *   ReasonGR (CoT) 变体：约 6.8 小时（60 epochs）。
    *   完整 ReasonGR：约 5.2 小时（55 epochs）。
*   **显存占用**：训练期间约在 8.5 GiB 到 25.4 GiB 之间（取决于是否开启 CoT）。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了 ReasonGR 的三个变体（完整版、无提示训练版、纯 CoT 版）以及两个强基准。
*   **消融实验**：通过不同变体的对比，验证了提示训练和 CoT 结合的有效性。
*   **充分性评价**：实验设计较为客观，涵盖了从检索精度到训练效率（内存、吞吐量）的多维度评估。虽然仅在一个数据集（FinQA）上进行了验证，但该数据集在数值推理领域具有代表性。

### 6. 主要结论与发现
*   **性能提升**：ReasonGR 在所有指标上均显著优于 DSI，且在部分指标（PM, SM）上超过了 BM25。
*   **推理的价值**：引入 CoT 和 Few-shot 提示显著增强了模型生成正确 DocID 的能力。
*   **效率权衡**：虽然 CoT 增加了训练时间和显存消耗，但带来的检索精度提升证明了这种开销是值得的。
*   **泛化性**：测试集表现与验证集一致，说明模型具有良好的泛化能力。

### 7. 优点与亮点
*   **范式创新**：首次系统地将多步语义推理与生成式检索相结合，解决了 GR 在复杂逻辑场景下的短板。
*   **参数高效**：利用 LoRA 和 QLoRA 技术，在保持高性能的同时极大地降低了对算力的需求。
*   **细粒度损失函数**：设计的自适应惩罚损失比传统的单一交叉熵更能捕捉 DocID 生成中的结构化错误。

### 8. 不足与局限
*   **模型规模限制**：使用了较小的 FLAN-T5 base 模型，作者承认更大的模型（如 Decoder-only 架构）可能会带来更强的推理增益。
*   **输入长度限制**：512 标记的输入限制可能导致长文档信息的丢失。
*   **数据集单一**：目前仅在财务领域（FinQA）验证，其在法律、医疗等其他需要复杂推理的领域的表现尚待观察。
*   **推理噪声**：在生成 DocID 前生成推理文本可能会引入噪声，未来可能需要专门的分类头来分离推理与检索任务。

（完）
