---
title: Test-Time Adaptation via Cache Personalization for Facial Expression Recognition in Videos
title_zh: 视频面部表情识别中基于缓存个性化的测试时自适应
authors: "Masoumeh Sharafi, Muhammad Osama Zeeshan, Soufiane Belharbi, Alessandro Lameiras Koerich, Marco Pedersoli, Eric Granger"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21309v2"
tags: ["query:ttt"]
score: 10.0
evidence: 分布偏移下通过缓存个性化进行的测试时适配
tldr: 针对视频表情识别中跨主体分布偏移导致的性能下降问题，本文提出了一种基于缓存个性化的测试时自适应方法（TTA-CaP）。该方法无需梯度优化，通过协同管理个性化源缓存、正向目标缓存和负向目标缓存，并结合三门控机制动态更新，实现了低功耗且鲁棒的视觉语言模型个性化，在多个数据集上显著提升了识别精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21309v2/fig-001.webp\", \"caption\": \"Table 1: Subject-wise performance (WAR, UAR) on the BioVid dataset for TTA-CaP and other TTA methods.\", \"page\": 22, \"index\": 1, \"width\": 666, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.21309v2/fig-002.webp\", \"caption\": \"Table 4: Subject-wise performance (F1 score) on the BAH dataset for TTA-CaP and other TTA methods. Best F1 scores are highlighted in bold.\", \"page\": 10, \"index\": 2, \"width\": 582, \"height\": 553}, {\"url\": \"assets/figures/arxiv/2603.21309v2/fig-003.webp\", \"caption\": \"Table 3: Subject-wise performance (F1 score) on the StressID dataset for TTA-CaP and other TTA methods. Best F1 scores are highlighted in bold.\", \"page\": 10, \"index\": 3, \"width\": 720, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2603.21309v2/fig-004.webp\", \"caption\": \"Table 2: Subject-wise performance (F1 score) on the BioVid dataset for TTA-CaP and other TTA methods. Best F1 scores are highlighted in bold.\", \"page\": 10, \"index\": 4, \"width\": 720, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2603.21309v2/fig-005.webp\", \"caption\": \"Fig. 5: (a) Average WAR for target subjects in BioVid for zero-shot CLIP, static personalized source cache only, dynamic target cache only, and TTA-CaP (static + dynamic). (b) Ablation of source-cache construction in BioVid. Average WAR across target subjects. Prototypes: the personalized source cache built from source-subject prototypes.\", \"page\": 14, \"index\": 5, \"width\": 725, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2603.21309v2/fig-006.webp\", \"caption\": \"Fig. 3: Qualitative visualization. Left: t-SNE visualization of the embedding space before/after cache fusion for two subjects from StressID. Right: gate pass rates (fraction of frames passing each criterion and updating the caches).\", \"page\": 12, \"index\": 6, \"width\": 675, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2603.21309v2/fig-007.webp\", \"caption\": \"Fig. 2: Overview of our TTA-CaP. (top) Offline source prototype extraction. For each source-domain subject, CLIP image embeddings are clustered, and representative prototypes are selected to define a labeled set of source prototypes. (bottom) Online TTA and tri-gate cache update. At test time, frozen CLIP encoders produce text and visual embeddings and an initial prediction through contrastive similarity. A fixed personalized source cache retrieves the top-m nearest source prototypes as a stable reference. A tri-gate controls updates to a dynamic target cache with positive and negative entries. Finally, predictions are obtained by embedding-level fusion of the CLIP visual embedding with retrieved cache items, enabling cost-effective personalization without training (backpropagation) at test-time.\", \"page\": 5, \"index\": 7, \"width\": 652, \"height\": 415}]"
motivation: 传统的测试时自适应方法依赖高开销的参数优化，且在处理视频表情识别中的主体差异和伪标签噪声时表现不佳。
method: 提出TTA-CaP方法，利用三缓存结构（源、正向、负向）和三门控机制在无需梯度更新的情况下实现模型个性化与预测优化。
result: 在BioVid、StressID和BAH三个挑战性数据集上的实验表明，该方法在应对主体和环境偏移时优于现有SOTA方法，且计算开销极低。
conclusion: TTA-CaP为实时视频表情识别提供了一种高效、稳健的个性化方案，证明了基于缓存的非参数化自适应在实际部署中的巨大潜力。
---

## 摘要
视频中的面部表情识别 (FER) 需要模型个性化，以捕捉受试者之间显著的差异。视觉语言模型 (VLMs) 通过图像-文本对齐在下游任务中表现出强大的迁移能力，但在受试者间的分布偏移下，其性能仍可能下降。使用测试时自适应 (TTA) 方法对模型进行个性化可以缓解这一挑战。然而，大多数最先进的 TTA 方法依赖于无监督参数优化，这引入了在许多实际应用中不切实际的计算开销。本文介绍了通过缓存个性化实现的 TTA (TTA-CaP)，这是一种基于缓存的 TTA 方法，能够为视频 FER 实现具有成本效益（无梯度）的视觉语言模型个性化。先前的基于缓存的 TTA 方法仅依赖于存储测试样本的动态内存，这可能会由于噪声伪标签而积累误差并产生漂移。TTA-CaP 利用三个协调的缓存：存储源域原型的个性化源缓存、积累可靠的受试者特定样本的正向目标缓存，以及存储低置信度案例作为负样本以减少噪声伪标签影响的负向目标缓存。缓存的更新和替换由基于时间稳定性、置信度以及与个性化缓存一致性的三门控机制控制。最后，TTA-CaP 通过嵌入融合来优化预测，产生支持时间稳定的视频级预测的精细化表示。我们在三个具有挑战性的视频 FER 数据集（BioVid、StressID 和 BAH）上的实验表明，TTA-CaP 在受试者特定和环境偏移下能够优于最先进的 TTA 方法，同时保持较低的计算和内存开销，适用于实际部署。

## Abstract
Facial expression recognition (FER) in videos requires model personalization to capture the considerable variations across subjects. Vision-language models (VLMs) offer strong transfer to downstream tasks through image-text alignment, but their performance can still degrade under inter-subject distribution shifts. Personalizing models using test-time adaptation (TTA) methods can mitigate this challenge. However, most state-of-the-art TTA methods rely on unsupervised parameter optimization, introducing computational overhead that is impractical in many real-world applications. This paper introduces TTA through Cache Personalization (TTA-CaP), a cache-based TTA method that enables cost-effective (gradient-free) personalization of VLMs for video FER. Prior cache-based TTA methods rely solely on dynamic memories that store test samples, which can accumulate errors and drift due to noisy pseudo-labels. TTA-CaP leverages three coordinated caches: a personalized source cache that stores source-domain prototypes, a positive target cache that accumulates reliable subject-specific samples, and a negative target cache that stores low-confidence cases as negative samples to reduce the impact of noisy pseudo-labels. Cache updates and replacement are controlled by a tri-gate mechanism based on temporal stability, confidence, and consistency with the personalized cache. Finally, TTA-CaP refines predictions through fusion of embeddings, yielding refined representations that support temporally stable video-level predictions. Our experiments on three challenging video FER datasets, BioVid, StressID, and BAH, indicate that TTA-CaP can outperform state-of-the-art TTA methods under subject-specific and environmental shifts, while maintaining low computational and memory overhead for real-world deployment.

---

## 论文详细总结（自动生成）

### 视频面部表情识别中基于缓存个性化的测试时自适应 (TTA-CaP) 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：视频面部表情识别（FER）在实际应用中面临严重的**跨主体分布偏移**（Inter-subject distribution shifts）。不同个体的表情细微差异、光照及拍摄条件的变化会导致预训练模型（如 CLIP）性能大幅下降。
*   **研究动机**：
    *   现有的测试时自适应（TTA）方法多依赖梯度优化（如 Prompt-tuning），计算开销大，难以在移动端或实时场景部署。
    *   现有的基于缓存（Cache-based）的无梯度方法通常只依赖测试样本的动态内存，容易受到视频中噪声伪标签的影响，导致模型产生漂移。
    *   **目标**：开发一种低功耗、无梯度、且能有效利用源域先验知识进行个性化适配的视频 FER 方法。

#### 2. 方法论：TTA-CaP 核心架构
TTA-CaP 的核心思想是结合**固定源域先验**与**动态目标域反馈**，通过嵌入级融合优化预测。
*   **三缓存结构**：
    1.  **个性化源缓存 (Personalized Source Cache)**：离线从源域受试者中提取类原型。在测试时，根据目标受试者的特征统计量（如中性表情分布），检索最相似的 Top-m 个源受试者的原型作为稳定参考。
    2.  **正向目标缓存 (Positive Target Cache)**：在线存储高置信度、时间稳定的目标域样本及其伪标签。
    3.  **负向目标缓存 (Negative Target Cache)**：存储低置信度或模糊的样本作为负面参考，用以抵消噪声影响。
*   **三门控更新机制 (Tri-gate Mechanism)**：
    *   **时间门控**：确保当前预测与滑动窗口内的多数预测一致，过滤瞬时波动。
    *   **熵门控**：通过预测分布的熵值衡量置信度，区分可靠样本与模糊样本。
    *   **原型门控**：检查当前样本预测是否与个性化源缓存中的原型一致，确保语义不偏离。
*   **嵌入级融合 (Embedding-level Fusion)**：
    将当前帧的 CLIP 嵌入与从三个缓存中检索到的嵌入进行加权合并（$z_{fuse} = z_{tgt} + z_{src} + z_{tgt+} - z_{tgt-}$），在特征空间直接进行个性化修正，随后通过余弦相似度输出最终预测。

#### 3. 实验设计
*   **数据集**：
    *   **BioVid**：疼痛评估数据集（热疼痛刺激）。
    *   **StressID**：多模态压力识别数据集。
    *   **BAH**：行为矛盾/犹豫识别数据集（环境更复杂、非受控）。
*   **Benchmark 与对比方法**：
    *   **零样本/微调基准**：CLIP-ViT-B/32、EmoCLIP、X-CLIP、Exp-CLIP。
    *   **TTA 对比方法**：包括基于 Prompt-tuning 的 **TPT**、**PromptAlign**，以及基于缓存/原型的 **TDA**、**DPE**、**ReTA**、**T3AL**。
*   **评价指标**：加权平均召回率 (WAR) 和 F1 分数。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 GPU (40GB/48GB)**。
*   **效率表现**：
    *   TTA-CaP 的推理延迟约为 **110ms/batch**（Batch Size=16），内存占用约 **904MB**。
    *   相比之下，Prompt-tuning 方法（如 PromptAlign）延迟高达 900ms，内存占用超过 3GB。
    *   该方法无需反向传播，显著降低了算力需求。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同性质的数据集上进行了测试，每个数据集选取了 10 名目标受试者进行个体化评估。
*   **消融实验**：
    *   验证了三门控机制中每一项的必要性。
    *   对比了“仅静态缓存”、“仅动态缓存”与“完整 TTA-CaP”的效果。
    *   探讨了源受试者选择数量（Top-m）对性能的影响。
    *   比较了嵌入级融合与 Logit 级融合的优劣。
*   **充分性评价**：实验设计较为全面，涵盖了准确性、鲁棒性、计算效率和可视化分析（t-SNE），对比对象包含了最新的 SOTA 方法，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：TTA-CaP 在所有数据集上均优于现有的 TTA 方法，尤其在 BioVid 和 StressID 上提升显著（WAR 提升约 5%-10%）。
*   **个性化价值**：引入个性化的源域原型（静态缓存）能有效锚定类别语义，防止在线更新时的模型漂移。
*   **效率优势**：在提升精度的同时，计算成本远低于基于优化的方法，证明了其在实时视频处理中的实用性。

#### 7. 优点
*   **无梯度适配**：避免了昂贵的反向传播，适合资源受限的设备。
*   **创新的缓存协同**：通过正负目标缓存与个性化源缓存的配合，有效解决了伪标签噪声问题。
*   **时间稳定性**：专门针对视频序列设计了时间门控和嵌入融合，使预测在时间轴上更平滑。

#### 8. 不足与局限
*   **源域依赖**：个性化源缓存的构建仍需访问源域数据的特征统计量，虽然不直接访问原始图像，但在完全无源（Source-free）的极端场景下受限。
*   **超参数敏感性**：三门控机制涉及多个阈值（如熵阈值、原型边际阈值），在不同领域的数据集上可能需要手动调优。
*   **极端环境挑战**：在 BAH 这种非受控、高噪声的数据集上，虽然仍是 SOTA，但绝对精度提升幅度较小，说明在极度模糊的表情识别上仍有提升空间。

（完）
