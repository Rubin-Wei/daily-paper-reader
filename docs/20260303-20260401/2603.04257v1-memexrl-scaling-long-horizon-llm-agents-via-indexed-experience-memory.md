---
title: "Memex(RL): Scaling Long-Horizon LLM Agents via Indexed Experience Memory"
title_zh: Memex(RL)：通过索引经验记忆扩展长程 LLM 智能体
authors: "Zhenting Wang, Huancheng Chen, Jiayun Wang, Wei Wei"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04257v1"
tags: ["query:am"]
score: 10.0
evidence: 为长周期LLM智能体引入索引经验记忆
tldr: 针对大语言模型智能体在长程任务中面临的上下文窗口限制，本文提出 Memex 机制。现有方法如截断或摘要往往会丢失关键信息，而 Memex 通过维护包含摘要和索引的紧凑上下文，并将完整交互存储在外部数据库中，实现了无损的信息保留。配合 MemexRL 强化学习框架，智能体能够自主学习何时归档、索引及检索证据。实验表明，该方法在显著降低上下文开销的同时，有效提升了长程任务的成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.04257v1/fig-001.webp\", \"caption\": \"Figure 2 | Task success rates for rollouts during training. The agent’s task success rate improves from approximately 20% to over 90%, demonstrating that MemexRL training effectively teaches the model to solve tasks using Memex agent loop.\", \"page\": 11, \"index\": 1, \"width\": 739, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.04257v1/fig-002.webp\", \"caption\": \"Figure 3 | Total penalty for rollouts during training. The penalty decreases in magnitude from −0.4 to approximately −0.1, showing that the agent learns better task execution and strategically squeezing the peak working context length using CompressExperience and ReadExperience.\", \"page\": 11, \"index\": 2, \"width\": 739, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.04257v1/fig-003.webp\", \"caption\": \"Figure 1 | Memex agent loop overview. CompressExperience replaces a long tool-use trajectory in the context with a compact indexed summary, while storing detailed contents in an external key–value store. Later, ReadExperience(index) dereferences an index to retrieve the exact content and re-inject it into the context, enabling long-horizon execution under a small context window.\", \"page\": 2, \"index\": 3, \"width\": 834, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2603.04257v1/fig-004.webp\", \"caption\": \"Figure 5 | Memory tool usage on the evaluation set during training. (a) Compress count decreases from 6.5 to 3 as the agent completes tasks more efficiently. (b) Retrieve count increases from 1 to 6–7, showing that RL reinforces retrieval behavior rather than suppressing it.\", \"page\": 13, \"index\": 4, \"width\": 984, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2603.04257v1/fig-005.webp\", \"caption\": \"Figure 4 | Effectiveness of MemexRL. (a) Task success rate improves from 24.2% to 85.6%. (b) Peak working context length reduces from 16,934 to 9,634 tokens, approaching the penalty threshold of 8,000 tokens.\", \"page\": 12, \"index\": 5, \"width\": 983, \"height\": 407}]"
motivation: 长程任务中有限的上下文窗口会导致智能体丢失关键的工具输出和推理证据，而传统的压缩方法存在严重的信息损耗。
method: 引入索引经验记忆机制，将完整交互存入外部数据库并仅在上下文中保留索引和摘要，通过 MemexRL 强化学习框架优化智能体的记忆读写行为。
result: 实验证明 Memex 智能体在处理复杂长程任务时，不仅显著减少了工作上下文的使用量，还取得了更高的任务成功率。
conclusion: 索引经验记忆为长程 LLM 智能体提供了一种低损耗且计算高效的记忆扩展方案，具有良好的理论保证和实证效果。
---

## 摘要
大语言模型 (LLM) 智能体在处理长程任务时，从根本上受到有限上下文窗口的限制。随着轨迹的增长，在上下文中保留工具输出和中间推理过程很快变得不可行：工作上下文变得过长，最终超出上下文预算，并且即使远端证据仍然存在，也变得难以利用。现有的解决方案通常通过截断或滚动摘要来缩短上下文，但这些方法从根本上是有损的，因为它们压缩或丢弃了过去的证据本身。我们引入了 Memex，这是一种索引经验记忆机制，它在不丢弃证据的情况下压缩上下文。Memex 维护一个由简洁结构化摘要和稳定索引组成的紧凑工作上下文，同时将高保真的底层交互存储在这些索引下的外部经验数据库中。智能体随后可以决定何时解引用索引，并恢复当前子目标所需的精确历史证据。我们通过强化学习框架 MemexRL 优化读写行为，使用针对上下文预算下索引记忆使用情况定制的奖励塑造，使智能体学会总结什么、归档什么、如何索引以及何时检索。与仅使用摘要的方法相比，这产生了一种损耗显著更低的长程记忆形式。我们进一步提供了理论分析，展示了 Memex 循环在保持有效上下文计算随历史增长而有界的同时，通过有界解引用来维持决策质量的潜力。实验表明，在具有挑战性的长程任务中，使用 MemexRL 训练的 Memex 智能体在显著减小工作上下文的同时，提高了任务成功率。

## Abstract
Large language model (LLM) agents are fundamentally bottlenecked by finite context windows on long-horizon tasks. As trajectories grow, retaining tool outputs and intermediate reasoning in-context quickly becomes infeasible: the working context becomes prohibitively long, eventually exceeds the context budget, and makes distant evidence harder to use even when it is still present. Existing solutions typically shorten context through truncation or running summaries, but these methods are fundamentally lossy because they compress or discard past evidence itself. We introduce Memex, an indexed experience memory mechanism that instead compresses context without discarding evidence. Memex maintains a compact working context consisting of concise structured summaries and stable indices, while storing full-fidelity underlying interactions in an external experience database under those indices. The agent can then decide when to dereference an index and recover the exact past evidence needed for the current subgoal. We optimize both write and read behaviors with our reinforcement learning framework MemexRL, using reward shaping tailored to indexed memory usage under a context budget, so the agent learns what to summarize, what to archive, how to index it, and when to retrieve it. This yields a substantially less lossy form of long-horizon memory than summary-only approaches. We further provide a theoretical analysis showing the potential of the Memex loop to preserve decision quality with bounded dereferencing while keeping effective in-context computation bounded as history grows. Empirically, on challenging long-horizon tasks, Memex agent trained with MemexRL improves task success while using a significantly smaller working context.

---

## 论文详细总结（自动生成）

### 论文总结：Memex(RL) —— 通过索引经验记忆扩展长程 LLM 智能体

#### 1. 核心问题与整体含义（研究动机和背景）
长程（Long-Horizon）任务要求 LLM 智能体在数十甚至数百步的交互中保持推理的一致性。然而，LLM 受限于有限的**上下文窗口**。随着任务推进，工具输出、中间推理和观察结果不断累积，导致上下文溢出。现有的解决方法（如截断或滚动摘要）通常是**有损的**，会丢弃或过度压缩关键证据（如具体的代码片段、日志或对象 ID），导致智能体在后续步骤中无法找回精确信息。本文旨在开发一种“无损”的记忆机制，使智能体能在保持紧凑上下文的同时，保留并精确检索历史中的高保真证据。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Memex** 机制及其配套的强化学习框架 **MemexRL**：
*   **核心思想（Indexed Experience Memory）**：将上下文解构为两部分：
    1.  **在内上下文（In-context）的索引摘要 ($\sigma$)**：包含简洁的进度说明和一组“索引-描述”对（Index-Description pairs）。
    2.  **外部经验存储（External Store, $D$）**：一个键值数据库，存储高保真的原始交互块（如完整的工具输出）。
*   **关键操作**：
    *   `CompressExperience`：将当前冗长的上下文压缩，把详细内容存入外部库，仅在上下文中保留索引摘要。支持手动编写内容或基于锚点（Anchors）的自动提取。
    *   `ReadExperience(index)`：通过解引用索引，将外部存储的精确内容重新注入当前上下文。
*   **MemexRL 强化学习框架**：
    *   使用 **GRPO** 算法优化智能体的读写策略。
    *   **奖励塑造（Reward Shaping）**：综合考虑任务成功率、上下文溢出惩罚、冗余工具调用惩罚（鼓励使用检索而非重复执行命令）以及格式错误惩罚。
    *   **分段轨迹处理**：将发生压缩的轨迹切分为独立段落进行训练，通过共享终端奖励来实现长程信用分配。
    *   **软触发机制**：通过在观察中加入“上下文状态”提示，让智能体自主学习何时触发压缩。

#### 3. 实验设计
*   **实验场景**：使用了修改后的 **ALFWorld** 交互式环境。为了增加难度，研究者隐藏了可用的命令列表和初始观察，限制了“查看（look）”动作的次数，并强制对摘要进行截断。这迫使智能体必须依赖记忆检索才能获取物体 ID 和位置信息。
*   **Benchmark**：3553 个复杂的家庭任务。
*   **对比方法**：主要对比了 **Memex 智能体在 RL 训练前（Base）与训练后（MemexRL）** 的表现，验证强化学习对记忆管理能力的提升。

#### 4. 资源与算力
*   **模型**：使用 Qwen3-30B-A3B-Thinking-2507（混合专家模型 MoE，约 30B 参数，激活参数约 3B）。
*   **算力细节**：
    *   使用 **Slime** 强化学习框架。
    *   采用 **INT4 量化** 进行推理采样，**量化感知训练（QAT）** 进行反向传播（梯度累积使用 BF16）。
    *   训练参数：学习率 $5 \times 10^{-6}$，KL 惩罚系数 0.001，Batch Size 为 32，GRPO 组大小为 8。
    *   **未明确说明**：具体的 GPU 型号、数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 3553 个任务上进行了训练和评估。
*   **充分性评价**：
    *   **动态分析**：展示了训练过程中成功率、惩罚项、压缩次数和检索次数的变化曲线，证明了策略的演进。
    *   **消融/对比**：通过对比训练前后的性能，清晰展示了 RL 在优化记忆读写行为上的作用。
    *   **局限性**：实验主要集中在修改后的 ALFWorld 这一类具身智能任务上，缺乏在其他领域（如长程代码开发、复杂文档分析）的横向对比实验。

#### 6 主要结论与发现
*   **性能提升**：MemexRL 将任务成功率从 **24.2% 提升至 85.6%**。
*   **上下文效率**：峰值工作上下文长度从约 1.7 万 token 降至约 0.96 万 token（降低了 **43%**），接近设定的 8000 token 惩罚阈值。
*   **行为转变**：经过训练，智能体学会了**减少盲目压缩**（压缩次数从 6.5 次降至 3 次），并**大幅增加精确检索**（检索次数从 1 次升至 6-7 次），证明其学会了构建并利用索引记忆。
*   **理论验证**：理论分析证明，只要索引摘要是决策充分的，Memex 就能在保持有界上下文计算的同时匹配全上下文最优策略。

#### 7. 优点
*   **低损耗压缩**：相比于传统的文本摘要，索引机制保证了原始证据（如复杂的 ID 或代码）的绝对精确性。
*   **自主性强**：通过 RL 让智能体自主决定压缩时机和检索内容，而非依赖硬编码的启发式规则。
*   **理论支撑**：提供了关于决策质量和计算边界的理论证明，增强了方法的说服力。
*   **可解释性**：索引和解引用的过程是显式的，便于审计智能体的推理依据。

#### 8. 不足与局限
*   **环境单一**：实验仅限于 ALFWorld 变体，虽然任务复杂，但环境的多样性（如多模态、真实网络环境）有待进一步验证。
*   **冷启动依赖**：MemexRL 需要通过监督演示（Warm-start）来引导初始的记忆行为，对高质量初始化数据的依赖程度较高。
*   **检索开销**：虽然节省了上下文，但频繁的 `ReadExperience` 动作会增加交互轮数，可能在某些实时性要求高的场景下产生延迟。
*   **泛化风险**：目前尚不清楚该记忆策略在未见过的全新任务类型上的泛化能力。

（完）
