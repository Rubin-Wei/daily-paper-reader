---
title: Pooling Engram Conditional Memory in Large Language Models using CXL
title_zh: 使用 CXL 在大语言模型中池化 Engram 条件记忆
authors: "Ruiyang Ma, Teng Ma, Zhiyuan Su, Hantian Zha, Xinpeng Zhao, Xuchun Shang, Xingrui Yi, Zheng Liu, Zhu Cao, An Wu, Zhichong Dou, Ziqian Liu, Daikang Kuang, Guojie Luo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10087v1"
tags: ["query:pm"]
score: 9.0
evidence: 用于解耦静态知识查找的 Engram 条件记忆
tldr: 本研究针对大语言模型中Engram条件记忆的大规模存储需求，提出利用CXL（Compute Express Link）内存池进行存储。Engram通过解耦静态知识与动态计算提升模型能力，但其庞大的嵌入表对内存容量提出挑战。相比RDMA，CXL能提供更细粒度且低延迟的访问。通过将CXL内存池集成至SGLang框架，该方案在保持接近DRAM性能的同时，为未来集成Engram的LLM提供了一种可扩展且高性价比的存储方案。
source: arxiv
selection_source: fresh_fetch
motivation: 解决大语言模型中Engram条件记忆庞大的嵌入表带来的存储压力与访问效率之间的矛盾。
method: 提出利用CXL内存池存储Engram，并将其集成到SGLang推理框架中，利用CXL的低延迟和细粒度访问特性。
result: 实验表明，基于CXL的Engram存储方案在端到端性能上接近原生DRAM。
conclusion: CXL内存池为集成Engram的大语言模型提供了一种兼顾高性能、高扩展性与成本效益的存储解决方案。
---

## 摘要
Engram 条件记忆通过将静态知识查找与动态计算解耦，已成为大语言模型中一个极具前景的组件。由于 Engram 表现出稀疏的访问模式并支持预取，其庞大的嵌入表非常适合卸载到低层级内存中。在本文中，我们提出使用 Compute Express Link (CXL) 内存池进行 Engram 存储。与 RDMA 相比，CXL 提供了 Engram 极小且离散的检索模式所需的细粒度、低延迟访问。我们将基于 CXL 的 Engram 池集成到 SGLang 中，实现了接近 DRAM 的端到端性能。这为未来集成 Engram 的大语言模型提供了一种可扩展且具有成本效益的存储解决方案，且不会损害推理性能。

## Abstract
Engram conditional memory has emerged as a promising component for LLMs by decoupling static knowledge lookup from dynamic computation. Since Engram exhibits sparse access patterns and supports prefetching, its massive embedding tables are well-suited for offloading to lower-tier memory. In this paper, we propose using Compute Express Link (CXL) memory pool for Engram storage. Compared to RDMA, CXL provides fine-grained and low-latency access required by minimal and discrete retrieval patterns of Engram. We integrate the CXL-based Engram pool into SGLang, achieving near-DRAM end-to-end performance. This provides a scalable and cost-efficient storage solution for future Engram-integrated LLMs without compromising inference performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Pooling Engram Conditional Memory in Large Language Models using CXL》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）新引入的 **Engram 条件记忆**（由 DeepSeek 提出）虽然能通过解耦静态知识查找与动态计算来提升模型能力，但其庞大的 N-Gram 嵌入表（可能达数百 GB）带来了巨大的内存压力。
*   **研究背景**：
    *   **内存墙挑战**：GPU 显存有限，无法承载海量的 Engram 嵌入表。
    *   **现有方案不足**：传统的 RDMA 内存池在处理 Engram 这种“小数据量（每 Token 仅约 5KB）、高频、离散”的稀疏访问模式时，由于协议栈开销，性能会大幅下降（吞吐量可能降至峰值的 25% 以下）。
*   **研究动机**：探索利用 **CXL (Compute Express Link)** 内存池的硬件级低延迟和细粒度访问特性，为 Engram 提供一种高性能、高扩展性且低成本的存储方案。

### 2. 论文提出的方法论
*   **核心思想**：将 Engram 参数卸载到通过 CXL 交换机连接的共享内存池中，利用 CXL 的 `load/store` 原语实现接近本地 DRAM 的访问性能。
*   **关键技术细节**：
    *   **CXL 内存池构建**：使用 CXL 交换机连接多个计算节点和一个中心化的 CXL 内存设备，实现多主机共享 Engram 嵌入表。
    *   **优化访问例程**：
        *   **CXL → CPU**：采用基于 OpenMP 的多线程并行读取策略，利用 `memcpy` 直接操作映射到用户空间的虚拟地址。
        *   **CXL → GPU**：开发自定义 CUDA Kernel，通过 `cudaHostRegister` 将 CXL 内存注册为固定内存，实现 GPU 绕过 CPU 直接通过 PCIe 总线进行 P2P 数据传输。
    *   **框架集成**：将该方案集成到 **SGLang** 推理框架中，通过异步预取机制，在计算当前层时提前从 CXL 池中拉取下一层所需的 Engram 嵌入，以掩盖传输延迟。

### 3. 实验设计
*   **实验场景**：大语言模型推理任务，模拟 Engram 模块的检索与计算过程。
*   **Benchmark 与对比方法**：
    *   **对比对象**：本地 DRAM（性能上限）、RDMA 内存池（基于 Mooncake Store 实现）。
    *   **评估指标**：读取延迟（Read Latency）、端到端推理吞吐量（Tokens/s）、扩展性（多节点/多 GPU）、硬件成本（CAPEX）。
*   **数据集/模型**：由于 DeepSeek 的 Engram 模型权重未开源，研究者使用了 **Qwen3-32B/4B/8B** 架构进行性能模拟，配置了 2 层 Engram 模块。

### 4. 资源与算力
*   **硬件配置**：
    *   **服务器**：2 台节点，每台配备双 Intel Xeon 6766E 处理器（共 288 核）。
    *   **GPU**：每台节点配备 2 块 **NVIDIA L20** GPU。
    *   **CXL 设备**：XConn XC50256 CXL 2.0 交换机，三星 256GB CXL 内存扩展卡（搭载 Montage 控制器）。
*   **训练/推理时长**：文中未提及模型训练时长，主要关注推理阶段的端到端吞吐量测试。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同 Batch Size（1 到 1024）下的延迟对比实验。
    *   针对不同规模模型（4B、8B）进行了端到端吞吐量测试。
    *   进行了跨节点（nnode=2）和数据并行（DP=2）的扩展性实验。
    *   提供了详细的硬件成本对比矩阵（从 100B 到 400B 参数规模）。
*   **充分性评价**：实验设计较为全面，涵盖了从底层算子延迟到上层框架吞吐量的多维度评估。对比了 RDMA 这一主流竞争方案，证明了 CXL 在稀疏访问场景下的优越性。但由于缺乏真实 Engram 模型权重，实验结果属于“性能模拟”而非“精度验证”。

### 6. 论文的主要结论与发现
*   **延迟表现**：CXL 内存池的读取延迟远低于 RDMA，且非常接近本地 DRAM，能够满足 Engram 预取的时间窗口要求（约 56μs）。
*   **端到端性能**：集成 CXL Engram 池后，SGLang 的推理吞吐量较本地 DRAM 方案仅有极微小下降（约 1% 左右），几乎实现了无损卸载。
*   **成本效益**：随着模型规模和节点数量增加，CXL 共享池方案的成本优势显著。例如在 16 节点、400B Engram 规模下，相比每台机器配置全量 DRAM，CXL 方案可节省约 16.6 万美元。

### 7. 优点
*   **针对性强**：精准捕捉到了 Engram 架构与 CXL 硬件特性之间的高度契合点（稀疏、离散、延迟敏感）。
*   **系统级优化**：不仅提出了想法，还实现了 CXL 到 GPU 的 P2P 传输 Kernel，并集成到了主流推理框架 SGLang 中。
*   **前瞻性成本分析**：为未来超大规模 LLM 的基础设施建设提供了实用的经济学参考。

### 8. 不足与局限
*   **模型真实性限制**：由于 DeepSeek Engram 权重未公开，实验基于 Qwen 模型模拟，可能无法完全反映真实模型中的所有动态特性。
*   **资源竞争探讨不足**：论文主要关注 Engram 存储，但在实际生产环境中，CXL 池可能同时承载 KV Cache 和 Engram，两者之间的带宽竞争和内存管理冲突未深入讨论。
*   **硬件普及度**：CXL 2.0/3.0 交换机和相关生态目前仍处于早期部署阶段，大规模集群的稳定性仍待验证。

（完）
