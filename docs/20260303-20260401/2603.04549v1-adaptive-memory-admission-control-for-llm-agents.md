---
title: Adaptive Memory Admission Control for LLM Agents
title_zh: LLM 智能体的自适应记忆准入控制
authors: "Guilin Zhang, Wei Jiang, Xiejiashan Wang, Aisha Behr, Kai Zhao, Jeffrey Friedman, Xu Chu, Amine Anoun"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04549v1"
tags: ["query:am"]
score: 10.0
evidence: LLM 智能体的长期记忆准入控制
tldr: 针对LLM智能体长期记忆管理中信息冗余、虚假及控制力不足的问题，本文提出A-MAC框架。该框架将记忆准入视为结构化决策问题，通过整合未来效用、事实置信度、语义新颖性、时间近因和内容类型先验五个维度，结合轻量化规则与LLM评估，实现了透明且高效的记忆筛选。实验证明，A-MAC在提升记忆质量的同时显著降低了延迟，为构建可扩展且可靠的智能体记忆系统提供了关键设计准则。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.04549v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison on LoCoMo test set (N=225). Latency marked with † includes LLM API calls. Best F1 in bold.\", \"page\": 6, \"index\": 1, \"width\": 647, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2603.04549v1/fig-002.webp\", \"caption\": \"Figure 2: Precision-recall tradeoff comparison. A-MAC achieves the best balance between precision and recall, occupying the upper-right region of the curve. Dashed lines indicate F1 iso-contours.\", \"page\": 7, \"index\": 2, \"width\": 471, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2603.04549v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of A-MAC. Candidate memories are extracted from conversation history and evaluated using five complementary signals capturing usefulness, reliability, redundancy, temporal relevance, and persistence. A learned linear admission policy aggregates these signals into a score S(m) and applies a threshold θ to determine whether to admit, update, or reject a candidate.\", \"page\": 4, \"index\": 3, \"width\": 829, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2603.04549v1/fig-004.webp\", \"caption\": \"Figure 3: Cross-domain F1 performance. Personal conversations achieve higher F1 due to explicit preference statements that align with Type Prior’s strengths. The dashed line indicates mean performance across domains.\", \"page\": 9, \"index\": 4, \"width\": 473, \"height\": 383}]"
motivation: 现有的LLM智能体记忆管理缺乏对冗余或错误信息的有效控制，且全LLM驱动的策略成本高昂且难以审计。
method: 提出A-MAC框架，通过分解记忆价值为五个可解释因子，并结合规则提取与LLM辅助评估来优化准入策略。
result: "在LoCoMo基准测试中，A-MAC将F1分数提升至0.583，同时比现有原生LLM记忆系统降低了31%的延迟。"
conclusion: 显式且可解释的准入控制是实现LLM智能体大规模、高可靠长期记忆的核心设计原则。
---

## 摘要
基于大语言模型（LLM）的智能体日益依赖长期记忆来支持多会话推理与交互，然而现有系统对保留哪些信息几乎没有控制。在实践中，智能体要么积累大量的对话内容（包括幻觉或过时的事实），要么依赖于不透明、完全由 LLM 驱动的记忆策略，而这些策略成本高昂且难以审计。因此，记忆准入在智能体架构中仍然是一个定义模糊且控制薄弱的组件。为了填补这一空白，我们提出了自适应记忆准入控制（A-MAC），这是一个将记忆准入视为结构化决策问题的框架。A-MAC 将记忆价值分解为五个互补且可解释的因素：未来效用、事实置信度、语义新颖性、时间近颖性以及内容类型先验。该框架结合了轻量级的基于规则的特征提取与单次 LLM 辅助的效用评估，并通过交叉验证优化来学习领域自适应的准入策略。这种设计实现了对长期记忆透明且高效的控制。在 LoCoMo 基准测试上的实验表明，与最先进的 LLM 原生记忆系统相比，A-MAC 实现了更优的精确率-召回率权衡，将 F1 分数提升至 0.583，同时将延迟降低了 31%。消融实验结果表明，内容类型先验是可靠记忆准入中影响最大的因素。这些发现表明，显式且可解释的准入控制是构建可扩展且可靠的 LLM 智能体记忆的关键设计原则。

## Abstract
LLM-based agents increasingly rely on long-term memory to support multi-session reasoning and interaction, yet current systems provide little control over what information is retained. In practice, agents either accumulate large volumes of conversational content, including hallucinated or obsolete facts, or depend on opaque, fully LLM-driven memory policies that are costly and difficult to audit. As a result, memory admission remains a poorly specified and weakly controlled component in agent architectures. To address this gap, we propose Adaptive Memory Admission Control (A-MAC), a framework that treats memory admission as a structured decision problem. A-MAC decomposes memory value into five complementary and interpretable factors: future utility, factual confidence, semantic novelty, temporal recency, and content type prior. The framework combines lightweight rule-based feature extraction with a single LLM-assisted utility assessment, and learns domain-adaptive admission policies through cross-validated optimization. This design enables transparent and efficient control over long-term memory. Experiments on the LoCoMo benchmark show that A-MAC achieves a superior precision-recall tradeoff, improving F1 to 0.583 while reducing latency by 31% compared to state-of-the-art LLM-native memory systems. Ablation results identify content type prior as the most influential factor for reliable memory admission. These findings demonstrate that explicit and interpretable admission control is a critical design principle for scalable and reliable memory in LLM-based agents.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：LLM 智能体的自适应记忆准入控制 (A-MAC)

#### 1. 核心问题与研究动机
随着 LLM 智能体在多轮交互和长期任务中的应用增加，**长期记忆管理**成为核心挑战。现有系统存在两个极端：
*   **启发式方法（如 MemGPT）**：依赖简单的近因或重要性评分，计算快但无法识别幻觉信息，且难以适应复杂场景。
*   **LLM 原生方法（如 A-mem）**：完全依赖 LLM 生成记忆属性并决策，虽然召回率高，但计算成本昂贵、延迟高且决策过程不透明（黑盒），难以审计。
**核心问题**：如何在保证记忆质量（准确、无幻觉、非冗余）的同时，实现高效、可解释且低延迟的记忆准入控制？

#### 2. 方法论：A-MAC 框架
A-MAC 将记忆准入建模为一个**结构化决策问题**，其核心思想是通过五个可解释的维度对候选记忆进行综合评分：

*   **五个核心价值信号**：
    1.  **未来效用 (Utility, U)**：通过单次 LLM 调用评估该信息对未来任务或用户偏好的潜在价值。
    2.  **事实置信度 (Confidence, C)**：利用 ROUGE-L 算法计算候选记忆与原始对话文本的对齐程度，直接拦截无根据的幻觉信息。
    3.  **语义新颖性 (Novelty, N)**：使用 Sentence-BERT 嵌入计算与已有记忆的余弦相似度，防止冗余存储。
    4.  **时间近因 (Recency, R)**：应用指数衰减函数，随时间推移降低旧信息的权重。
    5.  **内容类型先验 (Type Prior, T)**：基于规则和词性标注，识别信息类型（如“用户偏好”权重高于“临时状态”）。

*   **决策机制**：
    *   **加权评分公式**：$S(m) = w_1 U + w_2 C + w_3 N + w_4 R + w_5 T$。
    *   **策略学习**：通过 5 折交叉验证和网格搜索，在训练集上自动优化权重向量 $w$ 和准入阈值 $\theta$。
    *   **混合架构**：仅 Utility 维度使用 LLM，其余四个维度均采用轻量化规则或小模型计算，显著提升效率。

#### 3. 实验设计
*   **数据集/基准**：使用 **LoCoMo** 基准测试，包含 30 个长对话（涵盖个人助手、技术支持、研究协作），涉及约 1,500 个带标注的候选记忆。
*   **对比方法**：
    *   **Random**：随机准入。
    *   **MemGPT**：基于近因和 LLM 评分的启发式方法。
    *   **MemoryBank**：基于 Ebbinghaus 遗忘曲线的线性评分方法。
    *   **A-mem**：当前最先进的（SOTA）全 LLM 驱动记忆系统。
    *   **Equal Weights**：A-MAC 的等权重基准版本。

#### 4. 资源与算力
*   **模型使用**：本地部署的 **Qwen 2.5** 作为 Utility 评分的 LLM；使用 **Sentence-BERT** 生成语义嵌入。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。但提到了推理延迟：A-MAC 平均每条候选记忆处理耗时 2644ms（其中 LLM 占 97.6%）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了主效果对比、消融实验、阈值敏感性分析、延迟分解分析以及跨领域（个人 vs. 职业）泛化性测试。
*   **充分性评价**：实验设计较为全面。通过 5 折交叉验证确保了结果的稳定性；消融实验清晰地展示了各特征的贡献度；跨领域测试证明了模型并非过拟合于特定场景。虽然数据集（30 个对话）规模不算巨大，但对于记忆准入这一细分任务，其标注密度和分析深度具有说服力。

#### 6. 主要结论与发现
*   **性能领先**：A-MAC 的 F1 分数达到 **0.583**，优于 A-mem (0.541) 和 MemoryBank (0.452)。
*   **效率提升**：相比 A-mem，A-MAC 的端到端延迟降低了 **31%**，这主要归功于将多次 LLM 调用简化为单次。
*   **关键因子**：消融实验发现 **内容类型先验 (Type Prior)** 是最重要的特征，移除它会导致 F1 分数大幅下降。
*   **精准权衡**：A-MAC 在保持高召回率（0.972）的同时，显著提升了精确率，有效减少了记忆库的膨胀。

#### 7. 优点与亮点
*   **可解释性**：线性加权模型让开发者能清晰看到为何某条记忆被接受或拒绝，便于调试和审计。
*   **混合计算架构**：巧妙平衡了 LLM 的语义理解能力与传统算法的计算效率。
*   **幻觉感知**：显式引入“事实置信度”维度，将防幻觉作为记忆准入的一等公民。
*   **自适应性**：权重可根据不同领域自动学习，无需人工手动调参。

#### 8. 不足与局限
*   **LLM 依赖性**：尽管减少了调用次数，但 Utility 评分仍占总延迟的 97% 以上，系统瓶颈依然在 LLM。
*   **置信度度量单一**：使用 ROUGE-L 作为置信度指标可能无法捕捉深层的语义一致性，容易受到表达方式变化的影响。
*   **数据集局限**：LoCoMo 虽然专业，但样本量相对有限，在更大规模、更嘈杂的真实生产环境中的表现仍待验证。
*   **冲突解决策略简单**：目前的冲突合并逻辑（基于相似度阈值）在处理复杂逻辑矛盾时可能过于简化。

（完）
