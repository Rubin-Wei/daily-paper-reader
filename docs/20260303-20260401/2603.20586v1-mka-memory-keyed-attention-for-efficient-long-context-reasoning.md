---
title: "MKA: Memory-Keyed Attention for Efficient Long-Context Reasoning"
title_zh: MKA：用于高效长上下文推理的记忆键注意力
authors: "Dong Liu, Yanxuan Yu, Ben Lengerich, Ying Nian Wu"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20586v1"
tags: ["query:lcp"]
score: 10.0
evidence: 用于高效长上下文推理的分层注意力机制
tldr: 针对长文本语言模型中KV缓存带来的内存和计算瓶颈，本文提出Memory-Keyed Attention (MKA)。该方法通过分层注意力机制整合局部、会话和长期KV缓存，并实现动态路由。其变体FastMKA通过融合内存源进一步提升效率。实验表明，FastMKA在保持与MLA相当的困惑度时，训练吞吐量提升达5倍，推理延迟降低1.8倍，为长文本推理提供了高效且可扩展的框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20586v1/fig-001.webp\", \"caption\": \"Table 4: Prefill vs Decode latency breakdown on Qwen2.5-7B (batch=1, bf16). Prefill: total time to process input tokens. Decode: latency per generated token. Measurements use contiguous KV layout for ≤32K and paged layout for longer contexts.\", \"page\": 6, \"index\": 1, \"width\": 502, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2603.20586v1/fig-002.webp\", \"caption\": \"Table 6: Comparison across different model architectures on WikiText-2 (sequence length 32K, batch=2 for training, batch=1 for inference). Qwen2.5-14B experiments use 4 A800 GPUs with tensor parallelism (TP=4); all other models use a single A800 GPU. FastMKA maintains efficiency advantages across architectures.\", \"page\": 6, \"index\": 2, \"width\": 502, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.20586v1/fig-003.webp\", \"caption\": \"Table 11: Per-token computational cost breakdown across different model architectures (128K context, batch=1, bf16). FLOPs and memory access measured via Nsight Compute. FastMKA achieves speedup through algorithmic reduction of attention paths rather than implementation differences.\", \"page\": 8, \"index\": 3, \"width\": 502, \"height\": 252}]"
motivation: 现有的KV缓存压缩技术在减少内存占用的同时，往往会牺牲表示质量或增加运行开销，难以满足长文本建模的需求。
method: 提出分层注意力机制MKA，通过动态路由整合多级KV缓存，并引入FastMKA变体在计算前融合内存源以提升效率。
result: 实验显示FastMKA在保持与MLA相当精度的前提下，实现了最高5倍的训练吞吐量提升和1.8倍的推理延迟降低。
conclusion: MKA为高效长文本注意力机制提供了一个实用且可扩展的框架，成功在准确性与计算效率之间取得了平衡。
---

## 摘要
随着长上下文语言建模变得日益重要，维护和处理大规模键/值（KV）缓存的成本迅速增加，成为训练和推理中的主要瓶颈。虽然以往的工作（如多查询注意力 MQA 和多潜变量注意力 MLA）通过共享或压缩 KV 特征来减少内存占用，但它们往往会牺牲表示质量或产生额外的运行时开销。我们提出了记忆键注意力（MKA），这是一种分层注意力机制，它集成了多级 KV 缓存（局部、会话和长期），并学习在这些缓存之间动态路由注意力。我们进一步引入了路由融合 MKA（FastMKA），这是一种广播路由变体，在注意力计算之前融合记忆源，以提高效率。在不同序列长度上的实验表明，FastMKA 实现了良好的准确率与效率权衡：在困惑度与 MLA 相当的同时，实现了高达 5 倍的训练吞吐量提升和 1.8 倍的评估延迟降低。这些结果表明，MKA 是一个用于高效长上下文注意力的实用且可扩展的框架。

## Abstract
As long-context language modeling becomes increasingly important, the cost of maintaining and attending to large Key/Value (KV) caches grows rapidly, becoming a major bottleneck in both training and inference. While prior works such as Multi-Query Attention (MQA) and Multi-Latent Attention (MLA) reduce memory by sharing or compressing KV features, they often trade off representation quality or incur runtime overhead. We propose Memory-Keyed Attention (MKA), a hierarchical attention mechanism that integrates multi-level KV caches (local, session, and long-term) and learns to route attention across them dynamically. We further introduce Route-Fused MKA (FastMKA), a broadcast-routed variant that fuses memory sources before attention computation for improved efficiency. Experiments on different sequence lengths show that FastMKA achieves a favorable accuracy-efficiency trade-off: comparable perplexity to MLA while achieving up to 5x faster training throughput and 1.8x lower evaluation latency. These results highlight MKA as a practical and extensible framework for efficient long-context attention.

---

## 论文详细总结（自动生成）

### 论文总结：MKA：用于高效长上下文推理的记忆键注意力

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）支持的上下文长度扩展至 128K 甚至 1M 标记，**键/值（KV）缓存的维护与检索成本**成为了训练和推理的主要瓶颈。现有的优化方案如 MQA（多查询注意力）和 GQA（分组查询注意力）通过减少 KV 头部数量来节省空间，而 MLA（多潜变量注意力）通过低秩压缩 KV。然而，这些方法往往在表示精度上有所妥协，或者在处理超长文本时仍面临严重的内存带宽限制。本文旨在设计一种既能保持高表示质量，又能显著提升长文本处理效率的分层注意力机制。

#### 2. 论文提出的方法论
论文提出了 **Memory-Keyed Attention (MKA)** 及其高效变体 **FastMKA**：
*   **核心思想**：借鉴计算机存储架构，将 KV 缓存组织为三层分层体系：
    *   **L1（局部记忆）**：当前窗口内的标记，提供高带宽的即时访问。
    *   **L2（会话记忆）**：通过因果前缀摘要（如均值或 EMA）生成的会话级压缩表示。
    *   **L3（长期记忆）**：存储在 DRAM 中的历史块，通过向量化哈希（Vectorized Hashing）进行语义块检索。
*   **动态路由机制**：为每个查询标记学习一个路由门控 $\lambda \in \mathbb{R}^3$，动态决定对 L1、L2、L3 层的注意力权重分配。
*   **FastMKA（路由融合变体）**：为了避免多次注意力计算的开销，FastMKA 在进行 KV 投影前，先根据路由权重对三层记忆源进行标记级的“软融合”，从而将计算路径简化为单次注意力操作。
*   **关键技术细节**：
    *   **递归 MKA 公式**：采用类似 FlashAttention 的在线 Softmax 更新技巧，通过递归累加未归一化的分数和最大值偏移（Max-Shift），实现数值稳定的分层注意力计算。
    *   **分块计算**：支持 GPU 友好的算子融合，优化 HBM 与 SRAM 之间的数据交换。

#### 3. 实验设计
*   **模型家族**：Qwen2.5 (7B/14B)、Llama 3.1-8B、DeepSeek-V3（基于 MLA 架构）。
*   **数据集与场景**：
    *   **语言建模**：WikiText-2（评估困惑度 PPL）。
    *   **长文本能力**：LongBench（涵盖问答、摘要、代码任务）和 RULER（Passkey 随机数检索任务）。
*   **对比方法（Baselines）**：标准多头注意力（MHA）、分组查询注意力（GQA）、多潜变量注意力（MLA）。
*   **序列长度**：覆盖 4K、8K、16K、32K、64K、128K 直至 256K。

#### 4. 资源与算力
*   **硬件环境**：主要使用 **NVIDIA A800 80GB GPU**。
*   **配置细节**：
    *   对于 7B 模型及 32K 以内序列，使用单张 A800。
    *   对于 14B 模型及超长序列（64K-256K），使用 4-8 张 A800，并启用张量并行（TP=2-4）。
*   **训练参数**：微调 1 个 Epoch，使用混合精度（bf16）和 FlashAttention-2。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括不同模型规模（7B/14B）、不同架构（Llama/Qwen/DeepSeek）、不同任务类型（PPL/下游任务/检索）。
*   **消融实验**：针对记忆层级（L1/L2/L3 的组合）和路由策略（软路由 vs 固定权重 vs 硬路由）进行了专门的消融研究。
*   **客观性**：实验不仅报告了速度提升，还诚实地记录了 PPL 的微小损失（约 1.2%），并对比了 Prefill（预填充）和 Decode（解码）两个阶段的延迟，评估较为全面、客观。

#### 6. 主要结论与发现
*   **效率飞跃**：在 256K 长度下，FastMKA 的训练吞吐量比 MLA 快 **5.01 倍**，解码延迟降低 **1.86 倍**。
*   **性能权衡**：FastMKA 在大幅提升速度的同时，保持了与 MLA 相当的准确度。例如在 Qwen2.5-7B 上，FastMKA 的 PPL 为 3.26，仅略高于 MLA 的 3.22，但在长文本检索（RULER）中表现稳健。
*   **内存优化**：相比 MHA，FastMKA 的 KV 缓存内存占用减少了约 **66.8%**，且 HBM 带宽利用率更高。
*   **可扩展性**：实验证明该机制在不同模型架构上均具有一致的加速效果。

#### 7. 优点
*   **架构创新**：将计算机系统的分层存储概念引入注意力机制，解决了长文本中“所有标记一视同仁”导致的计算浪费。
*   **硬件友好**：FastMKA 通过路由融合避免了多路径计算，且支持算子融合，非常适合高性能 GPU 推理。
*   **动态性**：相比静态的 Token 剔除方法，MKA 的动态路由允许模型根据查询内容灵活选择记忆来源，信息保留更完整。

#### 8. 不足与局限
*   **精度微损**：虽然效率提升巨大，但在极短文本或对精度极度敏感的场景下，PPL 的轻微上升可能是一个考虑因素。
*   **L3 检索开销**：虽然论文提到了向量化哈希，但在超大规模分布式环境下，L3 层的 DRAM 访问和哈希冲突管理可能会带来额外的系统复杂性。
*   **微调依赖**：该方法需要对预训练模型进行一定程度的微调以学习路由权重，无法直接作为零样本（Zero-shot）插件应用于所有现成模型。

（完）
