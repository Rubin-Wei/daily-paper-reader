---
title: "D-Mem: A Dual-Process Memory System for LLM Agents"
title_zh: D-Mem：一种用于大语言模型智能体的双过程记忆系统
authors: "Zhixing You, Jiachen Yuan, Jason Cai"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18631v1"
tags: ["query:am"]
score: 10.0
evidence: 面向LLM智能体的双过程记忆系统，提供高保真访问
tldr: 针对LLM智能体在长程推理中记忆检索精度不足的问题，本文提出D-Mem双过程记忆系统。该系统结合了轻量级向量检索与详尽的“全审议”模块，并通过多维质量门控策略动态平衡效率与准确性。实验证明，D-Mem在保持低计算成本的同时，显著提升了复杂上下文理解能力，性能接近全量处理水平，为构建高保真自主智能体提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.18631v1/fig-001.webp\", \"caption\": \"Table 1: Summary of overall scores and efficiency across four settings. The baseline performance comes from (Nan et al., 2025) and (Zhou and Han, 2025).\", \"page\": 6, \"index\": 1, \"width\": 829, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2603.18631v1/fig-002.webp\", \"caption\": \"Table 3: Overall Performance Comparison (LoCoMo, GPT-4o-mini)\", \"page\": 15, \"index\": 2, \"width\": 941, \"height\": 188}, {\"url\": \"assets/figures/arxiv/2603.18631v1/fig-003.webp\", \"caption\": \"Figure 2: F1 Improvement of Quality Gating over Mem0∗ by Question Category on the LoCoMo Dataset.\", \"page\": 7, \"index\": 3, \"width\": 750, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2603.18631v1/fig-004.webp\", \"caption\": \"Table 2: Impact of Fallback Mechanism in Gated Deliberation across Datasets. W/O FB: Without Fallback; W/ FB: With Fallback; Rate: Percentage of queries falling into this routing status.\", \"page\": 8, \"index\": 4, \"width\": 713, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2603.18631v1/fig-005.webp\", \"caption\": \"Figure 1: Architectural overview of D-Mem. Components of Part A illustrates the memory mechanism of Mem0∗. The elements highlighted in soft pale pink denote our key modifications over the original Mem0 framework: (1) utilizing the top-10 similar memories rather than a general summary during the Extraction Phase, and (2) incorporating an additional relevance filtering step prior to memory updates. Part B illustrates the Quality Gating mechanism, which evaluates the initial answer from Mem0∗ and dynamically determines whether to trigger the Full Deliberation fallback.\", \"page\": 4, \"index\": 5, \"width\": 917, \"height\": 393}]"
motivation: 现有的基于检索的记忆框架依赖有损抽象，难以处理需要精细上下文理解的复杂查询。
method: 提出一种双过程系统，集成轻量级向量检索作为常规手段，并引入全审议模块作为高保真回退机制，由多维质量门控策略动态调度。
result: "在LoCoMo基准测试中，D-Mem以较低成本恢复了全审议模块96.7%的性能，且F1分数优于主流的Mem0基准。"
conclusion: D-Mem通过双过程架构实现了认知经济性与高精度记忆访问的平衡，为长程自主智能体提供了更可靠的记忆方案。
---

## 摘要
随着持久、自适应自主智能体的发展，为这些系统配备用于长程推理的高保真记忆访问已成为一项关键需求。然而，流行的基于检索的记忆框架通常遵循增量处理范式，将对话记忆持续提取并更新到向量数据库中，并在查询时依赖语义检索。虽然这种方法速度很快，但它本质上依赖于有损抽象，经常遗漏上下文中的关键信息，并且难以解决依赖细粒度上下文理解的查询。为了解决这一问题，我们引入了 D-Mem，一种双过程记忆系统。它保留了用于常规查询的轻量级向量检索，同时建立了一个详尽的全审议（Full Deliberation）模块作为高保真回退方案。为了在不牺牲准确性的情况下实现认知经济性，D-Mem 采用了一种多维质量门控策略来动态衔接这两个过程。在 LoCoMo 和 RealTalk 基准测试上使用 GPT-4o-mini 和 Qwen3-235B-Instruct 进行的实验证明了我们方法的有效性。值得注意的是，我们的多维质量门控策略在使用 GPT-4o-mini 的 LoCoMo 上达到了 53.5 的 F1 分数。这优于我们的静态检索基准 Mem0* (51.2)，并恢复了全审议性能 (55.3) 的 96.7%，同时计算成本显著降低。

## Abstract
Driven by the development of persistent, self-adapting autonomous agents, equipping these systems with high-fidelity memory access for long-horizon reasoning has emerged as a critical requirement. However, prevalent retrieval-based memory frameworks often follow an incremental processing paradigm that continuously extracts and updates conversational memories into vector databases, relying on semantic retrieval when queried. While this approach is fast, it inherently relies on lossy abstraction, frequently missing contextually critical information and struggling to resolve queries that rely on fine-grained contextual understanding. To address this, we introduce D-Mem, a dual-process memory system. It retains lightweight vector retrieval for routine queries while establishing an exhaustive Full Deliberation module as a high-fidelity fallback. To achieve cognitive economy without sacrificing accuracy, D-Mem employs a Multi-dimensional Quality Gating policy to dynamically bridge these two processes. Experiments on the LoCoMo and RealTalk benchmarks using GPT-4o-mini and Qwen3-235B-Instruct demonstrate the efficacy of our approach. Notably, our Multi-dimensional Quality Gating policy achieves an F1 score of 53.5 on LoCoMo with GPT-4o-mini. This outperforms our static retrieval baseline, Mem0$^\ast$ (51.2), and recovers 96.7\% of the Full Deliberation's performance (55.3), while incurring significantly lower computational costs.

---

## 论文详细总结（自动生成）

这是一份关于论文《D-Mem: A Dual-Process Memory System for LLM Agents》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）智能体在长程对话中的记忆瓶颈**。
*   **现状与挑战**：现有的记忆框架（如 Mem0, Zep）多采用“增量压缩”模式，将对话实时摘要并存入向量数据库。这种方式虽然检索快，但属于**有损抽象（Lossy Abstraction）**，会丢失细粒度的上下文（如相对时间、多跳逻辑链），导致智能体在处理复杂推理时表现不佳。
*   **核心动机**：借鉴人类认知的“双过程理论”（System 1 快速直觉，System 2 慢速深思），开发一种既能保持检索效率，又能通过“全量审议”确保高保真度的记忆系统。

### 2. 方法论：核心思想与技术细节
D-Mem 提出了一个双层架构，通过**多维质量门控（Quality Gating）**动态调度：

*   **System 1 (Mem0*)**：改进版的轻量级检索。在原始 Mem0 基础上增加了 Top-10 相似记忆作为背景上下文进行提取，并引入了严格的余弦相似度过滤（>0.8）以减少冗余和噪声。
*   **System 2 (Full Deliberation)**：高保真回退机制。当 System 1 无法解决问题时，系统直接扫描原始对话记录。它将对话分块（每块 60 条消息），由 LLM 逐块提取与查询相关的细节并打分（0-10分），最后筛选高分事实生成答案。
*   **质量门控策略（核心衔接）**：这是一个元认知检查点，从三个维度评估 System 1 的输出：
    1.  **相关性（Relevance）**：答案是否直接回答了问题。
    2.  **忠实度与一致性（Faithfulness & Consistency）**：答案是否与检索到的记忆冲突或存在幻觉。
    3.  **完整性（Completeness）**：是否遗漏了关键细节。
    *   若任一维度未通过，则触发 System 2。

### 3. 实验设计
*   **数据集**：
    *   **LoCoMo**：包含 10 个超长对话（平均 2.4 万 token），涵盖单跳、多跳、时间推理和开放域问题。
    *   **RealTalk**：真实的 21 天长程对话数据集，噪声更高，挑战性更大。
*   **对比方法**：
    *   **全上下文（Full Context）**：直接将所有历史喂给模型。
    *   **标准 RAG**：基于分块的向量检索。
    *   **主流记忆框架**：LangMem, Mem0, Zep, Nemori, EMem-G 等。
*   **评估指标**：F1 分数（词汇重叠）、BLEU（n-gram 忠实度）和 **LLM-as-a-Judge**（语义等价性判定）。

### 4. 资源与算力
*   **硬件**：使用了一台配备 **NVIDIA GeForce RTX 4070 Laptop GPU (8GB VRAM)** 和 Intel Core i9-13900HX CPU 的笔记本电脑进行实验编排。
*   **模型与 API**：主要使用 **GPT-4o-mini** 作为骨干模型，并使用 **Qwen3-235B-Instruct**（2350亿参数的 MoE 模型）验证泛化性。
*   **消耗**：整个评估过程在 24 小时内完成，总计消耗约 **4 亿个 token**。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流长程记忆基准上进行了全面测试，涵盖了 1540 个（LoCoMo）和 728 个（RealTalk）测试问题。
*   **维度覆盖**：进行了不同门控策略（多数投票、共识机制、质量门控）的对比实验，以及针对不同问题难度（单跳 vs 多跳）的分类分析。
*   **客观性**：使用了确定性解码（Temperature=0）以确保可重复性，并对比了多种最先进的（SOTA）基准方法，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能优异**：D-Mem 在 LoCoMo 上达到了 53.5 的 F1 分数，恢复了“全量审议”上限性能的 **96.7%**。
*   **效率平衡**：相比于无脑执行 System 2，D-Mem 通过门控机制节省了约 **65% 的 token 消耗**和 **2/3 的推理延迟**。
*   **难度敏感性**：实验发现，问题越难（如时间推理、多跳推理），D-Mem 带来的增益越明显，证明了其处理复杂逻辑的必要性。
*   **失败分析**：静态检索失败的主因是“有损压缩”，例如将“昨天”压缩为具体日期时，若缺乏查询引导，往往会丢失计算相对时间所需的原始锚点。

### 7. 优点
*   **认知启发式架构**：成功将心理学中的双过程理论工程化，解决了效率与精度的矛盾。
*   **即插即用**：其质量门控逻辑可以适配不同的底层检索器和 LLM。
*   **缓解“迷失中段”**：通过分块审议而非一次性输入全量上下文，有效缓解了长文本模型对中间信息关注度下降的问题。

### 8. 不足与局限
*   **扩展性瓶颈**：虽然比全量扫描省钱，但 System 2 依然需要逐块扫描原始文本。对于“无限长”或极大规模的历史记录，线性扫描的成本依然不可忽视。
*   **跨块逻辑缺失**：目前的 System 2 是逐块独立提取事实，缺乏显式的跨块状态跟踪（State Tracking），可能难以处理跨度极大的因果链。
*   **模型依赖**：质量门控的效果高度依赖于骨干模型的判断能力，较弱的模型可能无法准确识别答案的缺陷。

（完）
