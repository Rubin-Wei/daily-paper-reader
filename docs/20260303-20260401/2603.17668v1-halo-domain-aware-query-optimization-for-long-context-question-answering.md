---
title: "Halo: Domain-Aware Query Optimization for Long-Context Question Answering"
title_zh: Halo：面向长上下文问答的领域感知查询优化
authors: "Pramod Chunduri, Francisco Romero, Ali Payani, Kexin Rong, Joy Arulraj"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17668v1"
tags: ["query:lcp"]
score: 9.0
evidence: 长上下文问答的领域感知查询优化
tldr: 针对长文本问答中大模型处理效率低、成本高且难以有效利用领域知识的问题，本文提出 Halo 框架。Halo 能够自动从用户提示中提取领域知识，并将其转化为可执行算子，应用于文档剪枝、块过滤和结果排序等多个阶段。实验表明，Halo 在金融、文学和科学数据集上显著提升了准确率，大幅降低了推理成本，使开源模型能以极低成本达到顶尖模型的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.17668v1/fig-001.webp\", \"caption\": \"Figure 1: VanillaLLM and RAG cannot utilize user’s domain knowledge effectively for long-context QA. Halo extracts this knowledge and systematically applies it in a multi-stage query optimization pipeline.\", \"page\": 2, \"index\": 1, \"width\": 504, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2603.17668v1/fig-002.webp\", \"caption\": \"Figure 2: Workflow of Halo: (1) the KnowledgeParser extracts domain directives from the user prompt, (2) the ContextSelector prunes structural elements using the Structural operator and identifies the top-𝑘 relevant chunks, (3) the InferenceEngine applies the Filter operator to discard irrelevant chunks and generates candidate responses on the remaining chunks, (4) the Verifier re-ranks the responses using Validate operator–demoting hallucinatory responses.\", \"page\": 3, \"index\": 2, \"width\": 944, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2603.17668v1/fig-003.webp\", \"caption\": \"Table 4: Dataset characteristics.\", \"page\": 9, \"index\": 3, \"width\": 490, \"height\": 638}, {\"url\": \"assets/figures/arxiv/2603.17668v1/fig-004.webp\", \"caption\": \"Table 3: Representative examples of domain knowledge used in our experiments. The domain knowledge column shows the natural language instructions a domain expert provides alongside the question.\", \"page\": 9, \"index\": 4, \"width\": 1056, \"height\": 196}]"
motivation: 现有的长文本问答系统无法系统地利用用户提示中的领域知识来优化查询处理，导致准确率受限且计算成本高昂。
method: Halo 将领域知识转化为可执行算子，在查询执行流水线的不同阶段分别进行文档剪枝、无关块过滤和候选答案排序。
result: "实验显示 Halo 相比基准方法提升了高达 13% 的准确率，降低了 4.8 倍成本，并让开源模型以 78 倍更低的成本接近顶尖模型表现。"
conclusion: 通过将领域知识系统化为可执行的查询优化算子，Halo 有效解决了长文本问答中的精度与成本平衡问题。
---

## 摘要
针对长文档的长上下文问答（QA）对于金融分析、法律审查和科学研究等应用至关重要。目前的方法，如通过单次大语言模型（LLM）调用处理整个文档或通过检索增强生成（RAG）检索相关分块，存在两个缺点：首先，随着上下文规模的增加，回答质量可能会下降，从而影响准确性；其次，迭代处理数百个输入文档可能会产生极高的 API 调用成本。为了提高回答质量并减少获得所需回答所需的迭代次数，用户倾向于在提示词中添加领域知识。然而，现有系统未能系统地捕获并利用这些知识来指导查询处理。领域知识被视为与文档并列的提示词标记（tokens）：LLM 可能会也可能不会遵循它，计算成本没有降低，且当输出错误时，用户必须手动迭代。我们提出了 Halo，这是一个长上下文问答框架，它能自动从用户提示词中提取领域知识，并将其作为可执行算子应用于多阶段查询执行流水线。Halo 识别了三种常见的领域知识形式——在文档的何处查找、忽略哪些内容以及如何验证答案——并将每种形式应用于最有效的流水线阶段：在分块选择前剪枝文档、在推理前过滤无关分块，以及在生成后对候选回答进行排序。为了处理不精确或无效的领域知识，Halo 包含一种回退机制，可在运行时检测低质量算子并选择性地禁用它们。我们在金融、文学和科学数据集上的评估表明，与基准模型相比，Halo 的准确率提高了多达 13%，成本降低了 4.8 倍，并使轻量级开源模型能够以 78 倍低的成本接近前沿 LLM 的准确率。

## Abstract
Long-context question answering (QA) over lengthy documents is critical for applications such as financial analysis, legal review, and scientific research. Current approaches, such as processing entire documents via a single LLM call or retrieving relevant chunks via RAG have two drawbacks: First, as context size increases, response quality can degrade, impacting accuracy. Second, iteratively processing hundreds of input documents can incur prohibitively high costs in API calls. To improve response quality and reduce the number of iterations needed to get the desired response, users tend to add domain knowledge to their prompts. However, existing systems fail to systematically capture and use this knowledge to guide query processing. Domain knowledge is treated as prompt tokens alongside the document: the LLM may or may not follow it, there is no reduction in computational cost, and when outputs are incorrect, users must manually iterate.   We present Halo, a long-context QA framework that automatically extracts domain knowledge from user prompts and applies it as executable operators across a multi-stage query execution pipeline. Halo identifies three common forms of domain knowledge - where in the document to look, what content to ignore, and how to verify the answer - and applies each at the pipeline stage where it is most effective: pruning the document before chunk selection, filtering irrelevant chunks before inference, and ranking candidate responses after generation. To handle imprecise or invalid domain knowledge, Halo includes a fallback mechanism that detects low-quality operators at runtime and selectively disables them. Our evaluation across finance, literature, and scientific datasets shows that Halo achieves up to 13% higher accuracy and 4.8x lower cost compared to baselines, and enables a lightweight open-source model to approach frontier LLM accuracy at 78x lower cost.

---

## 论文详细总结（自动生成）

### 论文总结：Halo - 面向长上下文问答的领域感知查询优化

#### 1. 核心问题与研究动机
长文档问答（Long-Context QA）在金融、法律和科研领域至关重要。目前主流方法存在两难困境：
*   **VanillaLLM（全文本输入）：** 成本极高（处理数百份文档需数百美元），且存在“迷失中段”（Lost in the Middle）现象，导致准确率随上下文增加而下降。
*   **RAG（检索增强生成）：** 虽然降低了成本，但存在检索错误（Top-K 选错分块）和上下文不连贯导致的幻觉问题。
*   **领域知识利用不足：** 专家通常在提示词（Prompt）中加入领域知识（如“只看表格”、“忽略法律免责声明”），但现有系统仅将其视为普通 Token，模型往往不遵循这些指令，且无法通过这些知识减少计算开销。

#### 2. 方法论：核心思想与技术细节
Halo 的核心思想是将用户提示词中的非结构化领域知识提取为**结构化指令**，并将其转化为**可执行算子**，嵌入到多阶段查询流水线中。

*   **知识提取（KnowledgeParser）：** 使用 LLM 将提示词解析为三类指令：
    *   **S (Structural)：** 在哪看（如：表格、MD&A 章节）。
    *   **F (Filter)：** 忽略什么（如：风险披露、样板文本）。
    *   **V (Validate)：** 如何验证（如：要“稀释后每股收益”，不要“基本每股收益”）。
*   **多阶段执行流水线：**
    1.  **ContextSelector（结构剪枝）：** 应用 `Structural` 算子，根据 S 指令对原始文档进行物理剪枝（如只保留表格），大幅缩小搜索空间。
    2.  **InferenceEngine（语义过滤）：** 使用轻量级小模型（SLM）作为过滤器，应用 `Filter` 算子剔除 Top-K 中无关的分块，仅将剩余分块传给昂贵的 LLM。
    3.  **Verifier（响应排序）：** 应用 `Validate` 算子，通过计算候选答案与正向/负向约束的嵌入向量相似度进行重排序，抑制幻觉。
*   **回退机制（Fallback）：** 运行时检测算子质量。若所有分块均返回“无法回答”（上下文丢失）或排序分差过小（无效约束），则自动禁用算子并回退到基准模式。

#### 3. 实验设计
*   **数据集：**
    *   **DocFinQA：** 金融领域，平均 193K Token，侧重数值推理。
    *   **NovelQA：** 文学领域，平均 230K Token，侧重长篇叙事理解。
    *   **QASPER：** 科学论文，平均 7K Token，侧重信息寻求。
*   **基准方法（Benchmarks）：**
    *   VanillaLLM（全文本输入）及其领域知识增强版（+DK）。
    *   RAG（标准检索）及其领域知识增强版（+DK）。
    *   Chain-of-Agents (CoA)：多智能体协作处理长文本。
    *   Halo (No DK)：不带领域算子的 Halo 基础流水线。
*   **模型：** 闭源使用 Claude Sonnet 4.5，开源使用 Qwen3-30B（主模型）和 Qwen3-4B（过滤模型）。

#### 4. 资源与算力
*   **硬件环境：** 论文提到开源模型（Qwen3-30B/4B）托管在本地，使用 **vLLM** 推理框架。
*   **具体算力：** 文中**未明确说明**具体的 GPU 型号（如 A100/H100）及数量。
*   **成本计算：** 实验结果以 AWS Bedrock 的 API 定价为基准进行折算。

#### 5. 实验数量与充分性
*   **实验规模：** 在三个不同领域的代表性数据集上进行了测试，每组包含约 100 个复杂查询。
*   **消融实验：** 详细分析了 Structural、Filter、Validate 三个算子各自对准确率和成本的贡献。
*   **鲁棒性测试：** 专门设计了“低质量/错误领域知识”场景，验证回退机制的有效性。
*   **评价：** 实验设计较为充分，对比了当前最先进的闭源和开源模型，涵盖了成本、准确率、MRR 排序质量等多个维度，具有较强的客观性。

#### 6. 主要结论与发现
*   **性能提升：** Halo 相比基准方法准确率提升高达 13%，成本降低 4.8 倍。
*   **以小博大：** 使用 Halo 优化的 Qwen3-30B（开源）在准确率上接近未经优化的 Claude Sonnet 4.5（顶尖闭源），但**成本低 78 倍**。
*   **算子开销极低：** 领域知识提取和算子执行的额外开销仅占总查询成本的 2% 左右，却能带来巨大的推理节省。
*   **系统化优于提示词工程：** 简单在提示词中加入知识（+DK）有时反而会降低弱模型的表现，而 Halo 的算子化方法更稳健。

#### 7. 优点
*   **创新性：** 将数据库查询优化的思想（算子化、剪枝、过滤）引入 LLM 长文本处理。
*   **实用性：** 自动从自然语言提示词中提取指令，无需用户学习复杂的 API 或编程。
*   **高效性：** 通过模型级联（SLM 过滤 + LLM 生成）显著降低了长文档处理的 API 账单。
*   **鲁棒性：** 引入了回退机制，解决了领域知识不精确可能导致的负面影响。

#### 8. 不足与局限
*   **预处理依赖：** 依赖于高质量的文档解析（如 PDF 转结构化标签），虽然成本可摊销，但解析错误会影响后续算子。
*   **领域知识来源：** 实验中的领域知识是基于数据集元数据人工构建的，实际应用中用户能否提供高质量的指令仍有待观察。
*   **应用限制：** 在叙事性极强、缺乏明显结构（如散文）的文档中，`Structural` 算子的增益会大幅减弱。
*   **实时性：** 虽然降低了 Token 成本，但多阶段处理（解析、过滤、生成、排序）可能会增加端到端的首字延迟。

（完）
