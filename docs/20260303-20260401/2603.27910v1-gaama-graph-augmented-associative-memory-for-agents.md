---
title: "GAAMA: Graph Augmented Associative Memory for Agents"
title_zh: GAAMA：面向智能体的图增强关联记忆
authors: "Swarna Kamal Paul, Shubhendu Sharma, Nitin Sareen"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27910v1"
tags: ["query:am"]
score: 10.0
evidence: 智能体的图增强关联记忆
tldr: GAAMA 是一种为 AI 智能体设计的图增强关联记忆系统，旨在解决多会话交互中长期记忆的结构化关联问题。它通过 LLM 提取原子事实、主题概念和高阶反思，构建包含四种节点类型的层次化知识图谱。检索时结合了语义相似度与个性化 PageRank 算法，有效提升了跨会话的上下文关联能力。在 LoCoMo-10 基准测试中，GAAMA 显著优于 RAG 和 HippoRAG 等基准模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27910v1/fig-001.webp\", \"caption\": \"Table 7: Heatmap of reward delta (pp): PPR=0.1 − Semantic, per conversation and category.\", \"page\": 10, \"index\": 1, \"width\": 629, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2603.27910v1/fig-002.webp\", \"caption\": \"Table 1: GAAMA graph schema: four node types connected by five structural edge types.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2603.27910v1/fig-003.webp\", \"caption\": \"Table 6: Per-conversation overall reward (%) for Semantic vs. PPR=0.1. ∆ = PPR=0.1 − Semantic (pp).\", \"page\": 9, \"index\": 3, \"width\": 527, \"height\": 276}]"
motivation: 现有的 RAG 和向量检索方法难以捕捉多会话对话中的结构化关联和层次化推理需求。
method: 构建一个包含片段、事实、反思和概念四类节点的层次化知识图谱，并结合语义搜索与个性化 PageRank 进行混合检索。
result: "在 LoCoMo-10 基准测试中，GAAMA 达到了 78.9% 的平均奖励，优于 RAG (75.0%) 和 HippoRAG (69.9%)。"
conclusion: 通过图结构增强关联记忆并结合拓扑排序检索，能显著提升 AI 智能体在长程多会话任务中的记忆检索质量。
---

## 摘要
在多个会话中与用户交互的 AI 智能体需要持久的长期记忆，以维持连贯且个性化的行为。目前的方法要么依赖于扁平的检索增强生成 (RAG)，这会丢失记忆之间的结构关系；要么使用记忆压缩和向量检索，无法捕捉多会话对话的关联结构。虽然文献中提出了一些基于图的技术，但它们仍面临枢纽节点主导检索以及对演化记忆的分层推理能力不足的问题。我们提出了 GAAMA，一种图增强关联记忆系统，通过三步流水线构建以概念为中介的分层知识图谱：(1) 从原始对话中逐字保留片段 (episode)；(2) 基于大语言模型 (LLM) 提取原子事实和主题级概念节点；(3) 合成高阶反思 (reflection)。生成的图包含四种节点类型（片段、事实、反思、概念），由五种结构化边类型连接，其中概念节点提供了补充语义相似性的横切遍历路径。检索过程通过加法评分函数，将基于余弦相似度的 k-最近邻搜索与感知边类型的个性化 PageRank (PPR) 相结合。在 LoCoMo-10 基准测试（涵盖 10 个多会话对话中的 1,540 个问题）上，GAAMA 达到了 78.9% 的平均奖励，优于经过调优的 RAG 基准 (75.0%)、HippoRAG (69.9%)、A-Mem (47.2%) 和 Nemori (52.1%)。消融分析表明，将基于图遍历的排序（个性化 PageRank）与语义搜索相结合，相比于纯图节点语义搜索，性能得到了持续提升（整体提升 1.0 个百分点）。

## Abstract
AI agents that interact with users across multiple sessions require persistent long-term memory to maintain coherent, personalized behavior. Current approaches either rely on flat retrieval-augmented generation (RAG), which loses structural relationships between memories, or use memory compression and vector retrieval that cannot capture the associative structure of multi-session conversations. There are few graph based techniques proposed in the literature, however they still suffer from hub dominated retrieval and poor hierarchical reasoning over evolving memory. We propose GAAMA, a graph-augmented associative memory system that constructs a concept-mediated hierarchical knowledge graph through a three-step pipeline: (1)~verbatim episode preservation from raw conversations, (2)~LLM-based extraction of atomic facts and topic-level concept nodes, and (3)~synthesis of higher-order reflections. The resulting graph uses four node types (episode, fact, reflection, concept) connected by five structural edge types, with concept nodes providing cross-cutting traversal paths that complement semantic similarity. Retrieval combines cosine-similarity-based $k$-nearest neighbor search with edge-type-aware Personalized PageRank (PPR) through an additive scoring function. On the LoCoMo-10 benchmark (1,540 questions across 10 multi-session conversations), GAAMA achieves 78.9\% mean reward, outperforming a tuned RAG baseline (75.0\%), HippoRAG (69.9\%), A-Mem (47.2\%), and Nemori (52.1\%). Ablation analysis shows that augmenting graph-traversal-based ranking (Personalized PageRank) with semantic search consistently improves over pure semantic search on graph nodes (+1.0 percentage point overall).

---

## 论文详细总结（自动生成）

### GAAMA: 面向智能体的图增强关联记忆系统论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着 AI 智能体在多会话（Multi-session）场景下的应用增加，如何构建具备长期、连贯且个性化能力的记忆系统成为关键。现有的方法存在以下局限：
*   **扁平 RAG：** 仅依赖向量相似度检索，丢失了实体、事件和事实之间的结构化关联。
*   **全上下文输入：** 扩展上下文窗口成本极高，且难以处理无限增长的会话历史。
*   **现有图方法（如 HippoRAG）：** 往往以实体为中心，导致在多会话中产生“超级枢纽”节点（如“用户”节点连接数过多），稀释了检索精度，且缺乏对记忆演化的层次化推理。

#### 2. 方法论：核心思想与关键技术
GAAMA 提出了一种**以概念为中介的分层知识图谱**，通过三步流水线构建记忆：
*   **三步构建流水线：**
    1.  **片段保留（Episode Preservation）：** 逐字存储对话回合，保留原始时间戳和相对时间引用（如“上周”），无需 LLM 参与。
    2.  **事实与概念提取（Fact & Concept Extraction）：** 使用 LLM 提取原子事实（Atomic Facts）和主题级概念标签（Concepts，如 `pottery_hobby`）。概念节点取代了实体节点，避免了枢纽节点问题。
    3.  **反思合成（Reflection Synthesis）：** LLM 跨多个事实合成高阶洞察（如用户的偏好模式）。
*   **图模式（Schema）：** 包含 4 种节点（片段、事实、反思、概念）和 5 种边（NEXT、DERIVED_FROM 等）。
*   **混合检索算法：**
    *   **种子选择：** 通过 KNN 选取前 $k$ 个相似节点作为个性化 PageRank (PPR) 的种子。
    *   **感知边类型的 PPR：** 在局部子图上运行 PPR，对不同类型的边赋予不同权重，并引入**枢纽抑制（Hub Dampening）**机制。
    *   **加法评分函数：** 最终得分 = $w_{sim} \cdot \text{Similarity} + w_{ppr} \cdot \text{PPR\_Score}$。其中 $w_{ppr}$ 设为 0.1，旨在让图结构辅助而非主导检索。

#### 3. 实验设计
*   **数据集：** 使用 **LoCoMo-10** 基准测试，包含 10 个长程多会话对话，共 1,540 个问题。
*   **问题分类：** 涵盖多跳推理（Cat1）、时间推理（Cat2）、开放域（Cat3）和单跳检索（Cat4）。
*   **对比方法：**
    *   **A-Mem：** 基于 Zettelkasten 笔记系统的智能体记忆。
    *   **Nemori：** 受认知科学启发的自组织记忆。
    *   **HippoRAG：** 经典的基于实体的图检索系统。
    *   **RAG Baseline：** 经过调优的扁平向量检索系统。
*   **评估指标：** 采用 LLM-as-judge 评分（GPT-4o-mini），根据生成答案对参考答案中关键事实的覆盖率（0.0-1.0）计算平均奖励。

#### 4. 资源与算力
*   **模型使用：** 全文使用 **GPT-4o-mini** 作为提取、生成和评估的底层模型；使用 `text-embedding-3-small` 生成向量。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于该方法主要依赖 API 调用和图算法（PPR），其核心开销在于 LLM API 的 Token 消耗。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 1,540 个问题进行了详尽测试，样本量较大，覆盖了多种推理类型。
*   **消融实验：** 专门对比了“纯语义检索”与“PPR 增强检索”，验证了图结构带来的增量收益。
*   **公平性：** 所有基准测试均使用相同的 LLM（GPT-4o-mini）和相同的上下文预算（1,000 词），确保了检索质量是性能差异的唯一变量。实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先：** GAAMA 整体奖励达到 **78.9%**，显著优于 RAG (75.0%) 和 HippoRAG (69.9%)。
*   **时间推理优势：** 在时间类问题上，GAAMA 比 RAG 高出 **12.9%**，归功于其对原始片段的保留和 NEXT 边的时序连接。
*   **图结构的价值：** 消融实验显示，即使在已构建的结构化记忆上，加入 PPR 仍能带来 1.0% 的整体提升，尤其在单跳和时间问题上效果明显。
*   **轻量化 PPR：** 发现低权重的 PPR（0.1）效果优于高权重，说明图结构应作为语义相似性的补充而非替代。

#### 7. 优点
*   **层次化架构：** 将原始对话、原子事实和高阶反思解耦，既保留了细节又具备了抽象能力。
*   **概念中介设计：** 巧妙地解决了实体中心化导致的枢纽节点扩散问题，使图结构更稀疏、检索更精准。
*   **三步流水线：** 区分了昂贵的 LLM 提取和廉价的结构化操作，具有较好的工程落地性。

#### 8. 不足与局限
*   **概念冗余：** LLM 提取的概念存在单复数不一、近义词重复（如 `outdoor_adventure` 与 `outdoor_adventures`）的问题，导致图结构碎片化。
*   **泛化概念：** 部分概念过于宽泛（如 `personal_growth`），降低了路径的区分度。
*   **依赖 LLM 质量：** 记忆的准确性高度依赖于第一步的事实提取质量，若提取阶段出错，后续检索难以纠正。
*   **动态演化：** 虽然支持增量构建，但对于记忆的长期遗忘机制或冲突解决机制讨论较少。

（完）
