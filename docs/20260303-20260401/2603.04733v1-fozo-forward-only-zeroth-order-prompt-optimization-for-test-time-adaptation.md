---
title: "FOZO: Forward-Only Zeroth-Order Prompt Optimization for Test-Time Adaptation"
title_zh: FOZO：面向测试时自适应的纯前向零阶提示优化
authors: "Xingyu Wang, Tao Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04733v1"
tags: ["query:ttt"]
score: 10.0
evidence: 用于测试时自适应的前向零阶优化
tldr: 针对测试时自适应（TTA）中反向传播计算开销大且修改模型权重的问题，本文提出FOZO框架。FOZO采用仅前向传播的零阶优化方法，通过优化中间特征统计量和预测熵来调整提示词。该方法引入动态衰减扰动尺度以确保稳定性，在多个ImageNet变体数据集上超越了主流梯度方法及SOTA前向方法，且在量化模型上表现优异，适合资源受限场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.04733v1/fig-001.webp\", \"caption\": \"Figure 1. Convergence Curves of Forward-Only Test-Time Adaptation Algorithms. Average ACC@1 (%) of various Test-Time Adaptation (TTA) methods on ImageNet-C (level 5) versus adaptation time (s). Per original settings, FOA and ZOA use 28 forward propagation (FP), while our FOZO method employs 26 FP. FOZO (Dynamic) consistently surpasses FOZO (Base) in accuracy, demonstrating the effectiveness of our dynamic perturbation strategy. Furthermore, FOZO achieves superior performance and faster convergence than FOA and ZOA. Notably, FOZO reaches 65% ACC@1 in only 66% of the runtime required by FOA and ZOA, respectively.\", \"page\": 1, \"index\": 1, \"width\": 480, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.04733v1/fig-002.webp\", \"caption\": \"Table 1. Continual adaptation results: Comparison with forward-only methods on ImageNet-C (5k, level 5). FP denote the number of forward propagations per single sample. For a fair comparison with the same number of forward propagations, and referencing ZOA’s reproduced results, FOA’s population size K is set to 2, FOZO’s number of SPSA is set to 1, and ZOA’s steps is set to 1. Time (s) is measured for processing ImageNet-C (5k, level 5) on an NVIDIA RTX 4090 GPU. Memory usage (MiB) is the peak GPU memory allocated by tensors, measured on a single adaptation batch. #Params is the number of parameters updated during TTA.\", \"page\": 6, \"index\": 2, \"width\": 1048, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2603.04733v1/fig-003.webp\", \"caption\": \"Table 3. Comparison with the forward-only methods on quantized ViT models (8bit PTQ4ViT).\", \"page\": 7, \"index\": 3, \"width\": 1014, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2603.04733v1/fig-004.webp\", \"caption\": \"Table 2. Continual adaptation results: Comparison with backpropagation methods on ImageNet-C (5k, level 5). When FP=28, ZOA’s steps is set to 14, and FOA’s population size K is 28. For FOZO, the number of SPSA n is set to 13 when FP=26, and 14 when FP=28.\", \"page\": 7, \"index\": 4, \"width\": 1036, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2603.04733v1/fig-005.webp\", \"caption\": \"Figure 3. Ablation Study of FOZO Hyperparameters on ImageNet-C (Gaussian Noise, level 5).\", \"page\": 8, \"index\": 5, \"width\": 454, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2603.04733v1/fig-006.webp\", \"caption\": \"Figure 2. Overview of our proposed FOZO. (a) Forward-Only Zeroth-Order Optimization. This diagram illustrates how FOZO adaptively updates learnable visual prompts (P) at test time using a zeroth-order gradient estimation. For each test batch, the model performs two forward passes by perturbing the prompt P in positive (P + ϵtZ) and negative (P − ϵtZ) directions. Here, Z is a perturbation vector generated from a random seed s, and ϵt is a dynamically adjusted perturbation step size. (b) Deep-Shallow Aligning Loss Function. We compute the mean (µT ) and standard deviation (σT ) of the [CLS] token activations by grouping shallow (e01, . . . , e0N/2) and deep (e0N/2+1, . . . , e 0 N ) layers of the model, and align them with pre-computed source domain statistics (µS , σS).\", \"page\": 4, \"index\": 6, \"width\": 950, \"height\": 431}]"
motivation: 现有的TTA方法在低端设备上因反向传播的高内存需求和模型权重修改而受限，而传统无反向传播技术自适应能力不足。
method: 提出一种仅前向传播的零阶提示词优化范式，利用中间特征统计和预测熵作为目标，并引入动态衰减的扰动尺度。
result: "在ImageNet-C等多个基准测试中，FOZO的准确率达到59.52%，显著优于主流梯度方法和最先进的前向方法FOA。"
conclusion: FOZO证明了零阶优化在测试时自适应中的高效性与稳定性，是资源受限环境下部署TTA的极具竞争力的方案。
---

## 摘要
测试时自适应（TTA）对于使深度学习模型能够处理现实世界的数据分布偏移至关重要。然而，当前的方法面临显著的局限性：基于反向传播的方法由于其高计算和内存需求，以及在自适应过程中倾向于修改模型权重，不适用于低端部署设备；而传统的无反向传播技术则表现出受限的自适应能力。在这项工作中，我们提出了纯前向零阶优化（FOZO），这是一种用于 TTA 的新型且实用的无反向传播范式。FOZO 利用内存高效的零阶提示优化，该优化由同时优化中间特征统计量和预测熵的目标引导。为了确保在分布外数据流上进行高效且稳定的自适应，我们在零阶梯度估计过程中引入了动态衰减的扰动尺度，并在 TTA 数据流假设下理论证明了其收敛性。在 ImageNet-C、ImageNet-R 和 ImageNet-Sketch 上进行的大量持续自适应实验证明了 FOZO 的卓越性能，在 ImageNet-C（5K，5级）上实现了 59.52% 的 Top-1 准确率，优于主要的基于梯度的方法和最先进的（SOTA）纯前向方法 FOA（58.13%）。此外，FOZO 在量化（INT8）模型上表现出强大的泛化能力。这些发现表明，FOZO 是资源受限场景下 TTA 部署的一个极具竞争力的解决方案。

## Abstract
Test-Time Adaptation (TTA) is essential for enabling deep learning models to handle real-world data distribution shifts. However, current approaches face significant limitations: backpropagation-based methods are not suitable for low-end deployment devices, due to their high computation and memory requirements, as well as their tendency to modify model weights during adaptation; while traditional backpropagation-free techniques exhibit constrained adaptation capabilities. In this work, we propose Forward-Only Zeroth-Order Optimization (FOZO), a novel and practical backpropagation-free paradigm for TTA. FOZO leverages a memory-efficient zeroth-order prompt optimization, which is led by objectives optimizing both intermediate feature statistics and prediction entropy. To ensure efficient and stable adaptation over the out-of-distribution data stream, we introduce a dynamically decaying perturbation scale during zeroth-order gradient estimation and theoretically prove its convergence under the TTA data stream assumption. Extensive continual adaptation experiments on ImageNet-C, ImageNet-R, and ImageNet-Sketch demonstrate FOZO's superior performance, achieving 59.52% Top-1 accuracy on ImageNet-C (5K, level 5) and outperforming main gradient-based methods and SOTA forward-only FOA (58.13%). Furthermore, FOZO exhibits strong generalization on quantized (INT8) models. These findings demonstrate that FOZO is a highly competitive solution for TTA deployment in resource-limited scenarios.

---

## 论文详细总结（自动生成）

### FOZO: 面向测试时自适应的纯前向零阶提示优化论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
深度学习模型在实际部署中常面临训练与测试数据分布不一致（分布偏移）的问题。**测试时自适应（TTA）**旨在利用无标签测试数据动态调整模型。然而，现有方法存在两大痛点：
*   **反向传播（BP）开销大：** 基于梯度的 TTA 方法（如 TENT）需要大量的计算资源和内存，且难以在模型权重固定的硬件（如 FPGA、量化模型）上运行。
*   **纯前向方法性能受限：** 现有的无反向传播方法（如 FOA）虽然内存友好，但由于采用进化策略（CMA-ES）等算法，在处理高维提示词（Prompt）优化时收敛缓慢且精度不足。
**FOZO 的核心目标**是提出一种既不需要反向传播、又能高效稳定优化提示词的 TTA 范式，以适应资源受限的边缘设备。

#### 2. 方法论：核心思想与关键技术
FOZO 提出了一种**纯前向零阶优化（Forward-Only Zeroth-Order）**框架，其核心思想是通过两次前向传播估计梯度，从而更新输入层的可学习提示词（Visual Prompts），而保持模型主体权重冻结。

*   **零阶梯度估计（SPSA）：** 利用同时扰动随机逼近（SPSA）算法，通过在提示词空间施加随机扰动 $Z$，计算扰动前后的损失差值来估计梯度方向，完全避开了复杂的链式求导。
*   **动态扰动方案（Dynamic Perturbation）：** 为了平衡探索（防止陷入局部最优）与收敛（提高精度），引入了动态衰减的扰动尺度 $\epsilon_t$。当检测到损失波动剧烈（暗示领域切换）时重置扰动，在稳定阶段则按比例衰减。
*   **深浅层对齐损失函数：** 
    *   **特征统计对齐：** 分别提取 ViT 模型浅层和深层的 [CLS] 标记激活值，计算其均值和方差，并与源域统计量进行对齐。
    *   **预测熵最小化：** 鼓励模型对目标域样本做出更确定的预测。
*   **理论保证：** 论文在“局部 r-有效秩”假设下，证明了该零阶优化方法在 TTA 数据流中的收敛性，指出其收敛速度取决于内在维度而非参数总维度。

#### 3. 实验设计
*   **数据集：** ImageNet-C（常见损坏）、ImageNet-R（艺术风格）、ImageNet-Sketch（草图）。
*   **实验场景：** 持续自适应（Continual Adaptation，不重置模型处理连续偏移）、混合偏移（Mixed Shifts）、量化模型自适应（INT8）。
*   **对比方法：**
    *   **无梯度/纯前向：** LAME, T3A, FOA, ZOA。
    *   **基于反向传播：** TENT, EATA, SAR, DeYO。
*   **Benchmark：** 以 ViT-Base 为骨干网络，遵循 RobustBench 标准。

#### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA RTX 4090 GPU** 上进行。
*   **效率指标：** 论文明确对比了内存占用（Memory）和运行时间（Time）。
    *   FOZO 在单次自适应中的内存占用约为 831 MiB，远低于基于 BP 方法的 5495 MiB。
    *   在处理 ImageNet-C (5k) 任务时，FOZO 的运行时间（179s）显著优于同类前向方法 FOA（224s）和 ZOA（198s）。
*   **参数量：** 仅更新 2304 个提示词参数，远少于更新 LayerNorm 参数的方法。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度验证：** 涵盖了 15 种损坏类型、5 个严重等级，以及跨域泛化（R/Sketch）。
*   **消融实验：** 详细分析了零阶优化、特征对齐、动态扰动各组件的贡献，并探讨了提示词数量、Batch Size 对性能的影响。
*   **公平对比：** 在相同的“前向传播次数（FP）”预算下对比了 FOA、ZOA 和 FOZO，确保了效率对比的公正性。
*   **收敛曲线：** 提供了随时间变化的准确率曲线，直观展示了收敛速度。

#### 6. 主要结论与发现
*   **性能领先：** FOZO 在 ImageNet-C 上达到 59.52% 的准确率，超越了 SOTA 纯前向方法 FOA，甚至在某些指标上接近或超过了基于反向传播的方法。
*   **收敛极快：** FOZO 达到 65% 准确率所需的时间仅为 FOA 和 ZOA 的 66%。
*   **量化友好：** 在 INT8 量化模型上，FOZO 依然能显著提升性能，证明了其在实际部署中的鲁棒性。
*   **动态扰动的必要性：** 实验证实动态调整扰动尺度能有效帮助模型跳出局部最优并最终稳定收敛。

#### 7. 优点
*   **极高的部署价值：** 纯前向特性使其完美适配无法求导的黑盒模型、量化模型及低功耗硬件。
*   **理论与实践结合：** 不仅提出了算法，还给出了严谨的收敛性证明。
*   **内存效率极高：** 相比传统 TTA 方法，内存需求降低了数倍，且无需存储中间激活值。

#### 8. 不足与局限
*   **前向次数仍多于单次推理：** 虽然不需要反向传播，但为了估计梯度，每个 Batch 仍需进行至少 2 次（甚至更多，如 26 次）前向传播，这在极高实时性要求的场景下仍有优化空间。
*   **超参数敏感性：** 动态扰动的重置阈值 $\tau$ 和衰减因子 $\alpha$ 可能需要针对不同任务进行微调。
*   **模型架构限制：** 实验主要集中在 Vision Transformer (ViT) 上，对于 CNN 等其他架构的普适性虽有理论支持，但缺乏大规模实验验证。

（完）
