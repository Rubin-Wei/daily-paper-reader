---
title: "MDER-DR: Multi-Hop Question Answering with Entity-Centric Summaries"
title_zh: MDER-DR：基于以实体为中心摘要的多跳问答
authors: "Riccardo Campi, Nicolò Oreste Pinciroli Vago, Mathyas Giudici, Marco Brambilla, Piero Fraternali"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11223v1"
tags: ["query:rag"]
score: 9.0
evidence: 基于知识图谱索引和检索的多跳问答框架
tldr: 针对知识图谱增强生成（RAG）在多跳问答中因文本简化为三元组而丢失上下文的问题，本文提出MDER-DR框架。MDER通过生成实体中心摘要和上下文三元组描述来优化索引，DR则通过分解查询并迭代推理进行检索。该方法在多项基准测试中显著提升了性能，尤其在处理复杂关系和跨语言任务时表现优异，为复杂关系数据的问答提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.11223v1/fig-001.webp\", \"caption\": \"Figure 4. Performance evaluation across different datasets and metrics. Results are shown for WikiQA, HotpotQA, and BenchEE datasets using the scores obtained with LLM-as-a-Judge and Soft EM, comparing language match vs. language mismatch conditions.\", \"page\": 11, \"index\": 1, \"width\": 1017, \"height\": 1134}, {\"url\": \"assets/figures/arxiv/2603.11223v1/fig-002.webp\", \"caption\": \"Table 1. Evaluation scores on WikiQA, HotpotQA, and BenchEE grouped by evaluation method and language match/mismatch. ∆(%) indicates the percentage improvement in the score of MDER-DR compared with the best baseline.\", \"page\": 7, \"index\": 2, \"width\": 1025, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2603.11223v1/fig-003.webp\", \"caption\": \"Figure 2. Indexing pipeline including the Map-Disambiguate-Enrich-Reduce (MDER) workflow. It begins with preprocessing, where input documents are segmented, summarized, and translated. The core pipeline then proceeds through: (1) Map extracts subject-predicate-object triples; (2) Disambiguate unifies fragmented or redundant entities; (3) Enrich adds contextual descriptions to triples; and (4) Reduce generates entity-centric summaries. The final output is a structured graph of interconnected document, chunk, entity, relationship, and triple nodes, optimized for downstream KG tasks, built upon a domain-agnostic RDF/OWL ontology.\", \"page\": 3, \"index\": 3, \"width\": 988, \"height\": 266}]"
motivation: 传统的知识图谱RAG在将文本转化为三元组时会丢失关键上下文，导致多跳问答性能下降。
method: 提出MDER索引法生成实体摘要，并结合DR检索机制将复杂查询分解为三元组进行迭代推理。
result: "在标准和特定领域基准测试中，该方法比传统RAG基准提升了高达66%，并展现出跨语言鲁棒性。"
conclusion: MDER-DR通过结合实体中心摘要与分解推理，有效解决了稀疏和复杂关系数据下的多跳问答难题。
---

## 摘要
基于知识图谱（KG）的检索增强生成（RAG）面临一个挑战，即当文本被简化为三元组时，索引方法可能会丢失重要的上下文细微差别，从而降低下游问答（QA）任务的性能，特别是对于需要从多个实体、事实或关系中组合答案的多跳问答。我们提出了一种与领域无关的、基于知识图谱的问答框架，涵盖了索引和检索/推理阶段。一种名为“映射-消歧-丰富-归约”（MDER）的新型索引方法生成源自上下文的三元组描述，并随后将其与实体级摘要集成，从而避免了在问答检索阶段显式遍历图中的边。作为补充，我们引入了“分解-解决”（DR），这是一种检索机制，它将用户查询分解为可解决的三元组，并通过迭代推理将其在知识图谱中进行落地。MDER 和 DR 共同构成了一个由大语言模型（LLM）驱动的问答流水线，该流水线对稀疏、不完整和复杂的关联数据具有鲁棒性。实验表明，在标准和特定领域的基准测试中，MDER-DR 相比标准 RAG 基准实现了显著改进（最高达 66%），同时保持了跨语言的鲁棒性。我们的代码可在 https://github.com/DataSciencePolimi/MDER-DR_RAG 获取。

## Abstract
Retrieval-Augmented Generation (RAG) over Knowledge Graphs (KGs) suffers from the fact that indexing approaches may lose important contextual nuance when text is reduced to triples, thereby degrading performance in downstream Question-Answering (QA) tasks, particularly for multi-hop QA, which requires composing answers from multiple entities, facts, or relations. We propose a domain-agnostic, KG-based QA framework that covers both the indexing and retrieval/inference phases. A new indexing approach called Map-Disambiguate-Enrich-Reduce (MDER) generates context-derived triple descriptions and subsequently integrates them with entity-level summaries, thus avoiding the need for explicit traversal of edges in the graph during the QA retrieval phase. Complementing this, we introduce Decompose-Resolve (DR), a retrieval mechanism that decomposes user queries into resolvable triples and grounds them in the KG via iterative reasoning. Together, MDER and DR form an LLM-driven QA pipeline that is robust to sparse, incomplete, and complex relational data. Experiments show that on standard and domain specific benchmarks, MDER-DR achieves substantial improvements over standard RAG baselines (up to 66%), while maintaining cross-lingual robustness. Our code is available at https://github.com/DataSciencePolimi/MDER-DR_RAG.

---

## 论文详细总结（自动生成）

### MDER-DR：基于以实体为中心摘要的多跳问答论文总结

#### 1. 论文的核心问题与整体含义
论文针对**知识图谱检索增强生成（GraphRAG）**在处理**多跳问答（Multi-hop QA）**时的局限性提出了解决方案。
*   **研究动机**：传统的知识图谱索引将非结构化文本简化为原子化的“主-谓-宾”三元组，这一过程是“有损”的，会丢失诸如适用范围、前提条件、时间约束等关键上下文细微差别。
*   **核心挑战**：在查询阶段，现有的多跳推理通常依赖复杂的图遍历（Graph Traversal），这在图数据稀疏或不完整时表现较差，且计算开销大。

#### 2. 论文提出的方法论
论文提出了 **MDER-DR** 框架，将复杂关系的组合从“查询阶段”提前到了“索引阶段”。

*   **MDER 索引机制（Map-Disambiguate-Enrich-Reduce）**：
    1.  **Map（映射）**：从文本块中提取细粒度的三元组。
    2.  **Disambiguate（消歧）**：通过语义相似度和 LLM 判断，统一同义或缩写的实体（如“EU”与“European Union”）。
    3.  **Enrich（丰富）**：回溯原文，为每个三元组生成一段自然语言描述，保留上下文（如某项技术的发明时间及背景）。
    4.  **Reduce（归约）**：将与某一实体相关的所有三元组描述聚合，生成**以实体为中心的摘要（Entity-centric Summaries）**。这使得多跳信息被压缩在单个实体的描述中。

*   **DR 检索机制（Decompose-Resolve）**：
    1.  **Decompose（分解）**：将用户复杂问题分解为带占位符的结构化三元组（如“谁是 X 的作者？” -> `(X, HAS_AUTHOR, Y)`）。
    2.  **Resolve（解决）**：迭代地在知识图谱中检索实体并填充占位符。由于实体摘要已包含多跳信息，系统通过直接查找而非显式图遍历即可获取关联证据。
    3.  **Response Generation**：基于检索到的摘要合成最终答案。

#### 3. 实验设计
*   **数据集**：
    *   **WikiQA**：基于维基百科的事实性问答。
    *   **HotpotQA**：标准的多跳推理基准测试。
    *   **BenchEE**：能源效率领域的专业数据集（包含101个专家级QA对及相关文档）。
*   **对比基准（Baselines）**：
    *   **LLM-only**：不使用 RAG 的纯模型生成（如 ChatGPT）。
    *   **Vector-RAG**：基于向量相似度的标准 RAG。
    *   **GraphRAG**：微软提出的默认参数下的图 RAG 框架。
*   **评估指标**：LLM-as-a-Judge（0-10分）、Soft Exact Match（软精确匹配）、人工专家评估（针对专业领域）。

#### 4. 资源与算力
*   **硬件配置**：1 台 NVIDIA RTX PRO 6000 Blackwell GPU (96 GB VRAM)，48 核 Intel Xeon 处理器，125 GB 内存。
*   **耗时**：
    *   **索引阶段**：每个基准数据集约需 **1.5 天**（由于涉及多次 LLM 调用进行摘要生成）。
    *   **推理阶段**：生成一个答案平均耗时约 **5 秒**。
*   **模型使用**：采用了 `gpt-oss:120b` 和 `gpt-oss:20b` 开源大模型，嵌入模型使用 `mxbai-embed-large`。

#### 5. 实验数量与充分性
*   **实验规模**：从每个标准数据集中随机抽取 500 个 QA 对进行测试。
*   **多样性**：涵盖了**跨语言场景**（英语、意大利语、法语、西班牙语），测试了查询语言与索引语言匹配及不匹配（Mismatch）的情况。
*   **客观性**：使用了**配对自助法（Paired Bootstrapping）**进行 10,000 次重采样，以确保结果的统计显著性。同时，在专业领域引入了人类专家进行双盲评估，并证明 LLM 评分与人类评分具有高度相关性（Pearson 相关系数约 0.79）。实验设计较为严谨、客观。

#### 6. 论文的主要结论与发现
*   **性能大幅提升**：MDER-DR 在所有数据集上均优于基准。在 WikiQA 上，相比表现最好的 Vector-RAG，性能提升高达 **66%**。
*   **跨语言鲁棒性**：在语言不匹配的情况下（如用法语问关于英语文档的问题），MDER-DR 的性能下降远小于传统方法，这归功于其翻译预处理和语言无关的图表示。
*   **多跳效率**：通过将多跳信息压缩进实体摘要，避免了查询时的复杂路径搜索，使系统在处理复杂逻辑时更加稳健。

#### 7. 优点
*   **上下文保留**：通过“Enrich”步骤解决了传统三元组丢失语义的问题。
*   **架构创新**：将推理压力从实时的图遍历转移到了离线的索引阶段，提高了检索的确定性。
*   **领域无关性**：框架不依赖特定领域的本体，能够自动推断实体类型，具有很强的通用性。

#### 8. 不足与局限
*   **索引成本高昂**：MDER 阶段需要对每个文本块、三元组和实体进行多次 LLM 调用，对于海量数据的实时更新可能存在挑战。
*   **消融实验缺失**：论文未详细展示 MDER 中四个步骤（Map, Disambiguate, Enrich, Reduce）各自对最终性能的贡献比例。
*   **数据规模限制**：虽然在 5000 个文档规模上表现良好，但在超大规模（如数百万文档）知识图谱上的扩展性和冲突处理能力尚待验证。

（完）
