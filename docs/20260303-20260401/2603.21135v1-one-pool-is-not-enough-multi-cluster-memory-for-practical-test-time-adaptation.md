---
title: "One Pool Is Not Enough: Multi-Cluster Memory for Practical Test-Time Adaptation"
title_zh: 一个池子是不够的：面向实用测试时自适应的多簇存储器
authors: "Yu-Wen Tseng, Xingyi Zheng, Ya-Chen Wu, I-Bin Liao, Yung-Hui Li, Hong-Han Shuai, Wen-Huang Cheng"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21135v1"
tags: ["query:ttt"]
score: 10.0
evidence: 针对分布偏移下实际测试时自适应的多簇记忆机制
tldr: 本研究针对实际测试时自适应（PTTA）中测试流的非独立同分布和多模态特性，指出传统的单一内存池设计存在结构性缺陷。为此，作者提出了多簇内存（MCM）框架，通过像素级统计描述符将样本组织成多个簇，并引入相邻簇合并（ACC）和均匀簇检索（UCR）机制。实验证明，MCM在多个基准数据集上显著提升了现有TTA方法的性能，特别是在复杂分布下表现优异，确立了内存组织在PTTA中的重要地位。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21135v1/fig-001.webp\", \"caption\": \"Fig. 1: Motivation for multi-cluster memory. (a) Stream clusterability analysis on CIFAR-100-C (PTTA): we fit GMMs with varying K to sliding windows of the test stream and select the optimal K∗ via BIC across three descriptor types. The consistently high K∗ values (µK∗ = 5.9–9.7) confirm that the target distribution is inherently multi-modal, far exceeding the K=1 assumption of single-cluster memory. (b) Under the same total capacity, SCM samples concentrate around similar regions of the descriptor space, whereas MCM distributes samples across distinct modes, extending coverage to under-represented regions (highlighted in red circle).\", \"page\": 2, \"index\": 1, \"width\": 721, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2603.21135v1/fig-002.webp\", \"caption\": \"Fig. 4: Diagnostic comparison of memory quality between SCM and MCM over the CIFAR-100-C stream (PTTA, PeTTA). We fit a GMM to the evolving stream and measure three properties of the stored memory: (a) imbalance ratio (lower is better), (b) distributional entropy (higher is more uniform), and (c) cluster coverage (fraction of GMM components with >1% representation). MCM maintains near-constant balance, entropy, and coverage throughout adaptation, whereas SCM exhibits high variance and progressive degradation.\", \"page\": 13, \"index\": 2, \"width\": 725, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2603.21135v1/fig-003.webp\", \"caption\": \"Table 1: Practical Test-time Adaptation (PTTA) error rates (%) on CIFAR10-C, CIFAR100-C, ImageNet-C, and DomainNet (severity 5). Lower is better. MCM is integrated into three memory-based baselines (RoTTA, PeTTA, ResiTTA) and yields consistent improvements across all configurations, with an average error reduction of 2.96%. Numbers in parentheses indicate improvement over the respective baseline. † denotes our re-implementation. “–” indicates the method was not evaluated on that dataset in the original publication.\", \"page\": 9, \"index\": 3, \"width\": 679, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2603.21135v1/fig-004.webp\", \"caption\": \"Fig. 2: Overview of the TTA system with Multi-Cluster Memory (MCM). Incoming samples are assigned to clusters via pixel-level descriptors (left). Uniform Cluster Retrieval (UCR) draws balanced samples across all clusters for adaptation (center). Adjacent Cluster Consolidation (ACC) merges the closest temporally adjacent pair when capacity is reached (right). The three stages jointly preserve the multi-modal structure of the target stream under bounded memory.\", \"page\": 5, \"index\": 4, \"width\": 713, \"height\": 286}]"
motivation: 传统的单一内存池设计无法匹配实际测试流中固有的多模态分布，导致模型在非独立同分布数据下的自适应不稳定。
method: 提出MCM框架，利用轻量级像素描述符进行多簇样本组织，结合相邻簇合并控制内存并采用均匀检索确保监督平衡。
result: "在ImageNet-C和DomainNet等数据集上，MCM使现有TTA方法性能最高提升了12.13%，且在复杂标签空间下增益更明显。"
conclusion: 内存组织是提升实际测试时自适应性能的关键，多簇结构能比单池结构更好地维持分布平衡和模式覆盖。
---

## 摘要
测试时自适应 (TTA) 仅使用无标签测试数据，在推理阶段使预训练模型适应分布偏移。在实用 TTA (PTTA) 设置下，测试流具有时间相关性且非独立同分布 (non-i.i.d.)，存储器已成为稳定自适应不可或缺的组件，但现有方法普遍将样本存储在单一的无结构池中。我们证明了这种单簇设计与 PTTA 根本不匹配：流可聚类性分析表明，测试流本质上是多模态的，其最优混合分量数量始终远大于 1。为了弥补这一结构性差距，我们提出了多簇存储器 (MCM)，这是一个即插即用的框架，利用轻量级像素级统计描述符将存储的样本组织成多个簇。MCM 引入了三种互补机制：基于描述符的簇分配以捕获不同的分布模态；相邻簇合并 (ACC) 通过合并最相似的时间相邻簇来限制内存使用；以及均匀簇检索 (UCR) 以确保在自适应过程中所有模态都能获得平衡的监督。在 CIFAR-10-C、CIFAR-100-C、ImageNet-C 和 DomainNet 上与三种当代 TTA 方法集成后，MCM 在所有 12 种配置中均实现了持续改进，在 ImageNet-C 上提升高达 5.00%，在 DomainNet 上提升高达 12.13%。值得注意的是，这些收益随分布复杂度的增加而扩大：具有更强多模态性的更大标签空间从多簇组织中获益最多。基于高斯混合模型 (GMM) 的存储器诊断进一步证实，MCM 保持了近乎最优的分布平衡、熵和模态覆盖，而单簇存储器则表现出持续的不平衡和渐进的模态丢失。这些结果确立了存储器组织是实用测试时自适应的一个关键设计维度。

## Abstract
Test-time adaptation (TTA) adapts pre-trained models to distribution shifts at inference using only unlabeled test data. Under the Practical TTA (PTTA) setting, where test streams are temporally correlated and non-i.i.d., memory has become an indispensable component for stable adaptation, yet existing methods universally store amples in a single unstructured pool. We show that this single-cluster design is fundamentally mismatched to PTTA: a stream clusterability analysis reveals that test streams are inherently multi-modal, with the optimal number of mixture components consistently far exceeding one. To close this structural gap, we propose Multi-Cluster Memory (MCM), a plug-and-play framework that organizes stored samples into multiple clusters using lightweight pixel-level statistical descriptors. MCM introduces three complementary mechanisms: descriptor-based cluster assignment to capture distinct distributional modes, Adjacent Cluster Consolidation (ACC) to bound memory usage by merging the most similar temporally adjacent clusters, and Uniform Cluster Retrieval (UCR) to ensure balanced supervision across all modes during adaptation. Integrated with three contemporary TTA methods on CIFAR-10-C, CIFAR-100-C, ImageNet-C, and DomainNet, MCM achieves consistent improvements across all 12 configurations, with gains up to 5.00% on ImageNet-C and 12.13% on DomainNet. Notably, these gains scale with distributional complexity: larger label spaces with greater multi-modality benefit most from multi-cluster organization. GMM-based memory diagnostics further confirm that MCM maintains near-optimal distributional balance, entropy, and mode coverage, whereas single-cluster memory exhibits persistent imbalance and progressive mode loss. These results establish memory organization as a key design axis for practical test-time adaptation.

---

## 论文详细总结（自动生成）

### 论文总结：One Pool Is Not Enough: Multi-Cluster Memory for Practical Test-Time Adaptation

#### 1. 核心问题与整体含义（研究动机和背景）
在实际的测试时自适应（Practical Test-Time Adaptation, PTTA）场景中，测试数据流通常具有时间相关性和非独立同分布（non-i.i.d.）的特征。现有的 TTA 方法虽然引入了存储器（Memory）来稳定自适应过程，但普遍采用**单一无结构池（Single-Cluster Memory, SCM）**设计。
*   **研究动机**：作者通过流可聚类性分析发现，PTTA 的测试流本质上是**多模态**的（最优聚类数 $K \gg 1$），而 SCM 隐含假设分布是单模态的。这种结构性不匹配导致存储器无法有效覆盖分布的所有模态，容易出现分布偏移和模态丢失，限制了自适应的性能。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Multi-Cluster Memory (MCM)** 框架，将存储器划分为多个簇，以显式捕捉和保留测试流的多模态结构。
*   **核心思想**：利用轻量级的像素级统计描述符（通道均值和方差）对样本进行聚类组织，确保存储器在分布空间上的广度和平衡。
*   **关键技术细节**：
    1.  **基于描述符的簇分配**：计算新样本与现有簇中心的欧氏距离。若距离超过阈值 $\tau$ 则创建新簇，否则归入最近簇。
    2.  **相邻簇合并 (ACC)**：当簇数量达到上限 $K_{max}$ 时，合并时间上相邻且描述符最接近的两个簇。这一设计利用了 PTTA 流的时间相关性。
    3.  **均匀簇检索 (UCR)**：在自适应训练阶段，从每个簇中抽取相同数量的样本，确保模型在所有分布模态上获得平衡的监督信号。
    4.  **样本替换策略**：在簇内结合样本年龄、预测熵和描述符距离进行启发式评分，替换价值最低的样本。

#### 3. 实验设计
*   **数据集/场景**：CIFAR-10-C、CIFAR-100-C、ImageNet-C（包含 15 种腐蚀类型）以及 DomainNet（跨域迁移）。
*   **Benchmark 设置**：采用 PTTA 协议，测试流具有时间相关性。
*   **对比方法**：
    *   **基础 TTA 方法**：Source, BN, PL, TENT, LAME, CoTTA, NOTE, RDumb, ROID, TRIBE, NEO。
    *   **集成基准**：将 MCM 插入到三种主流的基于存储器的 TTA 方法中：**RoTTA、PeTTA 和 ResiTTA**。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **时长**：文中提供了部分运行时间对比（例如在 CIFAR-100-C 上，PeTTA+MCM 耗时约 1049 秒），并指出 MCM 在提升精度的同时，由于结构化组织，其运行效率往往优于单纯扩大容量的 SCM。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个主流数据集，共 12 种“基准方法+数据集”的组合配置。
*   **消融实验**：详细分析了组件贡献（MCM/ACC/UCR）、描述符类型（像素统计 vs. CNN 特征）、距离度量、阈值 $\tau$ 敏感性以及簇上限 $K_{max}$。
*   **充分性与公平性**：实验对比了相同总容量下的 SCM 与 MCM，证明了增益源于“组织结构”而非“存储容量”。引入了 GMM 诊断框架量化存储器的平衡度、熵和覆盖率，实验设计客观且论证严密。

#### 6. 主要结论与发现
*   **性能提升**：MCM 在所有 12 种配置下均实现改进，平均误差降低 2.96%，在 DomainNet 上最高降低 12.13%。
*   **复杂度正相关**：分布越复杂（标签空间越大、多模态性越强），MCM 的优势越明显。
*   **长期稳定性**：在循环 TTA（Recurring TTA）测试中，MCM 能在 20 轮自适应中保持稳定并持续优化，避免了模型崩溃。
*   **存储器质量**：MCM 能维持近乎最优的分布平衡和模态覆盖，而 SCM 存在严重的分布倾斜。

#### 7. 优点（亮点）
*   **即插即用**：作为一个通用的存储器管理模块，可以轻松集成到任何基于存储器的 TTA 算法中。
*   **理论支撑**：首次通过流聚类分析证明了 PTTA 的多模态本质，为方法论提供了坚实的动机。
*   **高效性**：使用轻量级像素描述符而非重型特征提取器，计算开销低且对领域偏移敏感。

#### 8. 不足与局限
*   **描述符偏差**：像素级统计描述符主要捕捉颜色、亮度、噪声等底层特征，对于涉及几何变换或高层语义变化的分布偏移可能效果有限。
*   **任务覆盖**：目前主要集中在图像分类任务，尚未在目标检测、分割或多模态任务（如视觉语言模型）中验证。
*   **超参数依赖**：虽然作者证明了 $\tau$ 和 $K_{max}$ 具有一定的鲁棒性，但在极端不同的数据流下，可能仍需微调以达到最优性能。

（完）
