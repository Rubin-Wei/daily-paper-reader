---
title: Evaluating Test-Time Adaptation For Facial Expression Recognition Under Natural Cross-Dataset Distribution Shifts
title_zh: 评估自然跨数据集分布偏移下人脸表情识别的测试时自适应
authors: "John Turnbull, Shivam Grover, Amin Jalali, Ali Etemad"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19994v1"
tags: ["query:ttt"]
score: 10.0
evidence: 评估自然跨数据集分布偏移下的测试时自适应方法
tldr: "本研究针对面部表情识别（FER）在现实部署中面临的自然分布偏移问题，首次系统评估了测试时自适应（TTA）方法的有效性。研究通过跨数据集实验，超越了以往的合成干扰研究，探讨了采集协议、标注标准和人口统计差异带来的真实偏移。结果表明，TTA能显著提升FER性能，最高增幅达11.34%，并揭示了不同TTA算法在不同偏移场景下的适用性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19994v1/fig-001.webp\", \"caption\": \"Fig. 1. t-SNE projection of features from a ViT-B/16 model pretrained on ImageNet trained on FER datasets.\", \"page\": 3, \"index\": 1, \"width\": 460, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2603.19994v1/fig-002.webp\", \"caption\": \"Table 2. Top-1 Accuracy (%) of various TTA methods under different FER cross-dataset scenarios.\", \"page\": 4, \"index\": 2, \"width\": 1052, \"height\": 304}]"
motivation: 深度学习模型在处理因采集协议和标注标准不同导致的跨数据集面部表情识别分布偏移时表现不佳。
method: 在多个主流FER数据集上对比评估了熵最小化、原型调整和特征对齐等多种测试时自适应（TTA）技术。
result: "实验证明TTA最高可提升11.34%的准确率，其中TENT在干净分布下表现优异，而T3A和SHOT分别在大幅度偏移和高噪声场景下更具优势。"
conclusion: TTA的性能增益受领域间分布距离和偏移严重程度的影响，为解决FER领域的自然分布偏移提供了有效路径。
---

## 摘要
深度学习模型在自然分布偏移下往往面临困难，这是实际部署中的一个常见挑战。测试时自适应（TTA）通过在推理阶段无需标注源数据的情况下对模型进行调整来解决这一问题。我们首次对自然领域偏移下人脸表情识别（FER）的 TTA 方法进行了评估，并使用广泛使用的 FER 数据集进行了跨数据集实验。这项研究超越了合成损坏，旨在考察由不同的采集协议、标注标准和人口统计特征引起的真实世界偏移。结果显示，在自然偏移下，TTA 可将 FER 性能提升高达 11.34%。当目标分布较为纯净时，TENT 和 SAR 等熵最小化方法表现最佳。相比之下，T3A 等原型调整方法在分布距离较大的场景下表现优异。最后，当目标分布比源分布噪声更多时，SHOT 等特征对齐方法带来的提升最大。我们的跨数据集分析表明，TTA 的有效性受分布距离和跨领域自然偏移严重程度的影响。

## Abstract
Deep learning models often struggle under natural distribution shifts, a common challenge in real-world deployments. Test-Time Adaptation (TTA) addresses this by adapting models during inference without labeled source data. We present the first evaluation of TTA methods for FER under natural domain shifts, performing cross-dataset experiments with widely used FER datasets. This moves beyond synthetic corruptions to examine real-world shifts caused by differing collection protocols, annotation standards, and demographics. Results show TTA can boost FER performance under natural shifts by up to 11.34\%. Entropy minimization methods such as TENT and SAR perform best when the target distribution is clean. In contrast, prototype adjustment methods like T3A excel under larger distributional distance scenarios. Finally, feature alignment methods such as SHOT deliver the largest gains when the target distribution is noisier than our source. Our cross-dataset analysis shows that TTA effectiveness is governed by the distributional distance and the severity of the natural shift across domains.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating Test-Time Adaptation For Facial Expression Recognition Under Natural Cross-Dataset Distribution Shifts》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：人脸表情识别（FER）模型在实验室基准测试中表现优异，但在实际部署中，由于不同数据集的采集协议、标注标准、种族分布及光照条件的差异，模型常面临“自然分布偏移”（Natural Distribution Shifts），导致性能大幅下降。
*   **核心问题**：现有的测试时自适应（TTA）研究大多集中在合成干扰（如噪声、模糊）上，而忽略了真实的跨数据集自然偏移。本文旨在评估 TTA 方法在解决 FER 领域真实自然偏移问题上的有效性。

### 2. 论文提出的方法论
*   **核心思想**：通过在推理阶段利用无标签的目标域数据实时调整预训练模型，以弥补源域与目标域之间的分布鸿沟。
*   **关键技术细节**：
    *   **分布距离度量**：引入最大均值差异（MMD）和 RBF 核函数来计算跨数据集的相似度得分 $S$，量化领域间的特征空间差距。
    *   **TTA 策略分类**：
        *   **熵最小化（Entropy Minimization）**：如 TENT、SAR，通过降低预测的不确定性来更新归一化层参数。
        *   **特征对齐（Feature Alignment）**：如 SHOT，通过信息最大化和伪标签将目标特征映射到源特征空间。
        *   **原型调整（Prototype Adjustment）**：如 T3A，通过高置信度样本动态更新类中心（原型），无需反向传播。
        *   **持续自适应（Continual Adaptation）**：如 CoTTA、RoTTA，利用教师-学生网络和内存回放处理长期偏移。

### 3. 实验设计
*   **数据集**：使用了三个主流 FER 数据集：**AffectNet**（规模大、噪声高）、**RAF-DB**（标注质量高、种族分布较窄）、**FERPlus**（众包标注、一致性好）。
*   **实验场景**：构建了 12 种跨数据集场景（包括单源到单目标、多源到单目标）。
*   **对比方法**：评估了 8 种 SOTA TTA 方法：TENT, EATA, SAR, SHOT, T3A, NOTE, CoTTA, RoTTA。
*   **基准（Benchmark）**：以在源域微调后直接在目标域测试的 ViT-L/16 模型作为 Baseline。

### 4. 资源与算力
*   **硬件**：使用单台 **NVIDIA A100 40GB GPU**。
*   **训练/推理细节**：
    *   预训练模型：ViT-L/16（303M 参数）。
    *   训练时长：RAF-DB 训练 60 轮，其他数据集 30 轮。
    *   TTA 推理：流式设置，Batch Size 为 16，单步更新。
    *   效率记录：基准推理延迟 124.6ms，内存占用 3628MB。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 12 种跨数据集迁移组合，每种组合均测试了 8 种 TTA 方法，并进行了 3 次随机种子实验取平均值。
*   **充分性与客观性**：
    *   实验设计较为充分，不仅对比了准确率，还分析了推理延迟和内存开销。
    *   通过 t-SNE 可视化和 MMD 相似度定量分析，解释了 TTA 性能波动的根本原因，具有较强的说服力。
    *   对比了单源和多源训练背景，增加了实验的广度。

### 6. 论文的主要结论与发现
*   **性能提升**：TTA 最高可提升 FER 准确率 **11.34%**（在 FERPlus → AffectNet 场景下）。
*   **方法适用性**：
    *   **熵最小化（TENT/SAR）**：在目标域较“干净”时效果最好，但在目标域噪声大时可能强化错误预测。
    *   **特征对齐（SHOT）**：在目标域噪声极高时增益最大，但在基准表现已很好时可能起反作用。
    *   **原型调整（T3A）**：在领域相似度极低（$S$ 值小）时表现最稳健，且计算开销最低。
    *   **持续自适应**：在跨数据集场景下表现不佳，CoTTA 甚至出现了性能负增长。

### 7. 优点
*   **现实意义强**：首次将 TTA 评估从“合成损坏”转向“自然跨数据集偏移”，更符合 FER 的实际应用场景。
*   **定量分析深入**：通过 MMD 相似度得分建立了“领域距离”与“方法选择”之间的直观联系。
*   **效率评估全面**：不仅关注精度，还提供了延迟和内存数据，为资源受限设备的部署提供了参考。

### 8. 不足与局限
*   **缺乏动态选择机制**：虽然指出了不同方法在不同场景下的优劣，但未提出一种能自动检测目标域噪声并切换最优 TTA 策略的统一框架。
*   **数据集覆盖有限**：仅使用了三个数据集，未涵盖更极端的红外图像、侧脸表情等更复杂的自然偏移。
*   **类别不平衡未深入探讨**：FER 数据集通常存在严重的类别不平衡，论文未详细分析 TTA 在少数类上的表现。

（完）
