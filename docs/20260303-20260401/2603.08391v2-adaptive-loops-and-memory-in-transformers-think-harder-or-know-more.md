---
title: "Adaptive Loops and Memory in Transformers: Think Harder or Know More?"
title_zh: Transformer 中的自适应循环与记忆：思考更久还是知识更广？
authors: "Markus Frey, Behzad Shomali, Ali Hamza Bashir, David Berghaus, Mehdi Ali"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08391v2"
tags: ["query:pm"]
score: 9.0
evidence: 用于权重中学习存储的门控记忆库
tldr: 本研究探讨了Transformer模型中自适应循环与记忆库的结合。针对循环模型参数效率高但存储容量有限的问题，作者提出了自适应逐层循环机制和门控记忆库。实验表明，循环机制主要增强数学推理（“想得更深”），而记忆库则提升常识任务表现（“知得更多”）。该组合模型在数学基准测试中超越了同等计算量的基准模型，并展现出层间的功能专业化分工。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08391v2/fig-001.webp\", \"caption\": \"Table 3: Full results at the final checkpoint. Best result within each group is bolded. Base = standard transformer; LN = Loop-N ; L3IF = iso-FLOP for Loop-3; M-BIP = memory iso-parameter baseline; Mg0 = memory model with gate init g0; M-BIF = memory iso-FLOP baseline.\", \"page\": 11, \"index\": 1, \"width\": 706, \"height\": 708}, {\"url\": \"assets/figures/arxiv/2603.08391v2/fig-002.webp\", \"caption\": \"Figure 1: Architecture overview. Left: A standard transformer passes hidden states through L unique blocks. Center: Our loop model allows each block to iterate up to N times, with a learned halting mechanism that produces a weighted combination of intermediate states. Per-step scales ζ(sn) are initialized near zero for training stability. Right: The combined model additionally retrieves from local (per-layer) and global (shared) memory banks, gated by learned input-dependent scalars.\", \"page\": 2, \"index\": 2, \"width\": 794, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.08391v2/fig-003.webp\", \"caption\": \"Figure 4: Memory gate activations across layers and training. Left: Local memory gate values show high variance across layers while later layers tend to have higher gate activations, and the spread increases over training. Right: Global memory gate values increase during training but converge to a more uniform profile across layers, with activations rising up to approximately layer 5 and then plateauing.\", \"page\": 10, \"index\": 3, \"width\": 821, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2603.08391v2/fig-004.webp\", \"caption\": \"Table 2: Model configurations. All models use the same base transformer architecture. Loop parameters include per-step scales and halting router weights. Memory parameters include local/global key-value banks and gating networks. Iso-parameter baselines add extra FFN capacity to match the corresponding model’s parameter count. Iso-FLOP baselines use 36 layers to approximate the forward-pass cost of 3-loop models.\", \"page\": 10, \"index\": 4, \"width\": 727, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2603.08391v2/fig-005.webp\", \"caption\": \"Table 5: Results at the mid checkpoint (step 20000).\", \"page\": 13, \"index\": 5, \"width\": 706, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2603.08391v2/fig-006.webp\", \"caption\": \"Table 1: Summary of results, averaged across benchmarks within each group. CS = commonsense; BPB = bits per byte (lower is better, see Appendix A.2 for details). Best result per column within each model group is bolded. Full per-benchmark breakdowns are in Appendix A.4.\", \"page\": 4, \"index\": 6, \"width\": 656, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2603.08391v2/fig-007.webp\", \"caption\": \"Table 4: Results at the early checkpoint (step 5000).\", \"page\": 12, \"index\": 7, \"width\": 706, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2603.08391v2/fig-008.webp\", \"caption\": \"Figure 3: Expected loop iterations vs. validation cross-entropy for all configurations. Each point represents one evaluation during training; curves are colored by model configuration. Across all looped models, the expected number of iterations begins to increase rapidly once the cross-entropy drops below approximately 3.27± 0.59. This phase transition is consistent across Loop-3, Loop-5, and Loop-7 configurations, suggesting it depends on the model’s language competence rather than the maximum number of allowed iterations.\", \"page\": 9, \"index\": 8, \"width\": 818, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2603.08391v2/fig-009.webp\", \"caption\": \"Figure 2: Expected number of loop iterations per layer over training. Left: Each curve represents one layer. Early layers (lighter colors) consistently use fewer iterations than later layers (darker colors). Middle: Expected steps at the end of training. Right: All models show a characteristic transition which occurs at approximately the same cross-entropy value across configurations (see Figure 3 in the Appendix for all configurations).\", \"page\": 5, \"index\": 9, \"width\": 817, \"height\": 244}]"
motivation: 旨在解决循环Transformer模型在提升参数效率的同时，因缺乏深度模型逐层权重而导致的存储容量不足问题。
method: 引入了自适应逐层循环机制（通过学习停止准则迭代隐藏状态）和门控记忆库（提供额外的学习存储空间）。
result: 循环机制显著提升了数学推理性能，记忆库则有效恢复了常识任务表现，两者结合在数学基准上优于三倍深度的等FLOP基准模型。
conclusion: 结合循环与记忆机制能有效平衡模型的推理深度与知识存储，且模型内部演化出了从浅层快速处理到深层重度循环与记忆访问的专业化分工。
---

## 摘要
思维链 (CoT) 提示能够实现语言模型的推理，但需要将中间步骤显式地表达出来。循环 Transformer 提供了一种替代方案，通过在隐藏状态内迭代地细化表示。这种参数效率是以牺牲性能为代价的，因为循环模型缺乏更深层模型所具有的存储容量，后者在每一层都使用唯一的权重。在这项工作中，我们研究了同时具备自适应逐层循环（其中每个 Transformer 块通过学习到的停机机制来学习迭代其隐藏状态）和门控记忆库（提供额外的学习存储）的 Transformer 模型。我们发现，与参数量和 FLOPs 匹配的模型相比，循环主要有利于数学推理，而记忆库则有助于恢复在常识任务上的性能。结合这两种机制所产生的模型在数学基准测试中优于具有三倍层数的等 FLOPs 基准模型。对模型内部的分析揭示了层的专业化：早期层学习进行极少的循环并谨慎地访问记忆，而后期层则更频繁地进行这两项操作。

## Abstract
Chain-of-thought (CoT) prompting enables reasoning in language models but requires explicit verbalization of intermediate steps. Looped transformers offer an alternative by iteratively refining representations within hidden states. This parameter efficiency comes at a cost, as looped models lack the storage capacity of deeper models which use unique weights per layer. In this work, we investigate transformer models that feature both adaptive per-layer looping, where each transformer block learns to iterate its hidden state via a learned halting mechanism, and gated memory banks, that provide additional learned storage. We find that looping primarily benefits mathematical reasoning, while memory banks help recover performance on commonsense tasks compared to parameter and FLOP matched models. Combining both mechanisms yields a model that outperforms an iso-FLOP baseline, with three times the number of layers, across math benchmarks. Analysis of model internals reveals layer specialization: early layers learn to loop minimally and access memory sparingly, while later layers do both more heavily.

---

## 论文详细总结（自动生成）

### 论文总结：Transformer 中的自适应循环与记忆

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了 Transformer 模型在“推理深度”与“知识容量”之间的权衡。
*   **背景**：循环 Transformer（Looped Transformers）通过多次调用同一层参数来模拟深层计算，具有极高的参数效率，但在存储事实性知识方面不如具有独立权重的深层模型。
*   **核心问题**：模型究竟是应该通过增加迭代次数来“想得更深”（Think Harder），还是通过增加参数量来“知得更多”（Know More）？作者试图通过结合**自适应循环机制**和**外部记忆库**，在不显著增加推理成本的前提下，同时提升模型的推理能力和知识储备。

#### 2. 方法论：核心思想与关键技术
作者在标准 Decoder-only Transformer 基础上引入了两个核心组件：

*   **自适应循环（Adaptive Looping）**：
    *   **停机机制**：借鉴 PonderNet，每层 Transformer 块配备一个停机路由器（Halting Router），预测当前迭代是否停止。最终输出是各次迭代结果的加权组合。
    *   **训练稳定性**：引入了可学习的缩放参数 $\alpha_t$（初始值设为 -7.0），使模型在训练初期接近恒等映射，随后逐渐学习如何介入计算。
*   **门控记忆库（Gated Memory Banks）**：
    *   **局部记忆（Local Memory）**：每层拥有独立的 Key-Value 存储空间，用于存放层特定的中间计算结果。
    *   **全局记忆（Global Memory）**：所有层共享的存储空间，用于存放通用的事实性知识。
    *   **集成方式**：使用带 QK 归一化的注意力机制进行检索，并通过输入相关的门控（Gating）机制将检索到的信息融入残差流。

#### 3. 实验设计
*   **数据集**：在去重的 FineWeb-Edu 数据集上预训练，消耗约 140 亿（14B）个 Token。
*   **Benchmark**：
    *   **常识任务**：ARC (Challenge/Easy), HellaSwag, PIQA, Winogrande 等（评估准确率 Acc 和 BPB）。
    *   **数学任务**：Algebra, Geometry, Precalculus 等（评估 BPB，即每字节比特数，数值越低越好）。
*   **对比方法（Baselines）**：
    *   **Iso-Parameter**：通过增加 FFN 宽度来对齐参数量的基准模型。
    *   **Iso-FLOP**：通过增加层数（如 36 层）来对齐推理计算量的基准模型。

#### 4. 资源与算力
*   **模型规模**：基础模型约 2 亿（200M）参数，12 层。
*   **训练细节**：使用 AdamW 优化器，Batch Size 约 36 万 Token，共训练 38,620 步。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 H100/A100）及具体的训练总时长，但提到了使用了 PyTorch 原生的 Modalities 训练框架。

#### 5. 实验数量与充分性
*   **实验组数**：作者测试了不同最大循环次数（$N_{max} \in \{3, 5, 7\}$）、三种不同的门控初始化偏置（闭合、平衡、开启），以及多种等参数/等 FLOPs 的基准对比。
*   **充分性与客观性**：实验设计较为严谨，通过 Iso-Parameter 和 Iso-FLOP 确保了对比的公平性。不仅关注最终性能，还深入分析了训练过程中的动态变化（如层级专业化现象），增强了结论的说服力。

#### 6. 主要结论与发现
*   **功能分离**：循环机制主要提升数学推理性能（BPB 降低约 22%），而记忆库则显著改善常识任务的表现，弥补了循环模型的容量短板。
*   **层级专业化**：模型自发演化出分工——早期层倾向于少循环、少访问记忆；后期层则重度依赖循环进行复杂推理，并频繁访问记忆库。
*   **相位切换（Phase Transition）**：模型并非从训练开始就使用循环。只有当验证集交叉熵下降到一定阈值（约 3.27）后，模型才开始大幅增加循环次数，这表明模型需要先具备基础语言能力才能进行“深度思考”。
*   **效率优势**：结合循环与记忆的模型在数学任务上优于层数多出三倍的等 FLOPs 基准模型。

#### 7. 优点（亮点）
*   **架构创新**：成功将自适应计算（PonderNet 变体）与静态学习记忆库结合，提供了处理“推理 vs 知识”矛盾的新思路。
*   **内部洞察**：通过可视化停机概率和门控值，揭示了 Transformer 内部如何根据任务难度动态分配计算资源。
*   **参数效率**：证明了在特定任务（如数学）上，增加循环比单纯增加深度更具参数效率。

#### 8. 不足与局限
*   **规模限制**：实验仅在 200M 参数规模下进行，尚不确定相关结论是否能完美迁移至百亿或千亿级的大规模模型。
*   **评估指标**：数学任务主要使用 BPB 指标而非最终答案的准确率（Accuracy），这可能无法完全反映模型解决复杂多步问题的实际成功率。
*   **计算开销**：虽然参数高效，但循环增加了推理延迟（Latency），论文对推理效率与性能之间的帕累托前沿（Pareto Frontier）描述尚不够详尽。

（完）
