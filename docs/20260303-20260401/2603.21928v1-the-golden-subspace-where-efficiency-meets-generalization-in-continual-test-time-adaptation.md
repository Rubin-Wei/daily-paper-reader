---
title: "The Golden Subspace: Where Efficiency Meets Generalization in Continual Test-Time Adaptation"
title_zh: 黄金子空间：持续测试时自适应中效率与泛化的平衡之道
authors: "Guannan Lai, Da-Wei Zhou, Zhenguo Li, Han-Jia Ye"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21928v1"
tags: ["query:ttt"]
score: 10.0
evidence: 分布偏移下无需源数据的持续测试时自适应
tldr: 本文针对持续测试时自适应（CTTA）中更新参数过多导致推理效率下降的问题，提出了GOLD方法。研究者证明了存在一个与预训练分类器行空间重合的“黄金子空间”，在此空间内进行微量特征更新即可实现高效自适应。通过引入样本级平均梯度外积（AGOP）动态维护该子空间，并结合轻量级适配器学习缩放向量，GOLD在保持高效率的同时显著提升了模型的泛化能力和稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21928v1/fig-001.webp\", \"caption\": \"Table 1. Online classification error rates (%) on CIFAR10-C, CIFAR100-C, and ImageNet-C under the CTTA setting. All methods are evaluated online at severity level 5, with our method’s results averaged over 5 independent runs. The best performance for each corruption type is highlighted in bold and the next best is underlined.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 781}, {\"url\": \"assets/figures/arxiv/2603.21928v1/fig-002.webp\", \"caption\": \"Figure 2. (a) Alignment between AGOP-derived subspace and source-derived ground-truth subspace as testing progresses. AGOP rapidly converges and remains highly aligned. (b) AGOP spectrum cumulative energy: the top 64–128 eigenvectors capture over 99% of the energy, indicating strong low-rank concentration.\", \"page\": 3, \"index\": 2, \"width\": 495, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2603.21928v1/fig-003.webp\", \"caption\": \"Figure 5. A visual comparison of segmentation results from different adaptive methods on the CarlaTTA sequence is presented. White boxes indicate areas where GOLD has made progress, and magnified images are shown in the supplementary material.\", \"page\": 8, \"index\": 3, \"width\": 964, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2603.21928v1/fig-004.webp\", \"caption\": \"Figure 3. Overview of GOLD: it maintains an online AGOP estimator, from which the top-r eigenvectors Vt are extracted via eigendecomposition to form the golden subspace. A residual low-rank adapter of rank r is then applied to learn a lightweight scaling vector St. Meanwhile, a combination of self-training loss and prototype-based contrastive loss provides stable supervision.\", \"page\": 5, \"index\": 4, \"width\": 662, \"height\": 359}]"
motivation: 旨在解决CTTA中由于更新大量参数而导致的在线推理效率与模型自适应效果之间的矛盾。
method: 提出GOLD方法，利用AGOP高效估算并动态更新分类器权重的“黄金子空间”，并通过轻量级适配器进行特征投影与缩放。
result: 在多个分类和分割基准测试中，GOLD在保证极高推理效率的同时，实现了超越现有方法的稳定性和准确率。
conclusion: 该研究揭示了分类器行空间在测试时自适应中的关键作用，为实现高效且稳健的在线模型演进提供了理论支持和实践路径。
---

## 摘要
持续测试时自适应 (CTTA) 旨在使模型能够在不访问源数据的情况下，在线适应分布偏移下的无标签数据流。现有的 CTTA 方法面临效率与泛化之间的权衡：更新更多参数虽然能提高自适应效果，但会严重降低在线推理效率。理想的解决方案是以最小的特征更新实现相当的自适应效果；我们将这一最小子空间称为“黄金子空间”。我们在单步自适应设置下证明了其存在性，并表明它与预训练分类器的行空间相重合。为了实现在线维护该子空间，我们引入了样本级平均梯度外积 (AGOP)，作为无需重新训练即可估计分类器权重的有效代理。基于这些见解，我们提出了引导式在线低秩定向自适应 (GOLD)，它使用轻量级适配器将特征投影到黄金子空间上，并学习一个紧凑的缩放向量，同时通过 AGOP 动态更新该子空间。在包括自动驾驶场景在内的分类和分割基准测试上的大量实验表明，GOLD 获得了卓越的效率、稳定性和整体性能。我们的代码可在 https://github.com/AIGNLAI/GOLD 获取。

## Abstract
Continual Test-Time Adaptation (CTTA) aims to enable models to adapt online to unlabeled data streams under distribution shift without accessing source data. Existing CTTA methods face an efficiency-generalization trade-off: updating more parameters improves adaptation but severely reduces online inference efficiency. An ideal solution is to achieve comparable adaptation with minimal feature updates; we call this minimal subspace the golden subspace. We prove its existence in a single-step adaptation setting and show that it coincides with the row space of the pretrained classifier. To enable online maintenance of this subspace, we introduce the sample-wise Average Gradient Outer Product (AGOP) as an efficient proxy for estimating the classifier weights without retraining. Building on these insights, we propose Guided Online Low-rank Directional adaptation (GOLD), which uses a lightweight adapter to project features onto the golden subspace and learns a compact scaling vector while the subspace is dynamically updated via AGOP. Extensive experiments on classification and segmentation benchmarks, including autonomous-driving scenarios, demonstrate that GOLD attains superior efficiency, stability, and overall performance. Our code is available at https://github.com/AIGNLAI/GOLD.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：The Golden Subspace (GOLD)

#### 1. 核心问题与整体含义（研究动机和背景）
本文关注的是**持续测试时自适应（Continual Test-Time Adaptation, CTTA）**。在实际应用（如自动驾驶）中，模型部署后会遇到不断变化的未知环境（如天气、光照变化），且无法访问原始训练数据。
*   **核心矛盾**：现有的 CTTA 方法面临“效率-泛化”的权衡。更新全量参数虽然能提升短期表现，但计算开销巨大，且容易导致**参数漂移（Parameter Drift）**和**错误累积**，破坏模型的长期泛化能力。
*   **研究动机**：寻找一个最小的特征更新空间，既能保证模型输出的修正（泛化），又能最小化计算量（效率）。作者将这一理想空间定义为**“黄金子空间”（Golden Subspace）**。

#### 2. 方法论：核心思想与技术细节
作者提出了 **GOLD (Guided Online Low-rank Directional adaptation)** 框架，其核心流程如下：
*   **黄金子空间的理论证明**：通过数学推导证明，在单步自适应设置下，实现目标输出修正所需的最小范数特征扰动，位于预训练分类器权重的**行空间（Row Space）**内。
*   **AGOP 在线估计**：由于测试时分类器权重是固定的，作者引入了**平均梯度外积（Average Gradient Outer Product, AGOP）**。利用高置信度样本的梯度外积来动态维护一个辅助矩阵 $G_t$，作为分类器权重结构的在线代理，从而捕捉目标域的语义信息。
*   **低秩投影与缩放**：
    1.  对 $G_t$ 进行特征值分解，提取前 $r$ 个特征向量构建投影矩阵 $V_t$。
    2.  将骨干网络提取的特征投影到该低秩子空间。
    3.  引入一个极轻量级的**缩放向量 $S_t$** 对投影坐标进行调制，再残差式地加回原特征。
*   **优化目标**：结合了**自训练一致性损失（SCE Loss）**和**基于原型的对比损失（Prototype-based Contrastive Loss）**。利用从源域预先提取的类原型作为“语义锚点”，防止特征在长时间自适应过程中发生溃散。

#### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：CIFAR10-C、CIFAR100-C、ImageNet-C（包含 15 种不同类型的环境腐蚀，最高严重等级 5）。
    *   **语义分割**：CarlaTTA（基于 CARLA 模拟器的动态驾驶场景，包含昼夜切换、雨雾天气、高速公路等）。
*   **Benchmark 与对比方法**：对比了包括 TENT、CoTTA（经典标杆）、SANTA、RMT、EATA、OBAO 等在内的十余种最先进的 CTTA 方法。
*   **评估指标**：分类错误率（Error Rate）、平均交并比（mIoU）、每批次处理时间（Efficiency）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 RTX 3090）及具体的总训练时长。
*   **效率数据**：文中提供了详细的推理效率分析。GOLD 的每批次处理时间稳定在约 **0.25 秒**，与最快的轻量级方法（如 SANTA）持平，但远快于 CoTTA 等全参数更新方法。
*   **内存占用**：在 ImageNet-C 实验中，峰值 GPU 显存占用约为 **5.37 GB**，可部署在消费级显卡上。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型数据集（CIFAR）到大规模数据集（ImageNet），从分类任务到复杂的分割任务。
*   **消融实验**：非常充分。作者分别验证了 $W^\top W$ 初始化、AGOP 更新机制、对比损失函数等各个组件的贡献。
*   **鲁棒性分析**：进行了超参数敏感性测试（如秩 $r$、置信度阈值 $\tau$、学习率等）以及长程泛化实验（重复 10 轮数据流）。
*   **客观性**：实验遵循标准的 CTTA 协议（单次通过数据流、无源数据、无领域切换通知），对比基准统一，结果具有较高的说服力。

#### 6. 主要结论与发现
*   **子空间的存在性**：实验证实了分类器行空间确实包含了自适应所需的关键方向，仅需更新极少量参数（可训练参数占比低于 **0.4%**）即可达到甚至超过全量更新的效果。
*   **AGOP 的有效性**：AGOP 能够快速收敛并高度对齐理想的黄金子空间，有效解决了测试时无法重训分类器的难题。
*   **稳定性**：GOLD 在面对连续且剧烈的领域偏移时，表现出极强的稳定性，有效抑制了其他方法中常见的性能骤降（Degradation）现象。

#### 7. 优点（亮点）
*   **理论支撑强**：不同于许多纯启发式的 CTTA 方法，本文为“更新哪里”提供了坚实的线性代数证明。
*   **极高效率**：通过低秩约束，极大地降低了计算开销，非常适合实时性要求高的嵌入式场景。
*   **即插即用**：作为一种特征层面的适配器，它可以轻松集成到各种预训练骨干网络中。

#### 8. 不足与局限
*   **线性假设**：黄金子空间的推导基于线性分类器的假设，对于某些非线性极强的复杂表征层，该理论的适用性可能受限。
*   **超参数依赖**：虽然实验证明了鲁棒性，但子空间的秩 $r$ 和 AGOP 的更新频率 $T_{eig}$ 仍需根据具体任务进行初步预设。
*   **极端偏移**：在语义空间发生根本性改变（如类别完全不同）的场景下，基于源域原型的锚定机制可能会失效。

（完）
