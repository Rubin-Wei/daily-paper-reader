---
title: Enhancing Web Agents with a Hierarchical Memory Tree
title_zh: 利用分层记忆树增强 Web 智能体
authors: "Yunteng Tan, Zhi Gao, Xinxiao Wu"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07024v1"
tags: ["query:am"]
score: 9.0
evidence: 用于Web智能体处理长程任务的分层记忆树
tldr: 针对大模型 Web 智能体在跨网站任务中因记忆结构扁平导致逻辑与操作混淆的问题，本文提出层级记忆树（HMT）框架。该框架通过自动化流水线将历史轨迹抽象为意图、阶段和动作三层结构，实现逻辑规划与具体执行的解耦。配合阶段感知的推理机制，HMT 在 Mind2Web 和 WebArena 实验中显著提升了智能体的跨域泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07024v1/fig-001.webp\", \"caption\": \"Fig. 6. Analysis of two representative failure modes in cross-website grounding. (a) Ambiguous Grounding: The agent retrieves a generic descriptor (text_contains: more) lacking hierarchical constraints. Consequently, the Actor assigns a higher similarity (score 0.92) to the distractor candidate than to the intended target (score 0.89), leading to an execution error. (b) State Verification Error: The Planner retrieves a strict success criterion expecting a NAVIGATION event (e.g., URL change or page reload). Although the action triggers a visual update (modal popup), the URL remains static. The Planner’s rigid verification logic (url_change = False and page_reload = False) fails to recognize the visual progress, incorrectly marking the step as incomplete and initiating a retry loop.\", \"page\": 10, \"index\": 1, \"width\": 969, \"height\": 591}]"
motivation: 现有的扁平化记忆结构将高层逻辑与特定网站的操作细节混淆，导致智能体在面对新环境时出现逻辑执行不一致的“工作流失配”问题。
method: 提出层级记忆树（HMT），将原始轨迹抽象为意图、阶段和动作三层，并结合规划器和执行器实现阶段感知的推理与动作对齐。
result: 在 Mind2Web 和 WebArena 基准测试中，HMT 在跨网站和跨领域场景下的表现显著优于传统的扁平记忆方法。
conclusion: 实验证明结构化的记忆框架能有效解耦规划与执行，是提升 Web 智能体在复杂多变环境中鲁棒性和泛化能力的关键。
---

## 摘要
基于大语言模型的 Web 智能体通过先进的推理和指令遵循能力，在自动化 Web 交互方面展现出巨大潜力。虽然源自历史轨迹的检索式记忆使这些智能体能够处理复杂的长程任务，但现有方法在跨未见网站的泛化方面仍面临挑战。我们发现，这一挑战源于扁平化的记忆结构，这种结构将高层任务逻辑与特定网站的操作细节纠缠在一起。这种纠缠在面对新环境时会导致工作流失配，即检索到的内容与当前网页混淆，从而导致逻辑不一致的执行。为此，我们提出了分层记忆树（Hierarchical Memory Tree, HMT），这是一个旨在显式解耦逻辑规划与动作执行的结构化框架。HMT 通过自动化抽象流水线从原始轨迹构建三层分层结构：意图层（Intent level）将多样的用户指令映射为标准化的任务目标；阶段层（Stage level）定义了以可观测的前置条件和后置条件为特征的可重用语义子目标；动作层（Action level）存储了与可迁移语义元素描述配对的动作模式。利用这一结构，我们开发了一种包含规划器（Planner）和执行器（Actor）的阶段感知推理机制。通过显式验证前置条件，规划器将当前状态与正确的逻辑子目标对齐以防止工作流失配，而执行器则通过将存储的语义描述与目标页面匹配来落地动作。在 Mind2Web 和 WebArena 上的实验结果表明，HMT 显著优于扁平记忆方法，特别是在跨网站和跨领域场景中，凸显了结构化记忆对于 Web 智能体鲁棒泛化的必要性。

## Abstract
Large language model-based web agents have shown strong potential in automating web interactions through advanced reasoning and instruction following. While retrieval-based memory derived from historical trajectories enables these agents to handle complex, long-horizon tasks, current methods struggle to generalize across unseen websites. We identify that this challenge arises from the flat memory structures that entangle high-level task logic with site-specific action details. This entanglement induces a workflow mismatch in new environments, where retrieved contents are conflated with current web, leading to logically inconsistent execution. To address this, we propose Hierarchical Memory Tree (HMT), a structured framework designed to explicitly decouple logical planning from action execution. HMT constructs a three-level hierarchy from raw trajectories via an automated abstraction pipeline: the Intent level maps diverse user instructions to standardized task goals; the Stage level defines reusable semantic subgoals characterized by observable pre-conditions and post-conditions; and the Action level stores action patterns paired with transferable semantic element descriptions. Leveraging this structure, we develop a stage-aware inference mechanism comprising a Planner and an Actor. By explicitly validating pre-conditions, the Planner aligns the current state with the correct logical subgoal to prevent workflow mismatch, while the Actor grounds actions by matching the stored semantic descriptions to the target page. Experimental results on Mind2Web and WebArena show that HMT significantly outperforms flat-memory methods, particularly in cross-website and cross-domain scenarios, highlighting the necessity of structured memory for robust generalization of web agents.

---

## 论文详细总结（自动生成）

### 论文总结：利用分层记忆树（HMT）增强 Web 智能体

#### 1. 核心问题与整体含义（研究动机和背景）
现有的基于大语言模型（LLM）的 Web 智能体在处理长程任务时，通常依赖检索历史成功轨迹的“扁平化记忆”结构。然而，这种结构存在**“意图-执行纠缠”**（Intention-Execution Entanglement）问题：它将高层的任务逻辑（如“预订机票”）与特定网站的底层操作细节（如特定的 HTML 元素 ID）混为一谈。当智能体被部署到未见过的网站时，这种纠缠会导致**工作流失配**和**环境污染**，即智能体试图在 A 网站上执行 B 网站特有的操作 ID，导致任务失败。本文的研究动机在于通过结构化记忆实现逻辑规划与具体执行的显式解耦，提升智能体的跨网站泛化能力。

#### 2. 方法论：核心思想与关键技术
论文提出了**分层记忆树（Hierarchical Memory Tree, HMT）**框架，其核心思想是将经验抽象为三层结构：
*   **意图层（Intent Level）**：将多样化的用户指令归一化为标准任务目标和约束，消除表述差异。
*   **阶段层（Stage Level）**：将任务分解为可重用的语义子目标（如“过滤结果”），并定义**前置条件**和**后置条件**（基于可观测的 UI 状态），用于确保检索到的步骤与当前进度对齐。
*   **动作层（Action Level）**：存储抽象的动作模式和**可迁移的语义元素描述**（如“角色：按钮，文本包含：搜索”），而非原始的元素 ID。

**推理流程（阶段感知推理机制）**：
1.  **规划器（Planner）**：通过状态抽象，将当前页面观测与检索到的子目标的前后置条件进行匹配，选择正确的逻辑阶段。
2.  **执行器（Actor）**：在确定的阶段内，利用语义描述在当前页面寻找匹配的 UI 元素并执行动作。
3.  **置信度感知回退**：当 Planner 对阶段选择不确定时，会自动扩大检索范围或回退到无记忆的基准策略，防止错误累积。

#### 3. 实验设计
论文在两个主流基准测试上验证了 HMT 的有效性：
*   **Mind2Web（离线设置）**：包含 137 个网站的 2000 多个任务。测试分为**跨任务（Cross-Task）**、**跨网站（Cross-Website）**和**跨领域（Cross-Domain）**三个子集，重点考察泛化性。
*   **WebArena（在线设置）**：一个真实的交互式 Web 环境，涵盖购物、内容管理（CMS）、社交（Reddit）、代码托管（GitLab）和地图（Maps）等领域。
*   **对比方法**：包括通用智能体（MindAct, WebArena Baseline）、分层/检索增强智能体（SteP, AutoEval, AWM）以及作者构建的“扁平检索”基准。

#### 4. 资源与算力
*   **模型**：所有组件均使用 **GPT-4** 作为骨干模型（Backbone）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法主要基于 LLM 的 Prompting（提示工程）和上下文学习（In-context Learning），而非大规模参数微调，其算力消耗主要集中在 API 调用推理上。

#### 5. 实验数量与充分性
实验设计较为充分且客观：
*   **多维度评估**：涵盖了离线轨迹分析和在线闭环执行。
*   **消融实验**：设计了 5 组消融变体（如移除分层结构、移除前后置条件、使用原始 ID 等），深入探讨了各组件的贡献。
*   **机制分析**：定量分析了检索质量（Recall@K）和动作落地的鲁棒性，并提供了定性的案例分析（成功与失败案例）。
*   **效率分析**：对比了 Token 消耗和推理延迟，证明了 HMT 在提升性能的同时降低了成本。

#### 6. 主要结论与发现
*   **跨网站泛化显著提升**：在 Mind2Web 的跨网站子集上，HMT 的步骤成功率（StepSR）比 AWM 提升了 6.0%；在 WebArena 上，总成功率达到 38.7%，位居榜首。
*   **解耦是关键**：实验证明，使用“语义描述”替代“原始 ID”是跨网站成功的核心，前者在跨网站场景下的匹配成功率（76.8%）远高于后者（12.4%）。
*   **效率优势**：通过抽象化记忆，HMT 将平均上下文 Token 减少了约 72.7%，推理成本降低了 71.0%。

#### 7. 优点（亮点）
*   **结构化解耦**：通过三层树状结构，优雅地解决了高层逻辑与底层细节的纠缠问题。
*   **阶段感知**：引入前后置条件验证，有效防止了长程任务中的“工作流失配”。
*   **高迁移性**：语义化的元素描述使得智能体能够像人类一样通过功能特征（而非代码 ID）识别组件。
*   **鲁棒性设计**：置信度感知回退机制增强了系统在面对不确定检索时的稳定性。

#### 8. 不足与局限
*   **空间任务受限**：在依赖精确空间坐标的任务（如地图操作）中，文本化的语义抽象提升不明显。
*   **语义歧义**：在 UI 极其复杂的页面，简单的语义描述（如“更多”按钮）可能指向多个候选目标，导致误操作。
*   **状态验证盲区**：对于单页应用（SPA），由于 URL 不变，Planner 依赖的某些后置条件（如 URL 跳转）可能失效，导致陷入重试循环。
*   **依赖强模型**：该框架高度依赖 GPT-4 等强推理模型进行抽象和规划，在较弱的小模型上的表现尚未验证。

（完）
