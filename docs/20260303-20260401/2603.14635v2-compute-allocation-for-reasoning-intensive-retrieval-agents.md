---
title: Compute Allocation for Reasoning-Intensive Retrieval Agents
title_zh: 推理密集型检索智能体的计算分配
authors: "Sreeja Apparaju, Nilesh Gupta"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14635v2"
tags: ["query:am"]
score: 9.0
evidence: 针对记忆库不断增长的智能体进行推理密集型检索
tldr: 本研究探讨了长程智能体在处理推理密集型检索任务时的算力分配问题。通过在 BRIGHT 基准上使用 Gemini 2.5 系列模型，研究者对比了查询扩展和重排序两个阶段中模型容量、推理思考时间及重排序深度的影响。研究发现，重排序阶段是性能提升的关键，而查询扩展和推理时思考的边际收益较低。该研究为优化检索流水线的资源配置提供了重要指导，建议将算力集中于重排序环节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.14635v2/fig-001.webp\", \"caption\": \"Figure 2: Impact of re-ranking depth across query expansion strategies. Increasing the candidate pool from k = 10 to k = 100 consistently improves NDCG@10 by approximately 20% across all query expansion models, demonstrating that episodic retrieval depth is a high-leverage compute allocation mechanism.\", \"page\": 4, \"index\": 1, \"width\": 706, \"height\": 452}]"
motivation: 针对智能体在长程任务中面临的复杂推理检索需求，探索如何在查询扩展和重排序等阶段优化推理算力分配。
method: 在 BRIGHT 基准上，通过调整 Gemini 2.5 模型的规模、推理思考过程及重排序深度，系统评估了不同阶段的性能表现。
result: 重排序阶段通过增强模型和增加候选深度获得了显著提升，而查询扩展在轻量级模型之外收益递减，推理时思考则几乎没有改善。
conclusion: 检索系统的算力资源应优先投入到重排序阶段，而非在整个流水线中均匀分布。
---

## 摘要
随着智能体在长周期内运行，其内存存储持续增长，使得检索成为获取相关信息的关键。许多智能体查询需要推理密集型检索，其中查询与相关文档之间的联系是隐性的，需要通过推理来衔接。大语言模型（LLM）增强的流水线通过查询扩展和候选重排序来解决这一问题，但引入了显著的推理成本。我们利用 BRIGHT 基准测试和 Gemini 2.5 模型系列，研究了推理密集型检索流水线中的计算分配。我们在查询扩展和重排序阶段改变了模型容量、推理时思考以及重排序深度。我们发现，重排序从更强大的模型（NDCG@10 提升 7.5）和更深的候选池（k 从 10 增加到 100 时提升 21%）中获益显著，而查询扩展在超过轻量级模型后收益递减（从弱模型到强模型仅提升 1.1 NDCG@10）。推理时思考在任何阶段提供的改进都微乎其微。这些结果表明，计算资源应集中在重排序上，而不是均匀分布在流水线的各个阶段。

## Abstract
As agents operate over long horizons, their memory stores grow continuously, making retrieval critical to accessing relevant information. Many agent queries require reasoning-intensive retrieval, where the connection between query and relevant documents is implicit and requires inference to bridge. LLM-augmented pipelines address this through query expansion and candidate re-ranking, but introduce significant inference costs. We study computation allocation in reasoning-intensive retrieval pipelines using the BRIGHT benchmark and Gemini 2.5 model family. We vary model capacity, inference-time thinking, and re-ranking depth across query expansion and re-ranking stages. We find that re-ranking benefits substantially from stronger models (+7.5 NDCG@10) and deeper candidate pools (+21% from $k$=10 to 100), while query expansion shows diminishing returns beyond lightweight models (+1.1 NDCG@10 from weak to strong). Inference-time thinking provides minimal improvement at either stage. These results suggest that compute should be concentrated on re-ranking rather than distributed uniformly across pipeline stages.

---

## 论文详细总结（自动生成）

这是一份关于论文《Compute Allocation for Reasoning-Intensive Retrieval Agents》的结构化深入分析总结：

### 1. 研究动机与背景
随着 AI 智能体（Agents）运行时间的增加，其记忆库（Memory Stores）会不断膨胀。在处理复杂任务时，许多查询属于**推理密集型（Reasoning-Intensive）**：即查询词与目标文档之间没有直接的词汇重叠，必须通过逻辑推理才能建立联系。虽然目前主流采用“LLM 查询扩展 + 向量/词法检索 + LLM 重排序”的流水线，但 LLM 的调用成本极高。本研究的核心问题是：**在有限的计算预算下，应该如何将算力（模型规模、推理深度、检索深度）分配到检索流水线的不同阶段，以获得最优的检索效果？**

### 2. 方法论与核心思想
研究者将检索过程建模为一个三阶段流水线，并系统性地调整了三个维度的变量：
*   **流水线架构**：
    1.  **查询扩展 (QE)**：使用 LLM 将原始查询 $q$ 扩展为包含隐性约束和领域术语的 $q_{exp}$。
    2.  **初始检索**：使用 BM25 算法从海量语料中召回候选列表。
    3.  **列表式重排序 (RR)**：使用 LLM 对前 $k$ 个候选文档进行深度推理和重新排序。
*   **关键变量（计算分配维度）**：
    *   **模型能力（Model Strength）**：使用 Gemini 2.5 系列（Flash-Lite, Flash, Pro），代表从轻量到重量的算力投入。
    *   **推理深度（Thinking Depth）**：对比标准模式与开启“思维链/思考”模式（Think vs. No-Think）。
    *   **重排序深度（Re-ranking Depth）**：调整重排序的候选文档数量 $k \in \{10, 20, 50, 100\}$。

### 3. 实验设计与基准
*   **数据集/基准**：采用 **BRIGHT** 基准测试。这是一个专门为推理密集型检索设计的挑战性数据集，涵盖 12 个领域，要求模型具备多步推理和隐性约束满足能力。
*   **对比设置**：
    *   **基准线**：仅使用 BM25 检索（无 LLM 增强）。
    *   **消融实验 I**：固定重排序深度，改变 QE 和 RR 阶段的模型规模。
    *   **消融实验 II**：固定模型，改变重排序的候选池深度 $k$。
*   **评估指标**：NDCG@10（排序质量）、Recall@10（召回上限）、每查询成本（美元）和延迟（秒）。

### 4. 资源与算力
*   **算力消耗**：论文未明确提及具体的 GPU 型号、数量或训练时长。
*   **推理成本**：论文详细记录了 API 调用成本。例如，使用 Gemini 2.5 Pro 进行重排序的成本约为 0.17 美元/查询，而 Flash-Lite 仅为 0.021 美元。
*   **延迟**：记录了端到端延迟，最复杂的配置（Pro 模型重排序）单次查询耗时接近 80 秒。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 4 种模型变体、4 种重排序深度以及多种 QE 策略的组合。
*   **充分性评价**：实验设计较为**客观且具有针对性**。通过控制变量法清晰地展示了算力在不同阶段的边际效用。虽然仅使用了 Gemini 一个模型家族，但该家族内部梯度明显，足以支持关于“算力分配策略”的结论。

### 6. 主要结论与发现
*   **重排序是性能核心**：重排序阶段对模型规模高度敏感。升级重排序模型可带来显著增益（+7.5 NDCG@10），而升级查询扩展模型收益极低（仅 +1.1）。
*   **查询扩展存在天花板**：从无增强到轻量级 LLM 增强有质的飞跃，但进一步增加 QE 阶段的算力（如使用 Pro 模型）几乎没有额外收益。
*   **“思考”模式在检索中性价比极低**：在 QE 或 RR 阶段开启推理思考（Think 模式）不仅增加了 20%-50% 的成本和延迟，性能提升却微乎其微，甚至在 QE 阶段因“过度思考”导致性能下降。
*   **增加重排序深度（k值）非常有效**：将 $k$ 从 10 增加到 100，NDCG@10 稳定提升了约 20%。

### 7. 优点与亮点
*   **实用性强**：直接回答了开发者在构建 RAG 或 Agent 系统时最关心的“钱该花在哪”的问题。
*   **反直觉的发现**：指出了推理时思考（Inference-time thinking）在检索任务中的局限性，打破了“思考越久效果越好”的迷思。
*   **成本效益分析**：不仅关注准确率，还引入了成本和延迟维度，提供了多维度的决策参考。

### 8. 不足与局限
*   **模型单一性**：实验完全基于 Gemini 2.5 系列，虽然具有代表性，但结论是否能完美迁移到 OpenAI O1 或 DeepSeek 等其他具备推理能力的模型尚待验证。
*   **检索算法局限**：初始检索仅使用了 BM25，未探讨向量检索（Dense Retrieval）或混合检索在不同算力分配下的表现。
*   **基准单一**：仅在 BRIGHT 一个基准上进行了测试，未来需要更多样化的智能体记忆负载（如长对话历史、多模态记忆）来验证普适性。

（完）
