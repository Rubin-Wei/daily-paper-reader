---
title: "IndexCache: Accelerating Sparse Attention via Cross-Layer Index Reuse"
title_zh: IndexCache：通过跨层索引复用加速稀疏注意力
authors: "Yushi Bai, Qian Dong, Ting Jiang, Xin Lv, Zhengxiao Du, Aohan Zeng, Jie Tang, Juanzi Li"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12201v1"
tags: ["query:lcp"]
score: 9.0
evidence: 加速长上下文智能体工作流中的稀疏注意力机制
tldr: "针对长文本大模型推理中稀疏注意力索引器计算开销大的问题，本文提出 IndexCache。该方法利用相邻层间索引选择的高度相似性，通过“全量层”计算索引并供“共享层”复用，大幅减少了冗余计算。研究提供了无需训练的贪婪搜索和基于蒸馏的训练感知两种优化方案。实验表明，在保持模型性能的同时，IndexCache 可减少 75% 的索引计算，显著提升了预填充和解码速度，并在生产级模型上得到了验证。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.12201v1/fig-001.webp\", \"caption\": \"Table 1: End-to-end inference performance of the 30B DSA model with IndexCache at two retention ratios. Prefill time: seconds (lower is better). Decode per request: tokens/s under single concurrency (higher is better). Decode full: total tokens/s (higher is better). Decode throughput is reported per GPU.\", \"page\": 7, \"index\": 1, \"width\": 460, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2603.12201v1/fig-002.webp\", \"caption\": \"Figure 2: Side-by-side comparison of inference loops. (a) Standard DSA runs the lightning indexer at every layer. (b) IndexCache adds a single conditional branch (red lines): F layers compute and cache fresh indices; S layers reuse the cached indices. Note that Tcache is a temporary buffer holding only the current index tensor; it is overwritten at each F layer and requires no additional GPU memory beyond what standard DSA already allocates.\", \"page\": 3, \"index\": 2, \"width\": 831, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.12201v1/fig-003.webp\", \"caption\": \"Table 2: Training-free IndexCache at 1/2, 1/4, and 1/8 indexer retention. ‘Long’ and ‘G&R’ aggregate benchmark scores. We compare uniform interleaving against searched patterns.\", \"page\": 8, \"index\": 3, \"width\": 830, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2603.12201v1/fig-004.webp\", \"caption\": \"Figure 4: Pairwise top-k index overlap ratio between all layer pairs of the 30B DSA model. Shared blocks according to the greedy-searched 1/4 IndexCache pattern are marked.\", \"page\": 16, \"index\": 4, \"width\": 572, \"height\": 481}]"
motivation: 虽然稀疏注意力降低了核心计算量，但其索引器在每一层仍保持平方级复杂度，且不同层之间的索引选择存在大量冗余。
method: 将模型层分为计算索引的全量层和复用索引的共享层，并利用贪婪搜索优化层配置或通过多层蒸馏训练来对齐索引分布。
result: "在 30B 模型上成功移除 75% 的索引器计算，实现最高 1.82 倍的预填充加速和 1.48 倍的解码加速，且模型质量几乎无损。"
conclusion: IndexCache 通过跨层索引复用有效解决了稀疏注意力的计算瓶颈，是提升长文本大模型推理效率的实用且高效的方案。
---

## 摘要
长上下文智能体工作流已成为大语言模型的一个核心用例，这使得注意力机制的效率对于推理速度和部署成本都至关重要。稀疏注意力有效地应对了这一挑战，其中 DeepSeek 稀疏注意力（DSA）是一个代表性的生产级解决方案：它通过轻量级的闪电索引器为每个查询选择前 k 个最相关的标记，将核心注意力的复杂度从 $O(L^2)$ 降低到 $O(Lk)$。然而，索引器本身仍保留 $O(L^2)$ 的复杂度，且必须在每一层独立运行，尽管连续层之间产生的前 k 个选择结果高度相似。我们提出了 IndexCache，通过将模型层划分为运行自身索引器的少量“全量层”（Full layers）和直接复用最近全量层前 k 个索引的大多数“共享层”（Shared layers），来利用这种跨层冗余。我们提出了两种互补的方法来确定和优化此配置。免训练（Training-free）IndexCache 采用贪婪搜索算法，通过直接最小化校准集上的语言建模损失来选择保留索引器的层，无需更新权重。训练感知（Training-aware）IndexCache 引入了多层蒸馏损失，针对其服务的所有层的平均注意力分布来训练每个保留的索引器，使得即使是简单的交错模式也能达到全量索引器的准确度。在 30B DSA 模型上的实验结果表明，IndexCache 可以在质量损失微乎其微的情况下减少 75% 的索引器计算，与标准 DSA 相比，预填充速度提升高达 1.82 倍，解码速度提升高达 1.48 倍。这些积极的结果在我们对生产级 GLM-5 模型的初步实验中得到了进一步证实（图 1）。

## Abstract
Long-context agentic workflows have emerged as a defining use case for large language models, making attention efficiency critical for both inference speed and serving cost. Sparse attention addresses this challenge effectively, and DeepSeek Sparse Attention (DSA) is a representative production-grade solution: a lightweight lightning indexer selects the top-k most relevant tokens per query, reducing core attention from $O(L^2)$ to $O(Lk)$. However, the indexer itself retains $O(L^2)$ complexity and must run independently at every layer, despite the fact that the resulting top-k selections are highly similar across consecutive layers. We present IndexCache, which exploits this cross-layer redundancy by partitioning layers into a small set of Full layers that run their own indexers and a majority of Shared layers that simply reuse the nearest Full layer's top-k indices. We propose two complementary approaches to determine and optimize this configuration. Training-free IndexCache applies a greedy search algorithm that selects which layers to retain indexers by directly minimizing language modeling loss on a calibration set, requiring no weight updates. Training-aware IndexCache introduces a multi-layer distillation loss that trains each retained indexer against the averaged attention distributions of all layers it serves, enabling even simple interleaved patterns to match full-indexer accuracy. Experimental results on a 30B DSA model show that IndexCache can remove 75% of indexer computations with negligible quality degradation, achieving up to 1.82$\times$ prefill speedup and 1.48$\times$ decode speedup compared to standard DSA. These positive results are further confirmed by our preliminary experiments on the production-scale GLM-5 model (Figure 1).

---

## 论文详细总结（自动生成）

这是一份关于论文《IndexCache: Accelerating Sparse Attention via Cross-Layer Index Reuse》的结构化分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：在长上下文场景下，虽然 DeepSeek 稀疏注意力（DSA）通过“闪电索引器”将核心注意力复杂度降至 $O(Lk)$，但**索引器本身在每一层仍需进行 $O(L^2)$ 的全量计算**。随着序列长度增加（如 200K），索引器计算占总推理时间的比例大幅上升（预填充阶段可达 81%），成为新的性能瓶颈。
*   **研究动机**：作者观察到不同层之间的 Top-k 索引选择具有极高的相似性（相邻层重合度达 70%-100%）。因此，可以通过跨层复用索引来消除冗余计算，进一步加速长文本推理。

### 2. 方法论
IndexCache 将模型层分为 **Full (F) 层**（计算并缓存索引）和 **Shared (S) 层**（复用最近 F 层的索引）。核心技术包括：
*   **免训练 IndexCache (Training-free)**：
    *   **贪婪层选择算法**：不使用简单的均匀间隔复用，而是通过在小型校准集上最小化语言模型（LM）损失，逐层确定哪些索引器是“可牺牲”的。实验发现早期和过渡层对索引器缺失更敏感。
*   **训练感知 IndexCache (Training-aware)**：
    *   **多层蒸馏损失**：在持续预训练阶段，让 F 层的索引器同时学习它所服务的所有层（包括后续 S 层）的平均注意力分布。
    *   **数学证明**：证明了对多层 KL 散度求和的优化等价于对目标层平均分布的蒸馏，确保了索引器能生成具有“共识性”的 Top-k 集合。

### 3. 实验设计
*   **模型对象**：主要基于 30B 参数的 DSA 模型（源自 GLM-4.7-Flash），并扩展验证了 744B 的生产级模型 GLM-5。
*   **评测基准 (Benchmarks)**：
    *   **长上下文**：MRCR v2、GraphWalks、LongBench v2、RULER、AA-LCR。
    *   **通用与推理**：AIME 2025、GPQA-Diamond、LiveCodeBench v6、IFBench。
*   **对比方法**：标准 DSA（Baseline）、均匀间隔复用（Uniform Interleaving）、基于相似度的搜索（见附录 C 的负面结果）。

### 4. 资源与算力
*   **硬件环境**：推理测试在 NVIDIA H100 节点上完成，使用 SGLang 推理框架，开启 8 卡数据并行（DP）注意力。
*   **训练细节**：
    *   30B 模型训练：包含 1,000 步密集预热和 4,000 步稀疏训练。
    *   上下文长度：训练和搜索均在 200K 长度下进行。
    *   校准集：搜索算法使用了 768 个 batch 的 SFT 数据。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 30B 到 744B 的模型跨度，验证了方案的可扩展性。
*   **消融实验**：对比了“均匀模式 vs. 搜索模式”、“有无跨层蒸馏损失”、“免训练 vs. 训练感知”等多个维度。
*   **客观性**：作者诚实地报告了 1/8 极高压缩率下的性能下降，并分享了基于“输出相似度”进行搜索的失败尝试（附录 C），体现了学术严谨性。实验覆盖了长文本和通用推理，确保加速手段没有破坏模型的基础能力。

### 6. 主要结论与发现
*   **显著加速**：在 200K 上下文下，保留 1/4 索引器即可实现 **1.82× 的预填充加速**和 **1.48× 的解码加速**。
*   **质量保持**：通过贪婪搜索或多层蒸馏，IndexCache 在减少 75% 索引计算的同时，长文本任务得分与全量索引的 DSA 几乎持平。
*   **模式敏感性**：在免训练模式下，层选择至关重要；而在训练感知模式下，模型能适应均匀分布的复用模式。

### 7. 优点
*   **工程实现简单**：推理时仅需增加一个条件分支（if-else），无需复杂的算子重写或额外的显存开销。
*   **理论与实践结合**：不仅提出了启发式搜索，还通过数学证明支撑了多层蒸馏的合理性。
*   **生产级验证**：在 GLM-5 上的成功应用证明了该技术在超大规模模型部署中的实用价值。

### 8. 不足与局限
*   **搜索成本**：贪婪搜索虽然比全量训练快，但仍需进行多次前向传播（$O(N^2)$ 复杂度，虽有并行优化）。
*   **压缩极限**：当索引器保留率降至 1/8 或更低时，长文本性能会出现不可忽视的下滑。
*   **依赖特定架构**：该方法主要针对 DSA 这种具有显式、动态 Top-k 选择机制的稀疏注意力架构，对固定模式的稀疏注意力（如滑动窗口）意义较小。

（完）
