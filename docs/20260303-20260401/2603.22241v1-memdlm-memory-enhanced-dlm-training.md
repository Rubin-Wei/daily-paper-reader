---
title: "MemDLM: Memory-Enhanced DLM Training"
title_zh: MemDLM：记忆增强的 DLM 训练
authors: "Zehua Pei, Hui-Ling Zhen, Weizhe Lin, Sinno Jialin Pan, Yunhe Wang, Mingxuan Yuan, Bei Yu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22241v1"
tags: ["query:parametric-memory"]
score: 10.0
evidence: 扩散语言模型中的参数化记忆
tldr: 扩散语言模型（DLM）在训练与推理间存在单步预测与多步去噪的不匹配。本文提出MemDLM，通过双层优化将模拟去噪过程嵌入训练，利用内循环更新的“快速权重”形成参数化记忆，捕捉样本的局部轨迹经验。该方法减轻了Token表示的记忆压力，加速了收敛，并在推理阶段通过激活参数化记忆显著提升了长文本理解和检索能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22241v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of MemDLM. Left: standard MDLM training uses a static single-step denoising objective from xt to x0. Right: MemDLM uses Bi-level Optimization in which an inner loop updates fast weights ϕ along an anchor-consistent local trajectory (xtpre → xt → x0), and the outer loop updates the base model θ on the anchor state xt conditioned on this parametric memory. Legend: dark tokens denote mask tokens, light tokens denote observed tokens, straight arrows denote forward or reverse prediction flow, and blue curved arrows denote inner-loop fast-weight updates.\", \"page\": 3, \"index\": 1, \"width\": 826, \"height\": 352}]"
motivation: 解决扩散语言模型在训练时的静态单步预测与推理时的多步渐进去噪过程之间存在的不匹配问题。
method: 提出一种双层优化框架，利用内循环更新快速权重以构建参数化记忆，并结合外循环优化基础模型。
result: 实验表明MemDLM具有更快的训练收敛速度和更低的损失，且在长文本检索任务中表现优异。
conclusion: 通过将记忆压力从Token表示转移到模型参数中，MemDLM有效缩小了训练推理差距并增强了模型的上下文处理能力。
---

## 摘要
扩散语言模型 (DLMs) 相比自回归 (AR) 模型具有显著优势，例如全注意力并行解码和灵活生成。然而，它们面临着明显的训练与推理不匹配问题：DLMs 使用静态单步掩码预测目标进行训练，但通过多步渐进式去噪轨迹进行部署。我们提出了 MemDLM（记忆增强型 DLM），通过双层优化将模拟去噪过程嵌入到训练中，从而缩小了这一差距。内循环更新一组快速权重，形成捕获每个样本局部轨迹经验的参数化记忆，而外循环则在以此记忆为条件的情况下更新基础模型。通过将记忆压力从 token 表示转移到参数上，MemDLM 实现了更快的收敛和更低的训练损失。此外，内循环可以在推理时作为适应步骤重新启用，从而在长上下文理解方面获得额外收益。我们发现，当在推理时激活时，这种参数化记忆充当了一种涌现的权重内检索机制，帮助 MemDLM 在具有挑战性的“大海捞针”检索任务中进一步减少 token 级注意力瓶颈。代码：https://github.com/JarvisPei/MemDLM。

## Abstract
Diffusion Language Models (DLMs) offer attractive advantages over Auto-Regressive (AR) models, such as full-attention parallel decoding and flexible generation. However, they suffer from a notable train-inference mismatch: DLMs are trained with a static, single-step masked prediction objective, but deployed through a multi-step progressive denoising trajectory. We propose MemDLM (Memory-Enhanced DLM), which narrows this gap by embedding a simulated denoising process into training via Bi-level Optimization. An inner loop updates a set of fast weights, forming a Parametric Memory that captures the local trajectory experience of each sample, while an outer loop updates the base model conditioned on this memory. By offloading memorization pressure from token representations to parameters, MemDLM yields faster convergence and lower training loss. Moreover, the inner loop can be re-enabled at inference time as an adaptation step, yielding additional gains on long-context understanding. We find that, when activated at inference time, this Parametric Memory acts as an emergent in-weight retrieval mechanism, helping MemDLM further reduce token-level attention bottlenecks on challenging Needle-in-a-Haystack retrieval tasks. Code: https://github.com/JarvisPei/MemDLM.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《MemDLM: Memory-Enhanced DLM Training》** 的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：扩散语言模型（DLMs）在训练和推理之间存在严重的**“训练-推理不匹配”**（Train-Inference Mismatch）。
    *   **训练阶段**：采用静态的单步掩码预测（MDLM），模型基于纯净的上下文预测被掩码的 Token。
    *   **推理阶段**：采用多步渐进式去噪，模型必须基于自身生成的、带有噪声的中间表示进行预测。
*   **研究动机**：这种不匹配导致了“曝光偏差”（Exposure Bias），使得模型在长文本处理和复杂检索任务中，仅依靠脆弱的 Token 级注意力（Token-space representations）难以维持上下文一致性。作者希望通过引入“参数化记忆”来减轻 Token 表示的负担，增强模型的鲁棒性。

### 2. 方法论：核心思想与技术细节
MemDLM 的核心思想是**双层优化（Bi-level Optimization）**，将推理时的去噪轨迹模拟嵌入到训练过程中：
*   **内循环（Inner Loop - 记忆形成）**：
    *   针对每个训练样本，初始化一组临时的“快速权重”（Fast Weights，通常使用 LoRA 适配器 $\phi$）。
    *   执行“锚点一致轨迹”模拟：首先从更嘈杂的状态 $x_{t_{pre}}$ 预测到锚点状态 $x_t$，再从 $x_t$ 预测到清洁状态 $x_0$。
    *   通过梯度下降更新 $\phi$，使其吸收该样本的局部轨迹经验，形成**参数化记忆**。
*   **外循环（Outer Loop - 基础模型更新）**：
    *   在内循环生成的参数化记忆 $\phi$ 的条件下，计算基础模型 $\theta$ 的去噪损失。
    *   使用一阶近似更新基础模型，使其学会利用这种参数化记忆来辅助去噪。
*   **推理阶段自适应**：
    *   在推理时，可以重新启用内循环，将长提示词（Prompt）的信息显式地“写入”快速权重中，实现一种**权重内检索（In-weight Retrieval）**机制。

### 3. 实验设计
*   **基座模型**：LLaDA-MoE-7B-A1B-Base 和 LLaDA2.1-mini。
*   **数据集与场景**：
    *   **微调数据**：LongAlpaca 数据集（过滤至 4096 长度）。
    *   **长文本检索（NIAH）**：RULER 评测集（包含 MV、VT、CWE 子任务）和 BABILong。
    *   **通用长文本理解**：LongBench（包含 TriviaQA、MultiNews 等 10+ 任务）。
*   **对比方法**：标准 MDLM（Standard Masked Diffusion Language Model）作为主要 Baseline。
*   **评估维度**：训练收敛速度、曝光偏差率、长文本检索准确率、长度外推能力（最高至 32K）。

### 4. 资源与算力
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和数量。
*   **实现细节**：
    *   模型加载采用 **4-bit 量化**。
    *   使用 **LoRA**（秩 $r=32$）进行内外循环更新。
    *   内循环仅针对最后 10% 层级的 FFN 模块进行更新，以减少计算开销。
    *   训练步数约为 1000 步，采用 AdamW 优化器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同的主流 DLM 架构上验证了有效性。
    *   进行了详尽的**消融实验**（约 10 组以上），涵盖了内循环监督信号（交叉熵 vs 蒸馏）、适配范围（FFN vs Attention）、梯度归一化方式、预锚点设计等。
*   **充分性评价**：实验设计非常**充分且客观**。作者不仅展示了最终性能的提升，还通过“曝光偏差率”量化了方法论的科学性，并对比了“仅训练增强”与“训练+推理增强”的不同效果，证明了基础模型本身在训练后也得到了优化。

### 6. 主要结论与发现
*   **性能提升**：在 LLaDA-MoE 上，8K 长度的变量追踪（RULER-VT）准确率从 78.8% 提升至 **95.8%**；在 LLaDA2.1 上，BABILong 8K 准确率从 47.4% 提升至 **57.0%**。
*   **优化加速**：MemDLM 在训练过程中收敛速度明显快于标准 MDLM，且能达到更低的训练和验证损失。
*   **鲁棒性增强**：即使在推理时不启用内循环（Train-Only 模式），MemDLM 训练出的基础模型在处理长文本时也比基准模型更强，说明参数化记忆训练改善了模型对 Token 表示的利用方式。
*   **长度外推**：模型在超出训练长度（如 16K、32K）的情况下，依然保持了对基准模型的领先优势。

### 7. 优点（亮点）
*   **理论优雅**：通过双层优化巧妙地解决了扩散模型固有的训练-推理不匹配问题。
*   **减轻瓶颈**：将记忆压力从“Token 注意力空间”转移到“参数空间”，有效缓解了长文本下的注意力稀释问题。
*   **灵活性高**：推理时的内循环是一个可选的插件，用户可以根据算力预算决定是否开启以获得额外性能。
*   **通用性**：实验证明该框架适用于不同的 DLM 骨干网络（如 MoE 架构和密集架构）。

### 8. 不足与局限
*   **推理开销**：虽然内循环是参数高效的，但在推理前对 Prompt 进行梯度更新仍会增加首字延迟（TTFT）。
*   **超参数敏感性**：内循环的学习率、步数和适配层级需要精细调优，文中虽给出了推荐值，但在不同任务上的泛化性仍需更多验证。
*   **任务覆盖**：实验重点在于长文本和检索，对于短文本生成质量、创意写作等通用 NLP 任务的影响讨论较少。
*   **算力透明度**：缺乏具体的训练时长和硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
