---
title: Model Editing for New Document Integration in Generative Information Retrieval
title_zh: 生成式信息检索中新文档集成的模型编辑
authors: "Zhen Zhang, Zihan Wang, Xinyu Ma, Shuaiqiang Wang, Dawei Yin, Xin Xin, Pengjie Ren, Maarten de Rijke, Zhaochun Ren"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02773v1"
tags: ["query:pm"]
score: 9.0
evidence: 生成式检索中针对参数修改的模型编辑
tldr: "生成式检索（GR）在处理新文档时面临泛化差和增量训练成本高的问题。本文提出DOME（面向docID的模型编辑）方法，通过识别关键层、优化编辑向量及应用更新三个阶段，实现对新文档的高效集成。DOME采用混合标签自适应训练策略，结合软硬标签以学习更具辨别力的编辑向量。实验证明，该方法在显著提升新文档检索性能的同时，保持了原始集合的效果，且训练时间仅为增量训练的60%，显著提升了GR模型的实用性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.02773v1/fig-001.webp\", \"caption\": \"Figure 1: Behavioral analysis of the initial and new documents over the NQ dataset [18] using a hierarchical k-meansbased docID GR model [38]. The model is trained on the initial document set, and docIDs for new documents are assigned using the same k-means clustering procedure. (a) T-SNE [24] visualization of intermediate representations, where different colors denote different docID prefixes (i.e., 10, 30, 50), with each prefix corresponding to a specific document type. Squares represent initial documents, and circles represent new documents. (b) Retrieval performance (Recall@1 and Recall@100) on the initial and new document sets.\", \"page\": 2, \"index\": 1, \"width\": 489, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2603.02773v1/fig-002.webp\", \"caption\": \"Table 2: Retrieval performance on the NQ [18] and MS-MARCO [31] datasets. Each cell reports score for initial documents / new documents. Bold and underlined denote the best and second-best results, respectively, for each metric. “From Scratch” is reported only as a theoretical upper bound and is not considered in ranking due to its high retraining cost. * Significant improvements against the best-performing baseline for each dataset are marked with * (t-test, 𝑝 < 0.05).\", \"page\": 7, \"index\": 2, \"width\": 990, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2603.02773v1/fig-003.webp\", \"caption\": \"Figure 5: Accuracies of average patching across decoder layers for predicting docID tokens at positions 0–3 on NQ.\", \"page\": 10, \"index\": 3, \"width\": 495, \"height\": 1125}, {\"url\": \"assets/figures/arxiv/2603.02773v1/fig-004.webp\", \"caption\": \"Figure 4: Percentage of pairwise cosine similarity of edit vector 𝛿 across GR edit requests on NQ. Yellow denotes pairs with different target docID, blue denotes pairs with the same target docID.\", \"page\": 8, \"index\": 4, \"width\": 467, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2603.02773v1/fig-005.webp\", \"caption\": \"Table 4: Ablation study on NQ.\", \"page\": 8, \"index\": 5, \"width\": 504, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2603.02773v1/fig-006.webp\", \"caption\": \"Figure 2: Percentage of pairwise cosine similarity of edit vector 𝛿 across GR edit requests applied to the GR method [38] on NQ [18]. Yellow denotes pairs with different target docID, blue denotes pairs with the same target docID.\", \"page\": 4, \"index\": 6, \"width\": 491, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.02773v1/fig-007.webp\", \"caption\": \"Figure 3: An overview of the proposed DOME framework. (a) A patching technique is used to diagnose the model and locate the critical layers responsible for docID mapping. (b) Our hybrid-label adaptive training strategy is employed to compute diverse and effective edit vectors (𝛿) for the new mappings. For clarity, the diagram retains only the crucial𝑊out component from the FFN layer. (c) These edit vectors are then used to construct an update matrix (Δ), which is applied to the crucial layer parameters (𝑊out) to inject the new docID mapping.\", \"page\": 5, \"index\": 7, \"width\": 972, \"height\": 429}]"
motivation: 针对生成式检索模型在集成新文档时面临的计算开销大、灾难性遗忘以及解码器难以准确映射新docID的瓶颈问题。
method: 提出DOME方法，通过识别关键层并利用结合软硬标签的自适应训练策略来优化编辑向量，实现对模型参数的精准修改。
result: "实验表明DOME在NQ和MS MARCO数据集上显著提升了新文档检索性能，且训练时间仅为传统增量训练的60%。"
conclusion: 模型编辑为生成式检索中的新文档集成提供了一种高效且可扩展的解决方案，在保证检索精度的同时大幅降低了更新成本。
---

## 摘要
生成式检索（GR）将信息检索（IR）任务重新表述为文档标识符（docID）的生成过程。尽管该方法前景广阔，但现有的 GR 模型对新添加文档的泛化能力较差，往往无法生成正确的 docID。虽然增量训练提供了一种直接的补救措施，但其计算开销大、资源消耗高，且容易出现灾难性遗忘，从而限制了 GR 的可扩展性和实用性。在本文中，我们发现核心瓶颈在于解码器将隐藏状态映射到新添加文档的正确 docID 的能力。模型编辑能够针对 docID 映射进行定向参数修改，是一种极具前景的解决方案。然而，将模型编辑应用于当前的 GR 模型并非易事，由于检索结果中共享 docID 的高度重叠，导致不同查询之间的编辑向量难以区分，这严重阻碍了其应用。为此，我们提出了 DOME（面向 docID 的模型编辑），这是一种能够有效且高效地使 GR 模型适应未见文档的新方法。DOME 包含三个阶段：(1) 关键层识别，(2) 编辑向量优化，以及 (3) 更新的构建与应用。DOME 的核心采用了一种混合标签自适应训练策略，通过结合软标签（保留特定查询的语义以实现可区分的更新）和硬标签（强制执行精确的映射修改）来学习具有辨别力的编辑向量。在包括 NQ 和 MS MARCO 在内的广泛使用的基准测试上的实验表明，我们的方法在保持对原始集合有效性的同时，显著提高了在新文档上的检索性能。此外，DOME 仅需增量训练约 60% 的训练时间即可实现这一目标，大幅降低了计算成本，并支持高效、频繁的模型更新。

## Abstract
Generative retrieval (GR) reformulates the Information Retrieval (IR) task as the generation of document identifiers (docIDs). Despite its promise, existing GR models exhibit poor generalization to newly added documents, often failing to generate the correct docIDs. While incremental training offers a straightforward remedy, it is computationally expensive, resource-intensive, and prone to catastrophic forgetting, thereby limiting the scalability and practicality of GR. In this paper, we identify the core bottleneck as the decoder's ability to map hidden states to the correct docIDs of newly added documents. Model editing, which enables targeted parameter modifications for docID mapping, represents a promising solution. However, applying model editing to current GR models is not trivial, which is severely hindered by indistinguishable edit vectors across queries, due to the high overlap of shared docIDs in retrieval results. To address this, we propose DOME (docID-oriented model editing), a novel method that effectively and efficiently adapts GR models to unseen documents. DOME comprises three stages: (1) identification of critical layers, (2) optimization of edit vectors, and (3) construction and application of updates. At its core, DOME employs a hybrid-label adaptive training strategy that learns discriminative edit vectors by combining soft labels, which preserve query-specific semantics for distinguishable updates, with hard labels that enforce precise mapping modifications. Experiments on widely used benchmarks, including NQ and MS MARCO, show that our method significantly improves retrieval performance on new documents while maintaining effectiveness on the original collection. Moreover, DOME achieves this with only about 60% of the training time required by incremental training, considerably reducing computational cost and enabling efficient, frequent model updates.

---

## 论文详细总结（自动生成）

这是一份关于论文《Model Editing for New Document Integration in Generative Information Retrieval》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：生成式检索（Generative Retrieval, GR）通过直接生成文档标识符（docID）来完成检索，但在处理**新加入系统的文档**时表现极差。
*   **研究动机**：
    *   **泛化瓶颈**：作者发现 GR 模型的编码器能理解新文档语义，但解码器无法将这些语义准确映射到未见过的 docID 序列。
    *   **传统方案缺陷**：现有的“增量训练”方法计算成本高昂，且容易导致“灾难性遗忘”（即学会新文档后忘记旧文档）。
    *   **模型编辑的挑战**：直接套用现有的模型编辑技术（如 ROME, MEMIT）在 GR 场景下效果不佳，因为多个查询往往对应相同的 docID 标记，导致编辑向量（edit vectors）缺乏辨别力，产生冲突。

### 2. 论文提出的方法论：DOME
DOME（docID-Oriented Model Editing）通过三个阶段实现高效的新文档集成：
*   **核心思想**：针对解码器中负责 docID 映射的关键 FFN 层进行微小且精准的参数修改，而非全量重训。
*   **关键技术细节**：
    1.  **关键层识别**：利用 **Average Patching** 技术诊断模型，定位出解码器中对 docID 生成影响最大的 FFN 模块（实验确定为第 14-18 层）。
    2.  **混合标签自适应训练（核心创新）**：为了解决编辑向量不可区分的问题，DOME 不直接使用 One-hot 硬标签，而是将模型的**原始输出分布（软标签）**与**真实 docID（硬标签）**结合。随着训练进行，权重逐渐从软标签转向硬标签，既保留了查询特有的语义多样性，又确保了最终生成的精确度。
    3.  **约束更新构建**：采用闭式解（Closed-form solution）计算参数更新矩阵 $\Delta$，并在更新中引入原始语料的键值对约束，确保在注入新知识的同时，最小化对旧知识空间的干扰。

### 3. 实验设计
*   **数据集**：使用了两个标准的大规模检索基准测试：**Natural Questions (NQ)** 和 **MS-MARCO**。
*   **场景设置**：将语料库分为 90% 的初始文档和 10% 的新加入文档。
*   **对比方法（Baselines）**：
    *   **无训练类**：BM25, DPR, DSI, Ultron。
    *   **增量训练类**：New-Doc FT, DSI++（目前最强的增量学习 GR 模型）。
    *   **模型编辑类**：ROME, MEMIT, AlphaEdit（经过适配 GR 任务的修改）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A100 GPU**。
*   **效率指标**：
    *   DOME 的每个文档更新时间（TPD）约为 **2.14 秒**。
    *   相比于增量训练方法（如 DSI++ 的 3.54 秒），DOME 减少了约 **40%** 的时间开销。
    *   训练过程仅需 50 个优化步骤即可完成编辑向量的提取。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括检索精度（Recall@K, MRR）、遗忘得分（$F_n$）以及效率测试。
*   **消融实验**：非常充分。作者探讨了伪查询数量、docID 分配方案（RQ, PQ, BM25-based）、编辑层范围的选择、以及混合标签策略中软/硬标签的必要性。
*   **客观性**：通过 T-SNE 可视化和余弦相似度分析验证了方法论的底层逻辑，实验对比涵盖了从传统检索到最新生成式检索的各类前沿方法，结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能卓越**：DOME 在新文档上的检索性能（如 NQ 上的 R@1）接近甚至在某些指标上超过了全量重训（From Scratch），显著优于现有的模型编辑方法。
*   **抗遗忘性强**：在集成新文档后，DOME 对原始文档的检索精度几乎没有下降（遗忘得分仅为 0.003），远优于其他编辑技术。
*   **高效可扩展**：随着新文档数量从 100 增加到 1000，DOME 的性能保持稳定，证明了其在动态环境下的实用性。

### 7. 优点
*   **针对性强**：深刻洞察了 GR 模型在解码阶段的映射瓶颈，而非盲目进行全模型更新。
*   **解决冲突**：通过混合标签策略巧妙解决了 GR 任务中 docID 词表受限导致的编辑向量重叠问题。
*   **低成本高收益**：在大幅降低计算资源和时间成本的同时，解决了灾难性遗忘这一长期困扰 GR 的难题。

### 8. 不足与局限
*   **模块局限性**：目前 DOME 仅针对解码器的 FFN 层进行编辑，尚未探索 Attention 层或 Encoder 部分的修改对性能的潜在提升。
*   **依赖伪查询**：新文档的集成高度依赖于伪查询生成模型（如 docTTTTTquery）的质量，如果伪查询质量不佳，编辑效果可能会受限。
*   **长序列挑战**：虽然在结构化数字 docID 上表现良好，但对于极长的文本标识符或更复杂的生成路径，其稳定性仍需进一步验证。

（完）
