---
title: "DiSCTT: Consensus-Guided Self-Curriculum for Efficient Test-Time Adaptation in Reasoning"
title_zh: DiSCTT：用于推理中高效测试时自适应的共识引导自课程框架
authors: "Mohammad Mahdi Moradi, Sudhir Mudur"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05357v1"
tags: ["query:ttt"]
score: 10.0
evidence: 推理任务的高效测试时自适应
tldr: 针对大语言模型推理任务中测试时自适应（TTA）效率低且不稳定的问题，本文提出DiSCTT框架。该框架通过采样轨迹的一致性评估实例难度，动态分配优化策略：对高一致性样本进行监督微调，对低一致性样本采用强化学习。实验表明，DiSCTT在多个推理基准上显著提升了准确率，同时大幅降低了计算开销和训练时间，实现了更稳定高效的推理自适应。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.05357v1/fig-001.webp\", \"caption\": \"Table 1. Mean accuracy (± standard deviation) across six mathematical and general reasoning benchmarks under test-time adaptation. Results are averaged over independent runs with different random seeds. DiSCTT consistently outperforms the Base model and prior test-time adaptation baselines across model families and scales. Bold denotes the best-performance for each benchmark and model size.\", \"page\": 6, \"index\": 1, \"width\": 1008, \"height\": 619}, {\"url\": \"assets/figures/arxiv/2603.05357v1/fig-002.webp\", \"caption\": \"Figure 5. Difficulty-level accuracy dynamics on MATH-500 under different training paradigms. We compare (a) SFT-only, (b) RL-only (GRPO), and (c) DiSCTT across five difficulty levels (L1–L5) over adaptation steps.\", \"page\": 8, \"index\": 2, \"width\": 808, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2603.05357v1/fig-003.webp\", \"caption\": \"Table 3. Compute efficiency comparison between DiSCTT and TTRL. Tables present (a) total training compute in FLOPs and (b) wall-clock training time. By routing high-consensus instances to SFT and applying RL only to low-consensus instances, DiSCTT substantially reduces compute relative to RL-only TTRL. Cost Ratio denotes relative compute cost (DiSCTT/TTRL).\", \"page\": 8, \"index\": 3, \"width\": 998, \"height\": 507}, {\"url\": \"assets/figures/arxiv/2603.05357v1/fig-004.webp\", \"caption\": \"Figure 3. Overview of DiSCTT. For each input, multiple reasoning completions are sampled and evaluated for consensus. High-consensus inputs are routed to supervised fine-tuning using majority-agreed solutions as pseudo-labels, while low-consensus inputs are optimized via reinforcement learning. This difficulty-aware bifurcation is periodically recomputed, yielding a self-evolving test-time curriculum.\", \"page\": 4, \"index\": 4, \"width\": 917, \"height\": 542}]"
motivation: 现有的测试时自适应方法对所有输入采用统一优化目标，难以应对不同难度推理问题的异质性，导致效率低下且不稳定。
method: 提出一种基于共识引导的自课程学习框架，利用采样一致性评估不确定性，并据此动态切换监督微调与强化学习策略。
result: 在数学和通用推理基准上，DiSCTT在提升准确率的同时，显著降低了方差、计算量和训练耗时。
conclusion: 通过显式建模实例难度和不确定性，可以实现更稳定、高效且有效的推理模型测试时自适应。
---

## 摘要
测试时自适应为在无需额外监督的情况下提升大语言模型的推理性能提供了一条极具前景的途径，但现有方法通常对所有输入应用统一的优化目标，导致在异构推理问题上的自适应效率低下或不稳定。我们提出了 DiSCTT，这是一个难度感知、共识引导的自课程框架，它根据从采样推理轨迹的一致性中估计出的实例级认知不确定性，动态分配测试时优化策略。高共识的输入通过监督微调进行巩固，使用多数一致的解作为伪标签；而低共识的输入则通过强化学习进行优化，采用共识正则化目标，在相关性约束下鼓励多样性。在广泛的数学和通用推理基准测试中，DiSCTT 一贯优于强大的测试时自适应基线，以更低的方差以及显著减少的计算量和实际训练时间实现了更高的准确率。这些结果表明，显式地考虑实例难度和不确定性，能够为推理模型实现更稳定、更高效且更有效的测试时自适应。

## Abstract
Test-time adaptation offers a promising avenue for improving reasoning performance in large language models without additional supervision, but existing approaches often apply a uniform optimization objective across all inputs, leading to inefficient or unstable adaptation on heterogeneous reasoning problems. We propose DiSCTT, a difficulty-aware, consensus-guided self-curriculum framework that dynamically allocates test-time optimization strategies based on instance-level epistemic uncertainty estimated from agreement among sampled reasoning trajectories. Inputs with high consensus are consolidated via supervised fine-tuning using majority-agreed solutions as pseudo-labels, while low-consensus inputs are optimized via reinforcement learning with a consensus-regularized objective that encourages diversity under relevance constraints. Across a broad suite of mathematical and general reasoning benchmarks, DiSCTT consistently outperforms strong test-time adaptation baselines, achieving higher accuracy with reduced variance and substantially lower computation and wall-clock training times. These results demonstrate that explicitly accounting for instance difficulty and uncertainty enables more stable, efficient, and effective test-time adaptation for reasoning models.

---

## 论文详细总结（自动生成）

### 论文总结：DiSCTT - 用于推理中高效测试时自适应的共识引导自课程框架

#### 1. 核心问题与整体含义（研究动机和背景）
大型语言模型（LLM）在推理任务中通常采用静态推理策略，无法根据输入问题的难度动态调整。现有的**测试时自适应（Test-Time Adaptation, TTA）**方法（如 TTRL 或 EVOL-RL）往往对所有输入应用统一的优化目标：
*   **问题：** 这种“一刀切”的做法在简单实例上会导致过度优化和不必要的计算开销，而在困难实例上则可能因探索不足或强化学习（RL）的不稳定性导致性能崩溃。
*   **核心目标：** 提出一种能够在线估计实例难度，并据此动态分配“监督巩固（SFT）”或“强化探索（RL）”策略的框架，以实现更高效、更稳定的测试时性能提升。

#### 2. 方法论：核心思想与关键技术
DiSCTT 的核心是**难度感知的自课程学习机制**，其流程如下：
*   **共识引导的难度估计：** 对每个输入采样 $M$ 条推理轨迹，计算最终答案的一致性比例（Consensus Score）。高一致性意味着低认知不确定性（简单题），低一致性意味着高不确定性（难题）。
*   **动态路由与自课程：** 
    *   **$D_{easy}$（高共识）：** 采用**监督微调（SFT）**。使用多数投票选出的答案作为伪标签，旨在巩固模型已掌握的正确推理模式。
    *   **$D_{hard}$（低共识）：** 采用**强化学习（基于 GRPO 算法）**。通过精心设计的奖励函数鼓励模型探索新的推理路径。
*   **稳定化奖励函数：** RL 阶段的奖励由三部分组成：
    1.  **正确性门控：** 仅奖励与多数票一致的解。
    2.  **群体相对新颖性：** 使用 **Jensen-Shannon (JS) 散度**衡量当前轨迹与群体主流轨迹的区别，鼓励多样化探索。
    3.  **相关性语义门控：** 利用句子嵌入（Embedding）计算推理步骤与题目的余弦相似度，防止模型为了追求“新颖”而产生跑题或无关的推理。
*   **周期性更新：** 每隔 $K$ 个训练步重新采样并重新划分难易集合，使课程随模型能力的提升而进化。

#### 3. 实验设计
*   **数据集（Benchmarks）：** 涵盖了数学推理（AMC, MATH-500, AIME-2024, GPQA）、多跳问答（HotpotQA）和通用知识推理（MMLU）。
*   **对比方法：** 
    *   **Base：** 原始预训练/指令微调模型。
    *   **TTRL：** 统一应用强化学习的测试时自适应。
    *   **EVOL-RL：** 引入多样性奖励的统一强化学习方法。
    *   **消融变体：** SFT-only、RL-only、固定难易划分等。
*   **覆盖模型：** Qwen-2.5 (0.5B, 7B), LLaMA-3.2 (1B, 3B), Qwen-3 (1.7B, 4B)，涵盖了不同参数规模和指令微调状态。

#### 4. 资源与算力
*   **硬件环境：** 使用单台配备 **8 张 NVIDIA RTX A6000 (50GB)** 的服务器。
*   **训练时长与开销：** 论文详细对比了计算效率（见表 3）。
    *   例如，LLaMA-3.2-1B 在 MMLU 上的总训练时间约为 124 小时，相比 TTRL 减少了约 48.7% 的时间。
    *   Qwen-2.5-7B 在 AMC 上的训练时间约为 15.5 小时，相比 TTRL 节省了约 47.9%。
    *   计算量（FLOPs）方面，DiSCTT 通常比全 RL 方法节省 30% 到 50% 的算力。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 6 个主流基准测试和 6 个不同规模的模型上进行了交叉验证，实验设计较为全面。
*   **充分性：** 
    *   包含了**分布外（OOD）泛化实验**，验证了模型在适应特定任务时不会发生灾难性遗忘或过拟合。
    *   进行了**奖励项分解消融实验**，证明了正确性门控、新颖性奖励和语义门控各自的必要性。
    *   展示了不同难度等级（Level 1-5）下的准确率动态变化曲线，直观证明了方法对难题的提升效果。
*   **客观性：** 实验采用了多次独立运行取平均值及标准差的方式，结果显示 DiSCTT 不仅提升了均值，还显著降低了性能波动的方差。

#### 6. 主要结论与发现
*   **性能领先：** DiSCTT 在所有基准测试中均优于现有的 TTA 方法，尤其在复杂数学推理（如 MATH-500）上提升显著。
*   **效率极高：** 通过将简单题路由至低成本的 SFT，DiSCTT 在达到更高准确率的同时，大幅削减了测试时训练的计算成本和时间。
*   **稳定性强：** 相比于纯 RL 方法容易出现的性能崩溃，DiSCTT 的自课程机制提供了更平滑的收敛曲线。
*   **泛化性：** 在一个领域（如数学）进行测试时自适应，不仅提升了该领域性能，还能带动其他推理领域（如代码、通用逻辑）的性能增长。

#### 7. 优点（亮点）
*   **难度感知路由：** 首次在推理 TTA 中引入基于共识的动态路由，解决了计算资源分配不均的问题。
*   **JS 散度奖励：** 相比 KL 散度，JS 散度在处理低概率事件时更稳定，为 label-free 的 RL 提供了更好的梯度。
*   **语义约束：** 引入语义门控有效解决了强化学习中常见的“奖励作弊（Reward Hacking）”和推理漂移问题。

#### 8. 不足与局限
*   **多数票依赖：** 该方法依赖“多数票即正确”的假设。如果模型初始能力极弱，导致多数票答案本身是错误的，SFT 阶段可能会强化错误的推理模式。
*   **采样开销：** 虽然训练开销降低了，但为了估计难度，每个样本仍需进行多次采样（如 $M=8$），这在实时性要求极高的推理场景中可能仍显沉重。
*   **超参数敏感性：** 阈值 $\rho$ 和更新间隔 $K$ 的选择可能需要针对不同模型进行微调，论文未深入探讨这些参数在极端情况下的鲁棒性。

（完）
