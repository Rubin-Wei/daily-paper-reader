---
title: Density-aware Soft Context Compression with Semi-Dynamic Compression Ratio
title_zh: 具有半动态压缩率的密度感知软上下文压缩
authors: "Yijiong Yu, Shuai Yuan, Jie Zheng, Huazheng Wang, Ji Pei"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25926v1"
tags: ["query:cd"]
score: 9.0
evidence: 具有半动态压缩率的软上下文压缩
tldr: 针对大语言模型长文本处理开销大的问题，本文提出了一种半动态上下文压缩框架。该方法通过离散比例选择器，根据文本信息密度的差异动态调整压缩率，并将其量化为预定义的离散值，解决了传统静态压缩率无法适应信息密度变化以及全动态参数难以训练的问题。实验证明该方法在性能与效率之间取得了更好的平衡，显著提升了长文本处理的质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25926v1/fig-001.webp\", \"caption\": \"Figure 4: Accuracy vs. Average Compression Ratio for fixed-ratio vs. semidynamic mean-pooling. For the semi-dynamic, the scale parameter is varied across {−2,−1.5,−1,−0.5, 0, 0.5, 1, 1.5, 2, 2.5, 3, 3.5, 4} to achieve gradually growing compression ratio. The baseline dashed line represents not using compression.\", \"page\": 7, \"index\": 1, \"width\": 498, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2603.25926v1/fig-002.webp\", \"caption\": \"Figure 3: Accuracy vs. Average Compression Ratio across three feature extraction methods (mean-pooling, last tokens, compression tokens), evaluated under fixed-ratio and fixedlength settings.\", \"page\": 7, \"index\": 2, \"width\": 582, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2603.25926v1/fig-003.webp\", \"caption\": \"Figure 2: Our semi-dynamic context compression method, utilizing mean-pooling as the optimal structural backbone.\", \"page\": 4, \"index\": 3, \"width\": 582, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.25926v1/fig-004.webp\", \"caption\": \"Figure 1: Three typical feature extraction mechanisms for soft context compression.\", \"page\": 3, \"index\": 4, \"width\": 498, \"height\": 302}]"
motivation: 现有方法采用统一压缩率，忽略了自然语言信息密度的差异，且全动态压缩率在模型训练中难以稳定收敛。
method: 提出半动态压缩框架，利用离散比例选择器预测信息密度并将其量化为预设的离散压缩率，结合均值池化进行联合训练。
result: 实验结果显示，该密度感知框架在各项评估中均优于静态基准，在压缩率与性能之间建立了更优的帕累托前沿。
conclusion: 通过引入离散化的动态压缩机制，模型能更灵活地分配计算资源，为高效长文本处理提供了新的有效路径。
---

## 摘要
软上下文压缩通过将长上下文编码为较少数量的潜标记（latent tokens），减少了大型语言模型（LLMs）处理长上下文的计算工作量。然而，现有框架采用统一的压缩率，未能考虑到自然语言信息密度的极大差异。虽然采用密度感知的动态压缩率看似直观，但实证研究表明，模型在处理由输入相关的连续结构超参数参数化的操作时存在内在困难。为了解决这一缺陷，我们引入了半动态上下文压缩框架。我们的方法具有一个离散率选择器（Discrete Ratio Selector），它根据内在信息密度预测压缩目标，并将其量化为一组预定义的离散压缩率。该选择器与压缩器在合成数据上进行高效的联合训练，并以摘要长度作为代理来创建压缩率预测的标签。广泛的评估证实，我们以均值池化为骨干的密度感知框架始终优于静态基准，为上下文压缩技术建立了稳健的帕累托前沿（Pareto frontier）。我们的代码、数据和模型权重可在 https://github.com/yuyijiong/semi-dynamic-context-compress 获取。

## Abstract
Soft context compression reduces the computational workload of processing long contexts in LLMs by encoding long context into a smaller number of latent tokens. However, existing frameworks apply uniform compression ratios, failing to account for the extreme variance in natural language information density. While adopting a density-aware dynamic compression ratio seems intuitive, empirical investigations reveal that models struggle intrinsically with operations parameterized by input dependent, continuous structural hyperparameters. To resolve this pitfall, we introduce Semi-Dynamic Context Compression framework. Our approach features a Discrete Ratio Selector, which predicts a compression target based on intrinsic information density and quantizes it to a predefined set of discrete compression ratios. It is efficiently jointly trained with the compressor on synthetic data, with the summary lengths as a proxy to create labels for compression ratio prediction. Extensive evaluations confirm that our density-aware framework, utilizing mean pooling as the backbone, consistently outperforms static baselines, establishing a robust Pareto frontier for context compression techniques. Our code, data and model weights are available at https://github.com/yuyijiong/semi-dynamic-context-compress

---

## 论文详细总结（自动生成）

这是一份关于论文《Density-aware Soft Context Compression with Semi-Dynamic Compression Ratio》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：大型语言模型（LLM）在处理长上下文时面临巨大的计算和内存开销。现有的“软上下文压缩”技术（将文本编码为较少的潜标记）通常采用**固定压缩率**，忽略了自然语言信息密度的巨大差异（例如，技术文档比日常对话更“稠密”）。
*   **研究动机**：
    *   **静态压缩的局限**：统一压缩率会导致冗余信息占用过多资源，或关键信息因过度压缩而丢失。
    *   **全动态压缩的陷阱**：作者发现，如果让模型预测并应用完全连续的动态压缩参数（如任意的标记数量），会导致模型性能崩溃。这是因为 LLM 难以适应无限变化的计算子图结构。
*   **整体含义**：提出一种“半动态”框架，在适应信息密度的同时，通过将预测结果量化为有限的离散集合，确保模型训练的稳定性和推理的高效性。

### 2. 论文提出的方法论
核心思想是引入**离散比例选择器（Discrete Ratio Selector, DRS）**，将连续的密度预测转化为离散的结构化操作。
*   **核心流程**：
    1.  **编码与预测**：Encoder 提取上下文特征，其末端连接一个线性回归头，预测文本的内在信息密度 $\hat{y}$（在对数空间）。
    2.  **用户可控缩放**：引入 `scale` 参数，允许用户在推理时手动调整全局压缩的激进程度（$\hat{y}_{scaled} = \hat{y} + scale$）。
    3.  **离散量化（DRS）**：将连续预测值映射到预定义的离散比例集合 $R$（如 $\{2\times, 4\times, 8\times, 16\times, 32\times\}$）。
    4.  **特征提取（均值池化）**：根据选定的离散比例，对 Encoder 的隐藏状态进行均值池化（Mean-Pooling），生成压缩后的潜标记。
    5.  **解码**：Decoder 接收压缩后的标记并生成响应。
*   **训练策略**：
    *   **单阶段联合训练**：同时优化语言模型损失（LM Loss）和密度预测损失（MSE Loss）。
    *   **密度代理标签**：利用 Teacher LLM 生成“超简洁摘要”，以摘要长度与原长度的比例作为信息密度的监督信号。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：基于 UltraFineWeb 构建的 1000 万个合成样本，涵盖问答、摘要、多跳推理等任务。
    *   **评估集**：从 HotpotQA、SQuAD、Natural Questions (NQ) 和 AdversarialQA 中均匀采样 1000 个实例（长度控制在 2048 标记内）。
*   **Benchmark 与对比方法**：
    *   **特征提取对比**：对比了 Mean-pooling、Last Tokens 和 Compression Tokens（可学习标记）。
    *   **压缩模式对比**：对比了固定比例（Fixed-ratio）、固定长度（Fixed-length）以及全动态（Fully-dynamic）压缩。
    *   **基准线**：以不压缩的原始模型性能作为上限参考。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen3-0.6B 和 Qwen3-4B 作为基础模型。
*   **微调技术**：采用 LoRA 训练（r=16, alpha=128/64）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了全局 Batch Size 为 80。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同特征提取机制的对比实验（图 3）。
    *   针对半动态 vs 静态比例进行了全谱系帕累托前沿分析（图 4）。
    *   包含了模型参数量缩放（0.6B 到 4B）的验证（图 6）。
    *   **消融实验**：对比了单阶段 vs 双阶段架构、因果注意力 vs 双向注意力对压缩效果的影响。
*   **充分性评价**：实验设计较为充分，通过多维度对比验证了“连续超参数陷阱”的存在，并证明了半动态方法的优越性。实验过程考虑了不同任务类型和语言（中英双语），具有较好的客观性。

### 6. 主要结论与发现
*   **均值池化占优**：在不进行大规模预训练的情况下，简单的均值池化在压缩质量上显著优于目前流行的“压缩标记（Compression Tokens）”方法。
*   **连续参数失效**：模型无法有效学习无限变化的结构超参数，而限制在有限的离散操作集合内则能保持高性能。
*   **密度感知有效**：半动态压缩在相同的平均压缩率下，准确率始终优于静态压缩，尤其在 4x 到 16x 的中等压缩区间提升最明显。
*   **双向注意力必要性**：在 Encoder 中使用双向注意力（而非因果掩码）能显著提升高压缩率下的信息保留能力。

### 7. 优点
*   **解决训练痛点**：通过离散化巧妙解决了动态压缩难以收敛的问题。
*   **灵活性高**：引入 `scale` 参数，使用户无需重新训练即可在“高保真”和“高效率”之间平滑切换。
*   **训练高效**：单阶段 SFT 流程，无需复杂的强化学习（RL）或昂贵的文本重构预训练。
*   **即插即用**：提出的单占位符动态扩展机制简化了推理时的提示词处理。

### 8. 不足与局限
*   **评估长度限制**：实验主要集中在 2048 标记以内的文本，对于超长上下文（如 32k+）的泛化能力尚待验证。
*   **合成数据依赖**：密度预测的准确性高度依赖于 Teacher LLM 生成摘要的质量，可能存在模型偏见。
*   **代理指标局限**：以摘要长度作为信息密度的唯一代理可能过于简化，某些复杂逻辑文本可能难以通过长度衡量密度。
*   **算力透明度**：缺乏具体的训练资源消耗数据，不便于其他研究者评估复现成本。

（完）
