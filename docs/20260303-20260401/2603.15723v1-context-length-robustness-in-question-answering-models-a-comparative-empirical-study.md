---
title: "Context-Length Robustness in Question Answering Models: A Comparative Empirical Study"
title_zh: 问答模型中的上下文长度鲁棒性：一项比较实证研究
authors: "Trishita Dhara, Siddhesh Sheth"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15723v1"
tags: ["query:lcp"]
score: 9.0
evidence: 大语言模型上下文长度鲁棒性的实证研究
tldr: 本研究针对大语言模型在长且多噪上下文中的表现，通过SQuAD和HotpotQA基准进行了受控实证研究。研究通过系统性增加无关上下文来隔离长度影响，发现模型性能随长度增加而下降，且多跳推理任务的鲁棒性显著低于单跳任务。该工作揭示了任务类型对长度鲁棒性的影响，为评估长文本处理能力提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.15723v1/fig-001.webp\", \"caption\": \"Figure 2: Context-length robustness across tasks and models. (a) Mean exact match accuracy as a function of total context length. (b) Accuracy with 95% bootstrap confidence intervals computed over evaluation instances. Performance degradation on HotpotQA remains consistent under uncertainty, while SQuAD exhibits comparatively stable behavior across context lengths.\", \"page\": 6, \"index\": 1, \"width\": 829, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2603.15723v1/fig-002.webp\", \"caption\": \"Figure 1: Controlled context construction used to evaluate context-length robustness. For each question, an answer-bearing signal segment is preserved across all settings, while increasing amounts of irrelevant context are added to reach target context lengths. This design isolates the effect of total context length while keeping the available evidence fixed.\", \"page\": 4, \"index\": 2, \"width\": 594, \"height\": 419}]"
motivation: 探究大语言模型在处理长且包含噪声的上下文时，其问答能力的鲁棒性如何随长度增加而变化。
method: 通过在保持答案相关信息不变的情况下系统性地增加无关上下文，对比模型在SQuAD和HotpotQA基准上的表现。
result: 随着上下文长度增加，模型准确率普遍下降，且多跳推理任务的性能下降幅度几乎是单跨度提取任务的两倍。
conclusion: 多跳推理对上下文稀释更为敏感，在评估长文档或检索增强生成应用的模型可靠性时，应明确评估其上下文长度鲁棒性。
---

## 摘要
大语言模型越来越多地被部署在相关信息嵌入长且多噪声上下文的场景中。尽管如此，在不同的问答任务中，模型对不断增长的上下文长度的鲁棒性仍未得到充分理解。在这项工作中，我们利用两个广泛使用的基准测试（SQuAD 和 HotpotQA），对大语言模型的上下文长度鲁棒性进行了一项受控的实证研究。我们通过系统地增加无关上下文的数量，同时保留包含答案的信号，来评估模型准确率随总上下文长度变化的情况。这使我们能够将上下文长度的影响与任务难度的变化隔离开来。我们的结果显示，随着上下文长度的增加，性能会出现一致的下降，且与单跨度抽取任务相比，多跳推理任务的下降幅度明显更大。特别是，在同等的上下文扩展下，HotpotQA 的准确率下降几乎是 SQuAD 的两倍。这些发现强调了鲁棒性在不同任务间的差异，并表明多跳推理特别容易受到上下文稀释的影响。我们认为，在评估模型可靠性时，应明确评估上下文长度的鲁棒性，特别是对于涉及长文档或检索增强生成的应用。

## Abstract
Large language models are increasingly deployed in settings where relevant information is embedded within long and noisy contexts. Despite this, robustness to growing context length remains poorly understood across different question answering tasks. In this work, we present a controlled empirical study of context-length robustness in large language models using two widely used benchmarks: SQuAD and HotpotQA.   We evaluate model accuracy as a function of total context length by systematically increasing the amount of irrelevant context while preserving the answer-bearing signal. This allows us to isolate the effect of context length from changes in task difficulty. Our results show a consistent degradation in performance as context length increases, with substantially larger drops observed on multi-hop reasoning tasks compared to single-span extraction tasks. In particular, HotpotQA exhibits nearly twice the accuracy degradation of SQuAD under equivalent context expansions.   These findings highlight task-dependent differences in robustness and suggest that multi-hop reasoning is especially vulnerable to context dilution. We argue that context-length robustness should be evaluated explicitly when assessing model reliability, especially for applications involving long documents or retrieval-augmented generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Context-Length Robustness in Question Answering Models: A Comparative Empirical Study》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在检索增强生成（RAG）和长文档分析中的广泛应用，模型经常需要在包含大量无关信息的长上下文中提取关键答案。尽管现有模型宣称支持极长的上下文窗口，但其**性能随长度增加的鲁棒性**（Robustness）仍不明确。
*   **核心问题**：当问题的难度和答案信息保持不变，仅仅通过增加无关干扰信息来延长上下文时，模型的问答准确率会如何变化？
*   **研究意义**：旨在量化“上下文稀释”对不同类型推理任务（单跳提取 vs. 多跳推理）的影响，从而评估模型在真实复杂场景下的可靠性。

### 2. 方法论：核心思想与技术细节
论文提出了一种**受控实证研究方法**，通过隔离变量来研究上下文长度的影响：
*   **核心思想**：构建一个“信号+噪声”的输入模式。保持包含答案的“信号段”不变，通过填充无关的“干扰段”来精确控制总长度。
*   **关键流程**：
    1.  **信号提取**：从原始数据集中提取包含答案的 256 标记（tokens）窗口作为核心信号。
    2.  **干扰注入**：从同数据集的其他实例中采样 128 标记的片段作为干扰项，并剔除包含答案字符串的片段以防泄露。
    3.  **长度控制**：将信号段与干扰段组合，构建 256、512、1024、2048 四种目标长度。
*   **数学建模**：
    *   定义准确率函数 $Acc_M(L)$ 为模型 $M$ 在长度 $L$ 下的准确率。
    *   引入**信噪比视角**：假设注意力机制在无先验情况下是均匀分布的，则分配给信号的注意力质量与 $|si|/L$ 成正比。对于需要 $k$ 步推理的多跳任务，成功概率随长度增加呈幂律衰减（$ \propto (1/L)^k$），解释了为何多跳任务更脆弱。

### 3. 实验设计
*   **数据集**：
    *   **SQuAD**：代表单跨度提取任务（Single-hop），侧重局部证据提取。
    *   **HotpotQA**：代表多跳推理任务（Multi-hop），需要整合多个文档的信息。
*   **评估模型**：对比了 `gpt-4.1`（大参数量）和 `gpt-4.1-mini`（小参数量），以观察模型规模对鲁棒性的影响。
*   **Benchmark 与指标**：使用验证集的子集（n=200），采用**精确匹配（Exact Match, EM）**作为核心评价指标。
*   **对比设置**：在相同的干扰项生成逻辑下，对比不同长度（256 到 2048）和不同任务类型间的表现差异。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于该研究主要基于 API 调用（GPT-4 系列模型）进行推理评估，而非从头训练模型，因此算力消耗主要集中在推理成本和 API 请求处理上。作者提到为了应对速率限制，执行了强制延迟和指数退避策略。

### 5. 实验数量与充分性
*   **实验规模**：每个数据集抽取 200 个样本，涵盖 2 个模型、2 种任务类型、4 种长度梯度，总计进行了约 3200 次模型查询。
*   **充分性**：
    *   **统计显著性**：报告了 95% 的自助法（Bootstrap）置信区间，证明了 HotpotQA 的性能下降是统计显著的。
    *   **客观性**：通过固定随机种子和缓存机制确保了实验的可重复性；使用确定性解码（Temperature=0）消除了采样随机性。
    *   **公平性**：不同模型面对的是完全相同的干扰项组合和提示词。

### 6. 主要结论与发现
*   **普遍退化**：随着上下文长度增加，所有模型的性能均呈非上升趋势，即使答案信息始终存在。
*   **任务依赖性（核心发现）**：多跳推理（HotpotQA）对长度增加极其敏感，准确率下降幅度（约 25-27%）几乎是单跳任务（SQuAD，约 2-6%）的**两倍以上**。
*   **规模并非万能**：虽然大模型（gpt-4.1）的绝对准确率更高，但其随长度增加而退化的**趋势/斜率**与小模型相似，说明增加参数量并不能消除上下文稀释带来的结构性问题。

### 7. 优点
*   **变量隔离精准**：通过固定信号窗口并系统性注入噪声，成功排除了任务难度、答案歧义等干扰因素，纯粹研究“长度”的影响。
*   **理论解释合理**：提出的信噪比与多跳推理概率衰减模型，为实验观察到的现象提供了直观的数学解释。
*   **实用性强**：研究结果对 RAG 系统的开发者有直接指导意义，警示了在处理复杂推理时盲目增加上下文的风险。

### 8. 不足与局限
*   **长度覆盖有限**：实验最高仅测试到 2048 tokens，对于目前动辄 128k 甚至 1M 的长文本模型来说，测试范围偏窄。
*   **模型多样性不足**：仅测试了 GPT-4 系列，未涵盖开源模型（如 Llama、Mistral）或其他架构（如长上下文优化的模型）。
*   **噪声类型单一**：干扰项仅来自同数据集的无关文本，未考虑对抗性噪声、矛盾信息或位置偏见（如“迷失在中间”效应）的深入交互。
*   **样本量较小**：每组 200 个样本虽然能显示趋势，但在更细粒度的分析上可能存在局限。

（完）
