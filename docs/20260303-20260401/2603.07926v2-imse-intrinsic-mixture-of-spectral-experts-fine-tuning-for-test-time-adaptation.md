---
title: "IMSE: Intrinsic Mixture of Spectral Experts Fine-tuning for Test-Time Adaptation"
title_zh: IMSE：用于测试时自适应的内在谱专家混合微调
authors: "Sunghyun Baek, Jaemyung Yu, Seunghee Koh, Minsu Kim, Hyeonseong Jeon, Junmo Kim"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07926v2"
tags: ["query:ttt"]
score: 10.0
evidence: 用于视觉Transformer测试时自适应的内在谱专家混合方法
tldr: 本研究针对测试时自适应（TTA）中预训练模型参数更新效率低及特征坍缩问题，提出了内在频谱专家混合（IMSE）方法。通过对线性层进行SVD分解并仅微调奇异值，结合多样性最大化损失函数，在显著减少可训练参数的同时，有效提升了模型在持续测试时自适应（CTTA）场景下的性能和知识保留能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07926v2/fig-001.webp\", \"caption\": \"Table 1: Test-time adaptation on ImageNet-C (50k). Accuracy(%)(↑) over 15 corruptions at severity level 5 using ViT-Base models with different pretraining strategies. Results with * are reproduced by us.\", \"page\": 6, \"index\": 1, \"width\": 824, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.07926v2/fig-002.webp\", \"caption\": \"Table 13: Ablation study on target modules. The best is bolded and the second best is underlined.\", \"page\": 15, \"index\": 2, \"width\": 700, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2603.07926v2/fig-003.webp\", \"caption\": \"Figure 2: (a) Comparison of top-R% vs. bottom-R% singular value selection. (b) Feature diversity across various training methods. (c) Adaptation performance across various training methods. (d) Impact of the threshold τ on domain shift detection and adaptation performance. CE and TTA losses denote Lce and Lentmin, respectively.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2603.07926v2/fig-004.webp\", \"caption\": \"Figure 4: (a) Diversity of alignment patterns in 3rd Transformer Block. (b) Diversity of alignment patterns in 6th Transformer Block. (c) Diversity of alignment patterns in 9th Transformer Block.\", \"page\": 16, \"index\": 4, \"width\": 807, \"height\": 201}, {\"url\": \"assets/figures/arxiv/2603.07926v2/fig-005.webp\", \"caption\": \"Figure 5: Domain distance matrix. Pairwise distance matrix among 15 domain descriptors.\", \"page\": 16, \"index\": 5, \"width\": 585, \"height\": 518}]"
motivation: 旨在解决测试时自适应中大模型参数更新开销大以及熵最小化导致的特征坍缩问题。
method: 通过SVD分解线性层并仅优化奇异值，同时引入专家输入对齐的多样性损失和领域感知频谱码检索机制。
result: "在多个基准测试中达到SOTA，特别是在CTTA任务中准确率提升高达3.4%，且可训练参数量减少了385倍。"
conclusion: IMSE证明了通过极少量的频谱参数微调即可实现高效且鲁棒的测试时自适应，为大模型的高效迁移提供了新思路。
---

## 摘要
测试时自适应 (TTA) 已被广泛研究，旨在防止测试数据与训练分布不一致时出现的性能下降。然而，如何以最小的参数更新充分利用大型预训练模型的丰富表示仍有待探索。在本文中，我们提出了内在谱专家混合 (IMSE)，它利用了视觉 Transformer (Vision Transformers) 中固有嵌入的谱专家。我们通过奇异值分解 (SVD) 对每个线性层进行分解，仅调整奇异值，同时保持奇异向量固定。我们进一步确定了 TTA 中熵最小化的一个关键局限性：它经常导致特征崩溃，使模型依赖于特定领域特征而非类别判别特征。为解决这一问题，我们提出了一种基于专家-输入对齐的多样性最大化损失，鼓励在自适应过程中多样化地利用谱专家。在持续测试时自适应 (CTTA) 场景中，除了保留预训练知识外，保留并重用来自先前观察到的领域的知识也至关重要。我们引入了领域感知谱代码检索，通过估计输入分布来检测领域偏移，并检索已适应的奇异值以实现快速自适应。因此，我们的方法在 TTA 设置下的各种分布偏移基准测试中实现了最先进的性能。在 CTTA 和渐进式 CTTA 中，它将准确率分别进一步提高了 3.4 个百分点 (pp) 和 2.4 个百分点，同时所需的可训练参数减少了 385 倍。我们的代码可在 https://github.com/baek85/IMSE 获取。

## Abstract
Test-time adaptation (TTA) has been widely explored to prevent performance degradation when test data differ from the training distribution. However, fully leveraging the rich representations of large pretrained models with minimal parameter updates remains underexplored. In this paper, we propose Intrinsic Mixture of Spectral Experts (IMSE) that leverages the spectral experts inherently embedded in Vision Transformers. We decompose each linear layer via singular value decomposition (SVD) and adapt only the singular values, while keeping the singular vectors fixed. We further identify a key limitation of entropy minimization in TTA: it often induces feature collapse, causing the model to rely on domain-specific features rather than class-discriminative features. To address this, we propose a diversity maximization loss based on expert-input alignment, which encourages diverse utilization of spectral experts during adaptation. In the continual test-time adaptation (CTTA) scenario, beyond preserving pretrained knowledge, it is crucial to retain and reuse knowledge from previously observed domains. We introduce Domain-Aware Spectral Code Retrieval, which estimates input distributions to detect domain shifts, and retrieves adapted singular values for rapid adaptation. Consequently, our method achieves state-of-the-art performance on various distribution-shift benchmarks under the TTA setting. In CTTA and Gradual CTTA, it further improves accuracy by 3.4 percentage points (pp) and 2.4 pp, respectively, while requiring 385 times fewer trainable parameters. Our code is available at https://github.com/baek85/IMSE.

---

## 论文详细总结（自动生成）

### 论文总结：IMSE (Intrinsic Mixture of Spectral Experts)

#### 1. 核心问题与整体含义（研究动机和背景）
在实际应用中，测试数据往往与训练数据存在分布偏移（Distribution Shift），导致模型性能下降。**测试时自适应 (TTA)** 旨在不访问源数据的情况下，在线调整模型以适应新领域。
论文指出了现有 TTA 方法的三个核心局限：
*   **表示利用不足**：如何在极少参数更新下充分挖掘大模型（如 ViT）的预训练表示能力？
*   **特征坍缩（Feature Collapse）**：常用的“熵最小化”目标往往导致模型过度关注领域特定特征，忽略了类别判别特征，造成特征多样性丧失。
*   **持续自适应中的遗忘**：在**持续测试时自适应 (CTTA)** 场景下，模型容易忘记之前学到的领域知识，且缺乏高效的知识重用机制。

#### 2. 方法论：核心思想与关键技术
论文提出了 **IMSE (内在谱专家混合)** 框架，包含以下三个关键组件：
*   **内在谱专家 (Intrinsic Mixture of Spectral Experts)**：
    *   对 ViT 的每个线性层进行奇异值分解 (SVD)：$W = U \Sigma V^\top$。
    *   将每个 rank-1 组件 $u_i v_i^\top$ 视为一个“谱专家”，奇异值 $\sigma_i$ 代表该专家的贡献权重。
    *   **核心操作**：冻结正交基（奇异向量 $U, V$），仅微调奇异值 $\sigma$（称为“谱代码” $S$）。这保留了预训练的特征提取空间，同时实现了极高参数效率。
*   **多样性最大化损失 ($L_{dm}$)**：
    *   通过计算专家与输入之间的对齐统计量（均值和标准差），引入多样性损失。
    *   公式逻辑：最大化谱专家响应的标准差，鼓励模型在自适应过程中激活更多样化的专家，从而补偿熵最小化导致的特征坍缩。
*   **领域感知谱代码检索 (Domain-Aware Spectral Code Retrieval)**：
    *   **领域描述符**：利用输入 Patch Token 的通道均值和方差作为分布估计。
    *   **检索机制**：使用对称 KL 散度检测领域偏移。当检测到新领域时，从“领域库”中检索最相似的已适应奇异值进行初始化，实现快速自适应并减轻遗忘。

#### 3. 实验设计
*   **数据集与场景**：
    *   **单领域 TTA**：ImageNet-C（15 种损坏）、ImageNet-R（渲染图）、ImageNet-A（自然对抗样本）。
    *   **持续 TTA (CTTA)**：ImageNet-C 的连续流式数据。
    *   **渐进式 CTTA**：损坏程度平滑变化的 135 个连续领域。
    *   **扩展测试**：ImageNet-3DCC（3D 几何损坏）、OfficeHome、DomainNet。
*   **Benchmark 与对比方法**：
    *   对比了 TENT, CoTTA, EATA, SAR, ViDA, DPAL 等十余种主流 TTA/CTTA 算法。
    *   涵盖了 Supervised, MAE, CLIP 等多种预训练权重的 ViT-Base 和 ViT-Large 模型。

#### 4. 资源与算力
*   **硬件**：实验主要在单张 **NVIDIA RTX A6000 GPU** 上完成。
*   **效率指标**：
    *   **参数量**：仅需 36.8K 可训练参数，比 ViDA 少 385 倍，比 CoTTA 少 2300 多倍。
    *   **运行时间**：Batch size 为 64 时，每 batch 运行时间为 0.99 秒，比 ViDA (3.49s) 快 3.5 倍。
    *   **预处理**：SVD 分解作为离线步骤仅需约 5 秒。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **多维度对比**：涵盖了不同预训练策略（MAE/CLIP）、不同模型规模（Base/Large）。
*   **消融实验**：验证了 $L_{dm}$ 损失、不同线性层组合、奇异值选择策略（Top-R% vs Bottom-R%）、领域描述符组成、检测阈值 $\tau$ 的敏感度等。
*   **鲁棒性测试**：在非 i.i.d.（标签极度不平衡）设置下验证了多样性损失的稳定性。
*   **客观性**：实验设置遵循标准 Benchmark 协议，对比方法均采用官方实现或在相同条件下复现，实验设计充分且客观。

#### 6. 主要结论与发现
*   **性能领先**：在所有 TTA 和 CTTA 任务中均达到 SOTA。在 CTTA 任务中，平均准确率比强基线 ViDA 高出 **6.7%**。
*   **奇异值的重要性**：实验发现高幅值的奇异值（Top 专家）在自适应中起主导作用，微调它们比微调低幅值奇异值更有效。
*   **多样性是关键**：证明了无监督 TTA 中特征多样性的丧失是性能瓶颈，通过 $L_{dm}$ 可以有效缓解。
*   **检索机制的有效性**：在长程 CTTA 和领域循环场景下，检索机制能显著提升收敛速度并减少遗忘。

#### 7. 优点（亮点）
*   **极高效率**：通过 SVD 重新定义参数空间，实现了极小参数量下的强大自适应能力。
*   **理论与直觉结合**：将线性层解释为“谱专家混合”，为参数高效微调提供了直观的几何解释。
*   **即插即用**：不需要复杂的架构修改（如 Adapter 或 Prompt），直接作用于现有线性层。
*   **解决特征坍缩**：针对性地提出了多样性损失，解决了 TTA 中长期存在的熵最小化副作用。

#### 8. 不足与局限
*   **存储开销**：虽然训练参数少，但为了重构权重，需要存储分解后的 $U$ 和 $V$ 矩阵，这会增加一定的静态内存占用。
*   **领域库增长**：在极长期的 CTTA 任务中，领域库的条目可能会持续增加，需要额外的存储管理机制。
*   **超参数依赖**：领域检测阈值 $\tau$ 和多样性损失权重 $\lambda_{dm}$ 在不同数据集间可能需要微调以达到最优效果。

（完）
