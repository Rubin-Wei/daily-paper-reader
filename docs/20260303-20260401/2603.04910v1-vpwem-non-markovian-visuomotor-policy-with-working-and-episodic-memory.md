---
title: "VPWEM: Non-Markovian Visuomotor Policy with Working and Episodic Memory"
title_zh: VPWEM：具有工作记忆和情节记忆的非马尔可夫视觉运动策略
authors: "Yuheng Lei, Zhixuan Liang, Hongyuan Zhang, Ping Luo"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04910v1"
tags: ["query:am"]
score: 9.0
evidence: 配备工作记忆和情节记忆的视觉运动策略
tldr: 针对机器人模仿学习中长时记忆缺失的问题，本文提出VPWEM框架。该方法结合了滑动窗口形式的短期工作记忆和基于Transformer压缩的长期情节记忆，通过递归压缩历史观测为固定数量的记忆Token，在保持低计算开销的同时捕捉长程依赖。实验表明，VPWEM在内存密集型任务和移动操作基准测试中显著优于扩散策略和VLA模型，为非马尔可夫任务提供了高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.04910v1/fig-001.webp\", \"caption\": \"Fig. 4. Performance on MoMaRT benchmark.\", \"page\": 6, \"index\": 1, \"width\": 960, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2603.04910v1/fig-002.webp\", \"caption\": \"Fig. 5. Performance on MIKASA benchmark.\", \"page\": 6, \"index\": 2, \"width\": 406, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2603.04910v1/fig-003.webp\", \"caption\": \"Fig. 7. Ablation studies on Long-Horizon Square task.\", \"page\": 7, \"index\": 3, \"width\": 987, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2603.04910v1/fig-004.webp\", \"caption\": \"Fig. 2. Overview of the VPWEM framework. (a) Policy architecture. Modules in green are learnable components, including the multi-modal encoder, contextual memory compressor, summary tokens, and the Transformer-based noise predictor in the diffusion policy. These components are optimized end-to-end with a behavior cloning loss. (b) Training process. Each training sample contains the complete trajectory from the beginning of the episode. The short-term memory component follows the standard diffusion policy baselines. Observations outside the context window are subsampled with a fixed ratio and passed to the contextual memory compressor, which outputs a fixed number of memory tokens. The combined long- and short-term memory tokens are used to condition the noise prediction network. (c) Inference process. Each decision step consists of three steps: encoding new frames and updating the observation and summary caches; compressing out-of-window tokens to obtain long-term memory tokens; and predicting the action chunk.\", \"page\": 4, \"index\": 4, \"width\": 997, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2603.04910v1/fig-005.webp\", \"caption\": \"TABLE I HYPERPARAMETERS OF DP-VPWEM\", \"page\": 5, \"index\": 5, \"width\": 504, \"height\": 398}]"
motivation: 现有的视觉运动策略难以处理需要长时记忆的非马尔可夫任务，且简单扩大上下文窗口会导致计算成本激增和过拟合。
method: 提出一种结合工作记忆与情节记忆的非马尔可夫策略，利用Transformer压缩器将历史观测递归转化为固定长度的记忆Token并与扩散策略结合。
result: "在MIKASA内存密集型任务上性能提升超过20%，在MoMaRT移动操作基准上平均提升5%，且每步计算量几乎恒定。"
conclusion: VPWEM通过高效的记忆压缩机制，为解决机器人长程非马尔可夫任务提供了一种兼顾性能与实时性的新方案。
---

## 摘要
从人类演示中进行模仿学习在机器人控制方面取得了显著成功，但大多数视觉运动策略仍以单步观测或短上下文历史为条件，这使得它们在处理需要长期记忆的非马尔可夫任务时面临困难。简单地扩大上下文窗口会带来巨大的计算和内存开销，并容易导致对伪相关的过拟合，从而在分布偏移下引发灾难性失败，并违反机器人系统的实时性约束。相比之下，人类可以将过去的重要经验压缩到长期记忆中，并利用这些记忆在整个生命周期内解决任务。在本文中，我们提出了 VPWEM，这是一种配备了工作记忆和情节记忆的非马尔可夫视觉运动策略。VPWEM 保留了最近观测 token 的滑动窗口作为短期工作记忆，并引入了一个基于 Transformer 的上下文记忆压缩器，该压缩器递归地将窗口外的观测转换为固定数量的情节记忆 token。该压缩器对过去摘要 token 的缓存使用自注意力机制，并对历史观测的缓存使用交叉注意力机制，且与策略进行联合训练。我们在扩散策略上实例化了 VPWEM，以利用短期和全情节信息进行动作生成，且每步的内存和计算量几乎保持恒定。实验表明，在 MIKASA 的记忆密集型操作任务中，VPWEM 的表现优于包括扩散策略和视觉-语言-动作（VLA）模型在内的最先进基准模型 20% 以上，并在移动操作基准测试 MoMaRT 上实现了平均 5% 的提升。代码可在 https://github.com/HarryLui98/code_vpwem 获取。

## Abstract
Imitation learning from human demonstrations has achieved significant success in robotic control, yet most visuomotor policies still condition on single-step observations or short-context histories, making them struggle with non-Markovian tasks that require long-term memory. Simply enlarging the context window incurs substantial computational and memory costs and encourages overfitting to spurious correlations, leading to catastrophic failures under distribution shift and violating real-time constraints in robotic systems. By contrast, humans can compress important past experiences into long-term memories and exploit them to solve tasks throughout their lifetime. In this paper, we propose VPWEM, a non-Markovian visuomotor policy equipped with working and episodic memories. VPWEM retains a sliding window of recent observation tokens as short-term working memory, and introduces a Transformer-based contextual memory compressor that recursively converts out-of-window observations into a fixed number of episodic memory tokens. The compressor uses self-attention over a cache of past summary tokens and cross-attention over a cache of historical observations, and is trained jointly with the policy. We instantiate VPWEM on diffusion policies to exploit both short-term and episode-wide information for action generation with nearly constant memory and computation per step. Experiments demonstrate that VPWEM outperforms state-of-the-art baselines including diffusion policies and vision-language-action (VLA) models by more than 20% on the memory-intensive manipulation tasks in MIKASA and achieves an average 5% improvement on the mobile manipulation benchmark MoMaRT. Code is available at https://github.com/HarryLui98/code_vpwem.

---

## 论文详细总结（自动生成）

### VPWEM: 具有工作记忆和情节记忆的非马尔可夫视觉运动策略深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的机器人视觉运动策略（Visuomotor Policies）大多基于马尔可夫假设，即仅依赖当前观测或极短的历史窗口（如2-10帧）来预测动作。这使得机器人在处理需要长时记忆的**非马尔可夫任务**（如寻找被遮盖的物体、长路径移动操作）时表现糟糕。
*   **研究动机**：
    *   **计算瓶颈**：简单增加上下文窗口长度会导致 Transformer 的计算量呈平方级增长，难以满足实时控制需求。
    *   **过拟合风险**：长历史容易引入“因果混淆”（Causal Confusion）和“复读机问题”（Copycat Problem），导致策略学习到伪相关性而非真正的逻辑。
    *   **生物启发**：借鉴人类大脑海马体将短期工作记忆转化为长期情节记忆的机制，实现对历史信息的有效压缩与提取。

#### 2. 方法论：核心思想与技术细节
VPWEM 框架的核心是将观测历史划分为两部分，并采用不同的处理机制：
*   **工作记忆（Working Memory, WM）**：保留最近 $L$ 帧的观测 Token，采用滑动窗口（FIFO）机制，捕捉即时的环境状态。
*   **情节记忆（Episodic Memory, EM）**：
    *   **上下文记忆压缩器**：这是一个基于 Transformer 的模块，负责将移出滑动窗口的观测 Token 递归地压缩为固定数量（$M$ 个）的摘要 Token。
    *   **双重注意力机制**：压缩器内部包含**自注意力层**（用于与过去的摘要 Token 交互）和**交叉注意力层**（用于检索历史观测缓存中的细节）。
    *   **递归更新**：随着任务推进，压缩器不断吸收新移出的 Token 并更新情节记忆，确保记忆容量不随时间增长而爆炸。
*   **动作生成**：将 WM 和 EM 的 Token 拼接，作为条件输入到扩散策略（Diffusion Policy）或流匹配策略（Flow Policy）的去噪网络中，指导动作序列的生成。
*   **端到端训练**：压缩器与策略网络通过行为克隆（BC）损失函数同步优化，使压缩器学会过滤无关噪声，仅保留对决策关键的信息。

#### 3. 实验设计
*   **数据集与场景**：
    *   **MIKASA**：专门用于评估长时记忆的任务（如 ShellGame 猜球位置、记住方块颜色）。
    *   **MoMaRT**：复杂的移动操作任务（如清理洗碗机、整理桌面），具有长时程和非马尔可夫特性。
    *   **Robomimic**：标准的桌面操作任务（Square, Transport），以及一个改进的“长程 Square”任务。
*   **对比基准（Baselines）**：
    *   **序列模型**：RNN (LSTM)、MaIL (基于 Mamba 的状态空间模型)。
    *   **扩散策略变体**：标准 Diffusion Policy (DP)、带过去 Token 预测的 DP-PTP。
    *   **大模型（VLA）**：Octo, OpenVLA, $\pi_0$, CronusVLA, MemoryVLA 等。

#### 4. 资源与算力
*   **硬件**：所有实验均在**单张 NVIDIA RTX 4090 GPU** 上运行。
*   **训练时长**：
    *   MIKASA 任务训练 60 万步。
    *   MoMaRT 和 Robomimic 任务训练 100 万步。
*   **模型规模**：增加的记忆模块非常轻量，仅约 2.24M 参数。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个大类基准测试，包含超过 10 个具体的子任务。
*   **消融实验**：非常详尽，涵盖了上下文长度（$L$）、情节记忆 Token 数量（$M$）、压缩器层数、缓存大小以及不同的缓存管理策略（FIFO, K-Means, AdjSim 等）。
*   **客观性**：每个实验均使用 3 个随机种子运行并报告均值与标准差。对比了从轻量级 RNN 到参数量巨大的 VLA 模型，评估维度全面（成功率、推理时间、内存占用）。

#### 6. 主要结论与发现
*   **性能突破**：在记忆密集型任务（MIKASA）中，VPWEM 比最强的 VLA 模型高出 **20%** 以上的成功率；在移动操作（MoMaRT）中平均提升 **5%**。
*   **效率优势**：相比于直接增加上下文长度的 DP-PTP，VPWEM 在推理速度和内存占用上表现出极佳的稳定性。当上下文极长时，传统方法性能反而下降，而 VPWEM 依然稳健。
*   **通用性**：该记忆机制可以无缝集成到不同的基础策略（如 DP 或 MaIL）中，并均能带来性能提升。
*   **马尔可夫任务兼容性**：在不需要长时记忆的简单任务中，VPWEM 的表现与标准基准持平，证明其不会因引入复杂机制而损害基础能力。

#### 7. 优点与亮点
*   **高效压缩**：通过递归压缩解决了长序列处理的计算瓶颈，实现了近乎恒定的每步计算开销。
*   **缓解过拟合**：通过端到端学习压缩，模型学会了忽略历史中的“干扰项”，有效缓解了模仿学习中的因果混淆问题。
*   **即插即用**：作为一种通用的记忆增强框架，它可以适配多种视觉运动策略架构。

#### 8. 不足与局限
*   **仿真依赖**：论文实验主要集中在仿真环境（IsaacGym, PyBullet 等），虽然使用了真实演示数据，但尚未展示在真实物理机器人上的长时程部署表现。
*   **视觉编码器固定**：实验中使用了预训练且冻结的视觉编码器，记忆压缩的效果可能受限于初始视觉特征的质量。
*   **任务复杂度限制**：虽然处理了长时记忆，但对于需要极高逻辑推理或跨情节（Cross-episode）长期学习的任务，该方法仍局限在单次任务（Single-episode）的范围内。

（完）
