---
title: "Mixture of Chapters: Scaling Learnt Memory in Transformers"
title_zh: 章节混合：扩展 Transformer 中的学习记忆
authors: "Tasmay Pankaj Tibrewal, Pritish Saha, Ankit Meda, Kunal Singh, Pradeep Moturi"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21096v1"
tags: ["query:pm"]
score: 10.0
evidence: 通过稀疏记忆库扩展Transformer的学习记忆
tldr: 针对Transformer缺乏显式知识存储机制的问题，本文提出“章节混合”（Mixture of Chapters）架构。通过引入可学习的稀疏内存库，利用交叉注意力机制检索知识，并借鉴混合专家（MoE）思想将内存划分为多个“章节”进行路由，从而在不增加计算负担的情况下将内存扩展至262K个token。实验证明该方法在预训练和微调中均优于同等算力的基线模型，并能有效缓解灾难性遗忘。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21096v1/fig-001.webp\", \"caption\": \"Figure 2: Loss curves. Left: Pretraining. Right: IFT (Freezing the bank yields identical curves).\", \"page\": 6, \"index\": 1, \"width\": 972, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-002.webp\", \"caption\": \"Figure 1: Single transformer block with chapter-based memory routing\", \"page\": 3, \"index\": 2, \"width\": 854, \"height\": 557}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-003.webp\", \"caption\": \"Table 6: Main training settings for pretraining and IFT.\", \"page\": 18, \"index\": 3, \"width\": 977, \"height\": 621}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-004.webp\", \"caption\": \"Table 3: Literature map (representative, not exhaustive).\", \"page\": 13, \"index\": 4, \"width\": 972, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-005.webp\", \"caption\": \"Table 5: Main MoC architecture used in our experiments.\", \"page\": 17, \"index\": 5, \"width\": 952, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2603.21096v1/fig-006.webp\", \"caption\": \"Table 2: Comparison of Vanilla, Mixture of Chapters (MoC) (∆ = IFT - pretrain; lower is better)\", \"page\": 5, \"index\": 6, \"width\": 786, \"height\": 188}]"
motivation: Transformer模型缺乏显式的架构机制来存储和组织训练过程中获取的知识。
method: 提出一种基于章节路由的可学习稀疏内存库，通过交叉注意力检索知识，并利用类似MoE的路由机制实现大规模内存扩展。
result: 在等算力设置下，该模型在预训练和指令微调基准测试中均超越了标准Transformer，并能扩展至262K个内存token。
conclusion: 显式关联内存为模型提供了与隐式参数互补的存储能力，并显著提升了模型在持续学习中的知识保留和抗遗忘能力。
---

## 摘要
Transformer 缺乏一种显式的架构机制来存储和组织训练过程中获取的知识。我们引入了可学习的稀疏记忆库：一组随机初始化并端到端训练的潜代币（latent tokens），Transformer 层通过交叉注意力机制对其进行查询，以检索存储的知识。为了在不产生过高注意力成本的情况下扩展记忆容量，我们受专家混合（Mixture-of-Experts）架构启发，提出了基于章节的路由机制，将记忆库划分为多个章节，并训练一个路由器为每个输入选择相关的子集。这使得在保持计算可控的同时，能够扩展到 262K 个记忆代币。我们在预训练和指令微调的相关基准测试中，将我们的方法与标准 Transformer（在等效 FLOPs 设置下）进行了评估。我们的模型超越了等效 FLOPs 的基准模型，这表明存在一个新的扩展维度，并证明了显式关联记忆提供了与模型参数中隐式捕获的知识互补的能力。此外，我们观察到在持续训练下知识保留能力有所提高，并且在不同训练阶段转换时（例如从预训练到指令微调）具有抗遗忘的鲁棒性。

## Abstract
Transformers lack an explicit architectural mechanism for storing and organizing knowledge acquired during training. We introduce learnable sparse memory banks: a set of latent tokens, randomly initialized and trained end-to-end, that transformer layers query via cross-attention to retrieve stored knowledge. To scale memory capacity without prohibitive attention costs, we propose chapter-based routing inspired by Mixture-of-Experts architectures, partitioning the memory bank into chapters and training a router to select relevant subsets per input. This enables scaling to 262K memory tokens while maintaining tractable computation. We evaluate our approach against standard transformers (in iso-FLOP settings) on pre-training and instruction fine-tuning across relevant benchmarks. Our models surpass iso-FLOP baselines suggesting scope for a new axis of scaling, demonstrating that explicit associative memory provides complementary capacity to what is captured implicitly in model parameters. Additionally, we observe improved knowledge retention under continued training, with robustness to forgetting when transitioning between training phases (e.g., pretraining to instruction fine-tuning).

---

## 论文详细总结（自动生成）

### 论文总结：Mixture of Chapters (MoC) —— 扩展 Transformer 中的学习记忆

#### 1. 核心问题与研究动机
Transformer 模型虽然强大，但缺乏显式的架构机制来存储和组织训练中获取的知识。目前知识主要隐式地存储在稠密参数中，导致知识难以独立扩展、检查或编辑。现有的长文本方案（如 KV 缓存压缩、RAG）多侧重于推理时的上下文管理，而非模型内部的长期事实记忆。
**核心问题：** 如何在不显著增加计算开销的前提下，为 Transformer 引入一个可大规模扩展、可学习且显式寻址的内部记忆库？

#### 2. 方法论：Mixture of Chapters (MoC)
论文提出了一种名为 **Mixture of Chapters (MoC)** 的架构，其核心思想是将记忆视为一组可学习的潜代币（Latent Tokens），并采用类似混合专家（MoE）的稀疏路由机制进行访问。

*   **学习记忆库（Learnt Memory Bank）：** 在 Transformer 层中引入一组随机初始化并随模型端到端训练的潜代币 $M$。
*   **交叉注意力检索：** Token 流作为查询（Query），记忆库提供键（Key）和值（Value）。通过交叉注意力机制，模型可以从记忆库中提取相关信息并合并回主干网络。
*   **章节路由（Chapter-based Routing）：** 
    *   为了避免对海量记忆代币进行全量注意力计算（开销巨大），将记忆库划分为 $C$ 个“章节”（Chapters）。
    *   引入轻量级路由器，对每个输入序列进行池化表示，并计算章节权重。
    *   **Top-k 选择：** 仅选择权重最高的 $k$ 个章节参与交叉注意力计算。这使得记忆库可以扩展到 262K 个代币，而计算量保持在可控范围内。
*   **残差连接：** 记忆读取的结果通过残差连接加回到隐藏状态中：$H' = H + \text{MemRead}(H, M_S)$。

#### 3. 实验设计
*   **数据集：** 
    *   **预训练：** 使用 FineWeb-Edu 数据集（约 9.6B tokens）。
    *   **指令微调 (IFT)：** 使用 UltraChat 200k 数据集（约 230M tokens）。
*   **Benchmark：** MMLU（综合知识）、ARC-Challenge（科学推理）、BoolQ（阅读理解）、OpenBookQA。
*   **对比方法（Baselines）：**
    1.  **Vanilla (backbone-only)：** 仅包含主干网络，不含记忆模块。
    2.  **Vanilla (iso-FLOP)：** 增加层数（从 16 层增加到 24 层）以匹配 MoC 模型的训练算力，确保对比的公平性。
    3.  **MoC (本文方法)：** 16 层主干 + 4 层 MoC 记忆层。

#### 4. 资源与算力
*   **算力配置：** 
    *   预训练阶段：使用 8 张 GPU 进行分布式训练（DDP），共 9600 个 step。
    *   指令微调阶段：使用 4 张 GPU，训练 2 个 epoch。
    *   **注意：** 文中未明确说明具体的 GPU 型号（如 A100 或 H100），但提供了详细的 **FLOPs 分析**，证明 MoC 在推理和训练时的计算成本与加深后的 Vanilla 模型相当。
*   **参数规模：** 主干约 148M，记忆库约 201M，总参数约 371M，但由于稀疏路由，激活参数量远低于总参数量。

#### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了从零开始的预训练、指令微调以及针对记忆库冻结的消融实验。
*   **充分性：** 
    *   采用了 **iso-FLOP（等效算力）** 设置，这是评估架构改进是否有效的金标准，避免了单纯靠堆参数量取胜的嫌疑。
    *   针对“灾难性遗忘”进行了专门测试，对比了预训练后和 IFT 后的性能变化。
    *   **客观性：** 实验结果显示 MoC 在多个知识密集型任务上均优于等效算力的稠密模型，验证了显式记忆的有效性。

#### 6. 主要结论与发现
*   **性能提升：** 在等效算力下，MoC 模型的预训练验证损失更低，且在 MMLU、BoolQ 等基准测试中表现更优。
*   **缓解遗忘：** 在重度指令微调（IFT）后，标准 Transformer 在 ARC-C 和 BoolQ 上出现了明显的性能退化（遗忘），而 MoC 模型表现极其稳定，证明显式记忆库能起到“知识锚点”的作用。
*   **记忆库可冻结：** 实验发现，在 IFT 阶段冻结预训练好的记忆库，其效果与持续更新记忆库相当。这意味着知识可以被稳定存储，且在后续微调中无需重复训练记忆部分。

#### 7. 优点与亮点
*   **创新的扩展维度：** 提出了一种不同于“增加层数”或“增加宽度”的新扩展路径——增加显式关联记忆。
*   **稀疏性与效率：** 借鉴 MoE 的路由思想解决了大规模记忆检索的计算瓶颈。
*   **鲁棒性：** 显著增强了模型在持续学习和多阶段训练中的知识保留能力。

#### 8. 不足与局限
*   **预训练规模有限：** 实验仅在 9.6B tokens 上进行，尚未在万亿级（Trillion-scale）代币上验证其长期扩展规律。
*   **路由粒度：** 目前采用序列级路由，可能不如 Token 级路由精细，尽管后者计算成本更高。
*   **黑盒性质：** 虽然记忆是显式的，但潜代币（Latent Tokens）的具体含义仍难以直接解释，缺乏对记忆内容的直接可视化或编辑手段。
*   **硬件适配：** 稀疏路由在某些硬件上可能存在利用率不高的问题，文中未深入讨论推理延迟的实际表现。

（完）
