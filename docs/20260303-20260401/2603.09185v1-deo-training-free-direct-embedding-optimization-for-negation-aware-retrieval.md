---
title: "DEO: Training-Free Direct Embedding Optimization for Negation-Aware Retrieval"
title_zh: DEO：用于否定感知检索的无需训练的直接嵌入优化
authors: "Taegyeong Lee, Jiwon Park, Seunghyun Hwang, JooYoung Jang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09185v1"
tags: ["query:rag"]
score: 9.0
evidence: 用于 RAG 的免训练否定感知检索
tldr: 针对现有检索模型在处理否定和排除查询时表现不佳的问题，本文提出了一种无需训练的直接嵌入优化方法（DEO）。该方法通过将查询分解为正向和负向组件，并利用对比目标在推理阶段优化查询嵌入。DEO在不增加额外训练成本或模型更新的情况下，显著提升了文本和多模态检索在否定约束下的准确性，为现实场景下的复杂检索提供了高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.09185v1/fig-001.webp\", \"caption\": \"Figure 2: Retrieval performance with respect to the number of optimization steps on NegConstraint.\", \"page\": 6, \"index\": 1, \"width\": 458, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2603.09185v1/fig-002.webp\", \"caption\": \"Figure 3: Retrieval performance with respect to the number of optimization steps on COCO-Neg.\", \"page\": 6, \"index\": 2, \"width\": 458, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2603.09185v1/fig-003.webp\", \"caption\": \"Figure 4: Examples of query decomposition. In NegConstraint (a) and COCO-Neg (b), the LLM decomposes queries into positive sub-queries capturing desired elements and negative sub-queries targeting excluded concepts.\", \"page\": 7, \"index\": 3, \"width\": 461, \"height\": 748}, {\"url\": \"assets/figures/arxiv/2603.09185v1/fig-004.webp\", \"caption\": \"Figure 5: Trajectory of optimized query embedding eu in PCA-projected space. The initial embedding (black •) moves toward the final state (blue •). Positive examples (green △) and the ground-truth (yellow ⋆) act as attractors, while negative examples (red ×) exert repelling forces. Other corpus embeddings are shown in light gray.\", \"page\": 7, \"index\": 4, \"width\": 458, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2603.09185v1/fig-005.webp\", \"caption\": \"Figure 1: Overview of the proposed Direct Embedding Optimization (DEO). (a) Given an input query containing negation, we use an LLM to decompose it into positive and negative sub-queries. (b) The input query embedding is then optimized with a contrastive loss by pulling it closer to positive query embeddings and pushing it farther from negative query embeddings, enabling negation- and exclusion-aware retrieval.\", \"page\": 3, \"index\": 5, \"width\": 928, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2603.09185v1/fig-006.webp\", \"caption\": \"Figure 6: Optimization trajectory of eu in PCAprojected CLIP space. Initial (black •) embedding evolves toward final state (blue •), attracted by positives (green △) and ground-truth (yellow ⋆) while repelled by negatives (red ×). Gray points denote the corpus.\", \"page\": 8, \"index\": 6, \"width\": 461, \"height\": 358}]"
motivation: 现有检索方法难以准确处理否定和排除类查询，且现有的微调方案存在计算成本高和部署复杂的局限性。
method: 提出一种无需训练的DEO方法，通过分解查询为正负组件并利用对比目标函数直接优化查询嵌入。
result: "DEO在NegConstraint基准上显著超越了现有基线，并在多模态检索中将CLIP的Recall@5提升了6%。"
conclusion: DEO证明了在无需模型微调的情况下，通过直接优化嵌入即可实现高效且实用的否定感知检索。
---

## 摘要
大语言模型（LLM）和检索增强生成（RAG）的最新进展实现了多种检索方法。然而，现有的检索方法往往无法准确检索针对否定和排除查询的结果。为了解决这一局限性，先前的方法依赖于嵌入适配或微调，这引入了额外的计算成本和部署复杂性。我们提出了直接嵌入优化（DEO），这是一种用于否定感知文本和多模态检索的无需训练的方法。DEO 将查询分解为正向和负向组件，并利用对比目标优化查询嵌入。在没有额外训练数据或模型更新的情况下，DEO 在 NegConstraint 上的表现优于基准模型，nDCG@10 提升了 0.0738，MAP@100 提升了 0.1028，同时在多模态检索中相比 OpenAI CLIP 将 Recall@5 提高了 6%。这些结果证明了 DEO 在现实场景中进行否定和排除感知检索的实用性。

## Abstract
Recent advances in Large Language Models (LLMs) and Retrieval-Augmented Generation (RAG) have enabled diverse retrieval methods. However, existing retrieval methods often fail to accurately retrieve results for negation and exclusion queries. To address this limitation, prior approaches rely on embedding adaptation or fine-tuning, which introduce additional computational cost and deployment complexity. We propose Direct Embedding Optimization (DEO), a training-free method for negation-aware text and multimodal retrieval. DEO decomposes queries into positive and negative components and optimizes the query embedding with a contrastive objective. Without additional training data or model updates, DEO outperforms baselines on NegConstraint, with gains of +0.0738 nDCG@10 and +0.1028 MAP@100, while improving Recall@5 by +6\% over OpenAI CLIP in multimodal retrieval. These results demonstrate the practicality of DEO for negation- and exclusion-aware retrieval in real-world settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《DEO: Training-Free Direct Embedding Optimization for Negation-Aware Retrieval》的结构化深入分析总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的密集检索（Dense Retrieval）模型在处理包含**否定（Negation）**或**排除（Exclusion）**逻辑的查询时表现极差（例如：“给我看最新的盈利预测，但排除2024年的结果”）。
*   **研究背景**：传统的解决方法通常依赖于对嵌入模型进行全量微调或添加适配器（Adapter），但这需要大量的计算资源、标注数据集，且部署复杂。此外，微调后的模型往往缺乏对特定查询意图的精确控制。
*   **整体含义**：本文旨在提出一种**无需训练（Training-Free）**、在推理阶段直接优化查询嵌入的方法，以低成本实现对复杂否定逻辑的精准检索。

### 2. 核心方法论：直接嵌入优化 (DEO)
DEO 的核心思想是在不改变预训练模型参数的前提下，通过 LLM 分解查询并在嵌入空间内进行短时间的迭代优化。
*   **查询分解（Query Decomposition）**：利用 LLM（如 GPT-4）将原始查询分解为：
    *   **正向子查询（Positive Sub-queries）**：强化用户想要包含的语义。
    *   **负向子查询（Negative Sub-queries）**：明确用户想要排除的语义。
*   **嵌入优化流程**：
    1.  获取原始查询、正向和负向子查询的初始嵌入向量。
    2.  将原始查询嵌入作为可学习参数 $e_u$。
    3.  **对比损失函数**：通过梯度下降最小化一个组合损失函数，包含三个部分：
        *   **吸引项**：拉近 $e_u$ 与正向子查询嵌入的距离。
        *   **排斥项**：推开 $e_u$ 与负向子查询嵌入的距离。
        *   **一致性正则项**：防止 $e_u$ 偏离原始查询语义太远。
    4.  使用 Adam 优化器进行少量步数（默认 20 步）的迭代，得到最终的查询向量用于检索。

### 3. 实验设计
*   **数据集与场景**：
    *   **文本检索**：NegConstraint（否定约束基准）、NevIR（神经检索中的否定）。
    *   **多模态检索（图文检索）**：COCO-Neg（来自 NegBench）。
*   **评估指标**：nDCG@10、MAP@100、Pairwise 准确率、Recall@5。
*   **对比基线**：
    *   **文本模型**：BGE 系列（BGE-small/large-en-v1.5, BGE-M3）。
    *   **多模态模型**：OpenAI CLIP, CLIP-laion400m, CLIP-datacomp, 以及专门针对否定优化的 NegCLIP。

### 4. 资源与算力
*   **推理开销**：DEO 在推理时非常高效。
    *   **CPU (AMD Ryzen 7 5800X)**：20 步优化仅需 **0.016 秒**。
    *   **GPU (NVIDIA RTX 3060)**：20 步优化仅需 **0.033 秒**。
*   **训练算力**：由于是 **Training-Free** 方法，该研究**不需要**大规模 GPU 集群进行模型训练，仅在推理阶段消耗极少量算力。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了文本和多模态两大领域，对比了 7 种主流预训练模型。
*   **消融实验**：非常充分，分别探讨了：
    *   不同 LLM（GPT-4 vs. Qwen-1.5B）对分解质量的影响。
    *   损失函数中各项超参数（$\lambda_o, \lambda_p, \lambda_n$）的敏感度。
    *   “仅分解”与“分解+优化”的效果对比（证明了优化步骤是性能提升的关键）。
    *   优化步数（Steps）对结果的影响。
*   **客观性**：实验在标准公开基准上进行，且证明了 DEO 在不同规模、不同架构的模型上均有稳定提升，具有较强的说服力。

### 6. 主要结论与发现
*   **显著提升**：在 NegConstraint 任务中，BGE-large 结合 DEO 后，MAP 提升了 **16.32%**，nDCG@10 提升了 **10.34%**。
*   **多模态通用性**：在 CLIP 模型上，Recall@5 提升了约 **6%**，甚至在已经针对否定优化过的 NegCLIP 上仍有额外增益。
*   **高效性**：仅需 20 步优化即可达到峰值性能，且对超参数具有一定的鲁棒性。
*   **可视化验证**：通过 PCA 轨迹分析，证实了优化过程确实让查询向量远离了“负向干扰项”，靠近了“真值文档”。

### 7. 优点与亮点
*   **即插即用**：无需重新训练模型，兼容任何现有的密集检索器（BGE, CLIP 等）。
*   **可解释性与控制力**：通过 LLM 分解，用户可以清晰看到系统是如何理解“排除”逻辑的，且可以通过调整超参数控制排除的强度。
*   **极低成本**：相比于需要数千小时 GPU 训练的微调方案，DEO 几乎可以在任何商用硬件上实时运行。

### 8. 不足与局限
*   **依赖 LLM 的分解质量**：如果 LLM 无法准确识别查询中的否定逻辑（实验显示 Qwen-1.5B 效果弱于 GPT-4），DEO 的效果会打折扣。
*   **推理延迟增加**：虽然优化过程很快，但相比于直接检索，DEO 增加了“LLM 调用”和“梯度优化”两个环节，在极高并发的实时检索场景下可能存在延迟瓶颈。
*   **超参数敏感性**：虽然具有鲁棒性，但在不同模态（文本 vs. 图像）间仍需手动调整 $\lambda_o$ 等参数以获得最佳效果。

（完）
