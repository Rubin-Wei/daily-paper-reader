---
title: "Adaptive Loops and Memory in Transformers: Think Harder or Know More?"
title_zh: Transformer 中的自适应循环与记忆：思考更深还是知识更多？
authors: "Markus Frey, Behzad Shomali, Ali Hamza Bashir, David Berghaus, Mehdi Ali"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08391v1"
tags: ["query:pm"]
score: 9.0
evidence: Transformer中用于学习存储的门控记忆库
tldr: 本研究探讨了Transformer模型中隐式推理与知识存储的平衡。针对循环Transformer参数效率高但存储容量有限的问题，提出结合自适应逐层循环（通过学习停止机制实现）与门控记忆库的新架构。实验表明，循环机制主要增强数学推理，而记忆库则提升常识任务表现。两者结合的模型在数学基准上优于同等计算量的深层基准模型，且内部层展现出从简单处理到复杂循环与记忆调用的演化特征。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08391v1/fig-001.webp\", \"caption\": \"Table 3: Full results at the final checkpoint. Best result within each group is bolded. Base = standard transformer; LN = Loop-N ; L3IF = iso-FLOP for Loop-3; M-BIP = memory iso-parameter baseline; Mg0 = memory model with gate init g0; M-BIF = memory iso-FLOP baseline.\", \"page\": 11, \"index\": 1, \"width\": 706, \"height\": 708}, {\"url\": \"assets/figures/arxiv/2603.08391v1/fig-002.webp\", \"caption\": \"Figure 1: Architecture overview. Left: A standard transformer passes hidden states through L unique blocks. Center: Our loop model allows each block to iterate up to N times, with a learned halting mechanism that produces a weighted combination of intermediate states. Per-step scales ζ(sn) are initialized near zero for training stability. Right: The combined model additionally retrieves from local (per-layer) and global (shared) memory banks, gated by learned input-dependent scalars.\", \"page\": 2, \"index\": 2, \"width\": 794, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.08391v1/fig-003.webp\", \"caption\": \"Figure 4: Memory gate activations across layers and training. Left: Local memory gate values show high variance across layers while later layers tend to have higher gate activations, and the spread increases over training. Right: Global memory gate values increase during training but converge to a more uniform profile across layers, with activations rising up to approximately layer 5 and then plateauing.\", \"page\": 10, \"index\": 3, \"width\": 821, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2603.08391v1/fig-004.webp\", \"caption\": \"Table 2: Model configurations. All models use the same base transformer architecture. Loop parameters include per-step scales and halting router weights. Memory parameters include local/global key-value banks and gating networks. Iso-parameter baselines add extra FFN capacity to match the corresponding model’s parameter count. Iso-FLOP baselines use 36 layers to approximate the forward-pass cost of 3-loop models.\", \"page\": 10, \"index\": 4, \"width\": 727, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2603.08391v1/fig-005.webp\", \"caption\": \"Table 5: Results at the mid checkpoint (step 20000).\", \"page\": 13, \"index\": 5, \"width\": 706, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2603.08391v1/fig-006.webp\", \"caption\": \"Table 1: Summary of results, averaged across benchmarks within each group. CS = commonsense; BPB = bits per byte (lower is better, see Appendix A.2 for details). Best result per column within each model group is bolded. Full per-benchmark breakdowns are in Appendix A.4.\", \"page\": 4, \"index\": 6, \"width\": 656, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2603.08391v1/fig-007.webp\", \"caption\": \"Table 4: Results at the early checkpoint (step 5000).\", \"page\": 12, \"index\": 7, \"width\": 706, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2603.08391v1/fig-008.webp\", \"caption\": \"Figure 3: Expected loop iterations vs. validation cross-entropy for all configurations. Each point represents one evaluation during training; curves are colored by model configuration. Across all looped models, the expected number of iterations begins to increase rapidly once the cross-entropy drops below approximately 3.27± 0.59. This phase transition is consistent across Loop-3, Loop-5, and Loop-7 configurations, suggesting it depends on the model’s language competence rather than the maximum number of allowed iterations.\", \"page\": 9, \"index\": 8, \"width\": 818, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2603.08391v1/fig-009.webp\", \"caption\": \"Figure 2: Expected number of loop iterations per layer over training. Left: Each curve represents one layer. Early layers (lighter colors) consistently use fewer iterations than later layers (darker colors). Middle: Expected steps at the end of training. Right: All models show a characteristic transition which occurs at approximately the same cross-entropy value across configurations (see Figure 3 in the Appendix for all configurations).\", \"page\": 5, \"index\": 9, \"width\": 817, \"height\": 244}]"
motivation: 旨在解决循环Transformer在参数效率上的优势与存储容量不足之间的矛盾，探索隐式推理与知识存储的协同效应。
method: 提出一种结合自适应逐层循环机制（学习停止准则）与门控记忆库（提供额外学习存储）的Transformer改进架构。
result: 循环机制显著提升了数学推理能力，而记忆库则弥补了常识任务的性能损失，组合模型在数学基准上超越了同等FLOPs的深层模型。
conclusion: 通过结合自适应循环与记忆机制，模型能更高效地分配计算资源，实现比单纯增加深度更好的推理与知识处理性能。
---

## 摘要
思维链（CoT）提示能够实现语言模型的推理，但需要对中间步骤进行显式的文本化表达。循环 Transformer 提供了一种替代方案，通过在隐藏状态内迭代地细化表示。这种参数效率是以牺牲性能为代价的，因为循环模型缺乏更深层模型所具有的存储容量，后者在每一层都使用唯一的权重。在这项工作中，我们研究了同时具备逐层自适应循环（其中每个 Transformer 块通过学习到的停止机制来学习迭代其隐藏状态）和门控记忆库（提供额外的学习存储）的 Transformer 模型。我们发现，与参数量和 FLOPs 匹配的模型相比，循环主要有利于数学推理，而记忆库则有助于恢复在常识任务上的性能。结合这两种机制所产生的模型在数学基准测试上的表现优于等效 FLOPs 的基准模型（该基准模型的层数是其三倍）。对模型内部的分析揭示了层的专业化：早期层学习进行极少的循环并谨慎地访问记忆，而后期层则更频繁地进行这两项操作。

## Abstract
Chain-of-thought (CoT) prompting enables reasoning in language models but requires explicit verbalization of intermediate steps. Looped transformers offer an alternative by iteratively refining representations within hidden states. This parameter efficiency comes at a cost, as looped models lack the storage capacity of deeper models which use unique weights per layer. In this work, we investigate transformer models that feature both adaptive per-layer looping, where each transformer block learns to iterate its hidden state via a learned halting mechanism, and gated memory banks, that provide additional learned storage. We find that looping primarily benefits mathematical reasoning, while memory banks help recover performance on commonsense tasks compared to parameter and FLOP matched models. Combining both mechanisms yields a model that outperforms an iso-FLOP baseline -- with three times the number of layers -- on math benchmarks. Analysis of model internals reveals layer specialization: early layers learn to loop minimally and access memory sparingly, while later layers do both more heavily.

---

## 论文详细总结（自动生成）

这篇论文《Adaptive Loops and Memory in Transformers: Think Harder or Know More?》探讨了 Transformer 架构中“计算深度”与“存储容量”之间的权衡。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型通常通过增加层数（深度）来提升性能，但这会显著增加参数量。**循环 Transformer（Looped Transformers）**通过多次调用同一层参数来实现“隐式推理”，虽然参数效率高，但由于缺乏每层唯一的权重，其**知识存储容量**远低于同等深度的标准模型。
*   **核心问题**：模型究竟是应该通过增加循环来“想得更深”（Think Harder），还是通过增加参数来“知道更多”（Know More）？论文旨在探索结合“自适应循环”与“外部记忆库”是否能同时解决推理能力和知识容量的问题。

### 2. 方法论：核心思想与关键技术
论文提出了一种增强型 Transformer 架构，包含两个核心组件：
*   **自适应循环（Adaptive Looping）**：
    *   基于 PonderNet 的停止机制：每个 Transformer 块可以迭代运行多次（最高 $N_{max}$ 次）。
    *   **停止路由器（Halting Router）**：在每次迭代时预测停止概率 $p_t$，最终输出是所有迭代状态的加权组合。
    *   **可学习缩放因子**：引入初始值接近零的缩放参数，确保训练初期模型表现稳定（接近恒等映射），随后逐渐学习何时介入计算。
*   **门控记忆库（Gated Memory Banks）**：
    *   **本地记忆（Local Memory）**：每层特有的存储空间，用于存放该深度相关的特定知识。
    *   **全局记忆（Global Memory）**：所有层共享的存储空间，用于存放通用知识。
    *   **集成方式**：使用 QK-归一化注意力机制检索记忆，并通过**输入相关的门控机制**（Input-dependent Gating）决定将多少记忆信息融入残差流。

### 3. 实验设计
*   **基础模型**：12 层、约 200M 参数的 Decoder-only Transformer。
*   **数据集**：在 FineWeb-Edu 数据集上预训练 14B Tokens。
*   **Benchmark**：
    *   **常识任务（Commonsense）**：ARC、HellaSwag、PIQA 等（评估准确率和 BPB）。
    *   **数学任务（Math）**：代数、几何、微积分等（评估 BPB，即每字节比特数，数值越低越好）。
*   **对比方法（Baselines）**：
    *   **Iso-Parameter (IsoPar)**：通过增加 FFN 宽度使参数量与实验模型对齐。
    *   **Iso-FLOP (IsoFLOP)**：使用 3 倍层数（36 层）的模型，使其前向计算成本与循环 3 次的模型对齐。

### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，Batch Size 约 360K tokens，共训练约 38,620 步。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但提到使用了 `Modalities` 训练框架，且实验规模属于中小规模（200M 参数，14B tokens）。

### 5. 实验数量与充分性
*   **实验组设置**：对比了无记忆循环模型（$N_{max}=3, 5, 7$）、带记忆循环模型（不同门控初始化策略）以及多种等参数/等计算量的基准模型。
*   **充分性**：实验涵盖了推理（数学）和知识（常识）两个维度，并进行了消融实验（如门控初始化、层间专业化分析）。虽然模型规模较小，但在受控变量（等参数、等 FLOPs）下的对比非常严谨，客观反映了不同机制的功能差异。

### 6. 主要结论与发现
*   **功能解耦**：**循环机制**主要提升数学推理能力（BPB 降低 22%）；而**记忆库**主要帮助恢复常识任务的性能。
*   **超越深层模型**：在等 FLOPs 条件下，12 层循环模型在数学任务上优于 36 层的深层模型，证明了循环在算法计算上的参数效率。
*   **层专业化**：模型内部出现了分工——早期层倾向于少循环、少用记忆；后期层则大量进行循环计算并频繁调用记忆库。
*   **阶段性演化**：模型并非从一开始就学习循环。实验发现，只有当验证集交叉熵损失降至约 3.27 以下（即模型具备一定语言能力后），自适应循环次数才会迅速增加。

### 7. 优点
*   **架构创新**：巧妙地将 PonderNet 的自适应计算与神经图灵机式的记忆库结合，实现了计算与存储的动态分配。
*   **深入的内部分析**：不仅关注最终分数，还详细分析了训练动态、层间差异和停止机制的触发时机。
*   **严谨的基准测试**：坚持在等参数和等 FLOPs 的前提下做对比，避免了因模型变大而产生的性能误导。

### 8. 不足与局限
*   **规模限制**：实验仅在 200M 参数级别进行，尚不确定这些结论在百亿（7B+）参数规模下是否依然成立。
*   **数学评估指标**：数学任务仅使用了 BPB（预测概率），而非最终的答案准确率（Accuracy），这可能无法完全反映模型解决复杂问题的实际成功率。
*   **效率权衡**：虽然循环节省了参数，但在推理时增加了计算延迟（FLOPs 增加），论文未深入讨论在实际部署中这种权衡的经济性。

（完）
