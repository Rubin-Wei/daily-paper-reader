---
title: "Agentic DAG-Orchestrated Planner Framework for Multi-Modal, Multi-Hop Question Answering in Hybrid Data Lakes"
title_zh: 混合数据湖中用于多模态、多跳问答的代理式 DAG 编排规划器框架
authors: "Kirushikesh D B, Manish Kesarwani, Nishtha Madaan, Sameep Mehta, Aldrin Dennis, Siddarth Ajay, Rakesh B R, Renu Rajagopal, Sudheesh Kairali"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14229v1"
tags: ["query:rag"]
score: 10.0
evidence: 用于多模态、多跳问答的智能体框架
tldr: 针对企业混合数据湖中多跳问答效率低、缺乏推理能力的问题，本文提出 A.DOT 框架。该框架将自然语言查询编译为有向无环图（DAG）执行计划，支持跨结构化表与非结构化文档的并行检索与多步推理。通过模式感知推理、语义验证及高级缓存机制，A.DOT 在提高问答准确率和响应速度的同时，提供了可追溯的证据链，显著增强了系统的可信度与数据溯源能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.14229v1/fig-001.webp\", \"caption\": \"Figure 1: Question Answering over Hybrid Data Lakes\", \"page\": 1, \"index\": 1, \"width\": 477, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2603.14229v1/fig-002.webp\", \"caption\": \"Table 4: In this example, the venue information is available in the structured data, but still the answer-text retrieves the same information from the unstructured context.\", \"page\": 11, \"index\": 2, \"width\": 882, \"height\": 802}, {\"url\": \"assets/figures/arxiv/2603.14229v1/fig-003.webp\", \"caption\": \"Table 3: In this example, the Great Smoky Mountains National Park is not available from the milvus chunks so the vectorstore retriever fails to gather the information or the document id to pass it to the next iceberg query.\", \"page\": 10, \"index\": 3, \"width\": 882, \"height\": 965}, {\"url\": \"assets/figures/arxiv/2603.14229v1/fig-004.webp\", \"caption\": \"Figure 2: Agentic DAG-Orchestrated (A.DOT) Planner Architecture\", \"page\": 3, \"index\": 4, \"width\": 1054, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2603.14229v1/fig-005.webp\", \"caption\": \"Figure 3: Example DAG plan\", \"page\": 8, \"index\": 5, \"width\": 488, \"height\": 856}, {\"url\": \"assets/figures/arxiv/2603.14229v1/fig-006.webp\", \"caption\": \"Table 5: In this example, the milvus retriever only retrieves the 3 chunks instead of 4\", \"page\": 12, \"index\": 6, \"width\": 882, \"height\": 577}]"
motivation: 现有的 RAG 系统在处理混合数据湖的多跳复杂查询时，存在检索效率低下、缺乏跨源推理能力以及结果难以验证的问题。
method: 提出 A.DOT 框架，通过将查询分解为可并行的 DAG 执行计划，协调异构数据源的并发检索，并结合模式感知推理与缓存机制进行优化。
result: "在基准数据集上，该框架相比基准模型在正确性上提升了 14.8%，在完整性上提升了 10.7%，并显著降低了处理延迟。"
conclusion: A.DOT 框架通过结构化的 DAG 编排有效解决了混合数据湖中的多模态多跳问答难题，为企业级知识检索提供了高效且可信的解决方案。
---

## 摘要
企业日益需要针对结合了结构化表格和非结构化文档的混合数据湖进行自然语言（NL）问答。目前部署的解决方案（包括基于 RAG 的系统）通常依赖于从每个存储库进行暴力检索并进行事后合并。这种方法效率低下且存在信息遗漏，更关键的是，它们缺乏对多跳推理的显式支持，即查询被分解为连续的步骤（跳），这些步骤可能在结构化和非结构化源之间来回穿梭。我们提出了代理式 DAG 编排 Transformer (A.DOT) 规划器，这是一个用于多模态、多跳问答的框架，它将用户的自然语言查询编译成跨越结构化和非结构化存储的有向无环图 (DAG) 执行计划。该系统将查询分解为可并行的子查询，结合了模式感知（schema-aware）推理，并在执行前应用结构和语义验证。执行引擎遵循生成的 DAG 计划，协调跨异构源的并发检索，将中间输出路由到依赖的子查询，并严格按照计划的逻辑依赖关系合并最终结果。先进的缓存机制结合了释义感知的模板匹配，使系统能够检测等效查询并重用先前的 DAG 执行计划以实现快速重新执行，而 DataOps 系统则处理验证反馈或执行错误。所提出的框架不仅提高了准确性并降低了延迟，还产生了显式的证据链，从而能够验证检索内容、追踪数据血缘，并增强用户对系统输出的信任。在基准数据集上，A.DOT 在正确性上比基准模型实现了 14.8% 的绝对提升，在完整性上提升了 10.7%。

## Abstract
Enterprises increasingly need natural language (NL) question answering over hybrid data lakes that combine structured tables and unstructured documents. Current deployed solutions, including RAG-based systems, typically rely on brute-force retrieval from each store and post-hoc merging. Such approaches are inefficient and leaky, and more critically, they lack explicit support for multi-hop reasoning, where a query is decomposed into successive steps (hops) that may traverse back and forth between structured and unstructured sources. We present Agentic DAG-Orchestrated Transformer (A.DOT) Planner, a framework for multi-modal, multi-hop question answering, that compiles user NL queries into directed acyclic graph (DAG) execution plans spanning both structured and unstructured stores. The system decomposes queries into parallelizable sub-queries, incorporates schema-aware reasoning, and applies both structural and semantic validation before execution. The execution engine adheres to the generated DAG plan to coordinate concurrent retrieval across heterogeneous sources, route intermediate outputs to dependent sub-queries, and merge final results in strict accordance with the plan's logical dependencies. Advanced caching mechanisms, incorporating paraphrase-aware template matching, enable the system to detect equivalent queries and reuse prior DAG execution plans for rapid re-execution, while the DataOps System addresses validation feedback or execution errors. The proposed framework not only improves accuracy and latency, but also produces explicit evidence trails, enabling verification of retrieved content, tracing of data lineage, and fostering user trust in the system's outputs. On benchmark dataset, A.DOT achieves 14.8% absolute gain in correctness and 10.7% in completeness over baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Agentic DAG-Orchestrated Planner Framework for Multi-Modal, Multi-Hop Question Answering in Hybrid Data Lakes》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：企业环境中的“混合数据湖”同时包含结构化表（SQL）和非结构化文档（文本）。现有的 RAG（检索增强生成）系统通常采用暴力检索后合并的方式，难以处理需要跨模态多次跳转的“多跳推理”（Multi-hop Reasoning）查询。
*   **研究动机**：
    *   **效率低下**：独立检索不同存储库会导致冗余计算和数据泄露。
    *   **缺乏逻辑**：现有系统难以在 SQL 查询结果和向量搜索之间建立显式的逻辑依赖。
    *   **不可信**：缺乏清晰的证据链（Evidence Trails）和数据血缘，用户难以验证答案来源。
*   **整体含义**：提出 A.DOT 框架，通过将自然语言查询编译为有向无环图（DAG）执行计划，实现跨异构数据源的精准、并行和可验证的推理。

### 2. 论文提出的方法论
A.DOT 框架由五个核心组件构成，旨在实现从规划到执行的闭环管理：
*   **核心思想**：将复杂查询分解为原子子查询节点，利用 DAG 表达节点间的依赖关系，并引入 DataOps 机制进行动态纠错。
*   **关键技术细节**：
    1.  **计划生成器（Plan Generator）**：单次 LLM 调用将 NL 查询分解为 DAG，每个节点标注了工具（SQL 或 Vector）、子任务描述和变量标签。
    2.  **计划验证器（Plan Validator）**：进行结构检查（语法、循环依赖）和语义检查（模式合规性、意图对齐），拦截无效计划。
    3.  **DataOps 系统**：包含诊断器、推荐器、修复器和重规划器。当验证失败或执行报错时，系统会自动尝试局部修复或全局重规划，而非简单报错。
    4.  **计划执行器（Plan Executor）**：按拓扑顺序并行执行无依赖节点。引入**变量绑定机制**，仅传递必要的 ID 或键值（Slim Variables），减少内存负载和上下文窗口压力。
    5.  **计划缓存（Plan Caching）**：支持精确匹配、模板匹配和语义匹配，允许重用已验证的 DAG 计划以降低延迟。

### 3. 实验设计
*   **数据集**：使用 **HybridQA** 基准数据集（包含 3,466 个开发集样本）。该数据集基于维基百科表格及其链接的文本段落，要求跨模态推理。
*   **场景构建**：将 HybridQA 的 JSON 格式转换为企业级的混合架构：结构化数据存入 SQL 数据库，非结构化文本存入 Milvus 向量数据库。
*   **对比方法（Baselines）**：
    *   **Standard RAG**：简单的检索后生成。
    *   **ReAct**：思维链与工具调用的交替迭代（顺序执行）。
    *   **LLM Compiler**：另一种基于 DAG 的并行函数调用框架。
*   **评估指标**：使用 Unitxt 库，通过 LLM-as-a-judge（Mistral Large 2 模型）评估“答案正确性”和“答案完整性”。

### 4. 资源与算力
*   **模型使用**：
    *   推理引擎：**LLaMA-3 70B**。
    *   评估裁判：**Mistral Large 2**。
    *   框架实现：基于 LangGraph。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于该框架主要基于现成大模型的 In-context Learning（上下文学习）和代理编排，不涉及大规模基座模型微调，因此算力消耗主要集中在推理阶段。

### 5. 实验数量与充分性
*   **实验规模**：在 HybridQA 开发集的 3,466 个样本上进行了完整测试，并在 500 个样本的子集上进行了消融实验。
*   **充分性与公平性**：
    *   **充分性**：通过消融实验验证了“验证器”和“DataOps 系统”的独立贡献，实验设计较为全面。
    *   **公平性**：所有基准模型均使用相同的 LLaMA-3 70B 底座和相同的检索工具（SQL + Milvus），确保了对比的客观性。
    *   **局限性**：实验主要集中在 HybridQA 这一学术数据集上，虽然提及了在 IBM 内部产品的评估，但缺乏更多样化的真实企业私有数据集的量化结果。

### 6. 论文的主要结论与发现
*   **性能飞跃**：A.DOT 在正确性上比 Standard RAG 提升了 **14.8%**，在完整性上提升了 **10.7%**。
*   **超越现有代理框架**：显著优于 ReAct 和 LLM Compiler，证明了“显式 DAG 规划 + 模式感知验证”在混合数据场景下的优越性。
*   **组件协同效应**：消融实验显示，仅有验证器而无 DataOps 修复会导致性能下降（因为系统会直接停止执行），只有两者结合才能实现最佳的鲁棒性。
*   **效率优化**：通过并行执行和变量瘦身，系统在保证复杂推理的同时维持了较低的端到端延迟。

### 7. 优点
*   **架构严谨**：将传统的“黑盒”代理推理转变为“白盒”DAG 计划，增强了可解释性和可调试性。
*   **企业级特性**：引入了数据血缘追踪、证据链生成和缓存机制，直接对标企业级部署需求。
*   **鲁棒性设计**：DataOps 系统的引入解决了 LLM 生成计划时偶尔出现的幻觉或语法错误问题。
*   **高效通信**：变量绑定机制解决了在多跳过程中传递大数据块导致的上下文溢出问题。

### 8. 不足与局限
*   **单轮限制**：目前主要针对单轮查询，尚未充分支持多轮对话上下文的维护。
*   **模态扩展性**：虽然声称支持多模态，但实验主要集中在“文本+表格”，对图像、图数据库等更复杂模态的集成尚待验证。
*   **模型依赖**：系统性能高度依赖于底座模型（LLaMA-3 70B）的规划能力，在更小规模的模型上可能表现不佳。
*   **错误传播**：如果初始的 DAG 规划在逻辑上完全偏离用户意图，后续的验证和修复机制可能无法完全纠正。

（完）
