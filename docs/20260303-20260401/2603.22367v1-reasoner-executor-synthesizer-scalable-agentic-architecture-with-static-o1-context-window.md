---
title: "Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window"
title_zh: Reasoner-Executor-Synthesizer：具有静态 O(1) 上下文窗口的可扩展智能体架构
authors: Ivan Dobrovolskyi
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22367v1"
tags: ["query:rag"]
score: 10.0
evidence: 用于RAG的静态上下文窗口智能体架构
tldr: 针对大语言模型智能体在处理大规模数据时存在的幻觉风险和Token成本随数据量线性增长的问题，本文提出RES（推理者-执行者-合成者）三层架构。该架构通过将意图解析、确定性数据检索与叙述生成分离，使执行器仅向合成器传递固定大小的统计摘要。实验证明，该架构在处理海量学术文献时实现了O(1)的Token复杂度，在显著降低成本的同时从根本上消除了数据幻觉。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22367v1/fig-001.webp\", \"caption\": \"Figure 2. RES token cost remains constant as dataset size grows exponentially (O(1) proof).\", \"page\": 3, \"index\": 1, \"width\": 492, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2603.22367v1/fig-002.webp\", \"caption\": \"Figure 3. Log-log scaling comparison — RES O(1) vs. Naive O(n). At 16M articles, RES is 1.2 million times cheaper.\", \"page\": 3, \"index\": 2, \"width\": 492, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2603.22367v1/fig-003.webp\", \"caption\": \"Figure 4. Direct token cost comparison across all 20 query types. RES (red) vs. Naive with 50 articles (blue).\", \"page\": 4, \"index\": 3, \"width\": 492, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.22367v1/fig-004.webp\", \"caption\": \"Figure 5. Distribution of RES token costs across 95 runs. Tight clustering around μ=1,574 confirms predictable cost.\", \"page\": 4, \"index\": 4, \"width\": 492, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.22367v1/fig-005.webp\", \"caption\": \"Figure 1. Detailed RES architecture flow for a sample query. Token costs shown per layer.\", \"page\": 2, \"index\": 5, \"width\": 492, \"height\": 283}]"
motivation: 旨在解决传统RAG架构中Token成本随数据集规模线性增长以及长上下文导致的幻觉风险问题。
method: 提出一种三层架构，将意图解析、不消耗Token的确定性数据处理以及基于固定大小摘要的叙述生成进行严格分离。
result: "在ScholarSearch测试中，无论数据集包含4万还是1600万条记录，平均Token消耗始终稳定在1,574个，实现了O(1)的复杂度。"
conclusion: RES架构通过在设计上隔离原始数据与生成过程，为构建低成本、无幻觉的大规模智能体系统提供了可扩展的解决方案。
---

## 摘要
部署为自主智能体的大语言模型（LLMs）通常使用检索增强生成（RAG），将检索到的文档输入上下文窗口，这引发了两个问题：幻觉风险随上下文长度增加而增长，且 Token 成本随数据集规模线性扩展。我们提出了 Reasoner-Executor-Synthesizer (RES) 架构，这是一种三层设计，严格分离了意图解析（推理器）、确定性数据检索与聚合（执行器）以及叙述生成（合成器）。执行器不消耗 LLM Token，且仅向合成器传递固定大小的统计摘要。我们从形式上证明了 RES 在数据集规模方面实现了 O(1) 的 Token 复杂度，并在基于 Crossref API（包含 1.3 亿多篇文章）的学术研究助手 ScholarSearch 上验证了这一点。在 100 次基准测试运行中，无论数据集包含 42,000 篇还是 1,630 万篇文章，RES 的平均 Token 成本均保持在 1,574 个。该架构通过构造消除了数据幻觉：LLM 永远不会接触原始记录。关键词：LLM 智能体；智能体架构；幻觉消除；Token 优化；上下文窗口；检索增强生成；确定性执行；学术元数据；Crossref API；O(1) 复杂度。

## Abstract
Large Language Models (LLMs) deployed as autonomous agents commonly use Retrieval-Augmented Generation (RAG), feeding retrieved documents into the context window, which creates two problems: the risk of hallucination grows with context length, and token cost scales linearly with dataset size. We propose the Reasoner-Executor-Synthesizer (RES) architecture, a three-layer design that strictly separates intent parsing (Reasoner), deterministic data retrieval and aggregation (Executor), and narrative generation (Synthesizer). The Executor uses zero LLM tokens and passes only fixed-size statistical summaries to the Synthesizer. We formally prove that RES achieves O(1) token complexity with respect to dataset size, and validate this on ScholarSearch, a scholarly research assistant backed by the Crossref API (130M+ articles). Across 100 benchmark runs, RES achieves a mean token cost of 1,574 tokens regardless of whether the dataset contains 42,000 or 16.3 million articles. The architecture eliminates data hallucination by construction: the LLM never sees raw records. KEYWORDS LLM agents; agentic architecture; hallucination elimination; token optimization; context window; retrieval-augmented generation; deterministic execution; scholarly metadata; Crossref API; O(1) complexity.

---

## 论文详细总结（自动生成）

以下是对论文《Reasoner-Executor-Synthesizer: Scalable Agentic Architecture with Static O(1) Context Window》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）智能体在处理大规模数据集时面临的两大瓶颈：
*   **Token 成本线性增长**：传统的检索增强生成（RAG）模式下，Token 消耗随检索到的文档数量线性扩展（$O(n)$），导致分析海量数据时经济成本极高。
*   **幻觉风险随上下文增加**：研究表明，LLM 在处理长上下文时更容易产生幻觉，尤其是当关键信息被淹没在大量无关数据中时（即“迷失在中间”问题）。
**研究动机**：设计一种新型架构，使智能体在处理任意规模的数据集时，其 Token 消耗保持恒定（$O(1)$），并从架构设计上彻底消除数据幻觉。

### 2. 论文提出的方法论
论文提出了 **RES（Reasoner-Executor-Synthesizer）** 三层架构，核心思想是**将概率性的语言推理与确定性的数据处理严格分离**：
*   **Reasoner（推理器 - LLM）**：负责自然语言意图解析。它将用户查询转换为结构化的查询计划（JSON 格式），确定分析类型（如趋势、对比、排名）。它不接触原始数据，仅处理查询逻辑。
*   **Executor（执行器 - 确定性代码）**：负责执行数据操作。它根据查询计划调用 API 或数据库，进行计数、聚合、过滤和排序。**关键点**：执行器不使用 LLM Token，且仅输出固定大小的统计摘要（如 JSON 格式的统计表），而非原始记录。
*   **Synthesizer（合成器 - LLM）**：负责叙述生成。它接收执行器输出的固定大小摘要，将其转化为人类可读的结论和可视化配置。
*   **数学证明**：论文通过公式证明，由于推理器输入受限、执行器不耗 Token、合成器输入为固定大小摘要，总 Token 复杂度 $T_{RES} = O(|Q|) + 0 + O(1) = O(1)$，即与数据集规模 $n$ 无关。

### 3. 实验设计
*   **应用场景**：开发了名为 **ScholarSearch** 的学术研究助手。
*   **数据集**：接入 **Crossref API**，涵盖超过 1.3 亿条学术文章元数据。
*   **Benchmark**：设计了 20 个多样化查询，涵盖趋势分析、对比、排名和通用统计四类意图。
*   **对比方法**：对比了 **Naive RAG（朴素检索增强生成）**。在 Naive 模式下，系统将 50 条原始文章记录直接喂给 LLM 进行处理。
*   **测试规模**：数据集规模从 4.2 万条记录（网络安全领域）跨越到 1630 万条记录（医学研究领域）。

### 4. 资源与算力
*   **模型使用**：使用了 Anthropic 的 **Claude 3.5 Sonnet** (claude-sonnet-4-20250514) 作为推理器和合成器的底层模型。
*   **算力说明**：论文未提及具体的 GPU 训练算力或时长，因为该研究侧重于**推理架构的设计**而非模型训练。系统基于现有的商业 LLM API 构建，前端使用 Streamlit，可视化使用 Plotly。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 100 次基准测试运行（20 个查询 × 5 次重复）。
*   **充分性评价**：
    *   **覆盖度**：实验覆盖了从万级到千万级的数据量级跨度，充分验证了 $O(1)$ 复杂度的理论推导。
    *   **客观性**：通过 5 次重复运行计算标准差（$\sigma=259$），证明了 Token 消耗的稳定性。
    *   **对比公平性**：RES 和 Naive 基准使用相同的 LLM 模型进行对比，确保了实验变量受控。

### 6. 论文的主要结论与发现
*   **Token 成本恒定**：无论数据集包含 4.2 万还是 1630 万篇文章，RES 的平均 Token 消耗始终稳定在 **1,574** 个左右。
*   **极端成本优势**：在处理 1600 万条记录时，RES 比传统 Naive 方法节省了 **99.9996%** 的 Token 成本（成本降低了约 120 万倍）。
*   **消除幻觉**：由于 LLM 永远无法接触到原始数据记录，只能基于执行器提供的确定性统计结果进行描述，因此从构造上消除了数据层面的幻觉。

### 7. 优点（亮点）
*   **极高的经济性**：使企业级海量数据的 LLM 分析在成本上变得可行。
*   **架构优雅**：通过严格的层级契约（Layer Contracts）解决了长上下文带来的性能下降问题。
*   **透明度高**：RES 允许 UI 实时显示每一层的输入输出，用户可以验证数据来源的真实性。
*   **领域无关性**：该架构可轻易推广至金融、医疗、物联网等任何拥有结构化/半结构化数据的领域。

### 8. 不足与局限
*   **查询类型限制**：RES 擅长统计分析类查询，但不适用于需要“深度阅读”单篇文档内容的任务（例如“总结某篇特定论文的实验细节”）。
*   **依赖底层 API 能力**：目前的实现高度依赖 Crossref API 的聚合（Faceted Search）功能。如果数据源不支持聚合，执行器需要自行编写复杂的处理逻辑。
*   **推理器瓶颈**：系统的准确性仍受限于推理器解析复杂自然语言指令的能力。

（完）
