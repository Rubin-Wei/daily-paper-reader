---
title: Scaling Attention via Feature Sparsity
title_zh: 通过特征稀疏性扩展注意力机制
authors: "Yan Xie, Tiansheng Wen, Tangda Huang, Bo Chen, Chenyu You, Stefanie Jegelka, Yifei Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.22300v1"
tags: ["query:lcp"]
score: 10.0
evidence: 通过特征稀疏性将 Transformer 扩展到超长上下文
tldr: "针对Transformer在处理超长上下文时面临的计算瓶颈，本文提出了稀疏特征注意力（SFA）。该方法另辟蹊径，通过将查询和键表示为k-稀疏编码而非在序列维度进行删减，在保留高维表达能力的同时显著降低了计算复杂度。配合专门开发的FlashSFA高效内核，该方法在GPT-2和Qwen3预训练中实现了与稠密模型相当的精度，同时提升了高达2.5倍的推理速度，并减少了近50%的FLOPs和KV缓存占用，为长文本扩展提供了高效的新方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22300v1/fig-001.webp\", \"caption\": \"Figure 3: Latency vs. feature sparsity. Latency Comparison of dense attention and SFA (ours) at different modular levels in Transformers under 16k context length. Higher sparsity brings substantial decrease in latency.\", \"page\": 6, \"index\": 1, \"width\": 784, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-002.webp\", \"caption\": \"Figure 11: Eigenvalue spectrum analysis for Qwen3-0.6B model. Layer-wise effective dimension of (a) query and (b) key activation with normalized cumulative eigenvalue of 0.9, evaluated on the same sampled subset of the Pile validation set in Appendix F.\", \"page\": 26, \"index\": 2, \"width\": 806, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-003.webp\", \"caption\": \"Figure 6: Comparison of latencies in different attention scenarios: (a) full attention(Time-to-firsttoken) and (b) kv-cache attention(Time-to-next-token).\", \"page\": 15, \"index\": 3, \"width\": 817, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-004.webp\", \"caption\": \"Table 6: Operation counts for standard flash attention and flash attention sparse. The number of floating-point operations (FLOPs) and integer operations (INOPs) were counted separately for feature dimensions of 64 and 128 under different context lengths.\", \"page\": 15, \"index\": 4, \"width\": 818, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-005.webp\", \"caption\": \"Figure 7: Entropy of Top-K feature selection across layers and heads. We plot the normalized entropy of the TopK index distribution for each attention head and layer of Qwen3-0.6B when applying SFA. (a) Entropy of the TopK positions of query vectors Q (16 heads, due to GQA). (b) Entropy of the TopK positions of key vectors K (8 heads). Each cell corresponds to one (layer, head) pair, and brighter colors indicate higher entropy (more balanced use of feature dimensions).\", \"page\": 21, \"index\": 5, \"width\": 783, \"height\": 764}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-006.webp\", \"caption\": \"Figure 4: Latency vs. feature sparsity with various config. Latency Comparison of dense attention and SFA (ours) at different head dimensions and context lengths. Notably, the latency of SFA can be much lower than dense attention under high dimension per head and long context, e.g., Figure 4d.\", \"page\": 7, \"index\": 6, \"width\": 827, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-007.webp\", \"caption\": \"Figure 5: Scaling dense attention and SFA with context length. SFA can consistently reduce both the computatin cost and KV cache size by a constant factor of at least 2.\", \"page\": 7, \"index\": 7, \"width\": 785, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-008.webp\", \"caption\": \"Table 11: Comparison results with token-sparse, KV-pruning, low-rank, and kernel baselines on GPT-2. Token-sparse training methods include Routing(Roy et al., 2021) and Longformer(Beltagy et al., 2020); KV-pruning (training-free) methods include H2O(Zhang et al., 2023), Quest(Tang et al., 2024), and SnapKV(Li et al., 2024); Loki(Singhania et al., 2024) is a low-rank key compression method (training-free); Performer(Choromanski et al., 2021) is a kernel-based approximation. Rows marked “+SFA (k = 8)” apply our feature-sparse SFA to Longformer and SnapKV, showing that SFA is orthogonal to these approaches and can be combined with them.\", \"page\": 22, \"index\": 8, \"width\": 827, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-009.webp\", \"caption\": \"Table 5: Configurations for fine-tuning MoE models\", \"page\": 14, \"index\": 9, \"width\": 861, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-010.webp\", \"caption\": \"Figure 8: Ablation of sparsity k on GPT-2 124M with fixed head dimension d = 64. Perplexity on OpenWebText (left) and latency at 32K context (right) as a function of the Top-k sparsity level used by SFA. The dashed gray line denotes the dense (full) attention baseline; the red curve shows SFA with different k.\", \"page\": 23, \"index\": 10, \"width\": 647, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-011.webp\", \"caption\": \"Figure 9: Ablation of head dimension dhead on GPT-2 124M with fixed sparsity k = 8. Perplexity on OpenWebText (left) and latency at 32K context (right) as a function of the head dimension dhead used by SFA. The dashed gray line denotes the dense (full) attention baseline; the red curve shows SFA with different dhead.\", \"page\": 23, \"index\": 11, \"width\": 646, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-012.webp\", \"caption\": \"Table 3: Evaluation on general reasoning tasks and synthetic retrieval (NIAH). Accuracy is in %.\", \"page\": 8, \"index\": 12, \"width\": 748, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-013.webp\", \"caption\": \"Table 9: Latency (ms) versus context length.\", \"page\": 19, \"index\": 13, \"width\": 706, \"height\": 753}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-014.webp\", \"caption\": \"Figure 10: Validation loss curves of SFA on GPT-2 (124M) pre-training. We compare varying sparsity levels k ∈ {2, 4, 8, 16}. The curves decrease smoothly and monotonically without divergent spikes, demonstrating that SFA maintains training stability even under aggressive sparsity (k = 2).\", \"page\": 24, \"index\": 14, \"width\": 660, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-015.webp\", \"caption\": \"Table 10: Latency, Perplexity and Accuracy results comparison with various compression and acceleration techniques, categorized into Token-Level and Feature-Level Operations. For token-level operations, \\\"Longforemer\\\" (Beltagy et al., 2020) denotes fixed token sparsity pattern, \\\"NSA\\\" (Yuan et al., 2025) denotes dynamic token sparsity pattern. \\\"Dense (full)\\\" baselines use full hidden size and uncompressed KV cache; \\\"Short (d = X)\\\" denotes baselines with half feature dimensions; \\\"Quant\\\" denotes 8-bit quantization aware training (QAT (Liu et al., 2024b)) on weights and activations; \\\"Low-Rank\\\" denotes PCA-based projection matrix fine-tuning; \\\"MLA\\\" denotes multi head latent attention (Liu et al., 2024a), and \\\"MLA + SFA\\\" combines SFA with latent key/value. \\\"Latency@128k\\\" is measured by \\\"Decoding with KV cache (TTNT) (ms)\\\" and \\\"Prefilling with full attention (TTFT) (s)\\\". PPL is evaluated on OpenWebText for GPT-2 and Pile for Qwen3.\", \"page\": 20, \"index\": 15, \"width\": 830, \"height\": 648}, {\"url\": \"assets/figures/arxiv/2603.22300v1/fig-016.webp\", \"caption\": \"Table 1: Perplexity and Accuracy results. Dense baselines use full hidden size and uncompressed KV cache; “Dense (d=X)” denotes short-embedding baselines with reduced feature dimension. PPL is evaluated on OpenWebText for GPT-2 and Pile for Qwen3. Note that “Dense (full)\\\" serves as a reference upper bound; we highlights the best results among the sparse/compressed baselines.\", \"page\": 5, \"index\": 16, \"width\": 830, \"height\": 284}]"
motivation: 现有的通过序列轴稀疏化降低Transformer计算成本的方法往往会不可避免地导致模型精度下降。
method: 提出将查询和键转换为k-稀疏编码的SFA方法，并开发了IO感知的FlashSFA内核以直接在稀疏重叠上进行运算。
result: SFA在预训练任务中匹配了稠密基准的精度，实现了2.5倍的加速，并显著降低了计算量和存储开销。
conclusion: 特征级稀疏性是优化注意力机制的一个重要且互补的维度，能够支持Transformer在极低质量损失下扩展至更长的上下文。
---

## 摘要
将 Transformer 扩展到超长上下文受到自注意力机制 $O(n^2 d)$ 开销的瓶颈限制。现有方法通过局部窗口、核近似或标记级（token-level）稀疏性在序列轴上降低这一开销，但这些方法往往会降低准确率。在本文中，我们探索了一个正交轴：特征稀疏性。我们提出了稀疏特征注意力（Sparse Feature Attention, SFA），其中查询（queries）和键（keys）被表示为 $k$-稀疏编码，在保留高维表达能力的同时，将注意力开销从 $Θ(n^2 d)$ 降低到 $Θ(n^2 k^2/d)$。为了在大规模下实现高效，我们引入了 FlashSFA，这是一种 IO 感知的算子，它扩展了 FlashAttention，使其能够直接在稀疏重叠上运行，而无需实例化稠密评分矩阵。在 GPT-2 和 Qwen3 的预训练中，SFA 在达到与稠密基准相当性能的同时，速度提升高达 2.5 倍，并减少了近 50% 的 FLOPs 和 KV 缓存。在合成和下游基准测试中，SFA 在长上下文下保持了检索准确性和鲁棒性，优于那些会破坏特征多样性的短嵌入基准。这些结果确立了特征级稀疏性作为高效注意力机制的一个互补且尚未被充分探索的维度，使 Transformer 能够以极小的质量损失扩展到长出几个数量级的上下文。代码可在 https://github.com/YannX1e/Sparse-Feature-Attention 获取。

## Abstract
Scaling Transformers to ultra-long contexts is bottlenecked by the $O(n^2 d)$ cost of self-attention. Existing methods reduce this cost along the sequence axis through local windows, kernel approximations, or token-level sparsity, but these approaches consistently degrade accuracy. In this paper, we instead explore an orthogonal axis: feature sparsity. We propose Sparse Feature Attention (SFA), where queries and keys are represented as $k$-sparse codes that preserve high-dimensional expressivity while reducing the cost of attention from $Θ(n^2 d)$ to $Θ(n^2 k^2/d)$. To make this efficient at scale, we introduce FlashSFA, an IO-aware kernel that extends FlashAttention to operate directly on sparse overlaps without materializing dense score matrices. Across GPT-2 and Qwen3 pretraining, SFA matches dense baselines while improving speed by up to $2.5\times$ and reducing FLOPs and KV-cache by nearly 50\%. On synthetic and downstream benchmarks, SFA preserves retrieval accuracy and robustness at long contexts, outperforming short-embedding baselines that collapse feature diversity. These results establish feature-level sparsity as a complementary and underexplored axis for efficient attention, enabling Transformers to scale to orders-of-magnitude longer contexts with minimal quality loss. Code is available at https://github.com/YannX1e/Sparse-Feature-Attention.

---

## 论文详细总结（自动生成）

这是一份关于论文《Scaling Attention via Feature Sparsity》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：Transformer 在处理超长上下文时，自注意力机制面临 $O(n^2d)$ 的计算和存储瓶颈（$n$ 为序列长度，$d$ 为特征维度）。
*   **研究动机**：现有优化方法（如局部窗口、低秩近似、Token 稀疏化）主要在**序列轴（$n$）**上做文章，但这往往会牺牲模型精度。本文提出从**特征轴（$d$）**出发，利用特征稀疏性（Feature Sparsity）来降低开销，旨在不损失高维表达能力的前提下实现大幅加速。

### 2. 方法论：核心思想与关键技术
*   **核心思想（SFA）**：将查询（Query, $Q$）和键（Key, $K$）向量转换为 **$k$-稀疏编码**。即每行向量只保留幅值最大的 $k$ 个维度，其余置零。
*   **关键技术细节**：
    *   **稀疏点积**：注意力分数仅在 $Q$ 和 $K$ 激活维度重叠时计算。计算复杂度从 $\Theta(n^2d)$ 降至 $\Theta(n^2k^2/d)$。
    *   **FlashSFA 内核**：受 FlashAttention 启发，开发了 IO 感知的 CUDA 内核。它直接在 SRAM 中处理稀疏特征的交集并进行在线 Softmax，避免了实例化 $n \times n$ 的稠密评分矩阵，解决了内存瓶颈。
    *   **训练策略**：支持从头预训练（使用直通估计器 STE 传递梯度）和对现有稠密模型进行正则化微调（通过 MSE 损失对齐稠密与稀疏注意力的输出）。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用 OpenWebText（GPT-2）和 The Pile（Qwen3-0.6B）。
    *   **长文本检索**：合成的“大海捞针”（Needle-in-a-Haystack, NIAH）任务。
    *   **下游任务**：PiQA、LAMBADA、ARC、HellaSwag、GSM-8K、Sci-papers 等。
*   **Benchmark 与对比方法**：
    *   **Dense (full)**：标准全维度稠密注意力。
    *   **Short Embedding**：直接缩减特征维度 $d$ 的基准。
    *   **其他稀疏/压缩技术**：包括 Longformer、NSA（Token 稀疏）、MLA（多头潜在注意力）、量化（Quantization）等。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA A800 GPU** 上运行。
*   **软件框架**：基于 CUDA 12.4、PyTorch，并使用了自定义的 FlashSFA 内核（基于 LeetCUDA 构建）。
*   **训练细节**：文中提到了针对不同规模模型（124M 到 8B）的微调设置（如 Batch Size 256，3 个 Epoch 等），但未明确给出总的 GPU 时数或总卡数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 124M、350M 到 0.6B、4B、8B 不同参数规模的模型。
*   **实验维度**：包括预训练性能对比、长文本泛化性测试、推理延迟/内存压力测试、微调适应性实验以及丰富的消融实验（针对 $k$ 值、特征维度 $d$、负载均衡等）。
*   **充分性评价**：实验设计非常全面且客观。作者不仅对比了精度，还深入分析了 FLOPs、KV 缓存占用和实际墙钟时间（Wall-clock time），并验证了 SFA 与 Token 级稀疏方法的正交性（可叠加使用），论证具有说服力。

### 6. 主要结论与发现
*   **性能对齐**：SFA 在 $k=16$ 或 $k=8$ 时，预训练精度几乎能持平稠密基准，远优于简单缩减维度的“短嵌入”方案。
*   **高效扩展**：在 128k 上下文下，SFA 实现了高达 **2.5 倍的加速**，并减少了约 **50% 的 FLOPs 和 41% 的 KV 缓存**。
*   **长文本优势**：在 NIAH 任务中，SFA 展现了比稠密模型更强的长度泛化能力，在 32k 长度下准确率更高且速度更快。
*   **特征分布**：SVD 分析显示 $Q/K$ 矩阵本身具有低秩特性，验证了特征稀疏化的合理性。

### 7. 优点（亮点）
*   **正交性**：开辟了特征轴稀疏化这一新路径，可与现有的序列轴优化技术完美兼容。
*   **系统级优化**：不仅有理论算法，还配套开发了高效的 FlashSFA 内核，使理论上的计算缩减转化为实际的工程加速。
*   **KV 缓存压缩**：直接减少了存储非零索引和数值的开销，对大模型推理的内存瓶颈有直接贡献。

### 8. 不足与局限
*   **硬件依赖**：稀疏张量运算在当前通用 GPU 硬件上仍面临挑战，虽然有 FlashSFA 优化，但其性能增益在短序列或低维度下不明显（受限于查找开销）。
*   **任务敏感性**：在某些复杂逻辑推理任务（如 GSM-8K）中，极度稀疏化（小 $k$ 值）仍会导致轻微的性能下降。
*   **超参数调优**：针对不同模型架构，可能需要精细调整 $k$ 值以平衡速度与精度。

（完）
