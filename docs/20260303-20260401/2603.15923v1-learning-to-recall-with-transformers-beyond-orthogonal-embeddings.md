---
title: Learning to Recall with Transformers Beyond Orthogonal Embeddings
title_zh: 超越正交嵌入的 Transformer 召回学习
authors: "Nuri Mert Vural, Alberto Bietti, Mahdi Soltanolkotabi, Denny Wu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15923v1"
tags: ["query:pm"]
score: 9.0
evidence: 分析Transformer如何在训练期间编码信息并在推理时检索
tldr: 本研究探讨了在非正交嵌入和有限数据集的现实条件下，Transformer模型如何学习知识检索。通过分析单层Transformer在令牌检索任务上的梯度下降过程，研究揭示了模型存储容量与样本量、嵌入维度及序列长度之间的乘法依赖关系。该工作填补了理想化理论与实际应用之间的空白，并通过数值实验和统计下界验证了这种内在的缩放规律，为理解大模型的记忆机制提供了新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.15923v1/fig-001.webp\", \"caption\": \"Figure 7: Empirical scaling of the parameter size for the Attention-only model (L ≍ V ), trained with Adam using a batch size of ⌊N/16⌋. Top row (a–d): N ≍ V log V . Bottom row (e–h): N ≍ V 1.5.\", \"page\": 11, \"index\": 1, \"width\": 929, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2603.15923v1/fig-002.webp\", \"caption\": \"Figure 2: Empirical scaling of parameter size via three-step GD for the Attention-only model. In (a) and (b), top-left and top-right use L ≍ V 0.5; bottom-left and bottom-right use L ≍ V . In (c), we compare the parameter counts from (a) and (b) for the L ≍ V case under two sample-size regimes, N ≍ V log V and N ≍ V 1.5. Line fitting: We identify in the heatmaps the smallest embedding dimension that achieves accuracies {0.1, 0.125, 0.15} and perform a least squares fit. The slopes of the fitted lines and their theoretical counterparts are reported on the heatmaps.\", \"page\": 7, \"index\": 2, \"width\": 829, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2603.15923v1/fig-003.webp\", \"caption\": \"Figure 5: Empirical scaling of the parameter size for the Attention-only model under two sample size regimes. Top row (a–d): N ≍ V log V . Bottom row (e–h): N ≍ V 1.5. The model uses L ≍ V and is trained using Adam over 16 epochs. We observe that the capacity improves as the number of epochs increases.\", \"page\": 10, \"index\": 3, \"width\": 929, \"height\": 474}, {\"url\": \"assets/figures/arxiv/2603.15923v1/fig-004.webp\", \"caption\": \"Figure 6: Empirical scaling of the parameter size for the Attention-only model with N ≍ V log V and L ≍ V 0.85. The model is trained using Adam over 16 epochs. The slopes are smaller than in Figure 5, which is consistent with the shorter sequence length.\", \"page\": 10, \"index\": 4, \"width\": 925, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2603.15923v1/fig-005.webp\", \"caption\": \"Figure 1: (a) Empirical scaling of the parameter size required for a GD-trained one-layer transformer to learn factual recall, where we use m ≍ d2 (see Section 4.2 for details). For small L, the trained model achieves the optimal capacity V ≍ md (purple line). As the sequence length L increases, the scaling changes, suggesting a multiplicative rate (blue and red lines). (b) Phase diagram for the theoretical scaling of the parameter count given in Corollary 2. Each region corresponds to a regime where a particular noise term in Theorem 1 is dominant. The parameter-size condition (md) in each region is given in Corollary 2.\", \"page\": 3, \"index\": 5, \"width\": 856, \"height\": 395}, {\"url\": \"assets/figures/arxiv/2603.15923v1/fig-006.webp\", \"caption\": \"Figure 4: Empirical scaling of embedding parameter size for the Attention-MLP model under two width regimes, m ≍ d2 and m ≍ d3. In (a) and (b), top-row uses L ≍ V 0.25; bottom-row uses L ≍ V 0.5. In (c), we compare the parameter counts from (a) and (b) for the L ≍ V 0.5 case under both width regimes.\", \"page\": 8, \"index\": 6, \"width\": 845, \"height\": 447}, {\"url\": \"assets/figures/arxiv/2603.15923v1/fig-007.webp\", \"caption\": \"Figure 3: Empirical scaling of parameter size for the Attention-MLP model under two sample size regimes, N ≍ V log V and N ≍ V 1.5. In (a) and (b), top-row uses L ≍ V 0.25; bottom-row uses L ≍ V 0.5. In (c), we compare the parameter counts from (a) and (b) for the L ≍ V 0.5 case under both sample-size regimes.\", \"page\": 8, \"index\": 7, \"width\": 839, \"height\": 448}]"
motivation: 现有理论多假设正交嵌入或无限数据，无法准确描述现实中Transformer在有限数据和随机嵌入下的知识检索行为。
method: 通过分析单层Transformer在令牌检索任务中梯度下降的早期阶段，推导出模型存储容量的显式数学公式。
result: 研究发现存储容量与样本量、嵌入维度及序列长度之间存在乘法依赖关系，且该规律在数值实验和统计下界分析中均得到证实。
conclusion: 该研究揭示了非正交嵌入下Transformer存储容量的内在缩放特性，为优化大模型的知识存储与检索能力提供了理论指导。
---

## 摘要
现代大语言模型 (LLM) 擅长处理需要存储和检索知识的任务，例如事实召回和问答。Transformer 是实现这一能力的核心，因为它们能够在训练期间编码信息并在推理时检索信息。现有的理论分析通常在理想化假设下研究 Transformer，例如无限数据或正交嵌入。然而，在现实场景中，模型是在具有非正交（随机）嵌入的有限数据集上训练的。我们通过分析一个在简单令牌检索任务上使用（经验）梯度下降训练的、具有随机嵌入的单层 Transformer 来填补这一空白。在该任务中，模型必须在长度为 $L$ 的序列中识别出信息令牌，并学习从令牌到标签的一一映射。我们的分析追踪了梯度下降的“早期阶段”，并推导出了模型存储容量的显式公式，揭示了样本量 $N$、嵌入维度 $d$ 和序列长度 $L$ 之间的乘法依赖关系。我们通过数值实验验证了这些缩放规律，并进一步为底层统计问题提供了下界，证明了这种乘法缩放是非正交嵌入下的固有特性。

## Abstract
Modern large language models (LLMs) excel at tasks that require storing and retrieving knowledge, such as factual recall and question answering. Transformers are central to this capability because they can encode information during training and retrieve it at inference. Existing theoretical analyses typically study transformers under idealized assumptions such as infinite data or orthogonal embeddings. In realistic settings, however, models are trained on finite datasets with non-orthogonal (random) embeddings. We address this gap by analyzing a single-layer transformer with random embeddings trained with (empirical) gradient descent on a simple token-retrieval task, where the model must identify an informative token within a length-$L$ sequence and learn a one-to-one mapping from tokens to labels. Our analysis tracks the ``early phase'' of gradient descent and yields explicit formulas for the model's storage capacity -- revealing a multiplicative dependence between sample size $N$, embedding dimension $d$, and sequence length $L$. We validate these scalings numerically and further complement them with a lower bound for the underlying statistical problem, demonstrating that this multiplicative scaling is intrinsic under non-orthogonal embeddings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Recall with Transformers Beyond Orthogonal Embeddings》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大语言模型（LLM）展现出强大的事实召回能力，但现有理论研究多基于“无限数据”或“正交嵌入（Orthogonal Embeddings）”等理想化假设。
*   **核心问题**：在现实场景中，令牌嵌入通常是非正交的（随机嵌入），且训练数据有限。论文旨在探讨：在非正交嵌入下，使用梯度下降（GD）训练的 Transformer 如何学习事实召回任务？其样本复杂度、嵌入维度、序列长度和模型容量之间存在怎样的定量关系？
*   **整体含义**：该研究填补了理想化理论与实际优化动力学之间的空白，揭示了 Transformer 在处理长序列检索时，存储容量与学习效率之间存在的根本权衡。

### 2. 论文提出的方法论
*   **核心思想**：分析一个包含“注意力层 + MLP块”的单层 Transformer，在有限样本 $N$ 上通过梯度下降学习令牌检索任务。研究重点关注梯度下降的“早期阶段”（Early Phase），即通过少数几步梯度更新来捕捉特征学习的本质。
*   **关键技术细节**：
    *   **任务设置**：模型需从长度为 $L$ 的序列中识别出一个“信息令牌”，并将其映射到正确的标签（置换矩阵 $\Pi^*$）。
    *   **架构模型**：对比了“仅注意力（Attention-only）”和“注意力+MLP（Attention-MLP）”两种结构。MLP 允许在较小的嵌入维度 $d$ 下通过增加宽度 $m$ 来维持容量。
    *   **三步梯度算法**：
        1.  更新值矩阵 $V$ 以建立初步关联。
        2.  更新键值矩阵 $W_{KQ}$ 以学习注意力机制（对齐触发向量 $z_{trig}$）。
        3.  再次更新 $V$ 以细化召回映射。
*   **理论公式**：提出学习成功的充分条件，即信号（Signal）必须大于噪声项（梯度噪声、均值偏差、MLP噪声）。核心缩放关系显示成功学习取决于 $(V, N, d, L, m)$ 的乘法依赖。

### 3. 实验设计
*   **数据集/场景**：使用合成的事实召回数据集。输入序列由随机令牌组成，其中一个位置包含与标签对应的信息令牌。
*   **Benchmark 与对比**：
    *   **基准**：以理论推导的存储容量极限（如 $md \gtrsim V$）为基准。
    *   **对比维度**：对比了不同序列长度 $L$、样本量 $N$、嵌入维度 $d$ 和 MLP 宽度 $m$ 下的学习准确率。
    *   **优化器对比**：除了理论分析的三步 GD，还使用了 Adam 优化器进行多轮（Epoch）训练，以验证理论在长期训练下的鲁棒性。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：作者提到代码已开源，实验涉及对不同超参数组合（$V, d, L$ 等）的大规模网格搜索，以绘制相图（Phase Diagram），这暗示了需要一定的中等规模计算资源来完成大量的重复实验验证。

### 5. 实验数量与充分性
*   **实验数量**：论文展示了多张热力图（Heatmaps），涵盖了从 $L=V^{0.25}$ 到 $L=V$ 的多种序列长度，以及 $N=V \log V$ 到 $N=V^{1.5}$ 的不同样本规模。
*   **充分性与客观性**：
    *   实验不仅验证了三步 GD 的理论预测，还通过 Adam 优化器的多轮训练验证了结论的普适性。
    *   引入了统计下界（Lower Bound）分析，从信息论角度证明了乘法缩放关系的必然性。
    *   实验设计考虑了消融（如 Attention-only vs Attention-MLP），结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **乘法依赖性**：学习成功所需的参数量与序列长度 $L$ 呈正相关，与样本量 $N$ 和嵌入维度 $d$ 呈负相关。
*   **容量权衡**：在长序列（大 $L$）下，为了实现成功学习，必须牺牲存储容量（增加 $d$ 使其更接近正交）或显著增加样本量 $N$。
*   **噪声主导区间**：识别了三个不同的失败区域：由均值偏差（Mean Bias）主导、由梯度噪声（Gradient Noise）主导和由 MLP 噪声主导的区域。
*   **训练演进**：虽然早期阶段缩放比例较差，但随着训练轮数增加，模型能够逐渐逼近最优存储容量。

### 7. 优点
*   **理论深度**：超越了简单的“正交假设”，提供了更符合实际的非正交嵌入分析。
*   **细粒度刻画**：给出了关于 $V, N, d, L, m$ 五个变量之间相互作用的显式解析式。
*   **理论与实践结合**：理论推导的相图与数值实验高度吻合，且在 Adam 优化器上也得到了定性验证。

### 8. 不足与局限
*   **模型简化**：主要分析的是单层 Transformer，对于多层堆叠产生的深度特征学习动力学覆盖不足。
*   **任务单一**：仅关注简单的事实召回（一比一映射），未涉及更复杂的推理或多对多关联任务。
*   **激活函数限制**：理论分析对 MLP 的激活函数（如多项式激活）有特定技术假设，可能不完全涵盖所有实际使用的激活函数（如 ReLU 的某些特性）。
*   **均值偏差敏感**：研究发现均值偏差是主要瓶颈之一，但在实际模型中，层归一化（LayerNorm）可能会缓解此问题，论文对此讨论较少。

（完）
