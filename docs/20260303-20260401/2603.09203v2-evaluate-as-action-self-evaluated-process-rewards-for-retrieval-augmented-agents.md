---
title: "Evaluate-as-Action: Self-Evaluated Process Rewards for Retrieval-Augmented Agents"
title_zh: Evaluate-as-Action：检索增强智能体的自评估过程奖励
authors: "Jiangming Shu, Yuxiang Zhang, Ye Ma, Xueyuan Lin, Jitao Sang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09203v2"
tags: ["query:rag"]
score: 9.0
evidence: 检索增强智能体的自评估过程奖励
tldr: 针对检索增强智能体在多步推理中因噪声检索和奖励信号粗糙导致的可靠性问题，本文提出 EvalAct 框架。该方法将检索质量评估转化为显式动作，通过“搜索-评估”协议生成过程信号。配合基于 GRPO 的 PCAR 优化方法，根据评估分数重缩放优势函数，显著提升了多跳问答任务的性能和可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.09203v2/fig-001.webp\", \"caption\": \"Figure 3: Training curves and ablation overview. (a) Training curves of EVALACT with 3B/7B backbones. (b) Model ablation across training variants. (c) Method ablation on removing the evaluation loop or PCAR. (d) Sensitivity to PCAR rescaling intensity.\", \"page\": 8, \"index\": 1, \"width\": 919, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2603.09203v2/fig-002.webp\", \"caption\": \"Table 1: Main results (EM, %) on seven open-domain QA benchmarks. Bold and underlined values indicate the best and second-best performance, respectively.\", \"page\": 6, \"index\": 2, \"width\": 944, \"height\": 539}, {\"url\": \"assets/figures/arxiv/2603.09203v2/fig-003.webp\", \"caption\": \"Figure 2: Effect of SFT on Format Alignment. The Base model exhibits high tool parsing failure rates.\", \"page\": 6, \"index\": 3, \"width\": 443, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2603.09203v2/fig-004.webp\", \"caption\": \"Figure 1: Overview of EVALACT with PCAR. The agent follows a coupled Search→Evaluate protocol, producing segment-wise self-evaluation scores {zi,k} that PCAR uses to rescale GRPO advantages.\", \"page\": 4, \"index\": 4, \"width\": 935, \"height\": 315}]"
motivation: 现有的检索增强智能体在多步推理中易受噪声干扰，且传统的仅基于结果的强化学习难以提供细粒度的中间步骤反馈。
method: 提出 EvalAct 框架，将检索评估设为显式动作，并引入 PCAR 优化方法，利用过程评估分数对优势函数进行分段校准。
result: 在七个开放域问答基准测试中，EvalAct 取得了最佳平均准确率，尤其在多跳问答任务上表现出显著的性能增益。
conclusion: 通过将检索评估显式化并结合过程奖励优化，可以有效提升检索增强智能体在复杂推理场景下的决策质量。
---

## 摘要
检索增强智能体可以查询外部证据，但其在多步推理中的可靠性仍然有限：噪声检索可能会干扰多跳问答，而仅基于结果的强化学习提供的信用分配信号过于粗糙，难以优化中间步骤。我们提出了 EvalAct (Evaluate-as-Action)，它将隐式的检索质量评估转化为显式的动作，并强制执行一种耦合的“搜索-评估”协议，使得每次检索后立即跟随一个结构化的评估分数，从而产生与交互轨迹对齐的过程信号。为了利用这些信号，我们引入了过程校准优势重缩放 (PCAR)，这是一种基于 GRPO 的优化方法，它根据评估分数在片段级别重缩放优势值，在强调可靠片段的同时保守地更新不确定片段。在七个开放域问答基准测试上的实验表明，EvalAct 实现了最佳的平均准确率，在多跳任务上的提升最为显著；消融实验证实，显式评估循环是性能提升的主要驱动力，而 PCAR 提供了持续的额外收益。

## Abstract
Retrieval-augmented agents can query external evidence, yet their reliability in multi-step reasoning remains limited: noisy retrieval may derail multi-hop question answering, while outcome-only reinforcement learning provides credit signals that are too coarse to optimize intermediate steps. We propose \textsc{EvalAct} (Evaluate-as-Action), which converts implicit retrieval quality assessment into an explicit action and enforces a coupled Search-to-Evaluate protocol so that each retrieval is immediately followed by a structured evaluation score, yielding process signals aligned with the interaction trajectory. To leverage these signals, we introduce Process-Calibrated Advantage Rescaling (PCAR), a GRPO-based optimization method that rescales advantages at the segment level according to evaluation scores, emphasizing reliable segments while updating uncertain ones conservatively. Experiments on seven open-domain QA benchmarks show that \textsc{EvalAct} achieves the best average accuracy, with the largest gains on multi-hop tasks, and ablations verify that the explicit evaluation loop drives the primary improvements while PCAR provides consistent additional benefits.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluate-as-Action: Self-Evaluated Process Rewards for Retrieval-Augmented Agents》的结构化深入分析总结：

### 1. 核心问题与研究动机
论文关注的是**检索增强智能体（RAG Agents）在多步推理中的可靠性问题**。
*   **研究背景**：虽然 LLM 智能体可以通过检索工具获取外部知识，但在处理复杂的多跳（Multi-hop）问题时，容易受到噪声检索结果的干扰，导致推理轨迹偏离。
*   **核心痛点**：
    1.  **错误传播**：缺乏显式的即时验证机制，一个无关文档可能毁掉整个推理链。
    2.  **信用分配粗糙**：现有的强化学习（如 GRPO、PPO）通常只根据最终答案的正确性给予奖励（Outcome-only Reward），无法区分长轨迹中哪些检索步骤是有用的，哪些是冗余或误导的。

### 2. 方法论：EvalAct 与 PCAR
论文提出了 **EvalAct (Evaluate-as-Action)** 框架，核心思想是将“评估”从隐式的内部思考转变为显式的、可学习的“动作”。

*   **Evaluate-as-Action 协议**：强制执行“搜索 $\rightarrow$ 评估”耦合协议。智能体在执行 `Search` 动作并获得文档后，必须立即执行 `Evaluate` 动作，输出一个结构化的文本评价和 0-10 的置信度分数 $z$。
*   **环境反馈循环**：环境根据分数 $z$ 返回预定义的指令（如：分数低则提示重构查询，分数高则提示基于证据回答），实现推理过程的动态控制。
*   **PCAR (过程校准优势重缩放)**：
    *   基于 GRPO 算法，利用 `Evaluate` 动作产生的置信度分数来调制优势函数（Advantage）。
    *   **公式逻辑**：计算轨迹内分数的标准化信号 $\tilde{z}_{i,k}$，然后根据该信号缩放片段级别的优势值 $\hat{A}_{i,t}$。
    *   **效果**：放大可靠、有进展步骤的梯度，同时对不确定或低质量的步骤进行保守更新，从而在没有人工标注过程奖励模型（PRM）的情况下实现过程监督。

### 3. 实验设计
*   **数据集**：涵盖 7 个开放域问答基准。
    *   **单跳任务**：Natural Questions (NQ), TriviaQA, PopQA。
    *   **多跳任务**：HotpotQA, 2WikiMultihopQA, MuSiQue, Bamboogle。
*   **对比基线**：
    *   基础方法：直接生成、朴素 RAG。
    *   推理增强方法：IRCoT。
    *   前沿智能体方法：Search-o1, Search-R1, AutoRefine。
*   **实验设置**：使用 Qwen2.5-3B/7B-Instruct 作为基座模型，统一使用 BM25 检索器和维基百科语料库。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   全参数微调（Full-parameter optimization）。
    *   训练阶段分为：2k 条协议对齐数据的 SFT 预热，以及 27k 条数据的 RL 阶段。
    *   RL 参数：2 个 Epoch，Global Batch Size 为 256，每个 Prompt 生成 5 个 Rollouts。
*   **时长**：文中未明确给出具体的训练总时长，但提到了使用了梯度检查点（Gradient Checkpointing）以节省显存。

### 5. 实验数量与充分性
*   **实验规模**：在 2 种模型规模（3B 和 7B）上进行了验证，跨越 7 个不同难度的任务。
*   **消融实验**：非常充分。
    *   验证了 SFT 对格式对齐的必要性。
    *   对比了“有无评估循环”和“有无 PCAR 优化”的效果。
    *   进行了超参数敏感性分析（如 PCAR 的缩放强度 $\lambda$）。
*   **客观性**：通过统一检索环境、语料库和搜索预算，确保了与基线方法在外部证据获取能力上的公平对比。

### 6. 主要结论与发现
*   **性能领先**：EvalAct 在所有基准测试中均取得了最佳平均 EM（Exact Match）分数，尤其在多跳任务上提升巨大（如在 2Wiki 上比 AutoRefine 高出约 10 个点）。
*   **结构胜过优化**：消融实验显示，**显式的评估循环（Eval Loop）**是性能提升的主因（贡献了约 7.5% 的增益），而 **PCAR 优化**在此基础上进一步提供了约 1.2% 的稳定提升。
*   **小模型逆袭**：经过 EvalAct 训练的 3B 模型在多跳任务上的表现甚至超过了许多未采用该框架的 7B 基线模型。

### 7. 优点与亮点
*   **创新视角**：将评估视为一种“动作”而非单纯的“思考”，巧妙地解决了过程奖励难以获取的问题，无需昂贵的人工 PRM 标注。
*   **即时纠错**：通过环境反馈机制，在推理早期就能识别并剪枝无效的检索分支，有效抑制了长程推理中的错误传播。
*   **自校准特性**：PCAR 机制自然地激励模型生成准确的自我评估，因为误导性的高分会导致优势函数失真，从而在 RL 迭代中受到惩罚。

### 8. 不足与局限
*   **推理开销**：由于每个检索步骤都必须跟随一个评估动作，推理时的 Token 消耗和延迟会显著增加。
*   **对 SFT 的依赖**：实验显示，如果没有初期的 SFT 引导，模型很难自发学会复杂的“搜索-评估”耦合协议，这增加了训练流水线的复杂性。
*   **任务局限**：目前主要集中在问答任务，对于更开放的智能体任务（如网页浏览、代码生成）的泛化性尚待验证。
*   **环境反馈简单**：目前的反馈模板是静态的，未来若能引入更具解释性的动态反馈，效果可能更好。

（完）
