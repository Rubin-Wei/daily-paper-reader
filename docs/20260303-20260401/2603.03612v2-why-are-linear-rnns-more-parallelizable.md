---
title: Why Are Linear RNNs More Parallelizable?
title_zh: 为什么线性 RNN 更易于并行化？
authors: "William Merrill, Hongjian Jiang, Yanhong Li, Anthony Lin, Ashish Sabharwal"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03612v2"
tags: ["query:pretrain"]
score: 9.0
evidence: 分析线性RNN作为Transformer的可并行替代方案
tldr: 本研究深入探讨了线性循环神经网络（LRNNs）相较于传统非线性RNN更易并行化的理论根源。通过将RNN类型与计算复杂性类建立联系，研究证明LRNNs可视为对数深度的算术电路，其并行效率接近Transformer，而非线性RNN则属于L-complete或P-complete类，存在本质的并行化障碍。此外，研究还揭示了不同LRNN变体在表达能力上的细微差异，为设计兼顾表达力与并行性的LLM架构提供了理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.03612v2/fig-001.webp\", \"caption\": \"Figure 1. Main results, summarized as a hierarchy of increasingly expressive RNN classes with popular models within each class. Each RNN shown is “tight” for the respective complexity class C (PNC1, L, etc.) in the sense that both falls in C and can solve a C-complete problem. LRNNs are in PNC1, implying they can be nearly as efficiently parallelized as transformers, incurring only a small O(log∗(n)) depth overhead in terms of bounded fan-in boolean circuits. Nonlinear RNNs, in contrast, can solve L-complete and even P-complete problems, but this comes at the cost of being less parallelizable, requiring notably deeper circuits. Bottom row lists the classes of automata that each RNN model class can simulate, where WFA stands for weighted finite automaton and DWFA stands for deterministic WFA.\", \"page\": 2, \"index\": 1, \"width\": 981, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.03612v2/fig-002.webp\", \"caption\": \"Table 1. Training hyperparameters for deterministic graph connectivity (DGC) and iterated matrix multiplication (IMM). dm, L, dh, h, dstate, and LR denote the model dimension, number of layers, hidden size, number of attention heads, SSM state expansion factor, and learning rate, respectively. All models are trained with AdamW, batch size 128, and gradient clipping at 1.0.\", \"page\": 30, \"index\": 2, \"width\": 541, \"height\": 279}]"
motivation: 旨在从理论层面解释为何线性RNN能像Transformer一样高效并行，而传统非线性RNN却难以实现并行化。
method: 采用计算复杂性理论，将不同类型的RNN映射到标准复杂性类，并结合自动机理论分析其模拟能力。
result: 证明了LRNNs属于对数深度电路类，而非线性RNN具有更高的复杂性等级，且不同LRNN变体在表达能力上存在细微差异。
conclusion: 揭示了非线性RNN与各类LRNN在表达能力与并行性之间的基本权衡，为优化大模型架构设计提供了理论指导。
---

## 摘要
社区正越来越多地探索将线性循环神经网络（LRNNs）作为语言模型，其动机在于它们的表达能力和可并行性。虽然先前的研究确立了 LRNNs 相比于 Transformer 的表达能力优势，但目前尚不清楚是什么使得 LRNNs（而非传统的非线性 RNNs）在实践中能像 Transformer 一样易于并行化。我们通过在不同类型的 RNN 与标准复杂度类之间建立紧密联系来回答这个问题。我们证明了 LRNNs 可以被视为对数深度（有界入度）的算术电路，与 Transformer 所允许的对数深度布尔电路相比，这仅代表了轻微的深度开销。此外，我们证明了非线性 RNNs 可以解决 $\mathsf{L}$-完全问题（在多项式精度下甚至是 $\mathsf{P}$-完全问题），揭示了在并行化效率上使其达到 Transformer 水平的根本障碍。我们的理论还识别了近期流行的 LRNN 变体之间细粒度的表达能力差异：置换对角 LRNNs 是 $\mathsf{NC}^1$-完全的，而对角加低秩 LRNNs 的表达能力更强（$\mathsf{PNC}^1$-完全）。我们通过将每种类型的 RNN 与其能够模拟的相应自动机理论模型相关联，提供了进一步的见解。总之，我们的结果揭示了非线性 RNNs 与不同 LRNN 变体之间的根本权衡，为设计在表达能力和并行性之间达到最佳平衡的大语言模型（LLM）架构奠定了基础。

## Abstract
The community is increasingly exploring linear RNNs (LRNNs) as language models, motivated by their expressive power and parallelizability. While prior work establishes the expressivity benefits of LRNNs over transformers, it is unclear what makes LRNNs -- but not traditional, nonlinear RNNs -- as easy to parallelize in practice as transformers. We answer this question by providing a tight connection between types of RNNs and standard complexity classes. We show that LRNNs can be viewed as log-depth (bounded fan-in) arithmetic circuits, which represents only a slight depth overhead relative to log-depth boolean circuits that transformers admit. Furthermore, we show that nonlinear RNNs can solve $\mathsf{L}$-complete problems (and even $\mathsf{P}$-complete ones, under polynomial precision), revealing a fundamental barrier to parallelizing them as efficiently as transformers. Our theory also identifies fine-grained expressivity differences between recent popular LRNN variants: permutation-diagonal LRNNs are $\mathsf{NC}^1$-complete whereas diagonal-plus-low-rank LRNNs are more expressive ($\mathsf{PNC}^1$-complete). We provide further insight by associating each type of RNN with a corresponding automata-theoretic model that it can simulate. Together, our results reveal fundamental tradeoffs between nonlinear RNNs and different variants of LRNNs, providing a foundation for designing LLM architectures that achieve an optimal balance between expressivity and parallelism.

---

## 论文详细总结（自动生成）

以下是对论文《Why Are Linear RNNs More Parallelizable?》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在大型语言模型（LLM）架构设计中，**并行性（Parallelism）**和**表达能力（Expressivity）**之间存在权衡。线性 RNN（LRNN，如 Mamba、RWKV-7、DeltaNet）因其在长序列处理上的高效并行能力而备受关注，但理论界一直缺乏对以下问题的明确解释：
    1.  为什么 LRNN 可以像 Transformer 一样高效并行，而传统的非线性 RNN（如 LSTM、RNN）却不行？
    2.  不同变体的 LRNN（如对角矩阵、低秩更新等）在表达能力上究竟有何细粒度差异？
*   **核心问题**：通过计算复杂性理论，界定不同 RNN 架构的并行化极限和表达能力层级。

### 2. 论文提出的方法论
*   **核心思想**：将 RNN 的计算过程映射到标准的**电路复杂性类（Circuit Complexity Classes）**。
*   **关键技术细节**：
    *   **LRNN 的并行化界限**：证明了所有基于有理数域 $\mathbb{Q}$ 的 LRNN 均属于 $\mathsf{PNC}^1$ 复杂性类。这意味着它们可以被深度为 $O(\log n \cdot \log^* n)$ 的有界入度布尔电路模拟。这种深度仅比 Transformer（属于 $\mathsf{TC}^0 \subseteq \mathsf{NC}^1$）略高，在实际硬件上几乎可以实现同等的并行效率。
    *   **非线性 RNN 的并行化障碍**：证明了非线性 RNN 在对数精度下是 $\mathsf{L}$-complete（对数空间完全），在多项式精度下是 $\mathsf{P}$-complete（多项式时间完全）。根据复杂性理论假设（$\mathsf{NC} \neq \mathsf{P}$），这意味着非线性 RNN 本质上是串行的，无法被压缩至多项式对数深度电路中。
    *   **细粒度 LRNN 分类**：
        *   **DPLR LRNN**（对角加低秩，如 DeltaNet, RWKV-7）：证明其为 $\mathsf{PNC}^1$-complete，能够模拟加权有限自动机（WFA）。
        *   **PD LRNN**（置换对角）：证明其仅属于 $\mathsf{NC}^1$，表达能力弱于 DPLR 变体。
*   **自动机联系**：将不同 RNN 关联到自动机模型（如计数器机、WFA、确定性 WFA），提供了直观的计算模型解释。

### 3. 实验设计
*   **数据集与场景**：采用合成任务验证理论预测。
    1.  **排序确定性图连通性（Sorted Deterministic Graph Connectivity）**：这是一个 $\mathsf{L}$-complete 问题，用于测试模型处理本质串行逻辑的能力。
    2.  **迭代 $3 \times 3$ 矩阵乘法**：这是一个 $\mathsf{PNC}^1$-complete 问题，分为有模数（有限域）和无模数（整数域）两种情况，用于测试模型的代数推理能力。
*   **对比方法**：非线性 RNN、RWKV-7、DeltaNet、Mamba、Transformer。
*   **Benchmark 逻辑**：重点考察**长度泛化（Length Generalization）**。模型在长度为 1-100 的序列上训练，在 101-200 和 201-300 的序列上测试。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到了使用 AdamW 优化器，学习率设为 $1 \times 10^{-4}$ 或 $3 \times 10^{-4}$，Batch Size 为 64 或 128，梯度裁剪阈值为 1.0，最大训练步数为 60,000 步。

### 5. 实验数量与充分性
*   **实验组数**：针对 3 个核心合成任务进行了对比实验，每个任务涵盖了 5 种主流架构。
*   **充分性与客观性**：实验设计非常具有针对性，直接对应论文提出的复杂性类结论。通过长度泛化测试，排除了模型仅靠“记忆”短序列模式的可能性。虽然实验仅限于合成任务，但对于验证**计算极限**这一理论命题而言是充分且公平的。

### 6. 主要结论与发现
1.  **并行性鸿沟**：非线性 RNN 能够解决比 LRNN 更难的问题（如 $\mathsf{L}$ 完全问题），但代价是无法像 LRNN 那样高效并行。
2.  **LRNN 的表达力上限**：DPLR 结构的 LRNN（如 RWKV-7 和 DeltaNet）在表达能力上达到了 LRNN 的理论上限（$\mathsf{PNC}^1$），显著强于早期的对角 LRNN 或 Mamba。
3.  **实验证实理论**：只有非线性 RNN 能在图连通性任务上实现完美的长度泛化；而 DPLR LRNN 在矩阵乘法任务上表现优异，Transformer 和 Mamba 则无法处理此类任务。

### 7. 优点
*   **理论深度**：首次为 LRNN 的并行化优势提供了严密的电路复杂性证明，填补了直觉与理论之间的空白。
*   **细粒度分类**：不仅区分了线性和非线性，还深入探讨了 LRNN 内部不同参数化方案（DPLR vs PD）的表达力差异。
*   **预测性**：理论结果成功预测了模型在特定算法任务上的表现，为未来架构设计提供了“路线图”。

### 8. 不足与局限
*   **实验场景局限**：所有实验均在合成任务上完成，缺乏在大规模自然语言数据集（如 Pile 或 SlimPajama）上的预训练验证。
*   **精度敏感性**：理论分析高度依赖于数值精度的假设（对数精度 vs 多项式精度），而在现代硬件（FP16/BF16）上的实际表现可能受舍入误差影响。
*   **架构简化**：分析主要集中在循环更新层，对于 LayerNorm、门控机制（Gating）以及多层堆叠产生的复杂交互分析尚显不足。

（完）
