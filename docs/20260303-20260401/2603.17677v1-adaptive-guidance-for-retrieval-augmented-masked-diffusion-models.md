---
title: Adaptive Guidance for Retrieval-Augmented Masked Diffusion Models
title_zh: 检索增强型掩码扩散模型的自适应引导
authors: "Jaemin Kim, Jong Chul Ye"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17677v1"
tags: ["query:rag"]
score: 10.0
evidence: 扩散语言模型的检索增强生成
tldr: 针对检索增强生成（RAG）中检索内容可能存在噪声或与模型知识冲突的问题，本文提出了ARAM框架。该框架专为掩码扩散模型（MDM）设计，无需额外训练，通过在去噪过程中根据信噪比动态调整引导尺度，有效平衡了检索信息与模型先验知识。实验证明，ARAM在多个知识密集型问答任务中显著提升了生成质量和准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.17677v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of ARAM. ARAM mitigates knowledge conflict via step-wise and token-wise adaptive guidance driven by the Signal-to-Noise Ratio.\", \"page\": 1, \"index\": 1, \"width\": 454, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.17677v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison across QA benchmarks. Bold indicates the best score within each backbone.\", \"page\": 6, \"index\": 2, \"width\": 846, \"height\": 612}, {\"url\": \"assets/figures/arxiv/2603.17677v1/fig-003.webp\", \"caption\": \"Figure 3: Dynamics of adaptive guidance. (a) Average guidance scale (λt) per step. ARAM maintains high guidance for gold documents but suppresses λt for noisy ones. (b) Token-wise guidance heatmap. Black squares mark unmasking positions, demonstrating a right-to-left generation tendency. ARAM assigns larger guidance to positions that align with the unmasking pattern.\", \"page\": 7, \"index\": 3, \"width\": 412, \"height\": 545}, {\"url\": \"assets/figures/arxiv/2603.17677v1/fig-004.webp\", \"caption\": \"Figure 5: Prompt template used for LLaDA.\", \"page\": 14, \"index\": 4, \"width\": 461, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2603.17677v1/fig-005.webp\", \"caption\": \"Figure 6: Few-shot prompt template used for the Dream and other AR backbones.\", \"page\": 14, \"index\": 5, \"width\": 458, \"height\": 1090}, {\"url\": \"assets/figures/arxiv/2603.17677v1/fig-006.webp\", \"caption\": \"Figure 4: Prompt template for LLM-based retrieval context quality annotation.\", \"page\": 13, \"index\": 6, \"width\": 458, \"height\": 637}]"
motivation: 解决检索增强扩散模型在面对噪声或冲突的检索上下文时，难以有效整合外部知识与模型内部先验的问题。
method: 提出一种无需训练的自适应引导框架ARAM，根据检索上下文引起的分布偏移信噪比，在去噪过程中动态校准引导尺度。
result: 在多个知识密集型问答基准测试中，ARAM的表现优于现有的竞争性检索增强生成基线模型。
conclusion: ARAM通过动态调整引导策略，为扩散模型在处理不确定检索信息时提供了一种高效且稳健的解决方案。
---

## 摘要
检索增强生成（RAG）通过将外部知识引入语言模型生成过程，提升了事实依据的准确性。然而，当检索到的上下文包含噪声、不可靠或与模型的参数化知识不一致时，会引入检索-先验冲突，从而降低生成质量。虽然这一问题在自回归语言模型中已得到研究，但在基于扩散的语言模型中仍鲜有探索，因为其迭代去噪过程为整合检索上下文带来了独特的挑战。在本研究中，我们提出了自适应检索增强掩码扩散（ARAM），这是一种针对 RAG 场景下掩码扩散模型（MDMs）的无需训练的自适应引导框架。ARAM 根据检索上下文引起的分布偏移的信噪比（SNR），在去噪过程中动态校准引导尺度。直观而言，当检索上下文提供可靠的修正证据时，模型会加强引导；而当上下文信号包含噪声或不支持生成时，则会抑制引导。在多个知识密集型问答（QA）基准测试上的广泛实验表明，ARAM 相比具有竞争力的 RAG 基线模型提升了整体问答性能。

## Abstract
Retrieval-Augmented Generation (RAG) improves factual grounding by incorporating external knowledge into language model generation. However, when retrieved context is noisy, unreliable, or inconsistent with the model's parametric knowledge, it introduces retrieval-prior conflicts that can degrade generation quality. While this problem has been studied in autoregressive language models, it remains largely unexplored in diffusion-based language models, where the iterative denoising process introduces unique challenges for integrating retrieved context. In this work, we propose Adaptive Retrieval-Augmented Masked Diffusion (ARAM), a training-free adaptive guidance framework for Masked Diffusion Models (MDMs) in RAG settings. ARAM dynamically calibrates the guidance scale during denoising according to the Signal-to-Noise Ratio (SNR) of the distributional shift induced by retrieved context. Intuitively, the model strengthens guidance when the retrieved context provides reliable corrective evidence and suppresses it when the contextual signal is noisy or non-supportive. Extensive experiments on multiple knowledge-intensive QA benchmarks show that ARAM improves overall QA performance over competitive RAG baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ARAM（Adaptive Retrieval-Augmented Masked Diffusion）** 的框架，旨在解决掩码扩散模型（MDM）在检索增强生成（RAG）任务中遇到的知识冲突问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 RAG 场景下，检索到的外部知识可能与模型内部的参数化知识发生冲突（如检索内容包含噪声、过时或错误信息）。
*   **研究背景**：虽然自回归（AR）模型的检索增强已有较多研究，但在基于扩散的语言模型（如 MDM）中，如何有效整合检索上下文仍是一个挑战。
*   **动机**：传统的分类器自由引导（CFG）在 MDM 中通常使用固定的引导尺度（Static Guidance）。这会导致两个极端：对噪声过度引导（导致幻觉）或对有效修正引导不足（无法纠正错误的先验知识）。

### 2. 方法论：ARAM 框架
*   **核心思想**：通过信息论视角，将检索上下文的影响视为从“无检索先验”到“有检索后验”的分布偏移。ARAM 根据这种偏移的**信噪比（SNR）**，在去噪的每一步和每个 Token 位置动态调整引导尺度 $\lambda$。
*   **关键技术细节**：
    *   **信号（Signal）**：定义为条件分布与先验分布之间的对称 KL 散度，衡量检索内容改变模型信念的强度。
    *   **噪声（Noise）**：采用条件熵 $H(p_{cond})$ 作为噪声代理。当检索内容引入歧义或矛盾时，熵值升高，表明不确定性增加。
    *   **自适应公式**：$\lambda_t = \lambda_{max} \cdot \tanh(\beta \cdot \frac{Signal}{Noise + \epsilon})$。通过 $\tanh$ 函数保证数值稳定性，并利用 $\beta$ 调节对信号的敏感度。
*   **算法流程**：在去噪的每一个 Step，模型同时计算有上下文和无上下文的 Logits，计算当前 Token 位置的 SNR，动态生成 $\lambda_t$，最后通过线性插值合成引导后的 Logits 进行采样。

### 3. 实验设计
*   **数据集**：涵盖了五种知识密集型问答（QA）基准：
    *   开放域问答：NQ, TriviaQA, MARCO QA。
    *   多跳推理：HotpotQA。
    *   槽位填充：T-REx。
*   **基准模型（Backbones）**：
    *   掩码扩散模型：LLaDA-8B, Dream-7B, LLaDA 1.5。
    *   自回归模型（对比用）：LLaMA-3.1-8B, Qwen2.5-7B。
*   **对比方法**：
    *   标准 RAG（固定 $\lambda=1$）。
    *   自回归适配方法：CAD, AdaCAD, COIECD。
    *   扩散模型专用方法：SPREAD, A-CFG。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **推理开销**：作者在附录中提到了计算成本分析。由于 ARAM 是**无需训练（Training-free）**的方案，其主要开销在于推理阶段。ARAM 需要两次前向传播（有条件和无条件），导致推理延迟（NFE）大约是标准 RAG 的两倍，且计算熵和 SNR 会带来微小的额外开销。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流数据集上进行了测试，每个数据集随机采样 1000 个样本。
*   **消融实验**：非常充分。包括对超参数（$\lambda_{max}, \beta$）的敏感度分析、不同噪声代理（方差 vs 熵）的对比、稳定性函数（Raw SNR vs Tanh）的验证，以及在不同模型架构（LLaDA 1.5）上的泛化性测试。
*   **客观性**：通过将检索结果分类为“黄金文档”、“无答案相关文档”和“无关文档”进行分层分析，客观地展示了 ARAM 在处理不同质量检索内容时的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：ARAM 在所有 QA 基准测试中的准确率（EM）和 F1 分数均优于静态 CFG 和其他竞争基线。
*   **冲突缓解**：ARAM 有效增加了“正向修正”（原本错、检索后对）的比例，同时降低了“负向干扰”（原本对、检索后错）的比例。
*   **动态特性**：可视化结果显示，ARAM 在面对高质量文档时保持高引导，而在面对噪声文档时会自动抑制引导尺度。

### 7. 优点
*   **无需训练**：作为一种解码时（Decoding-time）策略，它可以直接应用于现有的预训练掩码扩散模型，部署成本低。
*   **理论支撑**：引导规则基于 Donsker-Varadhan 变分下界的二阶泰勒展开推导，具有较强的信息论解释性。
*   **细粒度控制**：实现了 Token 级别的自适应，能够捕捉生成过程中不同位置的不确定性差异。

### 8. 不足与局限
*   **推理延迟**：由于需要双倍的前向传播次数（2 NFE），在对延迟极度敏感的生产环境中可能存在应用限制。
*   **任务覆盖**：实验主要集中在英语知识密集型 QA 任务上，尚未验证其在长文本生成、创意写作或多语言场景下的表现。
*   **词表依赖**：计算 SNR 需要遍历整个词表的概率分布，对于超大词表的模型，计算开销会进一步增加。

（完）
