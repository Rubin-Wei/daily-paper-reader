---
title: Test-Time Strategies for More Efficient and Accurate Agentic RAG
title_zh: 更高效、更准确的智能体 RAG 的测试时策略
authors: "Brian Zhang, Deepti Guntur, Zhiyang Zuo, Abhinav Sharma, Shreyas Chaudhari, Wenlong Zhao, Franck Dernoncourt, Puneet Mathur, Ryan Rossi, Nedim Lipka"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12396v1"
tags: ["query:rag"]
score: 10.0
evidence: 针对智能体RAG和多跳问题的测试时修改策略
tldr: 针对Agentic RAG系统在处理复杂多跳问题时存在的重复检索和上下文整合不足等效率与准确性问题，本文提出了针对Search-R1框架的测试时优化策略。通过引入上下文增强模块和去重模块，有效提升了检索信息的利用率并减少了冗余检索轮次。实验表明，该方法在HotpotQA等数据集上显著提高了答案准确率，同时降低了推理成本，为构建更高效的智能体检索增强生成系统提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.12396v1/fig-001.webp\", \"caption\": \"Figure 1: An illustration of the information flow for our proposed test-time strategies compared to the baseline during a single inference turn i. Baseline: This represents the standard Search-R1 framework, where the LLM sends a query (qi) to the retriever and directly receives the retrieved documents (Di) to continue its reasoning. Deduplication: This approach filters out previously seen content and returns only a set of novel documents (D′ i) to the LLM. Contextualization: This approach parses the retrieved documents (Di) and reformulates their content to improve integration into the LLM’s reasoning process, returning an enhanced set of information (D∗ i ). Hybrid: This approach combines both modules sequentially.\", \"page\": 2, \"index\": 1, \"width\": 458, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2603.12396v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison of our proposed modules against the Search-R1 baselines on our 500-question evaluation set. We report Exact Match, LLM Match, and the average number of turns. Our modules are applied as test-time enhancements to the Qwen2.5-7b base (PPO) model. Cont: Contextualization (Section 3.1). De-Dup: De-duplication (Section 3.2). Hybrid (Section 3.3). Best results for our approaches are in bold.\", \"page\": 5, \"index\": 2, \"width\": 459, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2603.12396v1/fig-003.webp\", \"caption\": \"Figure 2: Illustration that questions requiring more agentic turns are inherently more difficult, as shown by the downward trend in Exact Match (EM) score for both the Search-R1 baseline and our Contextualization module. While the Contextualization module achieves a slightly higher mean EM at some points, the overlapping 95% confidence intervals indicate that we are not seeing a statistically significant improvement or difference between the two compared approaches for any given search count.\", \"page\": 5, \"index\": 3, \"width\": 458, \"height\": 296}]"
motivation: 现有的Agentic RAG系统在迭代检索中存在信息重复和上下文整合不力的问题，导致推理效率低下且准确性受限。
method: 本文在Search-R1基础上集成了上下文增强模块以优化文档整合，并引入去重模块以替换已检索的重复信息。
result: "实验显示，该方案在HotpotQA数据集上使精确匹配率提升了5.6%，并成功减少了10.5%的检索轮次。"
conclusion: 通过在测试时对检索流程进行精细化管理，可以显著提升Agentic RAG系统的推理效率和回答质量。
---

## 摘要
检索增强生成 (RAG) 系统在处理复杂的、多跳问题时面临挑战，而像 Search-R1 (Jin et al., 2025) 这样以迭代方式运行的智能体框架已被提出以应对这些复杂性。然而，此类方法可能会引入效率低下的问题，包括对先前已处理信息的重复检索，以及在当前生成提示词中有效情境化检索结果的挑战。这些问题可能导致不必要的检索轮次、次优的推理、不准确的答案以及增加的 token 消耗。在本文中，我们研究了对 Search-R1 流线的测试时修改，以缓解这些已识别的缺点。具体而言，我们探索了两个组件及其组合的集成：一个用于将检索文档中的相关信息更好地整合到推理中的情境化模块，以及一个用次相关的文档替换先前已检索文档的去重模块。我们使用 HotpotQA (Yang et al., 2018) 和 Natural Questions (Kwiatkowski et al., 2019) 数据集评估了我们的方法，报告了精确匹配 (EM) 分数、LLM-as-a-Judge 的答案正确性评估以及平均轮次。我们表现最好的变体（利用 GPT-4.1-mini 进行情境化）与 Search-R1 基准相比，EM 分数提高了 5.6%，轮次减少了 10.5%，证明了答案准确性和检索效率的提升。

## Abstract
Retrieval-Augmented Generation (RAG) systems face challenges with complex, multihop questions, and agentic frameworks such as Search-R1 (Jin et al., 2025), which operates iteratively, have been proposed to address these complexities. However, such approaches can introduce inefficiencies, including repetitive retrieval of previously processed information and challenges in contextualizing retrieved results effectively within the current generation prompt. Such issues can lead to unnecessary retrieval turns, suboptimal reasoning, inaccurate answers, and increased token consumption.   In this paper, we investigate test-time modifications to the Search-R1 pipeline to mitigate these identified shortcomings. Specifically, we explore the integration of two components and their combination: a contextualization module to better integrate relevant information from retrieved documents into reasoning, and a de-duplication module that replaces previously retrieved documents with the next most relevant ones. We evaluate our approaches using the HotpotQA (Yang et al., 2018) and the Natural Questions (Kwiatkowski et al., 2019) datasets, reporting the exact match (EM) score, an LLM-as-a-Judge assessment of answer correctness, and the average number of turns.   Our best-performing variant, utilizing GPT-4.1-mini for contextualization, achieves a 5.6% increase in EM score and reduces the number of turns by 10.5% compared to the Search-R1 baseline, demonstrating improved answer accuracy and retrieval efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Test-Time Strategies for More Efficient and Accurate Agentic RAG》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的智能体检索增强生成（Agentic RAG）系统（如 Search-R1）在处理复杂多跳问题时，存在**效率低下**和**准确性受限**的问题。
*   **研究动机**：作者通过定性分析发现 Search-R1 存在两大缺陷：
    1.  **信息遗忘（Information Forgetting）**：模型难以保留前几轮检索的信息，导致生成重复查询，增加 token 消耗和延迟。
    2.  **信息提取无效（Ineffective Extraction）**：模型难以从检索到的长文档中精准识别关键信息，导致推理路径偏离。
*   **整体含义**：本文旨在不改变模型权重的前提下，通过**测试时（Test-Time）策略**优化检索流线，提升系统的推理效率和答案质量。

### 2. 方法论：核心思想与技术细节
论文提出了三种测试时修改方案，旨在增强 Search-R1 的推理链：
*   **情境化模块（Contextualization）**：
    *   引入一个外部轻量级 LLM（如 GPT-4o-mini）作为“信息加工员”。
    *   在每一轮检索后，该模块从新文档中提取与用户问题相关的简洁信息，并维护一个**持久化内存缓存（Memory Cache）**。
    *   推理模型在后续步骤中同时访问新提取的信息和累积的缓存，从而缓解遗忘并简化输入。
*   **去重模块（De-duplication）**：
    *   维护一个已检索文档 ID 的集合。
    *   如果新一轮检索返回了已见过的文档，系统会自动将其剔除，并从检索器的排名列表中取下一个最高分的未见文档。
    *   核心思想是强制模型接触更多样化的信息，防止陷入重复检索的死循环。
*   **混合模式（Hybrid）**：
    *   顺序结合上述两个模块，既保证检索内容的多样性，又对这些内容进行精炼加工。

### 3. 实验设计
*   **数据集**：使用了 **HotpotQA**（多跳问答）和 **Natural Questions (NQ)**（开放域问答）的验证集。
*   **评估子集**：为了控制 API 调用成本，随机抽取了 500 个问答对进行评估。
*   **基准模型（Benchmark）**：
    *   主要对比基准是 **Search-R1 (Qwen2.5-7b-PPO)**。
    *   同时也参考了 Qwen2.5-3b 的 PPO 和 GRPO 版本。
*   **评估指标**：
    *   **Exact Match (EM)**：精确匹配率。
    *   **LLM Match**：使用 GPT-4o-mini 作为裁判，评估语义一致性（解决 EM 过于严格导致的假阴性问题）。
    *   **平均检索轮次（Avg. # searches）**：衡量效率的关键指标。

### 4. 资源与算力
*   **模型实现**：基于 Search-R1 的开源代码，使用 HuggingFace Transformers 库进行推理。
*   **外部 API**：情境化模块和 LLM-as-a-Judge 评估均调用了 OpenAI 的 **GPT-4o-mini**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或推理总时长。由于是测试时策略（无需训练），其主要开销在于推理时的 API 调用和本地模型的前向传播。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集的 500 个样本上进行了测试，涵盖了基准模型、情境化、去重、混合模式四种配置。
*   **充分性评价**：
    *   **优点**：提供了 95% 置信区间分析，并对比了 EM 与 LLM Match 的差异，实验设计较为科学。
    *   **局限**：500 个样本的规模相对较小；主要针对 Qwen2.5-7b 这一种架构进行了深度测试，缺乏在其他闭源或更大规模开源模型上的泛化验证。

### 6. 主要结论与发现
*   **情境化最有效**：Contextualization 变体表现最佳，**EM 提升了 5.6%**，同时**检索轮次减少了 10.5%**。这证明了精炼的外部记忆能显著提升推理效率。
*   **去重的副作用**：单纯的去重虽然提升了准确率，但**增加了检索轮次**（从 2.39 增加到 2.49）。原因是当模型拿不到重复信息时，会倾向于继续搜索以寻找“安全感”，即便新信息并不一定有用。
*   **难度相关性**：实验观察到 EM 分数随检索轮次增加而下降，说明需要多轮检索的问题本质上更难，目前的智能体策略在极深的多跳推理上仍有挑战。

### 7. 优点
*   **即插即用**：无需重新训练模型（Training-free），通过简单的逻辑修改即可提升现有 Agentic RAG 的性能。
*   **效率与准确双赢**：情境化模块成功实现了“更少的步骤，更好的答案”。
*   **评估客观**：引入 LLM Match 纠正了传统 EM 指标在数值、缩写等表达上的评估偏差。

### 8. 不足与局限
*   **引入额外延迟/成本**：虽然减少了检索轮次，但引入外部 LLM 进行情境化处理会产生额外的 API 调用延迟和费用。
*   **模型依赖性**：实验发现较小的模型（如 3b 版本）在遵循 Search-R1 的结构化输出格式上存在困难，这限制了该策略在低算力设备上的应用。
*   **数据集覆盖**：仅在 Wikipedia 域的数据集上测试，未验证在医疗、法律等专业垂直领域的表现。

（完）
