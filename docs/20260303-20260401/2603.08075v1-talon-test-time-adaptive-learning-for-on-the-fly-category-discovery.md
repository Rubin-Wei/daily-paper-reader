---
title: "TALON: Test-time Adaptive Learning for On-the-Fly Category Discovery"
title_zh: TALON：用于即时类别发现的测试时自适应学习
authors: "Yanan Wu, Yuhan Yan, Tailai Chen, Zhixiang Chi, ZiZhang Wu, Yi Jin, Yang Wang, Zhenbo Li"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08075v1"
tags: ["query:ttt"]
score: 9.0
evidence: 从未标记的在线流中进行测试时自适应学习
tldr: 本研究针对在线类别发现（OCD）中固定特征提取器导致的类别爆炸和信息丢失问题，提出了TALON框架。该框架通过测试时自适应技术，结合语义感知原型更新和稳定的编码器更新，使模型能从在线流数据中持续学习。此外，引入离线边际感知逻辑校准以优化嵌入空间。实验证明，TALON在提升新类别识别准确率的同时，有效缓解了类别碎片化问题，显著优于现有的哈希基准方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08075v1/fig-001.webp\", \"caption\": \"Table 1. Comparison with other SOTA methods. “DiffGRE+S” and “DiffGRE+P” represent the integration of DiffGRE with SMILE and PHE, respectively. The best results are highlighted in bold, while the second-best results are underlined. Blue numbers indicate results not reported in the original papers but reproduced using the official code.\", \"page\": 7, \"index\": 1, \"width\": 1026, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2603.08075v1/fig-002.webp\", \"caption\": \"Table 7. Comparison with State-of-the-Art Methods Using Different Backbones\", \"page\": 14, \"index\": 2, \"width\": 1026, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2603.08075v1/fig-003.webp\", \"caption\": \"Figure 4. Hyperparameter analysis on CUB-200-2011, Stanford Cars, and Oxford Pets datasets. Each column corresponds to one dataset, showing the effects of adaptation batch size, angular margin m, and similarity threshold τ on accuracy (All, Old, New) and the number of newly discovered categories (NDC).\", \"page\": 8, \"index\": 3, \"width\": 489, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2603.08075v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of the proposed TALON framework. (a) During the offline stage, we introduce margin-aware logit calibration to enlarge inter-class margins and enhance intra-class compactness, reserving embedding space for future category discovery. (b) At test-time, we jointly update the encoder and class prototypes, enabling the model to learn through discovery rather than static inference.\", \"page\": 4, \"index\": 4, \"width\": 874, \"height\": 437}]"
motivation: 现有的OCD方法因使用固定的特征提取器和哈希量化，导致特征表达力不足且容易产生类别爆炸现象。
method: 提出一种包含语义感知原型动态更新、稳定测试时编码器优化以及离线边际感知逻辑校准的自适应学习框架。
result: 在多个标准基准测试中，该方法显著提升了新类别的分类性能，并有效抑制了单一类别被误分为多个伪类别的现象。
conclusion: TALON证明了通过测试时自适应动态扩展知识库，可以更有效地解决在线流式数据中的新类别发现挑战。
---

## 摘要
即时类别发现 (OCD) 旨在利用仅在有标签数据上训练的模型，从无标签的在线流中识别已知类别，同时发现新类别。现有方法通常冻结离线训练的特征提取器，并采用基于哈希的框架，将特征量化为二进制代码作为类原型。然而，使用固定的知识库来发现新类别是不直观的，因为这完全忽略了输入数据的学习潜力。此外，特征量化会引入信息损失，降低表示能力，并放大类内方差。这通常会导致类别爆炸，即单个类别被分割成多个伪类别。为了克服这些局限性，我们提出了一种测试时自适应框架，实现了“通过发现进行学习”。它包含两种互补策略：语义感知原型更新和稳定的测试时编码器更新。前者动态细化类原型以增强分类效果，而后者将新信息直接整合到参数空间中。这些组件共同作用，使模型能够利用新遇到的样本不断扩展其知识库。此外，我们在离线阶段引入了边际感知 Logit 校准，以扩大类间间距并提高类内紧凑性，从而为未来的类别发现预留嵌入空间。在标准 OCD 基准测试上的实验表明，我们的方法显著优于现有的基于哈希的最先进方法，在新类别准确率方面取得了显著提升，并有效缓解了类别爆炸问题。代码已在 https://github.com/ynanwu/TALON 公开发布。

## Abstract
On-the-fly category discovery (OCD) aims to recognize known categories while simultaneously discovering novel ones from an unlabeled online stream, using a model trained only on labeled data. Existing approaches freeze the feature extractor trained offline and employ a hash-based framework that quantizes features into binary codes as class prototypes. However, discovering novel categories with a fixed knowledge base is counterintuitive, as the learning potential of incoming data is entirely neglected. In addition, feature quantization introduces information loss, diminishes representational expressiveness, and amplifies intra-class variance. It often results in category explosion, where a single class is fragmented into multiple pseudo-classes. To overcome these limitations, we propose a test-time adaptation framework that enables learning through discovery. It incorporates two complementary strategies: a semantic-aware prototype update and a stable test-time encoder update. The former dynamically refines class prototypes to enhance classification, whereas the latter integrates new information directly into the parameter space. Together, these components allow the model to continuously expand its knowledge base with newly encountered samples. Furthermore, we introduce a margin-aware logit calibration in the offline stage to enlarge inter-class margins and improve intra-class compactness, thereby reserving embedding space for future class discovery. Experiments on standard OCD benchmarks demonstrate that our method substantially outperforms existing hash-based state-of-the-art approaches, yielding notable improvements in novel-class accuracy and effectively mitigating category explosion. The code is publicly available at \textcolor{blue}{https://github.com/ynanwu/TALON}.

---

## 论文详细总结（自动生成）

### 论文分析报告：TALON —— 用于即时类别发现的测试时自适应学习

#### 1. 核心问题与整体含义（研究动机和背景）
即时类别发现（On-the-Fly Category Discovery, OCD）要求模型在仅有已知类标签的情况下，从连续的未标记在线流中识别已知类并发现新类。
*   **研究动机**：现有方法（如 SMILE, PHE）通常冻结离线训练的特征提取器，并使用基于哈希的量化技术。这种做法存在两个致命缺陷：
    1.  **学习潜力被忽视**：固定编码器无法从在线流入的新数据中吸收知识。
    2.  **类别爆炸（Category Explosion）**：哈希量化导致信息丢失，且对类内方差敏感，常将一个真实类别错误地拆分为多个伪类别。
*   **核心目标**：提出一个能够“在发现中学习”的框架，通过测试时自适应（TTA）动态更新模型，并消除哈希量化的负面影响。

#### 2. 方法论：核心思想与技术细节
TALON 抛弃了哈希框架，直接在连续特征空间操作，其核心由三个部分组成：
*   **离线阶段：边际感知 Logit 校准 (MLC)**
    *   在有标签数据训练时，引入加性角度边际 $m$。通过强制拉大类间距离并压缩类内分布，为未来可能出现的新类别在嵌入空间中“预留”位置。
*   **在线阶段：语义感知原型更新 (TTA-P)**
    *   模型维护一个动态原型库。对于新样本，通过置信度控制的指数移动平均（EMA）更新对应类的原型。
    *   更新步长由样本数量和分配置信度共同决定，确保原型演化在面对离群点时具有鲁棒性。
*   **在线阶段：稳定测试时编码器更新 (TTA-M)**
    *   模型定期收集一小批在线样本，通过三个损失函数微调编码器参数：
        1.  **熵最小化损失**：增强预测的确定性。
        2.  **原型对齐损失**：确保特征中心与存储的原型一致。
        3.  **原型分离损失**：防止不同类别的中心发生坍缩。

#### 3. 实验设计
*   **数据集**：涵盖了 7 个标准基准，包括粗粒度（CIFAR10, CIFAR100, ImageNet-100）和细粒度（CUB-200, Stanford Cars, Oxford Pets, Food-101）场景。
*   **Benchmark 与评价指标**：采用 Greedy-Hungarian 和 Strict-Hungarian 协议，重点评估“全类（All）”、“已知类（Old）”和“新类（New）”的准确率。
*   **对比方法**：对比了包括 SMILE、PHE、DiffGRE 等最先进的 OCD 方法，以及 RankStat、WTA 等传统聚类/哈希方法。

#### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA RTX 3090 GPU (24GB)** 上完成。
*   **训练细节**：离线训练阶段为 100 个 Epoch，使用 AdamW 优化器。
*   **推理开销**：由于引入了 TTA（测试时更新），测试阶段的时间占比最高。例如在 ImageNet-100 上，测试耗时约为训练/原型计算耗时的 3 倍左右。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个数据集上进行了详尽的对比实验，并针对 DINO 和 CLIP 两种不同的预训练 Backbone 进行了验证。
*   **消融实验**：系统验证了 MLC、TTA-P 和 TTA-M 三个组件的增量贡献。
*   **超参数分析**：对自适应批大小、角度边际 $m$、相似度阈值 $\tau$、Logit 缩放 $s$ 等关键参数进行了敏感性分析。
*   **客观性**：通过估计类别数量（#Cls）的对比，直观展示了该方法在抑制“类别爆炸”方面的优越性，实验设计全面且对比公平。

#### 6. 主要结论与发现
*   **性能领先**：TALON 在所有数据集上均显著优于现有的哈希方法，尤其是在新类（New）准确率上提升巨大（如在 ImageNet-100 上新类准确率从 30% 左右提升至 60% 以上）。
*   **有效抑制类别爆炸**：相比哈希方法动辄估计出数千个类别，TALON 估计的类别数更接近真实值。
*   **TTA 的必要性**：实验证明，仅靠离线训练是不够的，测试时的参数微调对于处理语义偏移至关重要。

#### 7. 优点（亮点）
*   **范式创新**：首次将测试时自适应（TTA）引入 OCD 任务，打破了“固定编码器”的思维定式。
*   **哈希自由**：证明了在 OCD 任务中，连续特征空间比哈希空间更具表达力和稳定性。
*   **前瞻性设计**：离线阶段的 MLC 模块通过几何约束为未知类预留空间的思路非常巧妙。

#### 8. 不足与局限
*   **计算成本**：在线更新编码器参数虽然提升了精度，但也显著增加了推理时的计算延迟和内存占用，可能限制其在极低功耗嵌入式设备上的应用。
*   **Backbone 依赖**：性能高度依赖于高质量的预训练模型（如 CLIP 或 DINOv2），在缺乏强先验知识的特定领域可能表现受限。
*   **长程漂移风险**：虽然论文提到了稳定性，但在极长时间、类别极多的超长流数据中，模型是否会发生灾难性遗忘或原型漂移仍需进一步验证。

（完）
