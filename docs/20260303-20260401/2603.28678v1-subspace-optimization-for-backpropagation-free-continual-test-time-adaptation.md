---
title: Subspace Optimization for Backpropagation-Free Continual Test-Time Adaptation
title_zh: 无反向传播持续测试时自适应的子空间优化
authors: "Damian Sójka, Sebastian Cygert, Marc Masana"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28678v1"
tags: ["query:ttt"]
score: 10.0
evidence: 持续测试时适配系统
tldr: "针对持续测试时自适应（TTA）中无反向传播优化效率与性能难以平衡的问题，本文提出PACE系统。该系统通过Fastfood投影将高维归一化层参数映射到低维子空间，并利用CMA-ES进化策略进行直接优化。此外，引入自适应停止准则和领域专用向量库以减少冗余计算。PACE在多个基准测试中达到SOTA精度，且运行时间比现有无反向传播方法缩短50%以上。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28678v1/fig-001.webp\", \"caption\": \"Table 1. Accuracy on ImageNet-C (IN-C), ImageNet-R (IN-R) and DomainNet-126 (DN-126) with ViT-B and DeiT-B. Bold indicates best result, underlined second best.\", \"page\": 11, \"index\": 1, \"width\": 713, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-002.webp\", \"caption\": \"Fig. 1. Accuracy versus runtime trade-off on the ImageNet-C benchmark using a ViT-B model, across various adaptation stopping thresholds ϵ (star marks the default setting ϵ=0.045). The horizontal dotted line represents the NoAdapt baseline accuracy. Existing BP-free methods typically face a trade-off: they either achieve high accuracy at the expense of computational efficiency or reduce runtime by sacrificing precision. Our approach not only outperforms current baselines but also introduces a tunable mechanism to balance the accuracy-runtime trade-off, preventing the inefficient use of resources for diminishing returns.\", \"page\": 2, \"index\": 2, \"width\": 713, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-003.webp\", \"caption\": \"Table 5. Comparisons with state-of-the-art methods on ImageNet-C with ViT-B in long-term continual adaptation. We report average accuracy at each round of continual adaptation. Bold indicates best result, underlined second.\", \"page\": 14, \"index\": 3, \"width\": 602, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-004.webp\", \"caption\": \"Fig. 6. Smoothed per-batch accuracy for the repeated Glass Blur domain from ImageNet-C with ViT-B model. The throughput was measured on a Jetson Xavier NX. The gray vertical lines indicate the start of each repetition. The throughput from ZOA (fp=28) is covered by FOA.\", \"page\": 14, \"index\": 4, \"width\": 709, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-005.webp\", \"caption\": \"Table 4. Ablation study on each component of PACE on ImageNet-C with ViT-B.\", \"page\": 13, \"index\": 5, \"width\": 583, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-006.webp\", \"caption\": \"Table 3. Computation complexity comparison on ImageNet-C with ViT-B. Forward and backward passes (#FP/ #BP ) are counted for processing a single sample. The wall-clock time (hours) and memory usage (MB) are measured for processing the whole ImageNet-C on a single RTX 4090 GPU. Adapted batches are indicated by the percentage on which adaptation was performed.\", \"page\": 13, \"index\": 6, \"width\": 716, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-007.webp\", \"caption\": \"Table 2. Corruption Accuracy (%) on ImageNet-C with Quantized ViT-B models.\", \"page\": 12, \"index\": 7, \"width\": 716, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-008.webp\", \"caption\": \"Fig. 5. Diagram of PACE. 1) Subspace Adaptation: we adapt the model by adding a high-dimensional random projection of a small, learnable vector to the model’s normalization layer weights. We use the CMA-ES strategy to iteratively evolve a population of these vectors, selecting the one that minimizes the loss on current test samples. 2) Adaptation Stopping: for efficiency, we stop the adaptation when the mean of the distribution optimized by CMA-ES is lower than a threshold. Along with the DomainSpecialized Vector Bank, they make an effective and efficient TTA system.\", \"page\": 9, \"index\": 8, \"width\": 716, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-009.webp\", \"caption\": \"Table A.2. Comparisons with SOTA methods on ImageNet-C with ViT-B regarding Accuracy (%). BP is short for backward propagation.\", \"page\": 20, \"index\": 9, \"width\": 716, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-010.webp\", \"caption\": \"Table A.1. Accuracy (%) of PACE on ImageNet-C with a ViT-B model.\", \"page\": 20, \"index\": 10, \"width\": 703, \"height\": 655}, {\"url\": \"assets/figures/arxiv/2603.28678v1/fig-011.webp\", \"caption\": \"Fig. 3. Intrinsic dimensionality of continual TTA gradients. The affine parameters of normalization layers from a ViT-B model are optimized via SGD with the loss function from FOA [21]. The concatenated gradients from all ImageNet-C domains reveal that only 566 components explain 90% of the variance. This highlights the low-dimensional nature of the adaptation space. Note that only the most significant components are shown for clarity. The analysis is based on 11 729 gradient batches.\", \"page\": 5, \"index\": 11, \"width\": 626, \"height\": 251}]"
motivation: 现有的无反向传播方法在处理持续测试时自适应时，难以在运行效率、学习能力以及减少冗余计算之间取得平衡。
method: 利用CMA-ES进化策略与Fastfood投影在低维子空间优化归一化层参数，并引入停止准则和领域专用向量库。
result: "在多个基准测试中实现了最先进的准确率，同时比现有的无反向传播方法减少了超过50%的运行时间。"
conclusion: PACE通过子空间优化和效率增强机制，为高效且高性能的无反向传播持续测试时自适应提供了新方案。
---

## 摘要
我们提出了 PACE，这是一种无反向传播的持续测试时自适应系统，它直接优化归一化层的仿射参数。现有的无导数方法难以平衡运行效率与学习能力，因为它们要么将更新限制在输入提示（prompts）上，要么无论领域是否稳定都需要进行持续且资源密集型的自适应。为了解决这些局限性，PACE 利用协方差矩阵自适应进化策略（CMA-ES）结合 Fastfood 投影，在低维子空间内优化高维仿射参数，从而实现了卓越的自适应性能。此外，我们通过引入自适应停止准则和领域专用向量库来消除冗余计算，从而提高了运行效率。我们的框架在持续分布偏移下的多个基准测试中达到了最先进的准确率，与现有的无反向传播方法相比，运行时间减少了 50% 以上。

## Abstract
We introduce PACE, a backpropagation-free continual test-time adaptation system that directly optimizes the affine parameters of normalization layers. Existing derivative-free approaches struggle to balance runtime efficiency with learning capacity, as they either restrict updates to input prompts or require continuous, resource-intensive adaptation regardless of domain stability. To address these limitations, PACE leverages the Covariance Matrix Adaptation Evolution Strategy with the Fastfood projection to optimize high-dimensional affine parameters within a low-dimensional subspace, leading to superior adaptive performance. Furthermore, we enhance the runtime efficiency by incorporating an adaptation stopping criterion and a domain-specialized vector bank to eliminate redundant computation. Our framework achieves state-of-the-art accuracy across multiple benchmarks under continual distribution shifts, reducing runtime by over 50% compared to existing backpropagation-free methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Subspace Optimization for Backpropagation-Free Continual Test-Time Adaptation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不使用反向传播（Backpropagation-Free）的情况下，实现高效且高性能的持续测试时自适应（Continual TTA）。
*   **研究动机**：
    *   **硬件限制**：传统的基于反向传播的 TTA 方法（如 TENT, CoTTA）内存开销大，且不支持不可微的量化模型，难以部署在资源受限的边缘设备上。
    *   **现有无反向传播方法的缺陷**：现有的无导数方法（如 FOA, ZOA）面临两难境地：要么学习能力受限（仅更新输入 prompt），要么计算开销巨大（每个 batch 都需要多次前向传播进行梯度估计或进化搜索），且在环境稳定时仍持续计算，造成资源浪费。

### 2. 方法论：PACE 框架
PACE（Projected Adaptation via Covariance Evolution）由三个核心组件构成：
*   **子空间优化（Subspace Adaptation）**：
    *   **核心思想**：利用 TTA 梯度具有低内在维度的特性，在低维子空间中搜索归一化层（Normalization Layers）的仿射参数更新。
    *   **技术细节**：使用 **CMA-ES（协方差矩阵自适应进化策略）** 优化一个低维向量 $v$，通过 **Fastfood 变换**（一种高效的拟随机投影，利用快速沃尔什-哈达玛变换减少内存）将其投影到高维的参数空间。
    *   **目标函数**：结合预测熵（Entropy）和中间层激活统计量（与源域数据的均值/方差对齐）作为适应度函数。
*   **自适应停止准则（Adaptation Stopping）**：
    *   **机制**：监控 CMA-ES 分布均值的相对变化。当变化低于阈值 $\epsilon$ 时，认为模型已收敛至当前领域，停止自适应计算，仅保留一次前向传播。
    *   **重启触发**：通过监测模型底层激活统计量的 KL 散度来检测领域偏移（Domain Shift），一旦检测到新领域，重新启动优化。
*   **领域专用向量库（Domain-Specialized Vector Bank）**：
    *   **知识积累**：将过去领域的优化向量 $v$ 存储在内存库中。当检测到新领域时，先从库中检索最匹配的向量作为初始化，实现对重复领域的快速恢复。

### 3. 实验设计
*   **数据集**：ImageNet-C（15种损坏类型）、ImageNet-R（多样化渲染图）、DomainNet-126（4个不同领域）。
*   **模型架构**：ViT-B 和 DeiT-B，包括全精度模型和量化模型（8-bit, 6-bit）。
*   **对比方法**：
    *   **无反向传播类**：LAME, T3A, FOA, ZOA。
    *   **基于反向传播类**：TENT, CoTTA, SAR。
    *   **基准**：NoAdapt（不进行任何自适应的原始模型）。

### 4. 资源与算力
*   **硬件环境**：实验主要在单张 **NVIDIA RTX 4090 GPU** 上完成。
*   **边缘设备测试**：在 **NVIDIA Jetson Xavier NX** 上测量了实际吞吐量（FPS）。
*   **运行时间**：在 ImageNet-C 上，PACE 的运行时间约为 5.4 小时，而同类方法 FOA 需要 11.6 小时，ZOA (fp=28) 需要 10.0 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流 TTA 基准数据集，进行了全精度与量化模型的对比，并针对重复领域场景进行了长效循环实验。
*   **消融实验**：详细分析了子空间维度 $d$、向量库容量 $p$、领域偏移阈值 $\gamma$ 以及停止准则阈值 $\epsilon$ 对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了准确率，还重点对比了内存占用、运行时间、前向传播次数和设备吞吐量，这对于评价 TTA 系统的实用性至关重要。

### 6. 主要结论与发现
*   **性能领先**：PACE 在所有基准测试中均达到了 SOTA 准确率，平均超过 FOA 1.7% 和 ZOA 4.0%。
*   **效率极高**：通过停止准则，PACE 仅需对约 50% 的 batch 进行自适应计算，运行时间比现有无反向传播方法缩短了 50% 以上。
*   **量化友好**：在 6-bit 和 8-bit 量化模型上表现稳健，无需针对不同位宽调整超参数。
*   **子空间有效性**：验证了在低维子空间（如 2304 维）优化归一化层参数比在输入端进行 prompt tuning 更有效。

### 7. 优点与亮点
*   **实用性强**：解决了 TTA 方法在“持续运行”时的能耗问题，非常适合长效部署的边缘设备。
*   **内存高效**：利用 Fastfood 变换替代稠密投影矩阵，将投影矩阵的内存占用从 306 MB 降低到 0.13 MB。
*   **平衡性好**：通过可调阈值 $\epsilon$，PACE 允许用户在准确率和运行时间之间进行灵活权衡。

### 8. 不足与局限
*   **领域检测依赖**：自适应的重启依赖于基于统计量的领域偏移检测，如果两个领域极其相似但模型仍需微调，可能会漏检。
*   **超参数敏感性**：虽然论文给出了默认值，但停止准则阈值 $\epsilon$ 和偏移检测阈值 $\gamma$ 在极端不同的任务中可能仍需微调。
*   **应用限制**：主要针对分类任务进行了验证，在目标检测或分割等更复杂的下游任务中的表现尚待进一步探索。

（完）
