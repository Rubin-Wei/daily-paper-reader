---
title: Trained Persistent Memory for Frozen Decoder-Only LLMs
title_zh: 针对冻结的仅解码器大语言模型的训练式持久化内存
authors: Hong Jeong
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.22329v1"
tags: ["query:parametric-memory"]
score: 9.0
evidence: 冻结大语言模型的持久潜空间记忆
tldr: 本研究探讨了如何为无状态的 Decoder-only 语言模型（如 GPT-2）引入持久化内存。通过在冻结的模型中加入小型内存适配器，研究对比了六种不同的内存注入方法。实验发现，在低容量下，具有强架构先验的方法表现更优，而高容量下各方法趋于一致。该研究证明了持久化潜空间内存是适用于多种 Transformer 架构的通用范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22329v1/fig-001.webp\", \"caption\": \"Figure 7: M.5: A gated memory branch reads from P and contributes through a content-dependent gate. At initialisation bg < 0 keeps the gate nearly closed, preserving the frozen self-attention path.\", \"page\": 7, \"index\": 1, \"width\": 678, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2603.22329v1/fig-002.webp\", \"caption\": \"Figure 10: Absolute forgetting curves on LoCoMo for the GPT-2 (124M) backbone at 1× capacity. Three methods (M.2, M.4, M.6) produce substantial retained-memory scores; three (M.1, M.3, M.5) are indistinguishable from the baseline. Higher and flatter curves indicate stronger memory.\", \"page\": 10, \"index\": 2, \"width\": 771, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2603.22329v1/fig-003.webp\", \"caption\": \"Figure 9: Two learning phases. Type 1 trains the memory adapter parameters offline by backpropagation through the frozen decoder. Type 2 updates the persistent memory online at each conversation turn with all model weights frozen.\", \"page\": 8, \"index\": 3, \"width\": 486, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2603.22329v1/fig-004.webp\", \"caption\": \"Figure 2: Three decoder-only injection strategies. (a) KV prefix prepends memory-derived keys and values to the self-attention cache. (b) Parallel cross-attention inserts a new attention pathway alongside the existing self-attention. (c) Gated branch computes a memory readout and adds it through a learned content-dependent gate.\", \"page\": 4, \"index\": 4, \"width\": 553, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2603.22329v1/fig-005.webp\", \"caption\": \"Figure 12: Cumulative knowledge score Ks over 30 sessions at 1× capacity. Successful methods (M.2, M.4, M.6) accumulate knowledge substantially above the baseline; failing methods (M.1, M.3, M.5, not shown for clarity) produce ∆K < 0.2.\", \"page\": 12, \"index\": 5, \"width\": 701, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2603.22329v1/fig-006.webp\", \"caption\": \"Figure 11: Absolute forgetting curves at 10× capacity. All six methods now produce substantial retained-memory scores. Methods that failed at 1× (M.1, M.3, M.5) converge to comparable performance with the strong-bias methods, demonstrating that the 1× gap is architectural, not fundamental.\", \"page\": 12, \"index\": 6, \"width\": 764, \"height\": 322}]"
motivation: 旨在验证为 Encoder-Decoder 模型设计的持久化内存机制是否能成功迁移到缺乏交叉注意力机制的 Decoder-only 模型中。
method: 在冻结的 GPT-2 上测试了包括前缀、并行交叉注意力、Hebbian 内存和槽位稀疏写入在内的六种内存适配器方法。
result: 在低容量设置下，具有强架构先验的方法（如 Hebbian 和槽位写入）显著优于其他方法，但在高容量下所有方法均表现良好。
conclusion: 持久化潜空间内存被证实是一种跨越主要 Transformer 家族的通用内存增强范式。
---

## 摘要
仅解码器语言模型是无状态的：隐藏表示在每次前向传播后都会被丢弃，会话之间没有任何持久化内容。Jeong (2026a) 基于 Jeong (2026b,c) 的侧向记忆（lateral-memory）框架，表明经过训练的记忆适配器可以赋予冻结的编码器-解码器骨干网络持久的潜空间记忆。本文探究这一原理是否可以迁移到仅解码器设置中，在这种设置下不存在交叉注意力路径，记忆必须仅通过自注意力进入。我们将六种方法——前缀（prefix）、并行交叉注意力（parallel cross-attention）、KV 扩展（KV extension）、赫布记忆（Hebbian memory）、上下文门控分支（context-gated branch）以及基于槽位的稀疏写入（slot-based sparse write）——应用于冻结的 GPT-2，仅训练一个微小的适配器 $θ_{mem}$。写入规则是共享的；仅读取注入方式从解码器交叉注意力变为了自注意力 KV 前缀或并行分支。在 LoCoMo 数据集上，我们发现了一个显著的归纳偏置二分现象：在 $1\times$ 容量下，具有强架构先验的三种方法——交叉注意力 (M.2)、赫布 (M.4) 和槽位写入 (M.6)——实现了 $7-18\%$ 的记忆保留分数和 $7-10$ 的知识增益 $ΔK$，而其他三种方法则失败了（$< 0.4\%$）。在 $10\times$ 容量下，所有六种方法都趋于收敛，这表明差距在于架构而非根本原理。结合 Jeong (2026a) 的编码器-解码器结果以及 Jeong (2026b,c) 受大脑启发的模块，这些发现确立了持久化潜空间记忆作为一种跨越主要 Transformer 家族的通用范式。

## Abstract
Decoder-only language models are stateless: hidden representations are discarded after every forward pass and nothing persists across sessions. Jeong (2026a) showed that trained memory adapters give a frozen encoder-decoder backbone persistent latent-space memory, building on the lateral-memory framework of Jeong (2026b,c). Here we ask whether the same principle transfers to the decoder-only setting, where no cross-attention pathway exists and memory must enter through self-attention alone. We adapt six methods -- prefix, parallel cross-attention, KV extension, Hebbian memory, context-gated branch, and slot-based sparse write -- to a frozen GPT-2, training only a small adapter $θ_{mem}$. The write rule is shared; only the read injection changes from decoder cross-attention to self-attention KV prefix or parallel branch. On LoCoMo we find a striking inductive-bias dichotomy: at $1\times$ capacity, three methods with strong architectural priors -- cross-attention (M.2), Hebbian (M.4), and slot write (M.6) -- achieve retained-memory scores of $7-18\%$ and knowledge gains $ΔK$ of $7-10$, while the other three fail ($< 0.4\%$). At $10\times$ capacity all six converge, showing the gap is architectural, not fundamental. Together with the encoder-decoder results of Jeong (2026a) and the brain-inspired modules of Jeong (2026b,c), these findings establish persistent latent-space memory as a general paradigm spanning major transformer families.

---

## 论文详细总结（自动生成）

这是一份关于论文《Trained Persistent Memory for Frozen Decoder-Only LLMs》的结构化深入分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：现代仅解码器（Decoder-only）大语言模型（如 GPT-4、LLaMA）在本质上是“无状态”的，每次前向传播后隐藏表示即被丢弃，无法在不同会话间保留信息。
*   **研究动机**：作者此前已在 Encoder-Decoder 架构上实现了持久化潜空间记忆，但 Decoder-only 架构缺乏天然的“交叉注意力”路径来注入外部信息。本研究旨在探索如何通过自注意力机制为冻结的 Decoder-only 模型构建可训练的、跨会话的持久化记忆系统。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：引入一个小型可训练的内存适配器 $\theta_{Mem}$，在保持模型主体冻结的情况下，维护一个随对话更新的持久化内存库 $P_t$。
*   **写入规则（通用）**：所有方法共享一种基于注意力的写入机制。新内存 $P_t$ 是旧内存 $P_{t-1}$ 的衰减值与当前隐藏状态 $H_t$ 经过内容寻址聚合后的总和：$P_t = \gamma P_{t-1} + A_t^\top V_w$。
*   **六种读取/注入策略**：
    1.  **M.1 Prefix**：将内存投影为 KV 对，作为前缀拼接到自注意力中。
    2.  **M.2 Parallel XAttn**：在自注意力旁插入并行的交叉注意力层（类似 Flamingo 架构）。
    3.  **M.3 KV Ext**：每一层拥有独立的 KV 投影和写入更新。
    4.  **M.4 Hebbian**：利用赫布学习规则的关联矩阵进行模式补全，再注入 KV 缓存。
    5.  **M.5 Gated Branch**：通过内容相关的门控机制将内存读取结果加回到隐藏状态。
    6.  **M.6 Slot-based**：将内存组织为可寻址的槽位（Slots），采用稀疏 Top-k 覆盖写入。
*   **学习阶段**：分为“监督学习”（训练适配器参数）和“对话学习”（推理时在线更新内存向量，无需梯度）。

### 3. 实验设计
*   **数据集与场景**：主要使用 **LoCoMo** 数据集，这是一个专门用于评估长程对话记忆的基准，包含跨越 30 个会话的长对话及相关的问答对。
*   **Benchmark**：以冻结的 **GPT-2 (124M)** 作为骨干网络。
*   **对比维度**：
    *   **方法对比**：对比上述六种不同的内存注入策略。
    *   **容量对比**：设置 $1\times$（标准容量，64个向量）和 $10\times$（大容量，640个向量）两种条件，以区分“架构偏置”与“原始容量”的影响。
*   **评估指标**：
    *   **保留记忆分数（Retained-memory score）**：衡量消融掉持久内存后模型性能的下降程度。
    *   **知识增益（$\Delta K$）**：衡量模型在 30 个会话中积累事实知识的能力。

### 4. 资源与算力
*   **硬件**：论文提到使用了“单张 NVIDIA GPU”。
*   **精度与优化器**：使用 bfloat16 精度，AdamW 优化器。
*   **训练细节**：训练 10 个 Epoch，Batch Size 为 16（含梯度累积），学习率为 $10^{-4}$。
*   **说明**：文中未明确指出具体的 GPU 型号（如 A100 或 RTX 4090）以及总的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 种方法在 2 种容量下进行了完整的遗忘曲线测试和知识积累测试。
*   **充分性**：实验设计较为严谨，采用了“等投入原则（Equal-input principle）”，确保所有方法在推理时仅接触当前轮次的文本，性能提升完全归功于持久化内存。
*   **客观性**：通过引入 $10\times$ 容量实验，成功揭示了低容量下某些方法的失败并非因为不兼容，而是因为缺乏高效的架构先验（归纳偏置）。
*   **局限性**：实验仅基于 GPT-2 (124M) 这一较小模型，且仅使用了 LoCoMo 一个数据集和单一随机种子，跨模型和跨领域的泛化性验证略显不足。

### 6. 主要结论与发现
*   **归纳偏置二分法**：在 $1\times$ 低容量下，只有具有强架构先验的方法（M.2 交叉注意力、M.4 赫布、M.6 槽位写入）有效；而 M.1、M.3、M.5 几乎失效。
*   **容量弥补缺陷**：当容量提升至 $10\times$ 时，所有六种方法性能趋于一致，说明架构设计的优劣主要影响“内存效率”。
*   **知识积累差异**：M.6（槽位稀疏写入）在知识增益 $\Delta K$ 上表现最佳，表明离散的槽位更新更适合存储多样化的事实。
*   **范式确立**：证明了持久化潜空间记忆是一个通用的 Transformer 范式，可从 Encoder-Decoder 成功迁移至 Decoder-only。

### 7. 优点与亮点
*   **架构解耦**：提出了“通用写入规则 + 特定读取路径”的设计模板，易于适配未来的新架构。
*   **高效性**：适配器参数量极小（约 0.8M - 4.7M），在不微调大模型的前提下实现了长程记忆。
*   **理论深度**：不仅关注性能，还深入探讨了不同 Transformer 家族对持久化记忆的“架构亲和力”排序。

### 8. 不足与局限
*   **模型规模限制**：实验仅限于 124M 参数的模型，尚未在大规模模型（如 LLaMA-7B 及以上）上验证。
*   **隐私风险**：持久化内存会记录用户敏感信息，论文虽提及隐私保护，但未提出具体的技术过滤方案。
*   **训练开销**：虽然推理时是冻结的，但 Type 1 训练阶段仍需通过冻结的骨干网络进行反向传播，对于超大规模模型来说计算成本依然很高。

（完）
