---
title: Trajectory-Informed Memory Generation for Self-Improving Agent Systems
title_zh: 面向自我改进智能体系统的轨迹感知记忆生成
authors: "Gaodan Fang, Vatche Isahagian, K. R. Jayaram, Ritesh Kumar, Vinod Muthusamy, Punleuk Oum, Gegi Thomas"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10600v1"
tags: ["query:am"]
score: 9.0
evidence: 自我改进智能体系统的上下文记忆检索
tldr: 针对LLM智能体难以从过去经验中学习、重复错误的问题，本文提出一种基于轨迹信息的记忆生成框架。该框架通过分析执行轨迹，自动提取策略、恢复和优化三类指导性建议，并利用自适应检索系统在未来任务中注入相关经验。实验表明，该方法在AppWorld基准测试中显著提升了任务成功率，尤其在复杂任务上表现优异，实现了智能体的自我进化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.10600v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of our approach\", \"page\": 5, \"index\": 1, \"width\": 1056, \"height\": 429}]"
motivation: 现有的LLM智能体往往无法从执行经验中有效学习，导致其在面对相似任务时重复低效模式或无法从错误中恢复。
method: 提出一个包含轨迹智能提取、决策归因分析、上下文学习生成和自适应记忆检索四个组件的自动化框架，从执行模式中提取结构化见解。
result: "在AppWorld基准测试中，该方法使场景目标完成率最高提升了14.3个百分点，在复杂任务上更是实现了149%的相对增长。"
conclusion: 通过结构化提取和检索执行轨迹中的深度见解，可以显著增强智能体的自我改进能力和处理复杂任务的效率。
---

## 摘要
基于大语言模型（LLM）的智能体面临着一个持久的挑战：如何从执行经验中学习以提升未来的性能。尽管智能体能够成功完成许多任务，但它们往往会重复低效的模式，无法从类似的错误中恢复，并错失应用以往执行中成功策略的机会。我们提出了一种新型框架，用于从智能体执行轨迹中自动提取可操作的经验教训，并通过上下文记忆检索利用这些经验来提升未来的性能。我们的方法包含四个组件：(1) 轨迹智能提取器（Trajectory Intelligence Extractor），对智能体推理模式进行语义分析；(2) 决策归因分析器（Decision Attribution Analyzer），识别哪些决策和推理步骤导致了失败、恢复或低效；(3) 上下文学习生成器（Contextual Learning Generator），产生三种类型的指导——来自成功模式的策略提示、来自失败处理的恢复提示，以及来自低效但成功执行的优化提示；(4) 自适应记忆检索系统（Adaptive Memory Retrieval System），基于多维相似性将相关的学习成果注入智能体提示词中。与存储通用对话事实的现有记忆系统不同，我们的框架能够理解执行模式，提取具有溯源信息的结构化学习成果，并检索针对特定任务上下文定制的指导。在 AppWorld 基准测试上的评估表明，该方法带来了持续的改进，在留出任务的情景目标完成率上提升了高达 14.3 个百分点，在复杂任务上的收益尤为显著（情景目标提升 28.5 个百分点，相对增长 149%）。

## Abstract
LLM-powered agents face a persistent challenge: learning from their execution experiences to improve future performance. While agents can successfully complete many tasks, they often repeat inefficient patterns, fail to recover from similar errors, and miss opportunities to apply successful strategies from past executions. We present a novel framework for automatically extracting actionable learnings from agent execution trajectories and utilizing them to improve future performance through contextual memory retrieval. Our approach comprises four components: (1) a Trajectory Intelligence Extractor that performs semantic analysis of agent reasoning patterns, (2) a Decision Attribution Analyzer that identifies which decisions and reasoning steps led to failures, recoveries, or inefficiencies, (3) a Contextual Learning Generator that produces three types of guidance -- strategy tips from successful patterns, recovery tips from failure handling, and optimization tips from inefficient but successful executions, and (4) an Adaptive Memory Retrieval System that injects relevant learnings into agent prompts based on multi-dimensional similarity. Unlike existing memory systems that store generic conversational facts, our framework understands execution patterns, extracts structured learnings with provenance, and retrieves guidance tailored to specific task contexts. Evaluation on the AppWorld benchmark demonstrates consistent improvements, with up to 14.3 percentage point gains in scenario goal completion on held-out tasks and particularly strong benefits on complex tasks (28.5~pp scenario goal improvement, a 149\% relative increase).

---

## 论文详细总结（自动生成）

这是一份关于论文《Trajectory-Informed Memory Generation for Self-Improving Agent Systems》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 LLM 智能体存在“失忆症”，即无法从过去的执行经验中系统性地学习。即使智能体成功完成了任务，也可能存在效率低下、重复错误或无法复用成功策略的问题。
*   **研究背景**：目前的记忆系统（如 Mem0, MemGPT）多侧重于存储对话中的事实（Fact），缺乏对执行模式（Execution Patterns）和推理流（Reasoning Flows）的理解。此外，现有的自我改进方法往往只关注成功案例，忽略了从“失败后恢复”或“成功但低效”的轨迹中提取价值。

### 2. 方法论：轨迹感知记忆生成框架
该框架通过三个阶段将原始执行轨迹转化为可操作的指导建议：

*   **阶段 1：轨迹分析与提示提取（Extraction）**
    *   **轨迹智能提取器**：对智能体的推理进行语义分类（分析型、计划型、验证型、反思型），识别认知模式（如自纠错、API 探索）。
    *   **决策归因分析器**：利用 LLM 进行因果分析，区分失败的直接原因、近因和根源，并识别导致成功恢复的关键决策。
    *   **上下文学习生成器**：生成三类结构化提示（Tips）：
        1.  **策略提示（Strategy）**：来自干净成功的执行，强调预检和验证。
        2.  **恢复提示（Recovery）**：来自失败后成功的案例，记录错误识别与修正步骤。
        3.  **优化提示（Optimization）**：针对低效成功（如循环调用单体 API 而非批量 API），提出更优方案。
    *   **粒度选择**：支持**任务级**（整体策略）和**子任务级**（跨任务复用的逻辑片段，如身份验证、分页检索）的提取。

*   **阶段 2：存储与管理（Storage & Management）**
    *   **泛化与聚类**：对子任务描述进行实体抽象（去除具体用户名、ID 等），通过语义相似度进行层次聚类。
    *   **合并与去重**：利用 LLM 合并冗余提示，解决冲突（成功轨迹的经验优先于失败轨迹）。

*   **阶段 3：运行时检索（Retrieval）**
    *   **余弦相似度检索**：快速、低成本的向量搜索。
    *   **LLM 引导选择**：利用 LLM 分析当前任务上下文，进行元数据过滤和优先级排序，确保注入最相关的指导。

### 3. 实验设计
*   **数据集/场景**：使用 **AppWorld** 基准测试，这是一个模拟真实世界多应用交互（电子邮箱、日历、电商等）的复杂环境。
*   **评估指标**：
    *   **TGC (Task Goal Completion)**：单个任务的成功率。
    *   **SGC (Scenario Goal Completion)**：更严格的指标，要求完成一个场景下的所有任务变体，衡量一致性。
*   **对比配置**：
    *   **Baseline**：无记忆的 ReAct 智能体。
    *   **不同粒度**：任务级 vs. 子任务级提取。
    *   **不同检索策略**：余弦相似度（不同阈值 $\tau$）vs. LLM 引导选择。
    *   **任务难度**：分为 Easy (D1), Medium (D2), Hard (D3) 三个等级。

### 4. 资源与算力
*   **模型使用**：智能体执行和提示提取 pipeline 均使用 **GPT-4**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法主要基于 Prompt Engineering 和 LLM 推理进行记忆提取与检索，不涉及大规模模型微调，其算力消耗主要集中在 API 调用成本上。

### 5. 实验数量与充分性
*   **实验规模**：在 AppWorld 的训练集、开发集和测试集（Test-Normal）上均进行了实验。
*   **充分性**：实验设计较为全面，涵盖了消融实验（对比不同检索阈值、不同提取粒度）。
*   **客观性**：使用了留出集（Held-out tasks）进行验证，证明了记忆的泛化能力；同时对比了 TGC 和 SGC 两个维度的指标，评估视角较为客观。

### 6. 主要结论与发现
*   **显著提升**：在留出测试集上，SGC 提升了 **14.3 个百分点**。
*   **复杂任务受益最大**：在最难的 D3 任务上，SGC 从 19.1% 提升至 47.6%，相对增长 **149%**。
*   **粒度与策略的解耦**：
    *   **子任务级提取**对提升 TGC（单任务成功率）最有效，因为它提供了更精准的局部指导。
    *   **LLM 引导检索**对提升 SGC（场景一致性）至关重要，因为它能更好地处理上下文细微差别。
*   **自我改进**：智能体能够通过学习自己过去的错误和低效行为，在后续任务中表现得更像“专家”。

### 7. 优点
*   **多维度学习**：不局限于成功经验，能从失败恢复和低效成功中挖掘价值。
*   **结构化与溯源**：生成的 Tips 具有明确的触发条件、步骤和负面示例，且保留了原始轨迹的溯源信息。
*   **跨任务泛化**：通过子任务分解和实体抽象，使得在一个应用（如 Spotify）中学到的模式可以迁移到另一个应用（如 Venmo）。

### 8. 不足与局限
*   **成本开销**：LLM 引导的检索和多阶段的提取 Pipeline 会增加额外的 API 调用成本和延迟。
*   **模型依赖**：该框架的效果高度依赖于底层 LLM（如 GPT-4）的因果分析和自我反思能力，在较弱的模型上效果可能打折扣。
*   **潜在干扰**：实验显示在极简单的任务（D1）上，注入过多提示有时会产生轻微的负面干扰。
*   **动态环境挑战**：如果 API 发生重大变更，旧的记忆提示可能会失效，文中未深入讨论记忆的自动失效/更新机制。

（完）
