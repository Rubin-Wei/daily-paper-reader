---
title: "Stacked from One: Multi-Scale Self-Injection for Context Window Extension"
title_zh: 源于一体：用于上下文窗口扩展的多尺度自注入
authors: "Wei Han, Pan Zhou, Shuicheng Yan"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04759v1"
tags: ["query:lcp"]
score: 9.0
evidence: 通过多粒度上下文压缩扩展上下文窗口
tldr: 针对大语言模型上下文窗口受限及长文本微调成本高的问题，本文提出 SharedLLM 框架。该方法通过“自注入”机制，将同一模型的层堆叠为压缩器和解码器，利用多粒度上下文压缩和树状数据结构实现高效的信息检索。尽管仅在 8K 长度上训练，该模型能有效扩展至 128K 以上，在保持高性能的同时显著降低了显存占用并提升了推理速度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.04759v1/fig-001.webp\", \"caption\": \"Table 2: Langauge modeling results of the supervised fine-tuning setting. “OOM” denotes the outof-memory exception is raised during inference. Excessively large perplexities (> 102) are hidden with a dash (“-”).\", \"page\": 7, \"index\": 1, \"width\": 822, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2603.04759v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of SHAREDLLM. The architecture resembles general encoder-decoder architecture like T5 (Raffel et al., 2020), but the interaction occurs at the first M layers between lower and upper model through shared key-values which are encoded and compressed from the text chunk into a sequence of trees (top-left).\", \"page\": 3, \"index\": 2, \"width\": 754, \"height\": 467}, {\"url\": \"assets/figures/arxiv/2603.04759v1/fig-003.webp\", \"caption\": \"Figure 5: Accuracy comparison on passkey retrieval (single key-value pair) task.\", \"page\": 18, \"index\": 3, \"width\": 498, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2603.04759v1/fig-004.webp\", \"caption\": \"Table 3: Evaluation results of different SFT methods on two benchmarks from LongBench and Infini-Bench. Note that for some baselines we follow their default settings to truncate the input below their window length, which may cast positive effects on their performance.\", \"page\": 8, \"index\": 4, \"width\": 818, \"height\": 484}, {\"url\": \"assets/figures/arxiv/2603.04759v1/fig-005.webp\", \"caption\": \"Table 8: Perplexity increment as a negative effect from the lack of books3. † represents the values in corresponding rows are reproduced from open-sourced code.\", \"page\": 17, \"index\": 5, \"width\": 818, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.04759v1/fig-006.webp\", \"caption\": \"Table 13: Comparison of time and memory consumption when query-based retrieval is incorporated/not incorporated in SHAREDLLM. h, M and β are fixed at the default values.\", \"page\": 20, \"index\": 6, \"width\": 698, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2603.04759v1/fig-007.webp\", \"caption\": \"Figure 6: A living example of tree growth and split on the passkey retrieval task. The numbers above the text boxes are the correlation score between the text chunk and user query. Green/Red boxes indicate the chunk is selected/not selected.\", \"page\": 20, \"index\": 7, \"width\": 743, \"height\": 421}]"
motivation: 现有的长上下文扩展方法面临数据获取难和计算成本高的问题，限制了 LLM 在复杂场景下的应用。
method: 提出 SharedLLM 框架，通过将同一模型的层重组为压缩器和解码器，并结合树状结构进行多粒度信息自注入。
result: 模型在 8K 训练数据下实现了超过 128K 的上下文扩展，且推理速度比流式架构快 2 倍，比编解码架构快 3 倍。
conclusion: 该研究证明了通过多尺度自注入机制，可以在不增加额外训练负担的情况下，高效且低成本地扩展 LLM 的上下文处理能力。
---

## 摘要
当代大语言模型（LLMs）有限的上下文窗口仍然是其在各领域广泛应用的主要瓶颈。虽然在长上下文数据上进行持续预训练提供了一种直接的解决方案，但其数据获取和计算成本极高。为了应对这一挑战，我们提出了 SharedLLM，这是一个基于多粒度上下文压缩和查询感知信息获取的新颖框架。SharedLLM 由两个堆叠的短上下文 LLM 组成：下层模型作为压缩器，上层模型作为解码器。下层模型将长输入压缩为紧凑的多粒度表示，随后将其转发至上层模型进行上下文感知处理。为了最大化效率，这种信息传递仅发生在最底层，从而绕过了冗长的前向传递和冗余的交叉注意力操作。由于上层和下层模型均源自相同的底层 LLM 层，这一过程被称为“自注入”（self-injection）。为了支持该架构，一种专门的基于树的数据结构实现了上下文信息的有效编码和查询感知检索。尽管仅在 8K token 的序列上进行训练，SharedLLM 仍能有效地泛化到超过 128K token 的输入。在一系列全面的长上下文建模和理解基准测试中，SharedLLM 取得了优于或相当予强基准模型的性能，在效率和准确性之间达到了最佳平衡。此外，这些设计选择使 SharedLLM 能够显著减少内存占用，并带来显著的推理加速（比流式架构快 2 倍，比编码器-解码器架构快 3 倍）。

## Abstract
The limited context window of contemporary large language models (LLMs) remains a primary bottleneck for their broader application across diverse domains. Although continual pre-training on long-context data offers a straightforward solution, it incurs prohibitive data acquisition and computational costs. To address this challenge, we propose~\modelname, a novel framework based on multi-grained context compression and query-aware information acquisition. SharedLLM comprises two stacked short-context LLMs: a lower model serving as a compressor and an upper model acting as a decoder. The lower model compresses long inputs into compact, multi-grained representations, which are then forwarded to the upper model for context-aware processing. To maximize efficiency, this information transfer occurs exclusively at the lowest layers, bypassing lengthy forward passes and redundant cross-attention operations. This entire process, wherein the upper and lower models are derived from the same underlying LLM layers, is termed~\textit{self-injection}. To support this architecture, a specialized tree-based data structure enables the efficient encoding and query-aware retrieval of contextual information. Despite being trained on sequences of only 8K tokens, \modelname~effectively generalizes to inputs exceeding 128K tokens. Across a comprehensive suite of long-context modeling and understanding benchmarks, \modelname~achieves performance superior or comparable to strong baselines, striking an optimal balance between efficiency and accuracy. Furthermore, these design choices allow \modelname~to substantially reduce the memory footprint and yield notable inference speedups ($2\times$ over streaming and $3\times$ over encoder-decoder architectures).

---

## 论文详细总结（自动生成）

这是一份关于论文《Stacked from One: Multi-Scale Self-Injection for Context Window Extension》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLMs）受限于有限的上下文窗口，处理超长文本时性能下降且显存开销巨大。
*   **研究动机**：现有的长上下文扩展方法存在两难困境：
    *   **持续预训练**：需要海量长文本数据和极高的计算成本。
    *   **线性/流式注意力**：虽然降低了复杂度，但往往与高效的 FlashAttention 不兼容，导致推理速度慢。
    *   **外部压缩器**：引入异构模型（如用 BERT 压缩）需要复杂的特征对齐和预热阶段。
*   **整体含义**：提出 **SharedLLM** 框架，通过“自注入”机制，利用模型自身的浅层作为压缩器，实现高效、低成本的长上下文扩展。

### 2. 方法论：核心思想与技术细节
*   **核心思想（自注入 Self-Injection）**：将一个短上下文 LLM 拆分为“下层模型（压缩器）”和“上层模型（解码器）”。两者共享同一组预训练参数，消除特征空间不一致的问题。
*   **关键技术细节**：
    *   **分层架构**：下层模型仅由目标 LLM 的前 $M$ 层组成，负责将长文本块（Chunks）并行压缩；上层模型负责整合压缩信息并生成预测。
    *   **上下文树（Context Tree）**：将文本块组织成二叉树结构。根节点代表整个块，叶子节点代表细粒度片段。
    *   **查询感知动态搜索**：在推理时，根据当前查询（Query）与树节点的语义相似度，动态决定展开哪些节点。相关性高的部分保留细粒度表示，不相关的部分保留粗粒度表示。
    *   **位置感知交叉注意力**：通过在交叉注意力中引入块级旋转位置编码（RoPE），确保模型能够感知压缩块之间的全局顺序。
    *   **多尺度压缩**：通过分级下采样（Strided Selection），实现从顶层到底层压缩率逐渐降低的层次化表示。

### 3. 实验设计
*   **数据集**：
    *   **语言建模（LM）**：从 RedPajama 中采样 20B token 进行持续预训练（训练长度仅 8K）。
    *   **指令微调（SFT）**：混合 RedPajama、LongAlpaca 数据集及少量长程检索数据。
*   **基准测试（Benchmarks）**：
    *   **长文本建模**：PG19、ProofPile、Arxiv、CodeParrot（测试长度达 128K）。
    *   **长文本理解**：LongBench（14个任务）、InfiniBench（数学、多选、数字检索）。
    *   **检索能力**：Passkey Retrieval（大海捞针测试）。
*   **对比方法**：包括位置插值（PI）、YaRN、CEPE（编解码架构）、StreamingLLM（流式）、Activation Beacon（压缩）、SnapKV 和 OmniKV（推理优化）。

### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA A800 GPU**。
*   **训练配置**：
    *   **LM 阶段**：全局 Batch Size 为 128，训练 1 个 epoch。
    *   **SFT 阶段**：全局 Batch Size 为 8，训练 2 个 epoch。
*   **模型规模**：基于 LLaMA-2-7B、LLaMA-3-8B 和 Mistral-7B 进行实验。
*   **时长**：文中未明确给出总训练小时数，但强调了其“训练短、测试长”的高效性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4K 到 128K 的多种长度测试，并在三大主流开源模型底座上进行了验证。
*   **消融实验**：针对注入层位置（顶层/底层/交错）、查询感知机制、噪声增强、块位置编码等关键设计进行了详细对比。
*   **充分性评价**：实验设计较为全面，不仅关注困惑度（PPL），还深入评估了下游任务的准确性、推理速度和显存占用。对比基准涵盖了当前主流的各类长上下文技术，评价客观。

### 6. 主要结论与发现
*   **外推能力强**：仅在 8K 长度训练，即可在 128K 长度下保持稳定的困惑度，无性能崩溃。
*   **性能优异**：在 LongBench 和 InfiniBench 上，SharedLLM 在多个子任务中超越了专门的长文本模型（如 Activation Beacon）。
*   **效率极高**：推理速度比流式架构快 2 倍，比传统的编解码架构快 3 倍；显存占用远低于全注意力模型（YaRN）。
*   **自注入优势**：由于上下层模型同源，无需复杂的对齐训练，收敛速度快。

### 7. 优点（亮点）
*   **架构优雅**：利用“自注入”巧妙解决了异构模型压缩时的特征对齐难题。
*   **动态检索**：上下文树结构允许模型根据查询“按需分配”注意力资源，平衡了细节与全局信息。
*   **推理友好**：兼容 FlashAttention，且通过浅层压缩大幅减少了 KV Cache 的长度，直接提升了吞吐量。
*   **低成本**：无需海量长文本数据即可实现极长窗口的扩展。

### 8. 不足与局限
*   **训练数据偏差**：由于版权问题未包含 Book-S3 数据集，导致在某些 LM 任务上的绝对 PPL 数值略逊于包含该数据的模型。
*   **超参数敏感性**：实验显示树的高度（Height）和压缩率（$\beta$）对性能有一定影响，不同任务可能需要微调这些超参数。
*   **短文本开销**：虽然长文本效率高，但在处理极短文本时，由于引入了额外的树搜索和交叉注意力，可能存在微小的计算冗余。
*   **模型底座限制**：目前主要在 Dense 模型上验证，尚未探讨在混合专家模型（MoE）上的表现。

（完）
