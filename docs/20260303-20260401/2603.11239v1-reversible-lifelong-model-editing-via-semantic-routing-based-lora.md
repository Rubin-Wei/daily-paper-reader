---
title: Reversible Lifelong Model Editing via Semantic Routing-Based LoRA
title_zh: 基于语义路由 LoRA 的可逆终身模型编辑
authors: "Haihua Luo, Xuming Ran, Tommi Kärkkäinen, Zhonghua Chen, Jiangrong Shen, Qi Xu, Fengyu Cong"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11239v1"
tags: ["query:pm"]
score: 9.0
evidence: 通过终身模型编辑更新参数中的知识
tldr: 针对大语言模型在持续编辑过程中出现的语义漂移和灾难性遗忘问题，本文提出了 SoLA 框架。该方法将每次编辑封装为独立的 LoRA 模块，并通过语义路由机制进行动态激活。SoLA 不仅能有效保留新旧知识，还首次实现了精确的可逆回滚功能，允许通过移除路由键值来撤销特定编辑，且无需辅助网络即可实现端到端决策。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.11239v1/fig-001.webp\", \"caption\": \"Figure 4. Figure(a)–(c) presents every edit accuracy on SCOTUS datasets with BERT. All experiment settings are the same as Tab.1.\", \"page\": 6, \"index\": 1, \"width\": 1017, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.11239v1/fig-002.webp\", \"caption\": \"Figure 5. Visualization of zsRE dataset encoder output from T5small with t-SNE and the dots with same color and shape are input and rephrase sentence. All experiment settings are the same as Tab.1.\", \"page\": 6, \"index\": 2, \"width\": 492, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2603.11239v1/fig-003.webp\", \"caption\": \"Figure 2. Number of mismatches in LoRA allocation with ERR/TRR accuracy in MELO. All experiments are conducted on Scotus datasets with the same backbone.\", \"page\": 2, \"index\": 3, \"width\": 492, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.11239v1/fig-004.webp\", \"caption\": \"Table 2. Model Performance Comparison on UniEdit and WikiBigEdit datasets in hallucination correction tasks. All the scores are PPL metric and the best result is in bolded.\", \"page\": 7, \"index\": 4, \"width\": 1008, \"height\": 673}, {\"url\": \"assets/figures/arxiv/2603.11239v1/fig-005.webp\", \"caption\": \"Figure 3. Main framework of our method. a) indicates the edited layer in model, where we retain the transformer block of base model frozen and add trainable LoRA module to the edited layer of base model. b) shows the editing process, where every edit will be assigned LoRA module and the query of input will be mapped to assigned LoRA module. c) presents the matching process between query of input and LoRA module in reference. Colour green indicates trainable, while color grey is frozen.\", \"page\": 3, \"index\": 5, \"width\": 1017, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.11239v1/fig-006.webp\", \"caption\": \"Table 6. Edited layers of different model\", \"page\": 13, \"index\": 6, \"width\": 475, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.11239v1/fig-007.webp\", \"caption\": \"Table 1. Comparison of SoLA to existing methods. All the results are obtained after sequential edits. ERR indicates Edit Reliability Rate, TRR presents Task Retention Rate, and ARR is Accuracy Attention Rate. The best performance is shown in bold.\", \"page\": 5, \"index\": 7, \"width\": 1010, \"height\": 390}]"
motivation: 现有的模型编辑方法在持续更新时容易产生语义漂移和知识遗忘，且缺乏对特定编辑进行精确撤销的能力。
method: 提出 SoLA 框架，将每个编辑封装为独立的 LoRA 模块，并利用语义路由机制进行动态匹配与激活，实现模块化隔离。
result: 实验证明 SoLA 在学习和保留编辑知识方面表现优异，并成功实现了高效、精确且可逆的终身模型编辑。
conclusion: SoLA 通过语义路由与 LoRA 的结合，为大模型提供了一种无遗忘、抗漂移且支持回滚的稳健编辑方案。
---

## 摘要
现实世界的动态演变使得大语言模型（LLM）的模型编辑变得必不可少。虽然现有方法探索了模块隔离或参数高效策略，但由于持续更新，它们仍面临语义漂移或知识遗忘的问题。为了应对这些挑战，我们提出了 SoLA，一种用于终身模型编辑的基于语义路由的 LoRA 框架。在 SoLA 中，每次编辑都被封装为一个独立的 LoRA 模块，该模块在训练后被冻结，并通过语义路由映射到输入，从而允许通过语义匹配动态激活 LoRA 模块。这种机制避免了由聚类更新引起的语义漂移，并减轻了参数共享导致的灾难性遗忘。更重要的是，SoLA 支持通过从语义路由中移除键（key）来精确撤销特定编辑，从而恢复模型的原始行为。据我们所知，这种可逆的回滚编辑能力在现有文献中尚属首次实现。此外，SoLA 将决策过程集成到编辑层中，消除了对辅助路由网络的需求，并实现了端到端的决策过程。大量实验表明，SoLA 能够有效地学习并保留编辑后的知识，实现了准确、高效且可逆的终身模型编辑。

## Abstract
The dynamic evolution of real-world necessitates model editing within Large Language Models. While existing methods explore modular isolation or parameter-efficient strategies, they still suffer from semantic drift or knowledge forgetting due to continual updating. To address these challenges, we propose SoLA, a Semantic routing-based LoRA framework for lifelong model editing. In SoLA, each edit is encapsulated as an independent LoRA module, which is frozen after training and mapped to input by semantic routing, allowing dynamic activation of LoRA modules via semantic matching. This mechanism avoids semantic drift caused by cluster updating and mitigates catastrophic forgetting from parameter sharing. More importantly, SoLA supports precise revocation of specific edits by removing key from semantic routing, which restores model's original behavior. To our knowledge, this reversible rollback editing capability is the first to be achieved in existing literature. Furthermore, SoLA integrates decision-making process into edited layer, eliminating the need for auxiliary routing networks and enabling end-to-end decision-making process. Extensive experiments demonstrate that SoLA effectively learns and retains edited knowledge, achieving accurate, efficient, and reversible lifelong model editing.

---

## 论文详细总结（自动生成）

### 论文总结：基于语义路由 LoRA 的可逆终身模型编辑 (SoLA)

#### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）需要不断更新知识以应对现实世界的动态变化。传统的全量微调成本极高，而现有的模型编辑方法在“终身编辑”（持续不断地进行多次编辑）场景下表现不佳。
*   **核心问题**：
    *   **语义漂移（Semantic Drift）**：如 MELO 等方法依赖动态更新的聚类中心，随着编辑次数增加，聚类中心的变化会导致输入与编辑模块的匹配错误。
    *   **灾难性遗忘**：参数共享或持续更新会导致旧的编辑知识被新知识覆盖。
    *   **不可逆性**：现有文献中缺乏一种能够精确撤销（回滚）特定某次编辑而不影响其他编辑或模型原始性能的机制。

#### 2. 方法论
SoLA 框架的核心思想是将每次编辑操作**模块化隔离**，并通过**静态语义路由**进行精确检索。
*   **独立 LoRA 封装**：为每一次编辑分配一个独立的 LoRA 模块。在当前编辑任务训练完成后，该 LoRA 模块及其对应的语义键值（Key）将被永久冻结，不再参与后续编辑的更新。
*   **语义路由机制**：利用输入序列最后一个 Token 的隐藏层表示作为查询向量（Query），在存储库中匹配最接近的 Key。由于 Key 是冻结的，从根本上避免了语义漂移。
*   **主决策机制（Master Decision Mechanism）**：在模型的第一层编辑层中集成决策逻辑。通过计算 Query 与 Key 的距离并与阈值 $\alpha$ 比较，决定是否激活 LoRA。这种设计实现了端到端的决策，无需额外的辅助路由网络。
*   **可逆编辑（Rollback）**：由于编辑与 Key 是一一对应的映射关系，只需从路由表中删除特定的 Key，模型即可针对该输入恢复到原始行为，实现了精确的“撤销”功能。

#### 3. 实验设计
*   **数据集与场景**：
    *   **文档分类**：使用 SCOTUS 数据集（法律主题分类）。
    *   **问答任务（QA）**：使用 zsRE 数据集进行编辑，NQ 数据集作为上游测试集。
    *   **幻觉修正**：使用 SelfCheckGPT、UniEdit 和 WikiBigEdit 数据集。
*   **基座模型（Backbone）**：涵盖了 BERT、T5-small、GPT2-XL 以及更大规模的 LLaMA-3-8B、DeepSeek-R1-8B 和 Qwen2-7B。
*   **对比方法（Benchmarks）**：
    *   传统持续学习方法：EWC, CMR, CLEAR。
    *   主流模型编辑方法：MEND, SERAC, ROME, GRACE, ELDER, MELO。
*   **评估指标**：编辑可靠性（ERR）、任务保留率（TRR）、准确关注率（ARR）及困惑度（PPL）。

#### 4. 资源与算力
*   **硬件设备**：论文明确提到实验使用了 **NVIDIA A100 40GB GPU**。
*   **训练细节**：使用 SGD 优化器，学习率为 0.05，采用余弦衰减调度，训练轮数为 40 Epochs，LoRA Rank 设置为 4。
*   **效率表现**：SoLA 的新增参数量极低（约 0.08M），在编辑时间上也表现出较高的效率（如在 SCOTUS 上单次编辑约 6 分钟）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个核心任务、5 个不同规模的数据集上进行了广泛测试，并跨越了从轻量级（BERT）到大规模（8B 参数）的多种模型架构。
*   **消融实验**：针对“编辑层的位置”和“LoRA Rank 的大小”进行了详细的消融研究，验证了深层编辑效果优于浅层，且 Rank=4 是性能与效率的最佳平衡点。
*   **可视化验证**：通过 t-SNE 降维可视化了语义空间的聚类情况，证明了模型对重写句子（Rephrase）的泛化能力。
*   **客观性**：实验对比了多种不同范式的基准方法，且在多个指标上进行了量化分析，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：SoLA 在所有终身编辑任务中均达到或超过了现有最强基准（如 MELO），尤其在保持旧知识（TRR）方面表现卓越。
*   **解决漂移**：通过冻结 Key 和 LoRA，SoLA 彻底解决了持续更新中的语义匹配错误问题。
*   **成功实现回滚**：实验证实，删除特定 Key 后，模型能精准恢复原始输出，且不干扰其他已存在的编辑。
*   **模型规模效应**：在更大的模型（如 LLaMA-3）上，SoLA 的编辑可靠性更稳定，说明强语义表征能力有助于模型编辑。

#### 7. 优点与亮点
*   **首创性**：这是文献中首次实现**可控、可逆**的模型编辑回滚功能。
*   **参数极其高效**：相比于其他方法，SoLA 仅需极少的额外参数即可达到极高的编辑精度。
*   **架构简洁**：主决策机制消除了对外部辅助网络的依赖，简化了推理流程，支持端到端处理。
*   **稳定性高**：模块化隔离机制确保了编辑之间互不干扰，极大地缓解了灾难性遗忘。

#### 8. 不足与局限
*   **存储线性增长**：虽然单个 LoRA 模块很小，但随着编辑次数（如达到数万次以上）的增加，存储大量独立 LoRA 模块和 Key 可能会带来线性增长的存储压力。
*   **阈值敏感性**：主决策机制依赖于距离阈值 $\alpha$，在不同领域或不同模型上可能需要精细调优。
*   **泛化范围限制**：虽然支持语义重写（Rephrase）的泛化，但对于跨度极大的知识关联（如逻辑链条的连锁反应）的编辑效果尚需进一步验证。

（完）
