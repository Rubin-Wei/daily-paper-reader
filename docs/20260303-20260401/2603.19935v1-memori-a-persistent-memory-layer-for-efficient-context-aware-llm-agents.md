---
title: "Memori: A Persistent Memory Layer for Efficient, Context-Aware LLM Agents"
title_zh: Memori：用于高效、上下文感知型大语言模型智能体的持久化记忆层
authors: "Luiz C. Borro, Luiz A. B. Macarini, Gordon Tindall, Michael Montero, Adam B. Struck"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19935v1"
tags: ["query:am"]
score: 10.0
evidence: LLM智能体的持久记忆层与语义三元组
tldr: "Memori 是一个与模型无关的持久化内存层，旨在解决 LLM 智能体在多会话交互中上下文成本高和性能下降的问题。它通过将非结构化对话转换为语义三元组和摘要，实现了精准检索和连贯推理。在 LoCoMo 基准测试中，Memori 以极低的 Token 消耗实现了 81.95% 的准确率，显著降低了部署成本并提升了推理效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19935v1/fig-001.webp\", \"caption\": \"Figure 1: High-level architecture illustrating the system’s structure, data flow, and the interaction between its core components.\", \"page\": 2, \"index\": 1, \"width\": 786, \"height\": 493}, {\"url\": \"assets/figures/arxiv/2603.19935v1/fig-002.webp\", \"caption\": \"Figure 2: Accuracy of Memori across different reasoning categories. Bar heights represent the mean accuracy (%), while error bars indicate the standard deviation for n = 3 runs.\", \"page\": 5, \"index\": 2, \"width\": 786, \"height\": 490}]"
motivation: 现有的 LLM 内存方案依赖于注入大量原始对话，导致 Token 成本高昂、性能受限且存在供应商锁定问题。
method: 开发了一种高级增强流水线，将非结构化对话转化为紧凑的语义三元组和对话摘要，以实现结构化的内存管理。
result: "在 LoCoMo 基准测试中达到 81.95% 的准确率，仅使用约 5% 的上下文 Token，比全上下文方法节省了 20 倍以上的成本。"
conclusion: 研究证明有效的智能体内存应依赖于结构化表示而非单纯扩大上下文窗口，从而实现高效且可扩展的部署。
---

## 摘要
随着大语言模型（LLM）演变为自主智能体，API 层的持久化记忆对于实现跨模型和多会话交互的上下文感知行为至关重要。现有方法往往导致供应商锁定，并依赖于将大量原始对话注入提示词，从而导致高昂的 Token 成本和性能下降。我们推出了 Memori，这是一个与 LLM 无关的持久化记忆层，它将记忆视为一个数据结构化问题。其高级增强流水线将非结构化对话转换为紧凑的语义三元组和对话摘要，从而实现精确检索和连贯推理。在 LoCoMo 基准测试中，Memori 达到了 81.95% 的准确率，在每次查询仅使用 1,294 个 Token（约占全上下文的 5%）的情况下，性能优于现有的记忆系统。这显著降低了成本，包括比竞争方法减少了 67% 的 Token 使用量，且与全上下文方法相比节省了 20 倍以上的成本。这些结果表明，LLM 智能体中有效的记忆取决于结构化表示而非更大的上下文窗口，从而实现了可扩展且具有成本效益的部署。

## Abstract
As large language models (LLMs) evolve into autonomous agents, persistent memory at the API layer is essential for enabling context-aware behavior across LLMs and multi-session interactions. Existing approaches force vendor lock-in and rely on injecting large volumes of raw conversation into prompts, leading to high token costs and degraded performance.   We introduce Memori, an LLM-agnostic persistent memory layer that treats memory as a data structuring problem. Its Advanced Augmentation pipeline converts unstructured dialogue into compact semantic triples and conversation summaries, enabling precise retrieval and coherent reasoning.   Evaluated on the LoCoMo benchmark, Memori achieves 81.95% accuracy, outperforming existing memory systems while using only 1,294 tokens per query (~5% of full context). This results in substantial cost reductions, including 67% fewer tokens than competing approaches and over 20x savings compared to full-context methods.   These results show that effective memory in LLM agents depends on structured representations instead of larger context windows, enabling scalable and cost-efficient deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Memori: A Persistent Memory Layer for Efficient, Context-Aware LLM Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：随着 LLM 演变为自主智能体，持久化记忆成为其实现跨会话、跨模型一致性的核心支柱。
*   **核心问题**：现有的记忆方案（如直接将历史对话注入 Prompt）面临三大挑战：
    1.  **成本高昂**：Token 消耗随对话长度呈指数级增长。
    2.  **性能退化（Context Rot）**：长上下文会导致模型忽略关键信息或产生幻觉。
    3.  **供应商锁定**：许多记忆功能绑定在特定模型 API 中，缺乏通用性。
*   **整体含义**：论文认为记忆不应只是“存储问题”，而应是“数据结构化问题”。通过将非结构化对话提炼为结构化知识，可以在极低成本下实现高性能的上下文感知。

### 2. 方法论：Memori 架构与关键技术
Memori 作为一个解耦的记忆层，位于应用逻辑与 LLM 之间，其核心是 **Advanced Augmentation（高级增强流水线）**：
*   **语义提取与三元组生成**：系统不存储原始句子，而是将对话拆解为原子知识单元，构建为“主语-谓语-宾语”形式的**语义三元组**（Semantic Triples）。这极大地压缩了数据并提高了检索精度。
*   **对话摘要（Conversation Summarization）**：为了弥补三元组丢失的叙事流和意图演变，系统同步生成高层级的对话摘要。
*   **双层关联检索**：三元组与对应的摘要相互链接。检索时，通过向量搜索（Gemma-300 嵌入）和关键词匹配（BM25）找到相关三元组，并带入其背后的摘要背景，确保 LLM 既能获得精确事实，又能理解时间线和任务背景。

### 3. 实验设计
*   **数据集/基准测试**：使用 **LoCoMo (Long Conversation Memory)** 基准测试，该测试专门设计用于评估智能体在长达数周、多会话对话中的状态跟踪、时间推理和偏好检索能力。
*   **对比方法**：
    *   **Full-Context (Ceiling)**：将全部历史记录注入 Prompt（性能上限）。
    *   **主流记忆框架**：Zep、LangMem、Mem0。
*   **评估指标**：使用 **LLM-as-a-Judge**（基于 GPT-4o-mini）评估回答的准确性，并统计 Token 消耗和 API 成本。

### 4. 资源与算力
*   **模型使用**：实验中使用 **GPT-4o-mini** 作为生成模型和评估法官；使用 **Gemma-300** 模型进行向量嵌入。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于 Memori 侧重于推理侧的记忆管理而非模型训练，其主要算力消耗在于后台的流水线处理和向量数据库（FAISS）的检索。

### 5. 实验数量与充分性
*   **实验规模**：针对 LoCoMo 数据集中的四个核心维度（单跳推理、多跳推理、开放域、时间推理）进行了多轮测试（n=3 取平均值）。
*   **充分性评价**：
    *   **客观性**：对比了目前市面上最流行的开源记忆框架，具有较强的行业参考价值。
    *   **公平性**：使用了统一的评估提示词（Prompt）和基准数据集，并排除了干扰性的对抗性类别，确保了对比的纯粹性。
    *   **局限性**：实验主要集中在 LoCoMo 这一特定基准上，缺乏在实际生产环境（如真实客服或复杂科研助手）中的长周期实测数据。

### 6. 主要结论与发现
*   **高准确率**：Memori 达到了 **81.95%** 的综合准确率，在检索类系统中排名第一，显著优于 Zep (79.09%) 和 Mem0 (62.47%)。
*   **极致的 Token 效率**：Memori 每次查询仅需 **1,294 个 Token**，仅占全上下文方案的 **5%** 左右。
*   **成本优势**：相比 Zep 节省了 **67%** 的 Token 成本；相比全上下文方案，成本降低了 **20 倍以上**。
*   **结构化胜过规模**：实验证明，记忆的质量取决于其结构化程度，而非上下文窗口的大小。

### 7. 优点与亮点
*   **解耦设计**：作为独立记忆层，不依赖特定 LLM 供应商，具有极高的通用性。
*   **三元组+摘要双重表示**：巧妙解决了“原子事实”与“叙事背景”之间的矛盾，既精准又连贯。
*   **生产环境友好**：通过大幅降低 Token 消耗，解决了 LLM 智能体大规模部署时的经济可行性问题。

### 8. 不足与局限
*   **时间推理仍有提升空间**：虽然优于 Mem0，但在时间推理（Temporal Reasoning）上仍略逊于 LangMem 和 Zep，说明三元组结构在处理动态变化的偏好时可能存在滞后。
*   **开放域挑战**：在开放域推理中表现一般（63.54%），这反映了结构化压缩在处理模糊、宽泛查询时的固有短板。
*   **处理开销**：虽然减少了推理时的 Token 成本，但后台持续进行“高级增强”（提取三元组和摘要）会带来额外的预处理计算开销和延迟，文中未对此部分成本进行详细量化。

（完）
