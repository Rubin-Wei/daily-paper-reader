---
title: "One Model Is Enough: Native Retrieval Embeddings from LLM Agent Hidden States"
title_zh: 一个模型就够了：基于大语言模型智能体隐藏状态的原生检索嵌入
authors: Bo Jiang
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08429v1"
tags: ["query:rag"]
score: 9.0
evidence: 从 LLM 智能体隐藏状态中提取原生检索嵌入
tldr: "本研究针对LLM智能体在检索外部知识时依赖独立嵌入模型导致的复杂性和冗余问题，提出了一种原生检索方案。通过在LLM隐藏状态上添加轻量级投影头，将内部表示直接映射为检索向量，从而省去了生成文本查询和二次编码的步骤。实验表明，该方法在QReCC基准上保留了97%的基准检索质量，显著降低了系统延迟与架构复杂度，实现了“一个模型即足够”的高效检索增强。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08429v1/fig-001.webp\", \"caption\": \"Table 5: Full results for all 12 configurations across three experiment groups. Group A: loss ablation (30 epochs, lr 3× 10−4). Group B: loss weight tuning (30 epochs, lr 3× 10−4, λc = 0). Group C: training recipe variations. Baseline: Recall@10 = 0.637, MRR@10 = 0.329.\", \"page\": 8, \"index\": 1, \"width\": 1006, \"height\": 394}]"
motivation: 传统的“生成查询+独立嵌入模型”流程增加了系统复杂性和延迟，且忽略了LLM隐藏状态中已包含的丰富上下文信息。
method: 在LLM中引入轻量级投影头，并结合对齐、对比及排序蒸馏损失，将LLM的隐藏状态直接转换为检索嵌入向量。
result: "在QReCC对话搜索任务中，该方法达到了与传统流程相当的性能，成功保留了约97%的原始检索质量。"
conclusion: 证明了直接利用LLM内部隐藏状态进行原生检索的可行性，为构建更简洁、高效的检索增强型智能体提供了新路径。
---

## 摘要
检索外部知识的大语言模型（LLM）智能体通常先生成文本形式的搜索查询，然后运行一个独立的嵌入模型将其编码为向量。这种双模型流水线增加了基础设施的复杂性和延迟，且具有冗余性：LLM 已经在其隐藏状态中编码了完整的对话上下文。我们提出通过添加一个轻量级投影头，将隐藏状态直接映射到嵌入空间，从而赋予 LLM 智能体原生检索能力，消除了对独立嵌入模型的需求。通过结合对齐、对比和排名蒸馏损失进行训练，我们的方法保留了基准检索质量的 97%，同时使 LLM 智能体能够使用其自身的表示进行搜索。在 QReCC 对话搜索基准上的实验表明，与标准的“生成后编码”流水线相比，该方法具有竞争力的 Recall@10 和 MRR@10，系统的消融实验证实了各损失组件的贡献。

## Abstract
LLM agents that retrieve external knowledge typically generate a search query as text, then run a separate embedding model to encode it into a vector. This two-model pipeline adds infrastructure complexity and latency, yet is redundant: the LLM already encodes the full conversational context in its hidden states. We propose equipping LLM agents with native retrieval capability by adding a lightweight projection head that maps hidden states directly into the embedding space, eliminating the need for a separate embedding model. Trained with a combination of alignment, contrastive, and rank distillation losses, our method retains 97\% of baseline retrieval quality while enabling the LLM agent to search with its own representations. Experiments on the QReCC conversational search benchmark show competitive Recall@10 and MRR@10 compared to the standard generate-then-encode pipeline, with systematic ablations confirming the contribution of each loss component.

---

## 论文详细总结（自动生成）

### 论文总结：One Model Is Enough: Native Retrieval Embeddings from LLM Agent Hidden States

#### 1. 核心问题与整体含义（研究动机和背景）
传统的检索增强生成（RAG）流程存在**结构性冗余**。在标准流程中，LLM 智能体先生成文本查询（Query），再由一个独立的嵌入模型（Embedding Model）将其编码为向量。作者指出，LLM 在生成查询时，其内部隐藏状态（Hidden States）已经深度编码了对话上下文、用户意图和任务需求。现有的“生成再编码”模式不仅增加了推理延迟和系统复杂度，还因为将丰富的内部表示压缩为离散文本再重新编码，造成了潜在的信息损失。本研究旨在探索：**能否直接从 LLM 的隐藏状态中提取原生检索向量，从而省去独立的嵌入模型？**

#### 2. 论文提出的方法论
核心思想是在 LLM 的最后一层隐藏状态上附加一个**轻量级投影头（Projection Head）**，将其映射到现有的嵌入空间。

*   **核心架构**：
    *   **输入投影**：将 LLM 的隐藏维度（如 4096）映射到中间维度（1024）。
    *   **Transformer 编码器**：使用 2 层轻量级 Transformer 处理生成的隐藏状态序列，通过自注意力机制捕捉 token 间的依赖。
    *   **池化与输出**：采用平均池化（Mean Pooling）结合线性投影及 L2 归一化，生成最终的检索向量。
*   **训练目标（三合一损失函数）**：
    1.  **对齐损失（Alignment Loss）**：最小化投影向量与教师嵌入模型向量之间的角度距离。
    2.  **对比损失（Contrastive Loss）**：采用 InfoNCE 逻辑，确保批次内查询的区分度。
    3.  **排序蒸馏损失（Rank Distillation Loss）**：通过 KL 散度，使投影头输出的文档排序偏好与教师模型保持一致。

#### 3. 实验设计
*   **数据集**：使用 **QReCC**（对话式搜索基准），该数据集包含多轮对话、查询重写及相关的文档标注，非常适合测试需要上下文理解的检索任务。
*   **Benchmark（基准）**：标准的“生成后编码”流水线（LLM 生成文本 -> 独立 Embedding 模型编码）。
*   **对比设置**：采用同系列模型设置，以 **Qwen3-8B** 作为 LLM 智能体，**Qwen3-Embedding-8B** 作为教师模型。
*   **评估指标**：Recall@10、MRR@10 和 nDCG@10，并测量推理延迟。

#### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX PRO 6000** GPU（Blackwell 架构，98GB VRAM）。
*   **训练时长**：
    *   生成并缓存 LLM 隐藏状态轨迹（Trace）约需 **2 小时**。
    *   基于缓存轨迹训练投影头仅需 **15-20 分钟**。
*   **参数量**：投影头约 **25M** 参数，相对于 8B 的 LLM 几乎可以忽略不计。

#### 5. 实验数量与充分性
*   **实验规模**：作者设计了 **12 组配置**，涵盖了完整的消融实验（验证三种损失函数的各自贡献）、超参数调节（学习率、Epochs、Top-K 采样数）。
*   **客观性与严谨性**：
    *   引入了 **McNemar 显著性检验**来评估性能差异。
    *   计算了 **95% 自助法（Bootstrap）置信区间**。
    *   实验覆盖了从单一损失到组合损失的演进过程，证明了各组件的必要性。整体实验设计客观且逻辑严密。

#### 6. 论文的主要结论与发现
*   **性能近乎持平**：该方法保留了基准模型 **97%** 的检索质量（Recall@10 仅下降约 3%）。
*   **延迟大幅降低**：推理延迟从 43.5ms 降至 2.0ms，实现了 **21.8 倍** 的加速，彻底消除了嵌入模型的计算开销。
*   **损失函数协同**：对齐损失是基础（单独使用效果最好），排序蒸馏不能独立存在但能提供增量提升。
*   **优化敏感性**：投影任务对学习率高度敏感，较长的训练时间（80 Epochs）配合较低的学习率是达到最佳效果的关键。

#### 7. 优点
*   **架构优雅**：通过极小的插件实现了“一个模型完成所有任务”，简化了 RAG 系统的基础设施。
*   **高效性**：利用了 LLM 推理过程中产生的“副产品”（隐藏状态），几乎不增加额外计算负担。
*   **实用性强**：对于资源受限或对延迟极其敏感的智能体应用场景（如端侧 AI）具有重大价值。

#### 8. 不足与局限
*   **泛化性待验证**：实验仅限于 QReCC 单一数据集，尚未在更广泛的开放域检索任务中验证。
*   **同族模型依赖**：实验基于 Qwen3 系列，可能受益于预训练阶段的表示共享，跨模型家族（如用 Llama 投影到 OpenAI 空间）的效果未知。
*   **训练仍需教师**：虽然推理时不需要嵌入模型，但训练阶段仍需运行教师模型来生成蒸馏信号。
*   **长尾失效风险**：分析显示在处理罕见术语或复杂指代时，投影头仍略逊于完整的嵌入模型。

（完）
