---
title: "Understanding the Interplay between LLMs' Utilisation of Parametric and Contextual Knowledge: A keynote at ECIR 2025"
title_zh: 理解大语言模型对参数化知识与上下文知识利用的相互作用：ECIR 2025 主旨演讲
authors: Isabelle Augenstein
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09654v1"
tags: ["query:parametric-memory"]
score: 10.0
evidence: 大模型中参数化知识与上下文知识的相互作用
tldr: 本研究探讨了大语言模型（LLM）在处理参数知识与上下文知识时的相互作用。针对模型难以更新、知识过时及上下文与预训练记忆冲突等挑战，作者通过评估模型内部知识、开发冲突诊断测试以及分析上下文利用特征，旨在揭示模型如何整合不同来源的信息，为解决知识冲突和提升知识密集型任务表现提供见解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.09654v1/fig-001.webp\", \"caption\": \"Figure 1. Evaluation framework comparing Instance and Neuron Attribution methods [Yu et al., 2024].\", \"page\": 3, \"index\": 1, \"width\": 673, \"height\": 556}, {\"url\": \"assets/figures/arxiv/2603.09654v1/fig-002.webp\", \"caption\": \"Figure 2. Instances with varying dynamicity in the DynamicQA dataset [Marjanovic et al., 2024].\", \"page\": 4, \"index\": 2, \"width\": 1037, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2603.09654v1/fig-003.webp\", \"caption\": \"Figure 3. The DRUID dataset [Hagström et al., 2025] in comparison with prior synthetic datasets.\", \"page\": 5, \"index\": 3, \"width\": 669, \"height\": 564}]"
motivation: 随着模型规模扩大，理解并更新其内部参数知识变得困难，且模型常因预训练记忆与上下文冲突而忽略有用信息。
method: 通过评估LLM中的现有知识、设计诊断测试以揭示知识冲突，并分析成功利用上下文知识的关键特征。
result: 揭示了参数知识与上下文知识之间的复杂相互作用，包括模型内部及外部的知识冲突现象。
conclusion: 深入理解LLM对两种知识来源的利用机制，对于解决知识过时和提高模型在复杂任务中的可靠性至关重要。
---

## 摘要
语言模型 (LMs) 从训练过程中获取参数化知识，并将其嵌入到权重中。然而，语言模型日益增长的可扩展性为理解模型的内部运作机制带来了重大挑战，并且在不付出高昂重训成本的情况下更新或纠正这些嵌入知识也变得更加困难。此外，在将这些语言模型用于知识密集型语言理解任务时，模型必须整合相关的上下文，以缓解其固有的弱点，如知识不完整或陈旧。尽管如此，研究表明，语言模型经常忽略提供的上下文，因为上下文可能与预训练期间学习到的预存记忆发生冲突。冲突的知识也可能已经存在于语言模型的参数中，这被称为记忆内冲突 (intra-memory conflict)。这凸显了理解语言模型如何使用其参数化知识与检索到的上下文知识之间相互作用的重要性。在本次演讲中，我将通过介绍我们在评估语言模型现有知识、揭示知识冲突的诊断测试以及理解成功利用上下文知识的特征方面的研究，旨在阐明这一重要问题。

## 速览
**TLDR**：本研究探讨了大语言模型（LLM）在处理参数知识与上下文知识时的相互作用。针对模型难以更新、知识过时及上下文与预训练记忆冲突等挑战，作者通过评估模型内部知识、开发冲突诊断测试以及分析上下文利用特征，旨在揭示模型如何整合不同来源的信息，为解决知识冲突和提升知识密集型任务表现提供见解。 \
**Motivation**：随着模型规模扩大，理解并更新其内部参数知识变得困难，且模型常因预训练记忆与上下文冲突而忽略有用信息。 \
**Method**：通过评估LLM中的现有知识、设计诊断测试以揭示知识冲突，并分析成功利用上下文知识的关键特征进行研究。 \
**Result**：揭示了参数知识与上下文知识之间的复杂相互作用，包括模型内部及外部的知识冲突现象及其对模型输出的影响。 \
**Conclusion**：深入理解LLM对不同知识源的利用机制，对于解决知识过时、减少冲突并提升模型在复杂任务中的可靠性至关重要。

---

## Abstract
Language Models (LMs) acquire parametric knowledge from their training process, embedding it within their weights. The increasing scalability of LMs, however, poses significant challenges for understanding a model's inner workings and further for updating or correcting this embedded knowledge without the significant cost of retraining. Moreover, when using these language models for knowledge-intensive language understanding tasks, LMs have to integrate relevant context, mitigating their inherent weaknesses, such as incomplete or outdated knowledge. Nevertheless, studies indicate that LMs often ignore the provided context as it can be in conflict with the pre-existing LM's memory learned during pre-training. Conflicting knowledge can also already be present in the LM's parameters, termed intra-memory conflict. This underscores the importance of understanding the interplay between how a language model uses its parametric knowledge and the retrieved contextual knowledge. In this talk, I will aim to shed light on this important issue by presenting our research on evaluating the knowledge present in LMs, diagnostic tests that can reveal knowledge conflicts, as well as on understanding the characteristics of successfully used contextual knowledge.

---

## 论文详细总结（自动生成）

这篇论文是 Isabelle Augenstein 教授在 ECIR 2025 会议上的主旨演讲摘要，系统性地总结了其研究团队在大型语言模型（LLM）**参数化知识（内部记忆）**与**上下文知识（外部检索）**相互作用领域的最新进展。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：LLM 在处理知识密集型任务时，如何平衡其在预训练阶段习得的“参数化知识”与推理时提供的“上下文知识”？
*   **研究动机**：
    *   **知识更新困境**：LLM 规模巨大，通过重训来更新或纠正错误知识成本极高。
    *   **知识冲突**：当外部检索到的信息（如 RAG 场景）与模型内部记忆不一致时，模型往往会忽略正确上下文或产生幻觉。
    *   **黑盒特性**：目前尚不清楚模型内部哪些神经元或训练数据决定了特定的知识输出，导致难以进行精准的知识编辑。

### 2. 论文提出的方法论
论文通过三个维度的研究构建了理解知识相互作用的框架：
*   **归因分析框架（Attribution Framework）**：
    *   提出一种统一评估框架，将“实例归因”（识别最具影响力的训练数据）与“神经元归因”（识别 MLP 层中关键神经元）相结合。
    *   通过“忠实度测试”（包含充分性与完整性测试）来验证神经元激活与模型预测之间的因果关系。
*   **冲突探测技术（Conflict Probing）**：
    *   **记忆内冲突（Intra-memory conflict）**：使用“语义熵（Semantic Entropy）”衡量模型内部对同一事实表达的不一致性。
    *   **上下文-记忆冲突（Context-memory conflict）**：提出“连贯说服得分（Coherent Persuasion Score）”，衡量模型在面对冲突上下文时输出分布的偏移程度。
*   **真实场景评估（Real-world Evaluation）**：
    *   开发了 **DRUID** 数据集，弃用以往研究常用的合成数据，改用真实世界的声明验证（Claim Verification）场景。
    *   提出 **ACU（实际上下文利用率）** 指标，量化模型在多大程度上依赖了检索到的证据。

### 3. 实验设计
*   **数据集与场景**：
    *   **DynamicQA**：包含静态事实（如首都）、时间性事实（如现任总统）和争议性事实（不同观点）。
    *   **DRUID**：基于真实事实核查来源的声明验证数据集。
    *   **对比基准**：CounterFact、ConflictQA 等合成数据集。
*   **对比方法**：
    *   对比了多种归因方法（Instance vs. Neuron）。
    *   对比了不同规模的 LLM（从小型模型到前沿大模型）。
    *   测试了多种“上下文操纵技术（CMT）”，包括提示词工程和机械干预。

### 4. 资源与算力
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **相关提及**：作者提到归因分析（尤其是全参数级别）的计算成本极高，因此在实验中主要聚焦于分析 MLP 分类层而非全模型权重。

### 5. 实验数量与充分性
*   **实验规模**：论文总结了多项独立研究（Yu et al. 2024, Marjanovic et al. 2024, Hagström et al. 2025 等），涵盖了从底层神经元分析到高层 RAG 应用的多个维度。
*   **充分性评价**：实验设计较为充分。通过对比“合成数据”与“真实数据”，揭示了现有研究在合成环境下可能夸大某些效应（如知识冲突的影响）的风险。同时，引入了消融实验来验证神经元移除对预测的影响。

### 6. 主要结论与发现
*   **神经元分布的迷思**：发现大部分 MLP 神经元可以被移除而不影响预测，暗示知识可能更多地存储在注意力权重（Attention Weights）中，而非仅在 MLP 层。
*   **反直觉的说服效应**：模型更容易被关于“静态事实”的错误信息误导（说服），而对于经常变动的“动态事实”，模型反而表现出更强的抗干扰性（更难被上下文改变）。
*   **真实 RAG 的特征**：在真实场景下，模型更倾向于利用语气坚定、直接、且与查询相似度高的上下文；长上下文会降低模型的忠实度。
*   **合成数据的局限**：合成数据集往往会夸大“上下文排斥”现象，而在真实数据中，这种冲突的表现更为复杂且难以预测。

### 7. 优点
*   **现实对齐**：强调了从合成实验转向真实世界数据（如 DRUID 数据集）的重要性，为 RAG 研究提供了更可靠的基准。
*   **多维度视角**：不仅关注模型“输出了什么”，还通过归因分析探讨了“为什么输出”，试图打开 LLM 的黑盒。
*   **指标创新**：提出了 ACU 和连贯说服得分等新指标，能够更精细地刻画知识冲突。

### 8. 不足与局限
*   **归因局限性**：目前的归因分析主要集中在 MLP 层，可能忽略了 Transformer 架构中注意力机制对知识存储的贡献。
*   **缺乏通用解决方案**：研究发现不同的上下文操纵技术（CMT）在不同模型上表现不一，没有一种万能的方法能完美解决知识冲突。
*   **计算成本**：深层机制分析（如机械干预）在大规模模型上的可扩展性仍面临挑战。

（完）
