---
title: Boosting ASR Robustness via Test-Time Reinforcement Learning with Audio-Text Semantic Rewards
title_zh: 通过结合音文语义奖励的测试时强化学习提升 ASR 鲁棒性
authors: "Linghan Fang, Tianxin Xie, Li Liu"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05231v1"
tags: ["query:ttt"]
score: 10.0
evidence: 针对分布偏移下语音识别的无标签测试时自适应
tldr: 针对自动语音识别（ASR）系统在面对噪声和口音等分布偏移数据时鲁棒性不足的问题，本文提出了一种名为 ASR-TRA 的测试时强化学习自适应框架。该方法通过引入可学习的解码器提示词和随机解码生成多样化候选文本，并利用音频-文本语义对齐奖励模型进行强化学习更新。实验证明，该方法在保持低延迟的同时，显著提升了模型在复杂现实环境下的识别准确率和稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.05231v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of test-time adaptation strategies under noisy conditions. (a) Confidence-driven adaptation reinforces incorrect but high-probability predictions (e.g., word), leading to persistent errors. (b) Our reward-guided adaptation ASR-TRA favors semantically accurate alternatives (e.g., world) through reinforcement learning, even when their initial confidence is low.\", \"page\": 1, \"index\": 1, \"width\": 498, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2603.05231v1/fig-002.webp\", \"caption\": \"Table 1: WER (%) and inference latency (seconds) on Whisper-Tiny under eight noise conditions from MS-SNSD (SNR = 10 dB). Best values per row are in bold.\", \"page\": 6, \"index\": 2, \"width\": 864, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2603.05231v1/fig-003.webp\", \"caption\": \"Table 3: Ablation study of accuracy–latency trade-offs under different adaptation configurations on noisy LibriSpeech testother. Each row enables a combination of prompt tuning, parameter finetuning, and reward feedback (CLAP, LLM, or both). WER (↓) and average inference latency (s/utterance) are reported for Whisper-Tiny and Whisper-Base.\", \"page\": 7, \"index\": 3, \"width\": 776, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2603.05231v1/fig-004.webp\", \"caption\": \"Figure 4: Test-time self-adaptation for Whisper. A baseline transcript is decoded from the input Mel-spectrogram; soft-prompted variants are then sampled at varied temperatures, CLAP scores them all, and the aggregated rewards update the model, achieving on-the-fly correction without labels.\", \"page\": 4, \"index\": 4, \"width\": 1047, \"height\": 275}]"
motivation: 现有的测试时自适应方法主要依赖伪标签或熵最小化，容易导致确认偏差并强化高置信度的错误。
method: 提出 ASR-TRA 框架，利用可学习提示词和随机解码生成候选文本，并通过音频-文本语义对齐奖励进行强化学习优化。
result: 在 LibriSpeech 噪声和 L2 Arctic 口音数据集上的实验显示，该方法在提高识别准确率的同时保持了较低的推理延迟。
conclusion: 该研究为 ASR 系统在挑战性现实环境中的部署提供了一种高效、鲁棒且具有解释性的测试时自适应解决方案。
---

## 摘要
近年来，自动语音识别（ASR）系统（如 Whisper）在准确率上取得了显著提升，但对于现实世界中未见的数据（具有较大分布偏移的数据）仍高度敏感，包括噪声环境和多样的口音。为了解决这一问题，测试时自适应（TTA）在无需真实标签的情况下提高模型推理时的适应能力方面展现出巨大潜力，而现有的 TTA 方法通常依赖于伪标签或熵最小化。然而，通过将模型置信度作为学习信号，这些方法可能会强化高置信度的错误，导致确认偏差，从而损害自适应效果。为了克服这些局限性，我们提出了 ASR-TRA，这是一种受因果干预启发的创新测试时强化自适应框架。具体而言，我们的方法引入了一个可学习的解码器提示词，并利用温度控制的随机解码来生成多样化的转录候选。这些候选由一个衡量音文语义对齐程度的奖励模型进行评分，所得反馈通过强化学习用于更新模型和提示词参数。在带有合成噪声的 LibriSpeech 和 L2 Arctic 英语口音数据集上进行的全面实验表明，与现有的 TTA 基准相比，我们的方法在保持较低延迟的同时实现了更高的准确率。消融实验进一步证实了结合音频和语言奖励的有效性，突显了我们方法增强的稳定性和可解释性。总之，我们的方法为在具有挑战性的现实环境条件下部署 ASR 系统提供了一种实用且鲁棒的解决方案。

## Abstract
Recently, Automatic Speech Recognition (ASR) systems (e.g., Whisper) have achieved remarkable accuracy improvements but remain highly sensitive to real-world unseen data (data with large distribution shifts), including noisy environments and diverse accents. To address this issue, test-time adaptation (TTA) has shown great potential in improving the model adaptability at inference time without ground-truth labels, and existing TTA methods often rely on pseudo-labeling or entropy minimization. However, by treating model confidence as a learning signal, these methods may reinforce high-confidence errors, leading to confirmation bias that undermines adaptation. To overcome these limitations, we present ASR-TRA, a novel Test-time Reinforcement Adaptation framework inspired by causal intervention. More precisely, our method introduces a learnable decoder prompt and utilizes temperature-controlled stochastic decoding to generate diverse transcription candidates. These are scored by a reward model that measures audio-text semantic alignment, and the resulting feedback is used to update both model and prompt parameters via reinforcement learning. Comprehensive experiments on LibriSpeech with synthetic noise and L2 Arctic accented English datasets demonstrate that our method achieves higher accuracy while maintaining lower latency than existing TTA baselines. Ablation studies further confirm the effectiveness of combining audio and language-based rewards, highlighting our method's enhanced stability and interpretability. Overall, our approach provides a practical and robust solution for deploying ASR systems in challenging real-world conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Boosting ASR Robustness via Test-Time Reinforcement Learning with Audio-Text Semantic Rewards》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机与背景）
自动语音识别（ASR）模型（如 Whisper）虽然在标准数据集上表现优异，但在面对现实世界中的**分布偏移（Out-of-Distribution, OOD）**——如背景噪声、重口音或方言时，性能会大幅下降。
*   **现有技术的局限**：目前的测试时自适应（TTA）方法多依赖于“熵最小化”或“伪标签”。这种机制存在**确认偏差（Confirmation Bias）**，即模型会强化自身高置信度的错误预测（“盲目自信”），导致在恶劣环境下自适应效果适得其反。
*   **核心目标**：提出一种无需地面真值标签、不依赖模型内部置信度，而是通过外部语义奖励引导的鲁棒性自适应框架。

### 2. 论文提出的方法论：ASR-TRA
该论文提出了 **ASR-TRA**（ASR with Test-time Reinforcement Adaptation），其核心思想是将 TTA 建模为一个受因果干预启发的强化学习过程。

*   **核心思想**：利用因果干预（插入可学习提示词）和随机采样生成多个候选文本，通过外部预训练模型（CLAP）评估音文对齐程度作为奖励，进而更新模型。
*   **关键技术细节**：
    1.  **结构化因果模型 (SCM)**：将音频特征 $A$、可学习提示词 $P$、生成的转录 $Y$ 和奖励 $R$ 建立因果联系。
    2.  **提示词注入 (Prompt Injection)**：在 Whisper 解码器输入端插入长度为 4 的可学习向量 $P$，作为对生成过程的“因果干预”。
    3.  **反事实采样与奖励评估**：通过调整温度参数 $T$ 进行随机解码，生成 $K$ 个候选转录。使用 **CLAP**（音频-文本对比预训练）模型计算音频与候选文本的余弦相似度，作为标量奖励。
    4.  **强化学习优化**：采用 **REINFORCE** 算法，以平均奖励为基准（Baseline）计算优势函数，通过策略梯度同时更新提示词 $P$ 和模型参数 $\theta$。
*   **算法流程**：对每个测试样本，先进行确定性解码，再进行多次带干预的随机采样，计算奖励并执行梯度更新，最后输出自适应后的预测结果，并重置模型状态。

### 3. 实验设计
*   **数据集/场景**：
    1.  **噪声鲁棒性**：LibriSpeech (test-other) 叠加 MS-SNSD 噪声库中的 8 种环境噪声（如空调、机场、嘈杂人声等），信噪比（SNR）设为 10dB。
    2.  **口音鲁棒性**：L2-Arctic 数据集，包含 6 种非母语英语口音（阿拉伯语、中文、印地语、韩语、西班牙语、越南语）。
*   **Benchmark 与对比方法**：
    *   **Baseline**：原始 Whisper-Tiny 模型。
    *   **SUTA**：基于熵最小化和高置信度伪标签的 ASR TTA 方法。
    *   **SGEM**：基于序列级广义熵最小化的方法。
*   **评估指标**：词错误率（WER）和推理延迟（Latency）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 6000 Ada** GPU。
*   **模型规模**：主要针对 **Whisper-Tiny** (39M 参数) 进行实验，消融实验中涉及了 **Whisper-Base** (74M 参数)。
*   **训练/适配时长**：论文未明确给出总训练时长，但提供了推理延迟数据。ASR-TRA 的平均延迟约为 **0.720s/样本**，比 SUTA (1.690s) 更快，略快于或等同于 SGEM。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 种噪声场景和 6 种口音背景，每种场景均有详细的 WER 对比。
*   **消融实验**：非常充分。分别验证了“仅微调模型”、“仅提示词微调”、“不同奖励模型（CLAP vs LLM）”以及“模型大小”对结果的影响。
*   **客观性与公平性**：
    *   作者专门针对“高置信度样本”进行了子集分析，揭示了基准方法在模型“盲目自信”时的失效，证明了 ASR-TRA 的优越性并非偶然。
    *   对比实验均在相同的硬件和模型架构下进行，具有较高的公平性。

### 6. 主要结论与发现
*   **性能领先**：ASR-TRA 在噪声环境下的平均 WER 为 28.64%（Baseline 为 32.71%），在口音环境下的平均 WER 为 28.21%（Baseline 为 32.11%），均优于 SUTA 和 SGEM。
*   **解决“盲目自信”问题**：在模型初始置信度极高但预测错误的样本上，ASR-TRA 能将 WER 从 83.61% 降至 45.17%，而 SUTA 反而会使错误率恶化至 122.37%。
*   **效率优势**：通过并行采样和轻量化提示词更新，ASR-TRA 在提升准确率的同时，保持了比同类方法更低的推理延迟。

### 7. 优点（亮点）
*   **因果启发**：将提示词视为因果干预，为 ASR 的自适应提供了新的理论视角。
*   **外部奖励引导**：引入 CLAP 作为外部“裁判”，有效规避了传统 TTA 方法中常见的确认偏差和错误累积。
*   **即插即用与轻量化**：仅需极少量的额外参数（1536 个），且无需离线重训，适合边缘设备部署。
*   **解释性强**：通过消融实验和相关性分析（CLAP 分数与 WER 负相关），证明了奖励信号的有效性。

### 8. 不足与局限
*   **奖励模型的局限**：CLAP 目前主要支持英文，这限制了该框架在多语言 ASR 上的扩展性。
*   **LLM 延迟问题**：虽然引入大语言模型（如 DeepSeek V3）作为奖励能进一步降低 WER，但会导致延迟增加 7-9 倍，难以满足实时性要求。
*   **单句适配限制**：目前主要针对单句（Single-utterance）进行适配，未探讨在长音频流或连续对话中如何利用历史上下文进行持续学习。

（完）
