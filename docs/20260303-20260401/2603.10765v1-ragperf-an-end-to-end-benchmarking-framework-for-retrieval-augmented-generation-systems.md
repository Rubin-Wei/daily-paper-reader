---
title: "RAGPerf: An End-to-End Benchmarking Framework for Retrieval-Augmented Generation Systems"
title_zh: RAGPerf：检索增强生成系统的端到端基准测试框架
authors: "Shaobo Li, Yirui Zhou, Yuan Xu, Kevin Chen, Daniel Waddington, Swaminathan Sundararaman, Hubertus Franke, Jian Huang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10765v1"
tags: ["query:rag"]
score: 9.0
evidence: 检索增强生成系统的基准测试框架
tldr: RAGPerf是一个针对检索增强生成（RAG）系统的端到端基准测试框架。它通过将RAG工作流解耦为嵌入、索引、检索、重排序和生成等模块，支持对不同组件参数的细粒度性能分析。该框架集成了多种数据集、向量数据库和LLM，能自动收集吞吐量、资源利用率及准确性指标，为优化RAG系统提供了全面的评估工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.10765v1/fig-001.webp\", \"caption\": \"Figure 4: API for supporting different RAG pipelinemodules.\", \"page\": 6, \"index\": 1, \"width\": 498, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.10765v1/fig-002.webp\", \"caption\": \"Figure 9: Latency of the text pipeline under update workload.\", \"page\": 11, \"index\": 2, \"width\": 454, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2603.10765v1/fig-003.webp\", \"caption\": \"Figure 7: Resource utilization breakdown for different RAG applications.\", \"page\": 10, \"index\": 3, \"width\": 983, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2603.10765v1/fig-004.webp\", \"caption\": \"Table 1: Popular RAG-based applications.\", \"page\": 3, \"index\": 4, \"width\": 506, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2603.10765v1/fig-005.webp\", \"caption\": \"Table 4: Models used for generation, embedding, and rerank.\", \"page\": 8, \"index\": 5, \"width\": 456, \"height\": 354}]"
motivation: 现有的RAG系统缺乏一个能够对复杂工作流进行细粒度性能分析和端到端评估的标准化基准测试框架。
method: 设计并实现了一个模块化的框架，支持配置核心参数、模拟多样化工作负载，并自动化采集性能与质量指标。
result: 实验证明RAGPerf能够有效表征不同配置对系统行为的影响，且框架本身带来的性能开销极低。
conclusion: RAGPerf为RAG系统的性能调优和架构选择提供了强有力的支持，其开源特性有助于推动该领域的标准化评估。
---

## 摘要
我们介绍了 RAGPerf 的设计与实现，这是一个基于 RAG 的 AI 系统基准测试框架，用于表征 RAG 流水线的系统行为。为了便于详细的性能剖析和细粒度性能分析，RAGPerf 将 RAG 工作流解耦为几个模块化组件：嵌入（embedding）、索引（indexing）、检索（retrieval）、重排序（reranking）和生成（generation）。RAGPerf 为用户提供了配置各组件核心参数的灵活性，并能检查这些参数对端到端查询性能和质量的影响。RAGPerf 包含一个负载生成器，通过支持多样化的数据集（如文本、PDF、代码和音频）、不同的检索与更新比例以及查询分布，来模拟真实世界场景。RAGPerf 还支持不同的嵌入模型、主流向量数据库（如 LanceDB、Milvus、Qdrant、Chroma 和 Elasticsearch）以及用于内容生成的不同大语言模型（LLM）。它实现了性能指标（即端到端查询吞吐量、主机/GPU 内存占用和 CPU/GPU 利用率）和准确性指标（即上下文召回率、查询准确性和事实一致性）的自动化采集。我们通过一系列综合实验展示了 RAGPerf 的功能，并在 GitHub 上开源了其代码库。我们的评估表明，RAGPerf 产生的性能开销可以忽略不计。

## Abstract
We present the design and implementation of a RAG-based AI system benchmarking (RAGPerf) framework for characterizing the system behaviors of RAG pipelines. To facilitate detailed profiling and fine-grained performance analysis, RAGPerf decouples the RAG workflow into several modular components - embedding, indexing, retrieval, reranking, and generation. RAGPerf offers the flexibility for users to configure the core parameters of each component and examine their impact on the end-to-end query performance and quality. RAGPerf has a workload generator to model real-world scenarios by supporting diverse datasets (e.g., text, pdf, code, and audio), different retrieval and update ratios, and query distributions. RAGPerf also supports different embedding models, major vector databases such as LanceDB, Milvus, Qdrant, Chroma, and Elasticsearch, as well as different LLMs for content generation. It automates the collection of performance metrics (i.e., end-to-end query throughput, host/GPU memory footprint, and CPU/GPU utilization) and accuracy metrics (i.e., context recall, query accuracy, and factual consistency). We demonstrate the capabilities of RAGPerf through a comprehensive set of experiments and open source its codebase at GitHub. Our evaluation shows that RAGPerf incurs negligible performance overhead.

---

## 论文详细总结（自动生成）

### 论文总结：RAGPerf - 检索增强生成系统的端到端基准测试框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在专业领域的应用，检索增强生成（RAG）技术成为解决模型幻觉、引入私有知识的关键。然而，RAG 系统包含嵌入、索引、检索、重排序和生成等多个复杂环节，现有的基准测试存在明显局限：
*   **维度单一**：多数基准（如 RAGBench, TruthfulQA）仅关注语义准确性，忽视了系统层面的性能（如延迟、吞吐量、资源利用率）。
*   **组件孤立**：部分工具（如 VectorDBBench）仅测试向量数据库，无法捕捉全链路集成时的资源竞争和瓶颈转移。
*   **缺乏灵活性**：难以适配多样化的数据集、多模态输入及动态的数据更新场景。
**RAGPerf** 的提出旨在填补这一空白，提供一个可重现、模块化且端到端的性能与质量评估框架。

#### 2. 方法论：核心思想与关键技术
RAGPerf 的核心思想是将 RAG 流程**解耦**并**标准化**，通过统一接口实现各组件的灵活替换与细粒度监控。
*   **模块化架构**：定义了 `BaseEmbedder`、`DBInstance`、`BaseReranker` 和 `BaseLLM` 等抽象基类，支持用户无缝集成自定义模型或数据库。
*   **负载生成器（Workload Generator）**：
    *   支持“增、删、改、查”四种基本操作。
    *   引入 **Zipfian 分布** 模拟真实世界中的“热点”访问模式。
    *   **动态真值生成**：针对数据更新操作，利用 LLM（如 DistilBERT 和 T5）自动生成修改后的事实及对应的问答对，以验证系统对新知识的检索准确性。
*   **低开销监控系统**：通过多线程守护进程定期采样 Linux `procfs`（CPU/内存/IO）和 NVIDIA `NVML`（GPU SM 利用率、显存、带宽），确保监控对主任务的干扰降至最低（实验证明开销 < 0.26%）。

#### 3. 实验设计
*   **数据集与场景**：
    *   **文本**：Wikipedia (19.3GB) + Natural Questions。
    *   **PDF/图像**：Arxiv (48GB) 论文，支持 OCR 提取和多模态嵌入（ColPali）。
    *   **代码**：GitHub 仓库 (32GB)。
    *   **音频**：The People’s Speech (35.5GB)，使用 Whisper 进行转录。
*   **对比对象**：
    *   **向量数据库**：LanceDB, Milvus, Qdrant, Chroma, Elasticsearch。
    *   **模型**：Qwen2.5 (7B/72B), Llama-3.3, DeepSeek-R1, GPT-20B 等。
*   **评估指标**：
    *   **性能**：端到端延迟分解、吞吐量 (QPS)、硬件利用率。
    *   **质量**：上下文召回率 (Context Recall)、事实一致性 (Factual Consistency)、查询准确性 (Query Accuracy)。

#### 4. 资源与算力
*   **硬件配置**：
    *   **CPU**：2个 32核 AMD EPYC 9334。
    *   **内存**：1.4TB DDR5 主机内存。
    *   **GPU**：2个 NVIDIA H100 NVL (每张 94GB 显存)。
    *   **存储**：2TB Samsung 990 Pro SSD。
*   **软件环境**：Ubuntu 22.04, PyTorch 2.8.0, vLLM 作为推理后端。
*   **训练/运行时间**：文中未详细列出总运行时间，但展示了 60 分钟持续更新负载下的系统表现。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验，包括：
*   **端到端延迟分解**：针对不同模态和数据库的耗时占比分析。
*   **资源利用率剖析**：展示了不同阶段（如检索 vs. 生成）对 CPU 和 GPU 需求的动态变化。
*   **敏感性分析**：测试了 Batch Size（32-512）、嵌入维度、索引类型（HNSW, IVF, FLAT 等）对性能的影响。
*   **资源受限测试**：模拟不同 CPU 核心数、主机内存和显存容量下的系统降级情况。
实验设计覆盖了从宏观吞吐量到微观硬件瓶颈的各个层面，对比方法广泛，具有很高的客观性和说服力。

#### 6. 主要结论与发现
*   **瓶颈转移**：在文本 RAG 中，LLM 生成阶段占总延迟的 75%-91%；但在 PDF RAG 中，重排序（Reranking）可能成为主要瓶颈（占比高达 87%）。
*   **更新代价**：为了保证数据新鲜度，启用临时平坦索引（Flat Index）会导致查询延迟随更新量线性增长，直到索引重建。
*   **硬件敏感性**：CPU 核心数对吞吐量影响较小，但主机内存不足会强制使用磁盘索引，导致性能下降 85% 以上；GPU 显存是核心瓶颈，直接限制了 Batch Size 和模型规模。
*   **批处理悖论**：增加 Batch Size 虽能提升吞吐量，但过大的 Batch 会挤占 KV Cache 空间，反而导致性能下降。

#### 7. 优点
*   **全链路覆盖**：首个真正实现从原始数据预处理到最终生成结果全流程监控的框架。
*   **动态性支持**：能够模拟真实生产环境中的数据频繁更新，而非仅针对静态数据集。
*   **多模态兼容**：内置了对 PDF、音频、图像等非结构化数据的处理逻辑。
*   **极低开销**：监控模块设计精巧，对被测系统性能影响微乎其微。

#### 8. 不足与局限
*   **硬件平台局限**：目前的 GPU 监控高度依赖 NVIDIA 的 NVML 库，对于 AMD (ROCm) 或国产算力芯片的支持尚未提及。
*   **评估成本**：质量评估（Ragas）依赖于 LLM 作为裁判，在大规模基准测试中可能会产生较高的 API 调用成本或本地计算开销。
*   **长文本挑战**：虽然涉及了 PDF，但对于超长上下文（Long-context）导致的 KV Cache 管理复杂度探讨尚浅。

（完）
