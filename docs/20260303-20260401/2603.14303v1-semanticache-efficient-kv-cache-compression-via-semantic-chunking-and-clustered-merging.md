---
title: "SemantiCache: Efficient KV Cache Compression via Semantic Chunking and Clustered Merging"
title_zh: SemantiCache：通过语义分块与聚类合并实现的高效 KV 缓存压缩
authors: "Shunlong Wu, Hai Lin, Shaoshen Chen, Tingwei Lu, Yongqin Zeng, Shaoxiong Zhan, Hai-Tao Zheng, Hong-Gee Kim"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14303v1"
tags: ["query:cd"]
score: 9.0
evidence: 通过语义分块进行KV缓存压缩以实现高效推理
tldr: 针对现有KV缓存压缩方法因破坏语义连贯性导致性能下降的问题，本文提出SemantiCache框架。该方法通过分隔符进行语义分块，并利用贪心种子聚类算法（GSC）将块内token聚合成语义核心，辅以比例注意力机制平衡权重。实验证明，该方法在保持模型性能的同时，显著降低了内存占用，并将推理解码速度提升了高达2.61倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.14303v1/fig-001.webp\", \"caption\": \"Table 1. Average scores on LongBench for different compression methods, evaluated across various cache budgets and models.\", \"page\": 4, \"index\": 1, \"width\": 512, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2603.14303v1/fig-002.webp\", \"caption\": \"Fig. 1. An overview of SemantiCache.\", \"page\": 2, \"index\": 2, \"width\": 500, \"height\": 376}]"
motivation: 现有的KV缓存压缩方法通常基于离散token或非语义分块，容易导致语义碎片化和不可逆的信息丢失。
method: 提出SemantiCache框架，通过分隔符划分语义块，并利用贪心种子聚类算法将token合并为语义核心，结合比例注意力机制进行优化。
result: 实验显示该方法在多种基准测试中实现了高达2.61倍的解码加速，并大幅减少了内存占用。
conclusion: SemantiCache通过对齐语言的语义层次结构，在显著提升大模型推理效率的同时，有效维持了模型的性能表现。
---

## 摘要
现有的 KV 缓存压缩方法通常作用于离散的 token 或非语义分块。然而，这类方法往往会导致语义碎片化，破坏语言上的连贯单元，从而造成不可逆的信息损失和模型性能下降。为解决这一问题，我们提出了 SemantiCache，这是一种新型压缩框架，通过使压缩过程与语言的语义层次结构保持一致来维护语义完整性。具体而言，我们首先利用分隔符（天然的语义边界）将缓存划分为语义连贯的分块。在每个分块内部，我们引入了一种计算高效的基于种子的贪婪聚类（GSC）算法，将 token 归类为语义簇。这些簇被进一步合并为语义核心，并辅以比例注意力（Proportional Attention）机制，以重新平衡合并 token 后减少的注意力贡献。在多种基准测试和模型上的广泛实验表明，SemantiCache 将推理的解码阶段加速了高达 2.61 倍，并显著减少了内存占用，同时保持了与原始模型相当的性能。

## Abstract
Existing KV cache compression methods generally operate on discrete tokens or non-semantic chunks. However, such approaches often lead to semantic fragmentation, where linguistically coherent units are disrupted, causing irreversible information loss and degradation in model performance. To address this, we introduce SemantiCache, a novel compression framework that preserves semantic integrity by aligning the compression process with the semantic hierarchical nature of language. Specifically, we first partition the cache into semantically coherent chunks by delimiters, which are natural semantic boundaries. Within each chunk, we introduce a computationally efficient Greedy Seed-Based Clustering (GSC) algorithm to group tokens into semantic clusters. These clusters are further merged into semantic cores, enhanced by a Proportional Attention mechanism that rebalances the reduced attention contributions of the merged tokens. Extensive experiments across diverse benchmarks and models demonstrate that SemantiCache accelerates the decoding stage of inference by up to 2.61 times and substantially reduces memory footprint, while maintaining performance comparable to the original model.

---

## 论文详细总结（自动生成）

以下是对论文《SemantiCache: Efficient KV Cache Compression via Semantic Chunking and Clustered Merging》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大语言模型（LLM）在处理长文本时，KV Cache（键值缓存）随序列长度线性增长，导致巨大的内存开销和推理延迟。
*   **研究动机**：现有的压缩方法（如剔除法或简单的合并法）通常基于离散 Token 或固定大小的分块，忽略了语言的自然语义结构。这会导致“语义碎片化”（Semantic Fragmentation），即破坏了短语、从句或句子的完整性，造成不可逆的信息丢失和性能下降。
*   **整体含义**：论文旨在模仿人类阅读长文本时的认知策略（先分段理解，再提炼核心概念），通过对齐语言的语义层次结构来实现高效且无损的 KV 缓存压缩。

### 2. 方法论：SemantiCache 框架
SemantiCache 采用三阶段的语义层次化流水线：
*   **语义分块 (Semantic Chunking)**：利用自然语言分隔符（如标点符号、换行符等）作为边界，将 KV 缓存划分为语义连贯的分块。分隔符本身的 KV 状态被完整保留，作为结构锚点。
*   **相似性聚类 (Similarity Clustering)**：在每个分块内部，使用**基于种子的贪婪聚类算法 (GSC)**。该算法通过计算 Key 向量之间的余弦相似度，将语义相近的 Token 归为一簇。GSC 是一种单次扫描的高效算法，避免了复杂的迭代计算。
*   **聚类合并与比例注意力 (Clustered Merging & Proportional Attention)**：
    *   将每个簇内的 KV 状态通过均值池化（Mean-pooling）合并为一个“语义核心”。
    *   **关键技术细节**：为了防止合并后信息被稀释，引入了**比例注意力机制**。在计算 Attention Score 时，在 Softmax 之前的 Logits 中加入簇大小的对数值（$\log s$），确保合并后的核心在注意力分配中与其原始包含的 Token 数量成比例。

### 3. 实验设计
*   **数据集/场景**：
    *   **LongBench**：包含 21 个数据集的多任务基准，涵盖单/多文档问答、摘要、少样本学习、代码补全等。
    *   **Needle-in-a-Haystack (NIAH)**：测试模型在长文档中检索特定信息的能力。
*   **基准模型**：Llama-3-8B-Instruct 和 Mistral-7B-Instruct-v0.2。
*   **对比方法**：
    *   **剔除类 (Eviction-based)**：StreamingLLM, H2O, SnapKV。
    *   **合并类 (Merging-based)**：CaM, D2O。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100 80GB GPU** 上进行。
*   **训练/推理时长**：论文未明确提及具体的训练时长（该方法主要为推理侧的 Training-free 方案），但给出了推理效率数据：在 32k 上下文下，解码速度（TPOT）提升了 2.61 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流模型上进行了 20%、35%、50% 三种不同缓存预算的对比实验。
    *   针对 8k 和 32k 长度进行了大海捞针实验。
    *   进行了关于分块策略（语义 vs 固定大小 vs 无分块）和超参数 $\tau$（相似度阈值）的消融实验。
*   **充分性与客观性**：实验覆盖了长文本处理的主要维度（理解、检索、效率），对比了当前最先进的（SOTA）基线方法。实验设计较为客观，通过多维度指标（准确率、TTFT、TPOT、内存占用）全面评估了性能。

### 6. 主要结论与发现
*   **性能优越**：SemantiCache 在所有测试预算下均优于现有的剔除和合并方法，尤其在高压缩率下表现更稳健。
*   **效率提升**：在 Llama-3-8B 模型上实现了 **2.61 倍的解码加速**，并显著降低了 GPU 内存占用（从 24.27GB 降至 15.94GB）。
*   **语义完整性至关重要**：消融实验证明，基于分隔符的语义分块比固定大小分块能显著提高模型得分，验证了避免语义碎片化的必要性。

### 7. 优点与亮点
*   **认知启发**：方法设计借鉴了人类处理长文本的认知策略，具有较强的直观解释性。
*   **计算高效**：提出的 GSC 聚类算法是轻量级的，相比其他复杂的合并方法，其首字延迟（TTFT）更低。
*   **解决稀释问题**：通过比例注意力机制解决了合并方法中常见的“信息稀释”痛点，使合并后的向量能代表原始 Token 的权重。

### 8. 不足与局限
*   **分隔符依赖**：语义分块高度依赖预定义的分隔符集合，对于缺乏明显标点结构的文本（如某些代码片段或特定专业领域文本）可能效果受限。
*   **超参数敏感性**：相似度阈值 $\tau$ 对压缩率和性能的影响较大，不同模型或任务可能需要手动调优该参数以达到最佳平衡。
*   **极端长文本测试**：实验主要集中在 32k 长度，对于更极端长度（如 128k 或 1M）的性能表现和聚类开销尚待进一步验证。

（完）
