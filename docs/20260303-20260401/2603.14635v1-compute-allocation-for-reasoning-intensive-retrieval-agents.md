---
title: Compute Allocation for Reasoning-Intensive Retrieval Agents
title_zh: 推理密集型检索智能体的计算分配
authors: "Sreeja Apparaju, Nilesh Gupta"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14635v1"
tags: ["query:am"]
score: 9.0
evidence: 长周期智能体的存储与检索
tldr: 本研究探讨了长程智能体在处理推理密集型检索任务时的算力分配策略。通过在 BRIGHT 基准上使用 Gemini 2.5 系列模型，研究者对比了查询扩展和重排序两个阶段中模型容量、推理思考时间及重排序深度的影响。结果表明，重排序阶段是性能提升的关键，而查询扩展和推理时思考的边际收益较低。该研究为优化检索增强型智能体的推理成本提供了重要指导，建议将算力集中于重排序环节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.14635v1/fig-001.webp\", \"caption\": \"Figure 2: Impact of re-ranking depth across query expansion strategies. Increasing the candidate pool from k = 10 to k = 100 consistently improves NDCG@10 by approximately 20% across all query expansion models, demonstrating that episodic retrieval depth is a high-leverage compute allocation mechanism.\", \"page\": 4, \"index\": 1, \"width\": 706, \"height\": 452}]"
motivation: 随着智能体记忆规模增长，处理需要隐式推理的复杂检索任务变得至关重要，但目前缺乏对检索流水线中算力分配的最优指导。
method: 在 BRIGHT 基准上利用 Gemini 2.5 模型，通过调整模型规模、推理思考时间和重排序深度，系统评估了查询扩展与重排序阶段的性能表现。
result: 重排序阶段显著受益于更强的模型和更深的候选池，而查询扩展在轻量级模型之外收益递减，且推理时思考在两个阶段的提升均不明显。
conclusion: 推理算力应优先集中分配给重排序阶段，而非在检索流水线的各个阶段进行均匀分布。
---

## 摘要
随着智能体在长周期内运行，其内存存储持续增长，使得检索成为获取相关信息的关键。许多智能体查询需要推理密集型检索，其中查询与相关文档之间的联系是隐性的，需要通过推理来衔接。大语言模型（LLM）增强的流水线通过查询扩展和候选重排序来解决这一问题，但引入了显著的推理成本。我们利用 BRIGHT 基准测试和 Gemini 2.5 模型系列，研究了推理密集型检索流水线中的计算分配。我们在查询扩展和重排序阶段改变了模型容量、推理时思考以及重排序深度。我们发现，重排序从更强大的模型（NDCG@10 提升 7.5）和更深的候选池（k 从 10 增加到 100 时提升 21%）中获益显著，而查询扩展在超过轻量级模型后收益递减（从弱模型到强模型仅提升 1.1 NDCG@10）。推理时思考在任何阶段提供的改进都微乎其微。这些结果表明，计算资源应集中在重排序上，而不是均匀分布在流水线的各个阶段。

## Abstract
As agents operate over long horizons, their memory stores grow continuously, making retrieval critical to accessing relevant information. Many agent queries require reasoning-intensive retrieval, where the connection between query and relevant documents is implicit and requires inference to bridge. LLM-augmented pipelines address this through query expansion and candidate re-ranking, but introduce significant inference costs. We study computation allocation in reasoning-intensive retrieval pipelines using the BRIGHT benchmark and Gemini 2.5 model family. We vary model capacity, inference-time thinking, and re-ranking depth across query expansion and re-ranking stages. We find that re-ranking benefits substantially from stronger models (+7.5 NDCG@10) and deeper candidate pools (+21% from $k$=10 to 100), while query expansion shows diminishing returns beyond lightweight models (+1.1 NDCG@10 from weak to strong). Inference-time thinking provides minimal improvement at either stage. These results suggest that compute should be concentrated on re-ranking rather than distributed uniformly across pipeline stages.

---

## 论文详细总结（自动生成）

这是一份关于论文《Compute Allocation for Reasoning-Intensive Retrieval Agents》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：随着智能体（Agents）运行时间的增加，其记忆库不断膨胀。传统的基于嵌入（Embedding）的检索依赖表面语义相似度，难以处理“推理密集型”查询（即查询与目标文档之间没有直接的词汇重叠，需要逻辑推理才能建立联系）。
*   **核心问题**：虽然利用大语言模型（LLM）进行“查询扩展”和“结果重排序”可以提升检索质量，但会带来巨大的推理成本。本研究旨在探讨：**在有限的计算预算下，算力应该如何分配到检索流水线的不同阶段（扩展 vs. 重排序）？**

### 2. 方法论
论文将检索过程建模为一个三阶段流水线，并对每个阶段的算力投入进行变量控制：
*   **查询扩展 (Query Expansion, QE)**：使用 LLM 将原始查询 $q$ 扩展为包含隐式约束和领域术语的 $q_{exp}$。
*   **初始检索 (Initial Retrieval)**：使用 BM25 算法从大规模语料库中召回 Top-$k$ 候选文档。选择 BM25 是因为它对 LLM 生成的分布外查询具有较好的适应性。
*   **列表重排序 (List-wise Re-ranking, RR)**：使用 LLM 对 Top-$k$ 候选文档进行深度推理，识别隐性相关性并生成最终排名。
*   **关键技术变量**：
    *   **模型容量**：使用 Gemini 2.5 家族（从轻量级的 Flash-Lite 到高性能的 Pro）。
    *   **推理时思考 (Thinking)**：对比标准模式与开启“思维链”推理模式的性能。
    *   **重排序深度**：改变进入重排序阶段的候选文档数量 $k$（10, 20, 50, 100）。

### 3. 实验设计
*   **数据集/基准**：**BRIGHT** 基准测试。这是一个专门为推理密集型检索设计的挑战性数据集，涵盖 12 个领域，要求模型具备多跳推理和隐式约束处理能力。
*   **对比方法**：
    *   基准线：仅使用 BM25。
    *   消融实验 1：固定重排序设置，改变查询扩展阶段的模型强度。
    *   消融实验 2：固定查询扩展设置，改变重排序阶段的模型强度。
    *   消融实验 3：改变重排序的候选池深度 $k$。
*   **评估指标**：NDCG@10（排序质量）、Recall@100（召回上限）、每查询成本（美元）和延迟（秒）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件训练资源（如 GPU 型号或数量），因为该研究主要基于 API 调用（Gemini 2.5 系列模型）进行推理实验。
*   **推理成本量化**：论文详细记录了不同配置下的经济成本（美元/查询）和时间成本（秒/查询），例如 Pro 模型重排序的成本约为 Flash-Lite 的 8 倍。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 4 种模型变体、4 种重排序深度以及多种“思考”模式的组合。
*   **充分性评价**：实验设计较为**充分且客观**。通过控制变量法，研究者成功分离了 QE 和 RR 两个阶段对最终性能的贡献度。使用 Gemini 2.5 这一具有代表性的模型家族，保证了实验结果在当前主流 LLM 能力范围内的参考价值。

### 6. 主要结论与发现
*   **查询扩展收益递减**：从“无增强”到“轻量级 LLM 扩展”提升巨大（NDCG@10 +14.35），但进一步升级到 Pro 模型收益极微（仅 +1.1），且成本飙升。
*   **重排序是性能核心**：重排序阶段对模型强度高度敏感，使用更强的模型可带来显著提升（+7.5 NDCG@10）。
*   **重排序深度是高杠杆工具**：将候选池 $k$ 从 10 增加到 100，NDCG@10 持续提升约 20%，且未见明显平台期。
*   **“思考”模式在检索中表现不佳**：在 QE 或 RR 阶段开启推理思考模式（Thinking mode）带来的改进微乎其微，甚至在 QE 阶段因“过度思考”导致性能下降，同时显著增加了延迟和成本。

### 7. 优点
*   **实战指导性强**：为构建长周期智能体的开发者提供了清晰的算力分配建议——“重重排序，轻查询扩展”。
*   **成本效益分析**：不仅关注准确率，还引入了成本和延迟维度，具有很强的工程实践意义。
*   **发现直觉背后的反直觉**：揭示了在检索任务中，额外的推理时间（Thinking）并不总是转化为更好的检索效果。

### 8. 不足与局限
*   **模型家族单一**：实验仅限于 Gemini 2.5 系列，虽然具有代表性，但结论是否完全适用于 GPT-4o 或 Claude 系列尚待验证。
*   **检索算法局限**：初始检索仅使用了传统的 BM25，未探讨向量检索（Dense Retrieval）或混合检索在这一框架下的表现。
*   **场景覆盖**：虽然 BRIGHT 涵盖多领域，但对于特定垂直领域（如极长文本的法律或医疗文档）的适用性仍需进一步研究。

（完）
