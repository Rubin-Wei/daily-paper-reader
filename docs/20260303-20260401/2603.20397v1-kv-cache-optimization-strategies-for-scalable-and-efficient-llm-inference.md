---
title: KV Cache Optimization Strategies for Scalable and Efficient LLM Inference
title_zh: 面向可扩展且高效的大语言模型推理的 KV 缓存优化策略
authors: "Yichun Xu, Navjot K. Khaira, Tejinder Singh"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20397v1"
tags: ["query:lcp"]
score: 9.0
evidence: 长上下文建模中KV缓存优化的系统综述
tldr: 随着大语言模型上下文窗口的扩展，KV缓存带来的内存压力成为推理瓶颈。本文系统综述了KV缓存优化技术，将其分为缓存剔除、压缩、混合存储、新型注意力机制及组合策略五大方向。文章深入分析了各技术的机制与权衡，并针对长文本、高吞吐、边缘设备等七大实际场景提供了部署建议，为实现高效可扩展的LLM推理提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20397v1/fig-001.webp\", \"caption\": \"Figure 6: Upper plots illustrate symbolic plots of an attention map deploying different KV cache policies in LLM generation. Lower right: contrasts their accuracy-memory trade-off. Left: the overview of H2O framework [1].\", \"page\": 6, \"index\": 1, \"width\": 882, \"height\": 457}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-002.webp\", \"caption\": \"Figure 10: vLLM system overview [22].\", \"page\": 11, \"index\": 2, \"width\": 590, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-003.webp\", \"caption\": \"Figure 12: Standard linear attention (top) vs. loglinear attention (bottom). The input consists of query, key, and value vectors [30].\", \"page\": 15, \"index\": 3, \"width\": 590, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-004.webp\", \"caption\": \"Figure 1: Autoregressive generation, at each step the new token (orange) attends to all prior tokens (cyan). Without caching, keys and values for every past token would be recomputed from scratch at each step. The KV cache avoids this by storing and reusing them.\", \"page\": 2, \"index\": 4, \"width\": 546, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-005.webp\", \"caption\": \"Figure 7: The graph shows the simplified workflow of SnapKV, where the orange area represents the cluster of features per head selected by SnapKV. These features are then used to form new Key-Value pairs concatenated with the features in the observation window. Together, the selected prefix and observation windows constitute the new KV cache utilized for the generation. [2].\", \"page\": 7, \"index\": 5, \"width\": 882, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-006.webp\", \"caption\": \"Figure 2: Data-flow of the KV cache within a single transformer layer. Input token xt fans into three projections; Kt and Vt are appended to their respective caches (teal); Qt attends over the full caches to produce output ot. Cache size grows as O(T ) per head per layer.\", \"page\": 3, \"index\": 6, \"width\": 828, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-007.webp\", \"caption\": \"Figure 3: KV cache memory as a function of context length for three LLaMA-2 model variants under fp16 precision. Dashed lines mark GPU VRAM limits; dotted lines mark model parameter memory. At 128K tokens, a 7B model’s KV cache (≈64 GB) exceeds the capacity of an A100 GPU, illustrating the memory bottleneck that motivates KV cache optimization. Values computed as 2× L×Hkv × dh × 2 bytes per token; 70B uses GQA with 8 KV heads.\", \"page\": 3, \"index\": 7, \"width\": 646, \"height\": 433}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-008.webp\", \"caption\": \"Table 6: Comparison of KV Cache Optimization Techniques\", \"page\": 18, \"index\": 8, \"width\": 990, \"height\": 755}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-009.webp\", \"caption\": \"Figure 4: Causal self-attention weight matrix for “The apple tastes sweet.” visualised with the Viridis colormap (dark purple = low, yellow = high). Gray cells are causally masked future tokens. Each row sums to 1 (post-softmax). Query “sweet” concentrates 65% of its attention on “apple”, demonstrating that KV entries carry highly non-uniform importance, the core premise of attention-score-driven eviction methods such as H2O and SnapKV.\", \"page\": 4, \"index\": 9, \"width\": 588, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-010.webp\", \"caption\": \"Figure 14: System overview of TailorKV. Offline identification categorizes the layers into quantization-friendly and sparsity-friendly. For quantization-friendly layers, we employ aggressive static quantization. For sparsity-friendly layers, we dynamically retrieve Top-K tokens. Critical current query and critical key cache represent the outliers in the query and key cache, respectively. [36].\", \"page\": 17, \"index\": 10, \"width\": 976, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-011.webp\", \"caption\": \"Figure 13: During Pre-filling, ShadowKV offloads the value cache to the CPU while maintaining a low-rank key cache, landmarks, and outliers on the GPU. During decoding, it employs landmarks for sparse attention. [35].\", \"page\": 17, \"index\": 11, \"width\": 979, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-012.webp\", \"caption\": \"Figure 11: Operation flow of the prefetching module of InfiniGen. [23].\", \"page\": 12, \"index\": 12, \"width\": 590, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-013.webp\", \"caption\": \"Figure 9: Palu’s low-rank projection method for KV-cache reduction. A weight matrix W of linear projection is decomposed into two low-rank matrices. Input X is down-projected to a latent representation H, which is cached. Y can be reconstructed from H using the up-projection matrix B. [19].\", \"page\": 9, \"index\": 13, \"width\": 490, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2603.20397v1/fig-014.webp\", \"caption\": \"Figure 5: Taxonomy of KV cache optimization techniques surveyed in this paper, organized into five major categories.\", \"page\": 5, \"index\": 14, \"width\": 983, \"height\": 240}]"
motivation: 针对大语言模型在长文本推理中KV缓存占用内存过大、带宽受限及吞吐量低的核心瓶颈问题。
method: 系统性地回顾并分类了缓存剔除、压缩、混合存储、新型注意力机制及组合策略等五类主流优化技术。
result: 详细对比了不同技术在内存减少、吞吐量提升和模型精度保持方面的表现，并针对七种典型应用场景给出了选型指南。
conclusion: 没有任何单一技术能胜任所有场景，未来的研究方向应聚焦于根据硬件和负载特征进行自适应的多阶段优化。
---

## 摘要
键值（KV）缓存是基于 Transformer 的大语言模型（LLM）中的一项基础优化技术，它消除了自回归生成过程中对过去 token 表示的冗余重新计算。然而，其内存占用随上下文长度线性增长，随着生产级 LLM 将上下文窗口从数千个 token 推向数百万个，这给 GPU 显存容量、内存带宽和推理吞吐量带来了严峻瓶颈。因此，高效的 KV 缓存管理已成为可扩展 LLM 部署的首要挑战。本文对近期的 KV 缓存优化技术进行了系统性综述，将其归纳为五个主要方向：缓存逐出（cache eviction）、缓存压缩、混合存储方案、新型注意力机制以及组合策略。针对每个类别，我们分析了其底层机制、部署权衡，以及在内存减少、吞吐量和模型准确度指标方面的实证表现。我们进一步将这些技术映射到七个实际部署场景，包括长上下文单次请求、高吞吐量数据中心服务、边缘设备、多轮对话以及对准确度要求极高的推理任务，为从业者在竞争性方案中进行选择提供可操作的指导。我们的分析表明，没有任何单一技术能在所有场景下占据主导地位；相反，最优策略取决于上下文长度、硬件限制和工作负载特征，这指向了自适应、多阶段优化流水线作为未来研究的一个极具前景的方向。

## Abstract
The key-value (KV) cache is a foundational optimization in Transformer-based large language models (LLMs), eliminating redundant recomputation of past token representations during autoregressive generation. However, its memory footprint scales linearly with context length, imposing critical bottlenecks on GPU memory capacity, memory bandwidth, and inference throughput as production LLMs push context windows from thousands to millions of tokens. Efficient KV cache management has thus become a first-order challenge for scalable LLM deployment. This paper provides a systematic review of recent KV cache optimization techniques, organizing them into five principal directions: cache eviction, cache compression, hybrid memory solutions, novel attention mechanisms, and combination strategies. For each category we analyze the underlying mechanisms, deployment trade-offs, and empirical performance across memory reduction, throughput, and model accuracy metrics. We further map techniques to seven practical deployment scenarios, including long-context single requests, high-throughput datacenter serving, edge devices, multi-turn conversations, and accuracy-critical reasoning, providing actionable guidance for practitioners selecting among competing approaches. Our analysis reveals that no single technique dominates across all settings; instead, the optimal strategy depends on context length, hardware constraints, and workload characteristics, pointing toward adaptive, multi-stage optimization pipelines as a promising direction for future research.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）支持的上下文窗口从数千扩展到数百万个 token，基于 Transformer 架构的 **KV 缓存（Key-Value Cache）** 成为了推理性能的核心瓶颈。KV 缓存虽然通过存储历史 token 的表示避免了重复计算，但其内存占用随序列长度线性增长。这导致了 GPU 显存溢出、内存带宽受限以及推理吞吐量下降。本文旨在系统性地梳理和分类现有的 KV 缓存优化策略，为在不同硬件和业务场景下实现高效、可扩展的 LLM 推理提供指导。

### 2. 论文提出的方法论
论文将 KV 缓存优化技术归纳为五大核心方向：
*   **缓存逐出（Cache Eviction）：** 基于注意力分数识别并保留“重击者（Heavy-Hitters）”或关键 token，丢弃贡献较小的 token（如 H2O、SnapKV、Ada-KV）。
*   **缓存压缩（Cache Compression）：** 采用非对称量化（如 KIVI 的 2-bit 量化）、低秩投影（如 PALU）或跨层合并（如 MiniCache）来减小单个 KV 对的存储体积。
*   **混合存储方案（Hybrid Memory Solutions）：** 利用分层存储架构，将 KV 缓存从 GPU 显存卸载（Offload）到 CPU 内存或 SSD，并配合预取机制（如 PagedAttention、InfiniGen、LayerKV）。
*   **新型注意力机制（New Attention Mechanisms）：** 改变注意力计算方式，将 $O(N^2)$ 复杂度降低为线性 $O(N)$ 或对数线性 $O(N \log N)$（如 Linear Attention、KIMI Linear）。
*   **组合策略（Combination Methods）：** 综合运用上述多种手段，例如 FlexGen（卸载+压缩）和 ShadowKV（低秩+卸载）。

### 3. 实验设计与 Benchmark
作为一篇综述性论文，本文汇总并对比了数十项研究的实验数据：
*   **数据集/场景：** 涵盖了长文本生成、多轮对话、摘要任务及合成压力测试（如 Needle In A Haystack）。
*   **对比指标：** 内存减少倍数（Memory Reduction）、吞吐量提升（Throughput Speedup）、首字延迟（TTFT）以及模型准确度（如 Perplexity 或下游任务得分）。
*   **对比方法：** 以原始全量注意力（Full Attention）为基准，对比了各分类下的代表性算法（详见文中 Table 6）。

### 4. 资源与算力
论文在分析各技术时提到了多种硬件环境的适用性：
*   **高端 GPU：** 如 NVIDIA A100 (80GB)、H100、GH200（用于测试高带宽卸载方案）。
*   **消费级/边缘设备：** 如 RTX 4090 (24GB)、RTX 3090，用于验证在显存受限情况下的优化效果。
*   **算力说明：** 论文本身作为综述，未进行大规模模型训练，但详细记录了各优化方法在推理阶段的资源节省情况（例如某些方法可实现 10x-400x 的压缩比）。

### 5. 实验数量与充分性
*   **实验覆盖：** 论文引用了超过 30 篇核心研究，对比了不同参数规模（从 7B 到 70B）的模型表现。
*   **充分性与客观性：** 论文不仅展示了性能提升，还客观地列出了各技术的**权衡（Trade-offs）**，例如缓存逐出可能导致长程依赖丢失，量化可能引入精度下降。通过将技术映射到 7 个实际部署场景，增强了实验结论的实用价值。

### 6. 主要结论与发现
*   **无通用最优解：** 没有任何一种技术在所有指标上领先，最优策略取决于上下文长度、硬件约束和负载特征。
*   **场景匹配：** 逐出和压缩适合超长上下文单次请求；混合存储适合高吞吐数据中心；新型注意力机制虽具潜力但通常需要模型重训。
*   **未来趋势：** 动态、多阶段的优化流水线（Adaptive Pipelines）是未来的研究重点。

### 7. 优点
*   **分类清晰：** 五大分类逻辑严密，涵盖了从算法改进到系统架构优化的全维度。
*   **场景导向：** 针对“边缘设备”、“多轮对话”、“精度敏感”等 7 个具体场景给出了明确的选型建议，极具工程参考价值。
*   **深度分析：** 不仅罗列方法，还深入解释了如 LSH 哈希、低秩分解等底层数学机制。

### 8. 不足与局限
*   **缺乏统一实测：** 作为综述，数据来源于各原论文，由于各实验的硬件环境、模型版本不统一，横向对比时可能存在环境偏差。
*   **时间敏感性：** LLM 领域发展极快，文中提到的部分“未来”模型（如 GPT-5, Llama 4）带有预测性质，部分最新涌现的技术可能未被完全覆盖。
*   **工程实现难度：** 部分混合存储方案（如 INF2）依赖特定的硬件（CSD/FPGA），在通用云环境中的普及受限。

（完）
