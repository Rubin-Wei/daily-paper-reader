---
title: "GROUNDEDKG-RAG: Grounded Knowledge Graph Index for Long-document Question Answering"
title_zh: GROUNDEDKG-RAG：用于长文档问答的落地知识图谱索引
authors: "Tianyi Zhang, Andreas Marfurt"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04359v1"
tags: ["query:rag"]
score: 9.0
evidence: 长文档问答的RAG系统
tldr: 针对长文档问答中RAG系统存在的资源消耗高、内容重复及幻觉问题，本文提出GroundedKG-RAG。该系统通过语义角色标注（SRL）和抽象语义表示（AMR）从源文档中提取并锚定知识图谱，将实体和动作作为节点，关系作为边。实验表明，该方法在NarrativeQA数据集上达到了与闭源长文本模型相当的性能，且成本更低，具备更强的可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04359v1/fig-001.webp\", \"caption\": \"Table 1: Main result of GroundedKGRAG compared to baselines on our three selected books.\", \"page\": 7, \"index\": 1, \"width\": 709, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2604.04359v1/fig-002.webp\", \"caption\": \"Table 2: Ablation of our GroundedKG-RAG on Peter Rabbit.\", \"page\": 7, \"index\": 2, \"width\": 915, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.04359v1/fig-003.webp\", \"caption\": \"Table 3: Example comparing AMR and SRL graphs.\", \"page\": 8, \"index\": 3, \"width\": 1000, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.04359v1/fig-004.webp\", \"caption\": \"Table 5: Error analysis with an example for each type of error.\", \"page\": 10, \"index\": 4, \"width\": 948, \"height\": 1085}]"
motivation: 现有的长文档RAG系统过度依赖LLM生成描述，导致高延迟、内容冗余以及缺乏原文锚定引发的幻觉问题。
method: 利用SRL和AMR解析技术构建与原文句子显式关联的知识图谱，并基于此图谱进行查询转换与相关文本检索。
result: 在NarrativeQA数据集上的评估显示，该系统性能媲美顶尖闭源长文本模型，且显著优于竞争基准。
conclusion: GroundedKG-RAG通过显式的原文锚定提高了长文档问答的效率、事实准确性和结果的可解释性。
---

## 摘要
检索增强生成（RAG）系统在当代大语言模型（LLM）中得到了广泛应用，因为它们能够在提高生成质量的同时减少所需的输入上下文长度。在本研究中，我们专注于用于长文档问答的 RAG 系统。当前的方法过度依赖 LLM 生成的描述，导致资源消耗高、延迟大、层级间内容重复，并且由于缺乏或仅有有限的源文本落地（grounding）而产生幻觉。为了通过落地机制提高效率和事实准确性，我们提出了 GroundedKG-RAG，这是一种知识图谱显式提取自源文档并与之落地的 RAG 系统。具体而言，我们将 GroundedKG 中的节点定义为实体和动作，将边定义为时间或语义关系，且每个节点和边都落地于原始句子中。我们利用语义角色标注（SRL）和抽象语义表示（AMR）解析构建 GroundedKG，然后对其进行嵌入以供检索。在查询过程中，我们对查询应用相同的转换，并从落地的源文本中检索最相关的句子用于问答。我们在 NarrativeQA 数据集的示例上评估了 GroundedKG-RAG，发现它在成本更低的情况下，性能与最先进的商业长上下文模型相当，并优于竞争基线模型。此外，我们的 GroundedKG 具有可解释性和人类可读性，便于结果审计和错误分析。

## Abstract
Retrieval-augmented generation (RAG) systems have been widely adopted in contemporary large language models (LLMs) due to their ability to improve generation quality while reducing the required input context length. In this work, we focus on RAG systems for long-document question answering. Current approaches suffer from a heavy reliance on LLM descriptions resulting in high resource consumption and latency, repetitive content across hierarchical levels, and hallucinations due to no or limited grounding in the source text. To improve both efficiency and factual accuracy through grounding, we propose GroundedKG-RAG, a RAG system in which the knowledge graph is explicitly extracted from and grounded in the source document. Specifically, we define nodes in GroundedKG as entities and actions, and edges as temporal or semantic relations, with each node and edge grounded in the original sentences. We construct GroundedKG from semantic role labeling (SRL) and abstract meaning representation (AMR) parses and then embed it for retrieval. During querying, we apply the same transformation to the query and retrieve the most relevant sentences from the grounded source text for question answering. We evaluate GroundedKG-RAG on examples from the NarrativeQA dataset and find that it performs on par with a state-of-the art proprietary long-context model at smaller cost and outperforms a competitive baseline. Additionally, our GroundedKG is interpretable and readable by humans, facilitating auditing of results and error analysis.

---

## 论文详细总结（自动生成）

以下是对论文《GROUNDEDKG-RAG: Grounded Knowledge Graph Index for Long-document Question Answering》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**长文档问答（Long-document QA）**场景下的检索增强生成（RAG）优化。
*   **研究动机**：现有的长文档 RAG 系统（如 GraphRAG、RAPTOR）高度依赖大语言模型（LLM）来生成文档块的摘要或提取实体关系。这种做法存在三大弊端：
    1.  **高资源消耗与延迟**：频繁调用 LLM 生成摘要导致成本高昂。
    2.  **内容冗余**：层级化摘要结构常导致不同层级间信息重复。
    3.  **幻觉风险**：生成的摘要若未严格锚定（Grounding）在原文上，容易产生脱离事实的错误。
*   **核心目标**：构建一个**显式锚定于原文句子**的知识图谱索引，在降低成本的同时，提高检索的准确性、事实一致性和可解释性。

### 2. 论文提出的方法论
论文提出了 **GroundedKG-RAG** 框架，其核心思想是将非结构化文本转化为一个节点和边均有原文出处的结构化图谱。
*   **知识图谱定义**：
    *   **节点（Nodes）**：代表实体（Entity）和动作（Action）。
    *   **边（Edges）**：代表语义关系（如施事者、受事者）或动作之间的时间先后关系。
    *   **落地（Grounding）**：每个节点和边都包含一个属性 `grounded_texts`，直接指向原始文档中的句子。
*   **关键技术流程**：
    1.  **预处理**：对文档进行分句，并执行指代消解（Coreference Resolution）。
    2.  **图构建**：利用**语义角色标注（SRL）**或**抽象语义表示（AMR）**解析句子。AMR 表现更优，因为它能将不同表述的同一概念（如“它”和“茶”）映射到同一个节点。
    3.  **节点嵌入**：探索了三种嵌入方式：基础节点嵌入、平均邻居嵌入、基于注意力的邻居嵌入（考虑局部图结构上下文）。
    4.  **检索与生成**：将用户查询也转化为图结构，通过节点相似度匹配检索出最相关的“落地句子”，最后交给 LLM（如 GPT-5 系列）生成答案。

### 3. 实验设计
*   **数据集**：使用 **NarrativeQA** 基准测试，选取了三本篇幅不同的书籍：
    *   《彼得兔的故事》（约 5,000 词）
    *   《歌剧魅影》（约 90,000 词）
    *   《鲁滨逊漂流记》（约 120,000 词）
*   **评估指标**：Exact Match (EM), Sequence Match (SM), ROUGE-L F1, BERTScore。
*   **对比方法（Baselines）**：
    *   **No context**：闭卷问答，测试 LLM 的参数化记忆。
    *   **Full context**：将整本书作为上下文输入给长文本 LLM。
    *   **GraphRAG**：微软提出的基于 LLM 摘要的层级化图 RAG 方案。

### 4. 资源与算力
*   **硬件**：实验在 Google Colab 上运行，使用单块 **NVIDIA T4 或 L4 GPU**。
*   **模型调用**：使用 OpenAI API 进行批量处理，生成模型包括 `gpt-5-2025-08-07` 和 `gpt-5-nano-2025-08-07`（注：论文标注日期为 2026 年，此处模型版本号反映了论文设定的未来背景）。
*   **训练/处理时长**：文中未明确给出构建索引的总时长，但强调了其方法比依赖 LLM 摘要的方法更具资源效率。

### 5. 实验数量与充分性
*   **实验规模**：针对三本不同长度的书籍进行了主实验，并针对《彼得兔的故事》进行了详细的消融实验。
*   **消融实验内容**：
    *   SRL vs. AMR 解析效果对比。
    *   检索数量 K 值（5 vs. 10）的影响。
    *   三种不同节点嵌入技术的对比。
    *   检索过滤策略（向量相似度过滤、检索频次过滤）的效果。
*   **充分性评价**：实验设计较为客观，涵盖了从短篇到长篇的跨度，并对比了当前最先进的商业模型和开源基准。但样本书籍数量（3本）相对较少，若能覆盖更多 NarrativeQA 中的书籍会更具说服力。

### 6. 论文的主要结论与发现
*   **性能表现**：GroundedKG-RAG 在所有指标上均显著优于 GraphRAG。在短篇书籍上，其表现甚至超过了全上下文（Full context）输入；在长篇书籍上，其 ROUGE-L 指标与全上下文相当，但成本更低。
*   **解析技术**：基于 **AMR** 的图构建显著优于 SRL，因为 AMR 能更好地处理概念归一化和细粒度语义匹配。
*   **嵌入与过滤**：复杂的邻居嵌入和额外的向量过滤策略并未带来显著提升，有时反而因为过滤掉了关键证据句而导致性能下降。
*   **可解释性**：由于每个检索结果都能追溯到原文句子，系统具备极高的可审计性，方便进行错误分析。

### 7. 优点（亮点）
*   **强落地性（Strong Grounding）**：彻底消除了 RAG 索引构建阶段由 LLM 摘要引入的幻觉风险。
*   **高效率**：通过语义解析器而非 LLM 生成摘要来构建索引，大幅降低了 API 调用成本和计算延迟。
*   **人类可读性**：构建的知识图谱结构清晰，节点和边均可直观理解，便于人工纠错和审计。
*   **查询自适应**：相比固定层级的树状结构，基于图节点的检索能更灵活地适应多样化的查询需求。

### 8. 不足与局限
*   **长文档挑战**：随着文档长度增加（如《鲁滨逊漂流记》），其 BERTScore 相比全上下文模型仍有差距，说明在大规模图检索上仍有优化空间。
*   **解析器依赖**：系统的准确性高度依赖于 SRL/AMR 解析器和指代消解模型的质量，解析错误会直接传递到检索阶段。
*   **过滤策略失效**：文中尝试的多种检索后过滤方法效果不佳，说明在“减少干扰信息”与“保留关键证据”之间仍难以平衡。
*   **泛化性验证**：目前仅在文学类叙事文本（NarrativeQA）上验证，在专业性极强的技术文档或法律文档上的表现尚待观察。

（完）
