---
title: "Lightweight Query Routing for Adaptive RAG: A Baseline Study on RAGRouter-Bench"
title_zh: 面向自适应 RAG 的轻量级查询路由：基于 RAGRouter-Bench 的基准研究
authors: "Prakhar Bansal, Shivangi Agarwal"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03455v1"
tags: ["query:rag"]
score: 9.0
evidence: 自适应检索增强生成的查询路由
tldr: 本研究针对检索增强生成（RAG）中不同策略成本差异大的问题，在RAGRouter-Bench基准上首次系统评估了轻量级查询路由分类器。通过测试5种分类器与3类特征（TF-IDF、语义嵌入、手工特征）的组合，发现TF-IDF结合SVM的效果最优，在保持高准确率的同时显著降低了Token消耗。该研究为自适应RAG提供了可复现的基准，并揭示了词汇特征在区分查询复杂度方面的优势。
source: arxiv
selection_source: fresh_fetch
motivation: 旨在解决RAG系统中不同检索策略带来的成本与能力平衡问题，通过自动路由选择最合适的处理路径以提升效率。
method: 系统评估了基于TF-IDF、MiniLM嵌入和手工特征的15种轻量级分类器组合在多领域查询分类上的表现。
result: "最佳配置（TF-IDF+SVM）实现了0.928的F1分数和93.2%的准确率，并相比全量使用昂贵策略节省了28.1%的Token成本。"
conclusion: 实验证明词汇特征在查询路由中优于语义嵌入，为未来结合语料库感知的路由研究奠定了高效的查询端基准。
---

## 摘要
检索增强生成（RAG）流水线涵盖了广泛的检索策略，这些策略在 Token 成本和能力方面存在显著差异。为每个查询选择合适的策略是一个实际的效率问题，然而目前尚未有路由分类器在 RAGRouter-Bench 上进行训练。该基准测试最近发布，包含跨越四个知识领域的 7,727 个查询，每个查询都被标注为三种典型查询类型之一：事实型、推理型和摘要型。我们在该基准上首次对基于轻量级分类器的路由进行了系统性评估。我们在三种特征方案（即 TF-IDF、MiniLM 句子嵌入和手工构建的结构化特征）下评估了五种经典分类器，共产生 15 种分类器-特征组合。我们的最佳配置（TF-IDF 结合 SVM）实现了 0.928 的宏平均 F1 分数和 93.2% 的准确率，同时相比于始终使用最昂贵的范式，模拟实现了 28.1% 的 Token 节省。词汇 TF-IDF 特征的表现优于语义句子嵌入 3.1 个宏 F1 点，这表明表层关键词模式是查询类型复杂度的强预测指标。领域级分析显示，医学查询最难路由，而法律查询最易处理。这些结果建立了一个可复现的查询端基准，并突显了语料库感知路由需要弥补的差距。

## Abstract
Retrieval-Augmented Generation pipelines span a wide range of retrieval strategies that differ substantially in token cost and capability. Selecting the right strategy per query is a practical efficiency problem, yet no routing classifiers have been trained on RAGRouter-Bench \citep{wang2026ragrouterbench}, a recently released benchmark of $7,727$ queries spanning four knowledge domains, each annotated with one of three canonical query types: factual, reasoning, and summarization. We present the first systematic evaluation of lightweight classifier-based routing on this benchmark. Five classical classifiers are evaluated under three feature regimes, namely, TF-IDF, MiniLM sentence embeddings \citep{reimers2019sbert}, and hand-crafted structural features, yielding 15 classifier feature combinations. Our best configuration, TF-IDF with an SVM, achieves a macro-averaged F1 of $\mathbf{0.928}$ and an accuracy of $\mathbf{93.2\%}$, while simulating $\mathbf{28.1\%}$ token savings relative to always using the most expensive paradigm. Lexical TF-IDF features outperform semantic sentence embeddings by $3.1$ macro-F1 points, suggesting that surface keyword patterns are strong predictors of query-type complexity. Domain-level analysis reveals that medical queries are hardest to route and legal queries most tractable. These results establish a reproducible query-side baseline and highlight the gap that corpus-aware routing must close.

---

## 论文详细总结（自动生成）

这是一份关于论文《Lightweight Query Routing for Adaptive RAG: A Baseline Study on RAGRouter-Bench》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在检索增强生成（RAG）系统中，不同的检索策略（如简单检索与迭代检索）在处理能力和 Token 成本上差异巨大。为了平衡效率与效果，需要根据查询（Query）的复杂度自动选择最经济且足够的策略。
*   **核心问题**：针对 2026 年新发布的 RAGRouter-Bench 基准测试，目前尚缺乏系统性的轻量级路由分类器研究。本研究旨在探索仅利用查询文本特征（不依赖昂贵的 LLM 或语料库检索）能达到怎样的路由准确率和成本节省效果。

### 2. 论文提出的方法论
*   **核心思想**：将 RAG 路由建模为一个三分类任务（事实型、推理型、摘要型），并使用轻量级机器学习分类器在查询进入 RAG 流程前进行预判。
*   **关键技术细节**：
    *   **特征工程**：对比了三种特征：
        1.  **词汇特征 (TF-IDF)**：提取 1-gram 和 2-gram，保留 3000 个高频词汇。
        2.  **语义特征 (MiniLM)**：使用 22M 参数的 `all-MiniLM-L6-v2` 模型生成 384 维嵌入向量。
        3.  **结构化特征 (Hand-crafted)**：提取 23 种手工特征，如查询长度、疑问词类型（who/what 等）、否定词、命名实体计数、从句计数等。
    *   **分类器选择**：评估了逻辑回归、SVM（RBF核）、随机森林、KNN 和 MLP（多层感知机）。
    *   **路由映射逻辑**：将预测类型映射到特定 RAG 范式：`single_hop` $\rightarrow$ NaiveRAG；`multi_hop` $\rightarrow$ HybridRAG；`summary` $\rightarrow$ IterativeRAG。

### 3. 实验设计
*   **数据集 (RAGRouter-Bench)**：包含 7,727 个查询，覆盖四个领域：
    *   MuSiQue (维基百科)
    *   QuALITY (文学)
    *   UltraDomain (法律)
    *   GraphRAG-Bench (医学)
*   **对比基准 (Baselines)**：
    *   **多数类基准**：始终预测为 `single_hop`（占比 52.9%）。
    *   **完美标签参考 (Perfect-label ref.)**：假设分类准确率为 100% 时的理论最大节省。
*   **评估指标**：宏平均 F1 分数（Macro-F1）、准确率（Accuracy）以及模拟 Token 节省率（Savings %）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长。
*   **资源消耗推测**：由于使用的是轻量级分类器（如 SVM、随机森林）和小型嵌入模型（MiniLM），作者特别强调 MiniLM 可以在 CPU 上运行，无需 GPU。这表明该方法的计算开销极低，适合实时路由场景。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 15 组核心实验（5 种分类器 $\times$ 3 种特征集），并采用了 **5 折分层交叉验证**。
*   **充分性评价**：实验设计较为充分且客观。作者不仅提供了全局性能，还进行了跨领域的细分分析（Table 3），并探讨了成本与准确率之间的权衡关系。通过对比“完美标签”和“多数类”基准，清晰地界定了轻量级路由的性能区间。

### 6. 论文的主要结论与发现
*   **词汇特征占优**：TF-IDF + SVM 组合表现最好（F1=0.928），优于语义嵌入（F1=0.897）。这说明在 RAGRouter-Bench 中，表层关键词（如“summarize”、“compare”）是区分查询复杂度的强信号。
*   **成本节省显著**：最佳配置在保持 93.2% 准确率的同时，相比始终使用最昂贵的 IterativeRAG 节省了 **28.1%** 的 Token 成本。
*   **领域差异明显**：法律领域（UltraDomain）最容易路由（F1=0.967），而医学领域（GraphRAG-Bench）最具挑战性（F1=0.803），因为医学查询的词汇特征区分度较低。

### 7. 优点
*   **高效性**：证明了无需调用大模型即可实现高精度的查询路由，极大地降低了系统延迟和成本。
*   **可复现性**：为 RAGRouter-Bench 建立了第一个公开的、可复现的查询端基准。
*   **洞察力**：揭示了词汇特征在特定 RAG 任务分类中优于深度语义特征的现象，挑战了“语义嵌入总是更好”的直觉。

### 8. 不足与局限
*   **忽略语料库信号**：分类仅基于查询文本。原基准论文指出，最佳 RAG 策略往往取决于查询与语料库的交互（如语料库的连通性、密度等），本研究未考虑这些因素。
*   **映射简化**：将查询类型到 RAG 范式的映射（如 `multi_hop` 对应 HybridRAG）是一种基于文献的简化假设，实际应用中这种对应关系可能更复杂。
*   **泛化性未知**：实验仅限于 RAGRouter-Bench 的四个领域，对于分布外（OOD）数据的泛化能力尚未测试。
*   **静态成本模型**：Token 节省是基于固定比例模拟的，未考虑实际部署中不同模型 API 价格波动的动态影响。

（完）
