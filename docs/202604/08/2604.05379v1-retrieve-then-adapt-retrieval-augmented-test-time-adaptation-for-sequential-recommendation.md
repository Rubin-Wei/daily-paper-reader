---
title: "Retrieve-then-Adapt: Retrieval-Augmented Test-Time Adaptation for Sequential Recommendation"
title_zh: 检索后自适应：用于顺序推荐的检索增强测试时自适应
authors: "Xing Tang, Jingyang Bin, Ziqiang Cui, Xiaokun Zhang, Fuyuan Lyu, Jingyan Jiang, Dugang Liu, Chen Ma, Xiuqiang He"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05379v1"
tags: ["query:ttt"]
score: 9.0
evidence: 检索增强的测试时自适应
tldr: 针对序列推荐模型在推理阶段难以适应用户实时偏好偏移的问题，本文提出了一种名为 ReAd 的检索增强测试时自适应框架。该框架通过从构建的协作记忆库中检索相似项，利用轻量级模块生成包含协作信号的增强嵌入，并将其与原始预测融合以实现动态调整。在五个基准数据集上的实验证明，ReAd 能在不引入巨大开销的情况下显著提升推荐性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05379v1/fig-001.webp\", \"caption\": \"Figure 3: Effects of hyperparamter of the 𝐾 value of Top—𝑘 retrieved items and 𝜆 value controled KL loss.\", \"page\": 7, \"index\": 1, \"width\": 506, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2604.05379v1/fig-002.webp\", \"caption\": \"Figure 2: The overall framework of ReAd. (a) Online adaptation: The test user sequence is encoded by the encoder 𝑀𝜃 and used to retrieve the top-𝑘 most similar representations from the Collaborative Memory base. The retrieved representations are fused via the Retrieval Learning Module, then combined with the initial prediction 𝑝init to produce the final augmented prediction. (b) Offline retrieval learning: The retrieval representations are refined via a cross-attention layer, supervised by the recommendation loss Lrec and the alignment loss Lalign, with KL-divergence used to stabilize training. The red box indicates offline components, while the blue box corresponds to online inference.\", \"page\": 4, \"index\": 2, \"width\": 1042, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2604.05379v1/fig-003.webp\", \"caption\": \"Figure 5: The inference time considering batch inference and sample inference.\", \"page\": 9, \"index\": 3, \"width\": 506, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2604.05379v1/fig-004.webp\", \"caption\": \"Figure 6: A case study on the MovieLens dataset showing retrieval results. Red dashed lines represent the test sequence and target item; blue dashed lines represent two retrieved sequences and their corresponding items.\", \"page\": 9, \"index\": 4, \"width\": 506, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.05379v1/fig-005.webp\", \"caption\": \"Table 3: ReAd improvements on different backbones. All the results are significant with 𝑝-value < 0.01\", \"page\": 9, \"index\": 5, \"width\": 502, \"height\": 565}]"
motivation: 现有的序列推荐模型在面对推理时的分布偏移时，往往受限于参数固定或测试时自适应方法的计算开销与随机性。
method: 提出 ReAd 框架，通过检索协作相似项并利用轻量级模块将其转化为增强嵌入，最后通过融合机制修正初始预测。
result: 在五个基准数据集上的实验结果显示，ReAd 一致且显著地优于现有的序列推荐及测试时自适应方法。
conclusion: ReAd 证明了通过检索增强实现高效测试时自适应是提升序列推荐模型实时适应能力的有效方案。
---

## 摘要
顺序推荐（SR）任务旨在根据用户的历史交互序列预测下一个项目。SR 模型通常在历史数据上进行训练，由于分布差异和参数化约束带来的挑战，在推理过程中往往难以适应实时的偏好偏移。解决这一问题的现有方法包括测试时训练、测试时增强和检索增强微调。然而，这些方法要么引入了显著的计算开销，要么依赖于随机增强策略，或者需要精心设计的两阶段训练范式。在本文中，我们认为有效的测试时自适应的关键在于同时实现有效的增强和高效的自适应。为此，我们提出了 Retrieve-then-Adapt (ReAd)，这是一个通过检索到的用户偏好信号将已部署的 SR 模型动态调整到测试分布的新颖框架。具体而言，给定一个训练好的 SR 模型，ReAd 首先从构建的协同记忆数据库中为测试用户检索协同相似的项目。随后，一个轻量级的检索学习模块将这些项目整合到一个信息丰富的增强嵌入中，该嵌入同时捕捉了协同信号和预测细化线索。最后，通过融合该嵌入的融合机制对初始 SR 预测进行细化。在五个基准数据集上的广泛实验表明，ReAd 一致地优于现有的 SR 方法。

## Abstract
The sequential recommendation (SR) task aims to predict the next item based on users' historical interaction sequences. Typically trained on historical data, SR models often struggle to adapt to real-time preference shifts during inference due to challenges posed by distributional divergence and parameterized constraints. Existing approaches to address this issue include test-time training, test-time augmentation, and retrieval-augmented fine-tuning. However, these methods either introduce significant computational overhead, rely on random augmentation strategies, or require a carefully designed two-stage training paradigm. In this paper, we argue that the key to effective test-time adaptation lies in achieving both effective augmentation and efficient adaptation. To this end, we propose Retrieve-then-Adapt (ReAd), a novel framework that dynamically adapts a deployed SR model to the test distribution through retrieved user preference signals. Specifically, given a trained SR model, ReAd first retrieves collaboratively similar items for a test user from a constructed collaborative memory database. A lightweight retrieval learning module then integrates these items into an informative augmentation embedding that captures both collaborative signals and prediction-refinement cues. Finally, the initial SR prediction is refined via a fusion mechanism that incorporates this embedding. Extensive experiments across five benchmark datasets demonstrate that ReAd consistently outperforms existing SR methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Retrieve-then-Adapt: Retrieval-Augmented Test-Time Adaptation for Sequential Recommendation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的序列推荐（SR）模型在训练完成后参数即被冻结，难以应对推理阶段（Test-time）出现的**用户偏好偏移**（如季节变化、兴趣突变）以及**长尾分布偏移**。
*   **研究背景**：现有的测试时自适应方法存在局限性：
    *   **测试时训练（TTT）**：计算开销大，推理延迟高。
    *   **测试时增强（TTA）**：依赖随机掩码或裁剪，缺乏鲁棒性。
    *   **检索增强微调**：通常需要复杂的两阶段预训练，难以直接适配已部署的模型。
*   **整体含义**：本文旨在提出一种低成本、模型无关（Model-agnostic）且高效的框架，通过检索外部协作信号来动态修正推理时的预测结果。

### 2. 方法论：Retrieve-then-Adapt (ReAd)
ReAd 框架分为**线下准备**和**线上自适应**两个阶段：
*   **协同记忆库构建（Offline）**：利用训练好的 SR 模型编码器，将训练集中的序列表示（Key）与对应的目标项目嵌入（Value）存入数据库。
*   **检索学习模块（Retrieval Learning）**：
    *   **核心思想**：不只是简单平均检索到的项目，而是通过一个轻量级的**交叉注意力（Cross-attention）**层来学习如何融合这些信号。
    *   **关键技术**：引入了**对齐损失（Alignment Loss）**。利用 KL 散度将注意力权重与一个基于“预测贡献度”的参考分布进行对齐，确保模型优先关注那些真正能修正预测的项目，而非仅仅是相似的项目。
*   **线上自适应（Online）**：
    *   **检索**：给定测试序列，从记忆库中检索 Top-K 个相似项。
    *   **增强预测**：通过检索学习模块生成增强嵌入 $e_{aug}$，并计算增强后的预测分布 $p_{aug}$。
    *   **动态融合**：提出一种**基于熵的动态融合机制**。根据初始预测和增强预测的 Top-ρ 概率分布的熵（不确定性）来计算融合权重 $\alpha$，实现置信度驱动的最终预测。

### 3. 实验设计
*   **数据集**：使用了 5 个公开基准数据集：Amazon 的四个子集（Office, Beauty, Sports, Home）以及电影评分数据集 ML-1M。
*   **Benchmark 与对比方法**：
    *   **经典/深度模型**：GRU4Rec, SASRec, BERT4Rec。
    *   **对比学习模型**：CL4SRec, DuoRec, CoSeRec, S3-Rec, ICLRec, MCLRec, ICSRec。
    *   **测试时策略**：RaSeRec（检索增强微调）, TTA（测试时数据增强）。
*   **评估指标**：HR@K 和 NDCG@K (K=5, 10, 20)，采用全排名评估（无负采样偏差）。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验是在 **NVIDIA GeForce RTX 4090D** 上进行的。
*   **训练细节**：嵌入维度设为 64，Batch Size 为 256，使用 Adam 优化器。
*   **效率分析**：文中展示了推理时间的对比图，虽然 ReAd 增加了检索步骤，但通过并行化处理，其延迟在可接受范围内（相比于 TTT 类方法更具优势）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个数据集上进行了全量对比实验。
    *   针对 4 种不同的骨干网络（GRU, Transformer, BERT 等）验证了通用性。
    *   进行了详细的消融实验（验证推荐损失、对齐损失、注意力机制、动态权重的有效性）。
    *   进行了超参数敏感性分析（K 值、λ 值、Top-ρ 比例）。
*   **充分性评价**：实验设计非常充分且客观。通过在不同稀疏度的数据集上测试，证明了该方法在稀疏数据（偏好偏移更明显）上提升更大，符合直觉。

### 6. 主要结论与发现
*   **性能领先**：ReAd 在所有数据集上均达到了 SOTA 性能，尤其是在结合 DuoRec 骨干网络时表现最优。
*   **模型无关性**：ReAd 可以无缝集成到任何现有的序列推荐模型中，并带来显著提升（提升幅度通常在 10% 以上）。
*   **检索的价值**：案例研究（Case Study）表明，ReAd 能够通过检索历史相似模式，成功捕捉到用户从“剧情片”向“惊悚片”的实时兴趣偏移。
*   **熵融合的必要性**：动态权重分配比固定权重（0.5/0.5）效果更好，因为它能识别模型何时对原始预测不自信。

### 7. 优点与亮点
*   **即插即用**：不需要重新训练原始 SR 模型，只需训练一个极轻量级的注意力层。
*   **协同信号显式化**：将模型参数中隐式的协同信号转化为记忆库中显式的检索信号，增强了模型对长尾和偏移数据的处理能力。
*   **对齐机制创新**：通过 KL 散度对齐预测效用，解决了检索增强中常见的“噪声引入”问题。

### 8. 不足与局限
*   **存储开销**：构建协同记忆库需要存储训练集所有序列的表示，对于超大规模工业级场景，数据库的维护和检索压力较大。
*   **冷启动限制**：该方法依赖于训练集中的历史模式，如果测试时的偏移完全超出了训练集的覆盖范围（即记忆库中没有相似模式），检索增强的效果可能会打折扣。
*   **实时更新频率**：论文未详细讨论记忆库在实时流式数据下的更新频率和同步成本。

（完）
