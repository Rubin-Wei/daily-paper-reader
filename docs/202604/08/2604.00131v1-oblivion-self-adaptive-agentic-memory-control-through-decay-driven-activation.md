---
title: "Oblivion: Self-Adaptive Agentic Memory Control through Decay-Driven Activation"
title_zh: Oblivion：通过衰减驱动激活实现的自适应智能体记忆控制
authors: "Ashish Rana, Chia-Chien Hung, Qumeng Sun, Julian Martin Kunkel, Carolin Lawrence"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00131v1"
tags: ["query:am"]
score: 10.0
evidence: 记忆增强型LLM智能体的记忆控制框架
tldr: 针对LLM智能体在长程交互中面临的内存干扰和高延迟问题，本文提出Oblivion框架。该框架借鉴人类记忆的衰减与激活机制，将遗忘视为可访问性的降低而非直接删除。通过解耦读写路径，Oblivion能根据不确定性自主决定检索时机，并强化对响应有贡献的记忆，实现了高效的层次化记忆管理，显著提升了智能体在动态环境下的推理效能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00131v1/fig-001.webp\", \"caption\": \"Figure 2: (a) Ebbinghaus forgetting curve showing decay patterns with reinforcement. (b) Retention distributions at time steps t ∈ {50, 100, 150} for temperature T ∈ {1, 3, 5, 10, 20, 50}. (c) Reinforcement fraction over time.\", \"page\": 6, \"index\": 1, \"width\": 887, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-002.webp\", \"caption\": \"Table 1: LONGMEMEVAL results in accuracy (%): Oracle (clean) vs. S (noisy). For close comparisons in EVERMEMOS (Hu et al., 2026), we report only GPT-4o-mini and GPT-4.1-mini.\", \"page\": 5, \"index\": 2, \"width\": 948, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-003.webp\", \"caption\": \"Table 2: Evaluation results of GOODAILTM as solved scenarios out of (11, 10, 11, 11) for given memory span settings respectively. † Selected models are instruct.\", \"page\": 5, \"index\": 3, \"width\": 454, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-004.webp\", \"caption\": \"Figure 1: OBLIVION facilitates memory-augmented agents by decay-driven activation over hierarchical memory traces. The Executor orchestrates the read path for uncertainty-gated retrieval (⋄); and the write path for feedback-driven updates (⋄), enabling dynamic control over memory activation.\", \"page\": 1, \"index\": 4, \"width\": 449, \"height\": 347}]"
motivation: 现有的LLM智能体采用全量检索和扁平化存储，导致随着历史增长出现严重的干扰和延迟问题。
method: 提出一种基于衰减驱动的自适应记忆控制框架，通过解耦读路径（基于不确定性检索）和写路径（基于贡献度强化）来实现层次化记忆组织。
result: 在静态和动态长程交互基准测试中，Oblivion能够动态平衡学习与遗忘，有效降低了冗余访问并提升了推理质量。
conclusion: 记忆控制是实现高效LLM智能体推理的关键，通过模拟人类的衰减与激活机制可以显著优化长程任务表现。
---

## 摘要
人类记忆通过选择性遗忘进行适应：经验随着时间的推移变得难以获取，但可以通过强化或上下文线索重新激活。相比之下，记忆增强型大语言模型（LLM）智能体依赖于“始终开启”的检索和“扁平化”的记忆存储，随着历史记录的增长，这会导致严重的干扰和延迟。我们引入了 Oblivion，这是一个记忆控制框架，它将遗忘视为由衰减驱动的可访问性降低，而非显式的删除。Oblivion 将记忆控制解耦为读路径和写路径。读路径根据智能体的不确定性和记忆缓冲区的充足性来决定何时查阅记忆，从而避免冗余的始终开启式访问。写路径通过强化对生成响应有贡献的记忆，来决定需要加强的内容。结合起来，这实现了层级化的记忆组织，在保持持久的高层策略的同时，根据需要动态加载细节。我们在静态和动态的长程交互基准测试上进行了评估。结果表明，Oblivion 能够动态调整记忆访问和强化，在不断变化的上下文中平衡学习与遗忘，凸显了记忆控制对于有效的 LLM 智能体推理至关重要。源代码可在 https://github.com/nec-research/oblivion 获取。

## Abstract
Human memory adapts through selective forgetting: experiences become less accessible over time but can be reactivated by reinforcement or contextual cues. In contrast, memory-augmented LLM agents rely on "always-on" retrieval and "flat" memory storage, causing high interference and latency as histories grow. We introduce Oblivion, a memory control framework that casts forgetting as decay-driven reductions in accessibility, not explicit deletion. Oblivion decouples memory control into read and write paths. The read path decides when to consult memory, based on agent uncertainty and memory buffer sufficiency, avoiding redundant always-on access. The write path decides what to strengthen, by reinforcing memories contributing to forming the response. Together, this enables hierarchical memory organization that maintains persistent high-level strategies while dynamically loading details as needed. We evaluate on both static and dynamic long-horizon interaction benchmarks. Results show that Oblivion dynamically adapts memory access and reinforcement, balancing learning and forgetting under shifting contexts, highlighting that memory control is essential for effective LLM-agentic reasoning. The source code is available at https://github.com/nec-research/oblivion.

---

## 论文详细总结（自动生成）

### 论文总结：Oblivion 自适应智能体记忆控制框架

#### 1. 核心问题与整体含义
随着大语言模型（LLM）智能体在长程交互（Long-horizon Interaction）中的应用增加，传统的记忆管理面临三大挑战：
*   **检索冗余**：现有智能体通常采用“始终开启”的检索模式，无论是否需要都会查阅外部数据库，导致高延迟和上下文污染。
*   **记忆扁平化**：存储系统无法区分持久的高层策略与瞬时的底层细节，且通常采用“硬删除”方式管理容量，导致重要信息永久丢失。
*   **干扰严重**：随着历史记录增长，无关信息的堆积产生了“大海捞针”难题，干扰了模型的推理能力。

**研究动机**：借鉴人类心理学的“遗忘曲线”理论，将遗忘视为**可访问性的降低**而非物理删除。论文旨在通过自适应的读写控制，让智能体学会“何时检索”以及“强化什么”。

#### 2. 方法论
Oblivion 框架的核心是**读写路径的解耦**与**层次化记忆组织**：

*   **层次化记忆结构**：
    *   **L1（动态程序记忆）**：作为元索引，存储处理特定领域问题的策略和指令。
    *   **L2（语义记忆）**：存储带时间戳的事实性陈述。
    *   **L3（抢占式情节记忆）**：将原始对话转化为可操作的经验总结。
*   **读路径（Read Path）：不确定性门控检索**
    *   **Decayer（衰减器）**：利用艾宾浩斯遗忘公式计算记忆块的“保留分数”（Retention Score）。同时，通过 LLM 评估和嵌入相似度来估计当前查询的“不确定性”。只有当不确定性高且缓冲区不足时，才触发检索。
    *   **Activator（激活器）**：将查询分解为有向无环图（DAG）形式的子查询，进行多步证据发现，并将检索到的内容加载到工作记忆中。
*   **写路径（Write Path）：反馈驱动强化**
    *   **Recognizer（识别器）**：在生成响应后，识别哪些记忆条目对回答有贡献（信用分配）。
    *   **Memory Manager（记忆管理器）**：仅强化被使用的记忆条目，增加其保留分数；未被使用的条目则随时间自然衰减，降低其未来的检索优先级。

#### 3. 实验设计
论文在静态和动态两种长程任务基准上进行了评估：
*   **数据集/场景**：
    *   **LONG MEM EVAL**：静态基准，包含约 100K 上下文的多年对话历史，测试 6 种 QA 任务（如跨会话推理、知识更新）。
    *   **GOOD AILTM**：动态交互基准，上下文长达 120K，测试智能体在持续对话中保留、修正和更新信息的能力。
*   **对比方法**：
    *   **Direct（直接处理）**：全上下文输入或简单截断。
    *   **Memory-augmented（记忆增强类）**：包括 EverMemOS（自组织记忆系统）和基于 MemGPT 架构的 Beyond Prompts。
*   **Backbone 模型**：Phi-4-mini (4B), Qwen3-30B, GPT-4o-mini, GPT-4.1-mini。

#### 4. 资源与算力
*   论文**未明确说明**具体的训练算力（如 GPU 型号或数量），因为该框架主要是一个**推理时（Inference-time）控制层**，不需要对底层 LLM 进行微调。
*   文中提供了**推理成本分析**：在 120K 上下文设置下，Oblivion 相比全上下文模式减少了约 **73% 的 Token 消耗**，并显著降低了成本。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同规模的 LLM，并在两个主流长程记忆基准上进行了完整测试。
*   **消融实验**：详细测试了 L1/L2/L3 每一层级对性能的贡献，验证了全层级结构的优越性。
*   **参数分析**：对衰减温度 $T$ 进行了敏感度分析，展示了不同衰减速度对记忆保留的影响。
*   **客观性**：使用了 3 次运行的平均值及 LLM-as-a-judge 协议，并计算了 Hedges' g 效应量以确保结果的统计显著性。实验设计较为全面，兼顾了准确性、成本和延迟。

#### 6. 主要结论与发现
*   **性能领先**：Oblivion 在静态 QA 和动态交互任务中均优于现有的扁平化检索方法。
*   **成本效益**：通过减少冗余检索，Oblivion 在处理超长上下文时表现出极高的 Token 利用率，成本仅为全上下文模式的 1/4 左右。
*   **遗忘的价值**：实验证明，模拟生物遗忘（降低不常用信息的优先级）能有效减轻“大海捞针”效应，提升模型在核心任务上的专注度。

#### 7. 优点
*   **生物启发式设计**：将遗忘建模为可访问性衰减而非删除，更符合认知科学，且保留了记忆重新激活的可能性。
*   **读写解耦**：通过不确定性门控避免了“始终开启”检索带来的计算浪费。
*   **模型无关性**：作为一个控制层，可以即插即用地应用于各种预训练 LLM。
*   **层次化组织**：L1-L3 的结构有效解决了策略、事实与情节之间的存储冲突。

#### 8. 不足与局限
*   **依赖模型能力**：框架中的不确定性估计和记忆提取高度依赖 LLM 自身的能力，在较小规模的模型（如 4B 以下）上效果可能打折扣。
*   **超参数敏感**：衰减温度 $T$ 需要根据具体任务进行调优，缺乏一种完全自动化的参数设置机制。
*   **评估范围限制**：目前主要集中在文本对话，尚未在多模态、多语言或极度开放的智能体环境（如复杂游戏或真实物理环境）中验证。

（完）
