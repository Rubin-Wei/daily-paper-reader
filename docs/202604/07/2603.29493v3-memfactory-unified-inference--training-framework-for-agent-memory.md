---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Bo Tang, Feiyu Xiong, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v3"
tags: ["query:am"]
score: 10.0
evidence: 智能体记忆的统一训练框架
tldr: "针对长时AI智能体中记忆管理实现碎片化、缺乏统一框架的问题，本文提出了MemFactory。这是首个统一且高度模块化的记忆增强智能体训练与推理框架，采用类乐高架构，支持原子化组件拼装。它集成了GRPO算法以优化记忆策略，并兼容Memory-R1等前沿范式。实验显示，该框架在MemAgent任务上最高提升了14.8%的性能，显著降低了记忆驱动智能体的开发门槛。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v3/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v3/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 219}]"
motivation: 现有的记忆增强大模型实现方案过于碎片化且缺乏通用基础设施，限制了复杂记忆管理策略的开发与训练。
method: 提出一种模块化的“类乐高”架构，将记忆生命周期抽象为原子组件，并集成GRPO算法进行多维度奖励驱动的策略微调。
result: "在MemAgent架构上的实验表明，MemFactory相比基础模型平均性能有所提升，最高相对增益达到14.8%。"
conclusion: MemFactory为记忆驱动的AI智能体提供了标准化且易用的基础设施，有效推动了该领域的创新与应用。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近期，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为包括 Memory-R1、RMM 和 MemAgent 在内的近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在各评估集上，MemFactory 相比相应的基座模型平均提升了性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across the evaluation sets, MemFactory improves performance over the corresponding base models on average, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MemFactory**，这是一个专为记忆增强型智能体（Memory-augmented Agents）设计的统一推理与训练框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）在对话中表现出色，但要进化为真正的 AI 智能体，需要具备长期记忆和个性化积累能力。
*   **背景问题**：
    *   早期的记忆管理依赖启发式规则（如简单的 RAG），难以处理冗余、冲突和长程上下文。
    *   近期虽出现了利用强化学习（RL）优化记忆操作（提取、更新、检索）的研究（如 Memory-R1, MemAgent），但实现方案高度碎片化、任务耦合度高，缺乏像 LLaMA-Factory 那样通用的基础设施。
*   **核心目标**：提供一个模块化、可扩展的统一框架，降低记忆驱动型智能体的开发、训练和评估门槛。

### 2. 方法论
MemFactory 采用了“类乐高”的模块化架构，分为四个核心层：
*   **Module Layer（模块层）**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：从原始上下文中解析事实和经验。
    *   **Updater**：对比新旧记忆，执行 ADD、DEL、UPDATE 或 NONE 操作。
    *   **Retriever**：执行语义检索或利用推理模型（LRM）进行重排序（Rerank）。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent 范式）。
*   **Agent Layer（智能体层）**：作为策略执行器，通过组合上述模块构建不同的记忆策略，并生成交互轨迹（Rollout）。
*   **Environment Layer（环境层）**：负责数据加载和奖励管理。支持 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短程长上下文）。提供格式奖励和 LLM-as-a-Judge 等多维反馈。
*   **Trainer Layer（训练层）**：核心优化引擎。
    *   **关键技术**：原生集成 **GRPO（群体相对策略优化）**。
    *   **算法流程**：针对每个输入采样一组候选响应，通过组内奖励归一化计算优势函数（Advantage），无需额外的 Critic 模型，显著降低显存占用。

### 3. 实验设计
*   **实验对象**：基于 MemAgent 架构构建的智能体。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集**：使用 MemAgent 公开的训练和评估数据。
*   **Benchmark**：
    *   `eval_50` 和 `eval_100`：主任务测试集。
    *   `eval_fwe_16384`：分布外（OOD）测试集。
*   **对比方法**：对比了各基座模型在经过 MemFactory RL 微调前后的性能表现。

### 4. 资源与算力
*   **硬件环境**：整个训练和评估流水线可以在 **单张 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练 250 个有效步（steps）。为了提高效率，对训练样本进行了简化（将上下文长度缩减至原有的约 1/3，每样本包含 50-80 个文档）。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型在三个测试集上进行了验证。
*   **客观性保障**：为了应对生成的不确定性，所有评分均采用 **avg@4**（即针对每个问题进行 4 次独立实验并取平均值）。
*   **充分性评价**：实验证明了框架在不同规模模型上的有效性，且涵盖了分布内和分布外场景。虽然实证部分主要集中在 MemAgent 范式上，但框架本身已内置了对 Memory-R1 和 RMM 的支持，展示了较好的通用性。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 测试集上表现出更稳健的提升，说明 RL 优化后的记忆策略具有良好的迁移性。
*   **门槛降低**：证明了在有限算力（单卡）下，通过统一框架优化复杂的记忆策略是切实可行的。

### 7. 优点
*   **高度模块化**：解耦了提取、更新、检索等操作，支持“乐高式”快速拼装和算法迭代。
*   **训练高效**：通过集成 GRPO 算法，规避了 PPO 算法中 Critic 模型的显存开销，适合长上下文任务。
*   **开箱即用**：预置了多种前沿记忆算法（Memory-R1, RMM 等）的实现，方便研究者复现和对比。

### 8. 不足与局限
*   **范式覆盖有限**：虽然支持多种范式，但目前的实证研究主要集中在 MemAgent 上，对其他范式（如 Memory-R1 的 CRUD 操作）的 RL 训练效果缺乏同等深度的量化对比。
*   **训练效率仍有空间**：尽管使用了 GRPO，但在处理极长上下文时的训练吞吐量仍有优化空间。
*   **任务假设**：目前的评估主要基于现有的长上下文数据集，对于更复杂的、动态演化的真实世界长期交互场景的覆盖尚不充分。

（完）
