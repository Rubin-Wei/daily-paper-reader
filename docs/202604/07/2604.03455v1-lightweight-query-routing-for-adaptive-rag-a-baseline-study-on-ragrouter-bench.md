---
title: "Lightweight Query Routing for Adaptive RAG: A Baseline Study on RAGRouter-Bench"
title_zh: 面向自适应 RAG 的轻量级查询路由：基于 RAGRouter-Bench 的基准研究
authors: "Prakhar Bansal, Shivangi Agarwal"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03455v1"
tags: ["query:rag"]
score: 9.0
evidence: 自适应RAG的查询路由
tldr: "本研究针对检索增强生成（RAG）中不同策略的成本与能力差异，利用RAGRouter-Bench基准测试，系统评估了轻量级分类器在查询路由中的表现。通过对比TF-IDF、MiniLM嵌入及手工特征，发现TF-IDF结合SVM的效果最佳，在实现93.2%准确率的同时节省了28.1%的Token成本，为自适应RAG提供了高效的基准方案。"
source: arxiv
selection_source: fresh_fetch
motivation: 旨在解决RAG系统中不同查询对检索策略需求不一导致的资源浪费问题，通过自动路由选择最合适的处理路径。
method: 在RAGRouter-Bench上评估了5种经典分类器与3种特征提取方式（TF-IDF、语义嵌入、手工特征）的15种组合。
result: TF-IDF配合SVM表现最优，宏F1值达0.928，且词汇特征在区分查询复杂度上优于语义嵌入。
conclusion: 该研究确立了可复现的查询端路由基准，并指出未来需进一步探索结合语料库信息的路由策略。
---

## 摘要
检索增强生成（RAG）流水线涵盖了广泛的检索策略，这些策略在 Token 成本和能力方面存在显著差异。为每个查询选择合适的策略是一个实际的效率问题，然而目前尚未有路由分类器在 RAGRouter-Bench \citep{wang2026ragrouterbench} 上进行训练。该基准测试最近发布，包含跨越四个知识领域的 7,727 个查询，每个查询都被标注为三种典型查询类型之一：事实型、推理型和摘要型。我们在该基准上首次对基于轻量级分类器的路由进行了系统性评估。我们在三种特征方案（即 TF-IDF、MiniLM 句子嵌入 \citep{reimers2019sbert} 和手工构建的结构化特征）下评估了五种经典分类器，共产生 15 种分类器-特征组合。我们的最佳配置（TF-IDF 结合 SVM）实现了 \mathbf{0.928} 的宏平均 F1 分数和 \mathbf{93.2\%} 的准确率，同时相对于始终使用最昂贵的范式，模拟实现了 \mathbf{28.1\%} 的 Token 节省。词汇 TF-IDF 特征的表现优于语义句子嵌入 3.1 个宏 F1 点，这表明表面关键词模式是查询类型复杂性的强预测指标。领域级分析显示，医学查询最难路由，而法律查询最易处理。这些结果建立了一个可复现的查询端基准，并突显了语料库感知路由（corpus-aware routing）必须弥补的差距。

## Abstract
Retrieval-Augmented Generation pipelines span a wide range of retrieval strategies that differ substantially in token cost and capability. Selecting the right strategy per query is a practical efficiency problem, yet no routing classifiers have been trained on RAGRouter-Bench \citep{wang2026ragrouterbench}, a recently released benchmark of $7,727$ queries spanning four knowledge domains, each annotated with one of three canonical query types: factual, reasoning, and summarization. We present the first systematic evaluation of lightweight classifier-based routing on this benchmark. Five classical classifiers are evaluated under three feature regimes, namely, TF-IDF, MiniLM sentence embeddings \citep{reimers2019sbert}, and hand-crafted structural features, yielding 15 classifier feature combinations. Our best configuration, TF-IDF with an SVM, achieves a macro-averaged F1 of $\mathbf{0.928}$ and an accuracy of $\mathbf{93.2\%}$, while simulating $\mathbf{28.1\%}$ token savings relative to always using the most expensive paradigm. Lexical TF-IDF features outperform semantic sentence embeddings by $3.1$ macro-F1 points, suggesting that surface keyword patterns are strong predictors of query-type complexity. Domain-level analysis reveals that medical queries are hardest to route and legal queries most tractable. These results establish a reproducible query-side baseline and highlight the gap that corpus-aware routing must close.

---

## 论文详细总结（自动生成）

这是一份关于论文《Lightweight Query Routing for Adaptive RAG: A Baseline Study on RAGRouter-Bench》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在检索增强生成（RAG）系统中，不同的检索策略（如简单检索 vs. 迭代检索）在处理能力和 Token 成本上差异巨大。目前大多数系统对所有查询采用统一策略，导致简单查询浪费资源，复杂查询处理不足。
*   **研究动机**：虽然已有研究提出自适应 RAG，但缺乏在最新专门基准（RAGRouter-Bench）上的系统性评估。本文旨在探索仅利用**查询端（Query-side）文本特征**的轻量级分类器，能否在保证准确率的前提下，有效降低 RAG 的运行成本。

### 2. 论文提出的方法论
*   **核心思想**：将 RAG 路由建模为一个三分类任务（事实型、推理型、摘要型），通过轻量级机器学习模型在查询进入 RAG 流程前预测其类型，并映射到对应的检索范式。
*   **关键技术细节**：
    *   **特征工程**：对比了三种特征：
        1.  **词汇特征 (TF-IDF)**：提取 1-gram 和 2-gram，保留 3000 个高频词汇。
        2.  **语义特征 (MiniLM)**：使用 22M 参数的 `all-MiniLM-L6-v2` 模型生成 384 维嵌入向量。
        3.  **结构化特征 (Hand-crafted)**：提取 23 种手工特征，如查询长度、疑问词类型（who/what 等）、否定词、命名实体计数、从句计数等。
    *   **分类器选择**：评估了逻辑回归、SVM（RBF核）、随机森林、KNN 和 MLP。
    *   **路由映射逻辑**：将预测类型映射到特定 RAG 范式：`single_hop` $\rightarrow$ NaiveRAG；`multi_hop` $\rightarrow$ HybridRAG；`summary` $\rightarrow$ IterativeRAG。

### 3. 实验设计
*   **数据集 (RAGRouter-Bench)**：包含 7,727 个查询，涵盖四个领域：MuSiQue (维基百科)、QuALITY (文学)、UltraDomain (法律) 和 GraphRAG-Bench (医学)。
*   **对比基准 (Baselines)**：
    *   **Majority Class**：始终预测出现频率最高的事实型（single_hop）。
    *   **Perfect-label Reference**：假设分类器 100% 准确时的理想成本节省。
    *   **方法对比**：15 种“特征+分类器”的组合交叉对比。
*   **评估指标**：宏平均 F1 分数（Macro-F1）、准确率（Accuracy）以及模拟 Token 节省比例（Savings %）。

### 4. 资源与算力
*   **算力说明**：论文强调了“轻量级”属性。MiniLM 模型（22M 参数）明确提到可以在 **CPU** 上运行，无需 GPU。
*   **训练时长**：文中未明确给出具体的训练分钟数，但鉴于使用的是经典机器学习算法（SVM, RF 等）和数千条数据，训练开销在现代个人电脑上通常仅需数秒至数分钟。

### 5. 实验数量与充分性
*   **实验规模**：进行了 15 种模型组合的对比，并采用了 **5 折分层交叉验证**（5-fold stratified cross-validation），确保了结果的统计可靠性。
*   **充分性与客观性**：实验覆盖了四个不同性质的知识领域，并进行了领域级的细分分析。通过对比“成本 vs. 准确率”的权衡，客观地展示了单纯追求成本节省（如 Majority 策略）会导致准确率崩塌，证明了实验设计的严谨性。

### 6. 论文的主要结论与发现
*   **最佳配置**：**TF-IDF + SVM** 表现最好，宏 F1 达 0.928，准确率 93.2%，可节省 28.1% 的 Token 成本。
*   **词汇 vs. 语义**：令人意外的是，**TF-IDF（词汇特征）优于 MiniLM（语义嵌入）**。这表明在 RAGRouter-Bench 中，查询的表面关键词（如“summarize”、“compare”）是判断复杂度的强信号，而语义嵌入可能会模糊不同领域间相似表述的类型差异。
*   **领域差异**：法律领域最容易路由（F1=0.967），而医学领域最难（F1=0.803），这与医学语料库结构单一、词汇区分度低有关。

### 7. 优点
*   **高效实用**：证明了无需调用昂贵的 LLM 即可实现高效路由，对工业界落地 RAG 系统具有直接参考价值。
*   **基准确立**：为 RAGRouter-Bench 提供了第一个公开的分类器基准，填补了该数据集发布后的空白。
*   **深入分析**：不仅给出了总分，还深入探讨了特征类型的影响和跨领域的泛化难度。

### 8. 不足与局限
*   **仅限查询端信息**：分类器完全忽略了语料库侧（Corpus-side）的信息。原基准论文指出 RAG 性能受查询与语料库交互的影响，本文的方法无法捕捉这种动态关系。
*   **映射简化**：将查询类型到 RAG 范式的映射（如 multi_hop 对应 HybridRAG）是一种基于文献的简化假设，实际应用中这种对应关系可能更复杂。
*   **泛化风险**：实验仅限于 RAGRouter-Bench 的四个领域，对于完全脱离分布（OOD）的新领域表现尚不可知。

（完）
