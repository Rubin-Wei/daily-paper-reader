---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:am"]
score: 10.0
evidence: 智能体记忆训练与推理的统一框架
tldr: "针对记忆增强型大语言模型智能体在训练与推理中缺乏统一框架的问题，本文提出了MemFactory。该框架采用模块化“乐高式”架构，将记忆生命周期抽象为原子组件，并原生支持GRPO算法以优化记忆管理策略。MemFactory兼容多种前沿范式，显著降低了开发门槛，并在实验中实现了最高14.8%的性能提升，为长短期记忆智能体的发展提供了标准化基础设施。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强智能体实现高度碎片化且缺乏统一的训练与推理基础设施。
method: 提出一种模块化的统一框架，通过原子化组件和GRPO强化学习算法实现记忆管理策略的灵活构建与优化。
result: "在MemAgent架构上的实验表明，该框架在多个评估集上均优于基础模型，性能提升最高达14.8%。"
conclusion: MemFactory通过提供标准化且易扩展的工具链，有效降低了记忆驱动型AI智能体的研发门槛。
---

## 摘要
记忆增强的大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近年来，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为了填补这一空白，我们提出了 MemFactory，这是首个专为记忆增强智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均有一致的性能提升，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深入总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：现有的记忆增强型大语言模型（LLM）智能体在记忆管理（提取、更新、检索）上正从启发式规则转向强化学习（RL）优化，但相关研究存在**实现碎片化、任务高度耦合、缺乏统一基础设施**的问题。
*   **研究动机**：开发者难以复现或组合不同的记忆策略（如将 Memory-R1 的检索模块替换为 RMM 的重排序器）。受 LLaMA-Factory 成功的启发，作者旨在构建一个标准化的框架，降低记忆驱动型智能体的开发与训练门槛。

### 2. 方法论：核心思想与技术细节
MemFactory 采用高度模块化的“乐高式”架构，分为四个核心层：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作。
    *   **Extractor**：解析原始上下文为结构化事实。
    *   **Updater**：执行增删改（ADD/DEL/UPDATE/NONE）操作维护记忆库。
    *   **Retriever**：基于语义或重排序（Rerank）检索相关记忆。
    *   **Agent Module**：支持端到端的循环记忆策略（如 MemAgent）。
*   **智能体层 (Agent Layer)**：负责模块组装与策略执行，支持训练模式（生成轨迹）和纯推理模式。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器。提供 `MemoryBankEnv`（长期记忆库）和 `LongcontextEnv`（短期长文本）两种环境，并支持格式奖励和 LLM-as-a-Judge 评估。
*   **训练层 (Trainer Layer)**：核心优化引擎。
    *   **关键算法**：原生集成 **GRPO (Group Relative Policy Optimization)**。该算法通过组内奖励归一化估计基准，无需额外的 Critic 模型，显著降低了显存占用，适合长上下文场景。

### 3. 实验设计
*   **实验对象**：以 **MemAgent** 架构为基础，验证框架的有效性。
*   **基座模型**：Qwen3-1.7B 和 Qwen3-4B-Instruct。
*   **数据集/Benchmark**：使用 MemAgent 公开的数据集。
    *   **主任务评估**：`eval_50` 和 `eval_100`（不同长度的文档集）。
    *   **分布外（OOD）评估**：`eval_fwe_16384`（测试模型的泛化能力）。
*   **对比方法**：对比了经过 MemFactory RL 微调后的模型与原始基座模型（Base Checkpoint）的性能。

### 4. 资源与算力
*   **硬件设备**：实验在**单台 NVIDIA A800 80GB GPU** 上完成。
*   **训练细节**：每个模型训练 250 个有效步（steps）。为了提高效率，训练样本的上下文长度被简化为原始长度的约 1/3（每样本 50-80 个文档）。
*   **可复现性**：作者强调该框架在有限算力下具有高度的可复现性。

### 5. 实验数量与充分性
*   **实验规模**：针对两种不同规模的模型（1.7B 和 4B）进行了对比，并在三个不同的测试集上进行了验证。
*   **客观性保障**：所有评分均采用 **avg@4**（即针对每个问题进行 4 次独立试验并取平均值），以减少生成随机性带来的偏差。
*   **充分性评价**：实验涵盖了域内和分布外场景，验证了框架在性能提升和策略迁移上的表现。虽然实验主要集中在 MemAgent 这一种范式上，但框架本身已内置了对 Memory-R1 和 RMM 的支持。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的表现。Qwen3-1.7B 的平均得分相对提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **模型规模效应**：较大的模型（4B）在 OOD 任务上表现更稳健，能够更好地将学习到的记忆策略迁移到未见过的设置中；而较小模型（1.7B）在 OOD 任务上出现了轻微的性能下降。
*   **框架有效性**：证明了通过统一框架和 GRPO 算法，即使在有限算力下也能有效优化复杂的记忆管理策略。

### 7. 优点与亮点
*   **高度模块化**：通过“原子操作”抽象，研究者可以像搭乐高一样组合不同的记忆提取、更新和检索策略。
*   **降低门槛**：统一了数据格式、训练流水线和评估标准，填补了记忆增强智能体缺乏通用基础设施的空白。
*   **显存友好**：原生支持 GRPO，解决了长上下文 RL 训练中显存易溢出的痛点。
*   **开箱即用**：预置了多种前沿算法（Memory-R1, RMM 等）的实现。

### 8. 不足与局限
*   **范式覆盖有限**：虽然框架设计通用，但目前的实证研究主要集中在 MemAgent 范式上，对其他内置范式（如 Memory-R1）的跨任务对比尚不充分。
*   **训练效率**：论文提到在训练效率方面仍有提升空间。
*   **小模型泛化风险**：实验显示小参数模型在经过 RL 优化后，在分布外（OOD）任务上可能存在性能退化的风险。

（完）
