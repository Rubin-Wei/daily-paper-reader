---
title: "MemFactory: Unified Inference & Training Framework for Agent Memory"
title_zh: MemFactory：智能体记忆的统一推理与训练框架
authors: "Ziliang Guo, Ziheng Li, Zhiyu Li"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29493v1"
tags: ["query:am"]
score: 10.0
evidence: 记忆增强智能体的统一框架
tldr: MemFactory是首个针对记忆增强型智能体设计的统一训练与推理框架，旨在解决现有记忆管理实现碎片化且难以通用的问题。该框架将记忆生命周期抽象为原子化、即插即用的模块，支持开发者以“乐高”式架构快速构建自定义智能体。通过原生集成GRPO强化学习算法，MemFactory能够根据环境奖励优化记忆的提取与检索策略，并兼容Memory-R1等前沿范式，显著降低了长短期记忆智能体的开发与研究门槛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29493v1/fig-001.webp\", \"caption\": \"Figure 1 The overall architecture of MemFactory, illustrating the interdependencies among the Modules, Agent, Environment, and Trainer layers.\", \"page\": 4, \"index\": 1, \"width\": 929, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2603.29493v1/fig-002.webp\", \"caption\": \"Table 1 Standard interfaces supported by modules in the Module Layer.\", \"page\": 5, \"index\": 2, \"width\": 968, \"height\": 221}]"
motivation: 现有的记忆增强型智能体实现方案高度碎片化且缺乏统一的基础设施，导致记忆操作的集成、训练和评估过程异常复杂。
method: 提出模块化框架MemFactory，将记忆生命周期抽象为原子组件，并集成GRPO算法以环境奖励驱动记忆管理策略的微调。
result: "在MemAgent架构上的实验表明，MemFactory在域内和分布外评估集中均优于基础模型，相对性能增益最高达14.8%。"
conclusion: MemFactory通过提供标准化、可扩展且易用的基础设施，为未来记忆驱动型AI智能体的创新与普及铺平了道路。
---

## 摘要
记忆增强型大语言模型（LLMs）对于开发具备长期能力的 AI 智能体至关重要。近年来，应用强化学习（RL）来优化记忆操作（如提取、更新和检索）已成为一个极具前景的研究方向。然而，现有的实现方式仍然高度碎片化且针对特定任务，缺乏统一的基础设施来简化这些复杂流水线的集成、训练和评估。为填补这一空白，我们推出了 MemFactory，这是首个专为记忆增强型智能体设计的统一、高度模块化的训练与推理框架。受 LLaMA-Factory 等统一微调框架成功的启发，MemFactory 将记忆生命周期抽象为原子化的、即插即用的组件，使研究人员能够通过“乐高式”架构无缝构建自定义记忆智能体。此外，该框架原生集成了群体相对策略优化（GRPO），以微调由多维环境奖励驱动的内部记忆管理策略。MemFactory 为 Memory-R1、RMM 和 MemAgent 等近期前沿范式提供开箱即用的支持。我们在开源的 MemAgent 架构上，利用其公开的训练和评估数据对 MemFactory 进行了实证验证。在域内和分布外评估集上，MemFactory 相比相应的基座模型均能持续提升性能，相对增益最高达 14.8%。通过提供标准化、可扩展且易于使用的基础设施，MemFactory 显著降低了准入门槛，为记忆驱动型 AI 智能体的未来创新铺平了道路。

## Abstract
Memory-augmented Large Language Models (LLMs) are essential for developing capable, long-term AI agents. Recently, applying Reinforcement Learning (RL) to optimize memory operations, such as extraction, updating, and retrieval, has emerged as a highly promising research direction. However, existing implementations remain highly fragmented and task-specific, lacking a unified infrastructure to streamline the integration, training, and evaluation of these complex pipelines. To address this gap, we present MemFactory, the first unified, highly modular training and inference framework specifically designed for memory-augmented agents. Inspired by the success of unified fine-tuning frameworks like LLaMA-Factory, MemFactory abstracts the memory lifecycle into atomic, plug-and-play components, enabling researchers to seamlessly construct custom memory agents via a "Lego-like" architecture. Furthermore, the framework natively integrates Group Relative Policy Optimization (GRPO) to fine-tune internal memory management policies driven by multi-dimensional environmental rewards. MemFactory provides out-of-the-box support for recent cutting-edge paradigms, including Memory-R1, RMM, and MemAgent. We empirically validate MemFactory on the open-source MemAgent architecture using its publicly available training and evaluation data. Across both in-domain and out-of-distribution evaluation sets, MemFactory consistently improves performance over the corresponding base models, with relative gains of up to 14.8%. By providing a standardized, extensible, and easy-to-use infrastructure, MemFactory significantly lowers the barrier to entry, paving the way for future innovations in memory-driven AI agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《MemFactory: Unified Inference & Training Framework for Agent Memory》的结构化深度分析总结：

### 1. 研究动机与核心问题
*   **核心问题**：尽管记忆增强型大语言模型（LLM Agents）在长期对话和个性化任务中至关重要，但目前的记忆管理（提取、更新、检索）实现方案高度**碎片化**且**任务特定**。
*   **研究背景**：现有的前沿研究（如 Memory-R1, MemAgent）虽然证明了利用强化学习（RL）优化记忆策略的有效性，但缺乏像 LLaMA-Factory 那样统一、模块化且易用的基础设施，导致研究人员在复现、组合不同记忆模块时面临巨大的工程障碍。

### 2. 核心方法论
MemFactory 提出了一个四层架构的统一框架，旨在实现“乐高式”的智能体构建：
*   **模块层 (Module Layer)**：将记忆生命周期抽象为原子操作，包括：
    *   **Extractor**：从原始上下文解析事实。
    *   **Updater**：执行增删改（ADD/DEL/UPDATE/NONE）操作维护记忆库。
    *   **Retriever**：基于语义或重排序（Rerank）检索相关记忆。
    *   **Agent Module**：支持端到端的循环记忆（Recurrent Memory）策略。
*   **智能体层 (Agent Layer)**：负责模块集成与策略执行，支持训练模式（生成轨迹）和纯推理模式。
*   **环境层 (Environment Layer)**：充当数据加载器和奖励管理器，提供格式奖励、LLM-as-a-Judge 等多维信号。
*   **训练层 (Trainer Layer)**：核心引擎。原生集成 **GRPO (Group Relative Policy Optimization)** 算法。
    *   **关键技术**：GRPO 通过组内奖励归一化估计基准，无需额外的 Critic 网络，极大地节省了显存，适合长上下文的记忆训练任务。

### 3. 实验设计与 Benchmark
*   **实验对象**：以 **MemAgent** 架构为基准进行实证研究。
*   **数据集**：使用 MemAgent 公开的训练和评估集。
    *   **域内测试**：`eval_50` 和 `eval_100`（主要任务）。
    *   **分布外测试 (OOD)**：`eval_fwe_16384`（测试泛化能力）。
*   **对比基准**：对比了 Qwen3-1.7B 和 Qwen3-4B-Instruct 的原始 Checkpoint 与经过 MemFactory RL 微调后的性能。
*   **内置支持**：框架预装了 Memory-R1、MemAgent 和 RMM 三种 SOTA 范式的实现。

### 4. 资源与算力
*   **硬件设备**：实验明确指出整个训练和评估流水线可以在 **单台 NVIDIA A800 80GB GPU** 上运行。
*   **训练细节**：每个模型训练了 **250 个有效步数 (steps)**。
*   **效率体现**：通过集成 FlashAttention-2 和使用 GRPO 算法，框架展现了极高的算力友好性，降低了 Memory-RL 的研究门槛。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同规模的基座模型（1.7B 和 4B）进行了对比实验。
*   **重复性**：所有评分均基于 **4 次独立试验的平均值 (avg@4)**，以减少生成随机性带来的偏差。
*   **充分性评价**：实验覆盖了从小型模型到中型指令微调模型，并包含了 OOD 测试，能够客观反映框架的优化效果。虽然消融实验较少，但作为框架类论文，其重点在于验证“统一框架能否有效提升性能”，这一点已得到充分证明。

### 6. 主要结论与发现
*   **性能提升**：MemFactory 显著提升了基座模型的记忆管理能力。Qwen3-1.7B 的平均性能提升了 **14.8%**；Qwen3-4B-Instruct 提升了 **7.3%**。
*   **泛化能力**：较大规模的模型（4B）在 OOD 任务上表现出更强的策略迁移能力，而小模型在 OOD 上略有下降。
*   **易用性验证**：证明了通过简单的模块组合和标准的 RL 流程，即可复现并超越现有的复杂记忆策略。

### 7. 优点与亮点
*   **高度模块化**：首个将记忆操作原子化的框架，支持“即插即用”，极大方便了研究人员交换不同的检索器或更新器。
*   **显存优化**：原生支持 GRPO，解决了记忆增强任务中“长上下文 + RL 训练”导致的显存爆炸问题。
*   **开箱即用**：集成了多种 SOTA 算法模板，降低了从零开发记忆智能体的难度。

### 8. 不足与局限
*   **范式覆盖有限**：目前仅涵盖了三种代表性范式，对于更复杂的分布式记忆或多模态记忆支持尚待扩展。
*   **训练效率**：虽然支持单卡训练，但在处理极长序列时的训练吞吐量仍有优化空间。
*   **评估维度**：实验主要集中在性能分数上，对于记忆更新过程中的幻觉率、冗余度等细粒度指标分析较少。

（完）
