---
title: Adaptive Stopping for Multi-Turn LLM Reasoning
title_zh: 大语言模型多轮推理的自适应停止
authors: "Xiaofan Zhou, Huy Nguyen, Bo Yu, Chenxi Liu, Lu Cheng"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01413v1"
tags: ["query:rag"]
score: 9.0
evidence: 自适应检索增强生成 (RAG) 和 ReAct 风格智能体
tldr: 针对多轮推理（如RAG和ReAct）中缺乏可靠停止准则的问题，本文提出了首个面向多轮LLM推理的符合预测框架MiCP。该框架通过在不同轮次间动态分配误差预算，实现了在保证预测覆盖率（准确性）的同时能够自适应地提前停止。实验证明MiCP在多个问答基准上有效降低了推理成本和延迟，并为高风险领域的应用提供了形式化的可靠性保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01413v1/fig-001.webp\", \"caption\": \"Figure 2: Empirical gold retention rate vs. the target 1 − α across five datasets on adaptive RAG and ReAct for retrieval calibration. All models maintain coverage at or above the 1 − α guarantee for all tested error rates.\", \"page\": 8, \"index\": 1, \"width\": 820, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.01413v1/fig-002.webp\", \"caption\": \"Table 1: Average number of retrieval turns (Avg #Turns), prediction set size, and composite objective across five datasets, averaged over all α values. Bold indicates the best result per row. Lower is better for all metrics. w/o ES means no early stop.\", \"page\": 9, \"index\": 2, \"width\": 824, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2604.01413v1/fig-003.webp\", \"caption\": \"Figure 3: Empirical coverage rate vs. the target 1 − α across five datasets on adaptive RAG and ReAct for the final prediction set. All models maintain coverage at or above the 1 − α guarantee for all tested error rates.\", \"page\": 9, \"index\": 3, \"width\": 817, \"height\": 192}]"
motivation: 现有的多轮推理方法依赖启发式停止规则或固定轮次，缺乏正式的可靠性保证，导致在追求准确性的同时难以平衡推理成本与延迟。
method: 提出了MiCP框架，通过在多轮推理过程中分配不同的误差预算，将符合预测技术扩展到多轮交互场景以实现自适应停止。
result: 在单跳和多跳问答任务中，MiCP在满足预设覆盖率目标的同时，显著减少了推理轮次、推理成本和预测集大小。
conclusion: MiCP为多轮LLM推理提供了一种兼顾效率与可靠性的自适应停止方案，并引入了联合评估覆盖率与效率的新指标。
---

## 摘要
大语言模型 (LLM) 越来越依赖多轮推理和交互（例如自适应检索增强生成 (RAG) 和 ReAct 风格的智能体）来回答难题。这些方法通过迭代地检索信息、推理或行动来提高准确性，但也引入了一个关键挑战：模型应该在何时停止？现有方法依赖于启发式停止规则或固定的轮次预算，且无法提供最终预测仍包含正确答案的形式化保证。这种局限性在金融和医疗等高风险领域尤为突出，在这些领域，不必要的轮次会增加成本和延迟，而过早停止则面临决策错误的风险。符合预测 (Conformal Prediction, CP) 提供了形式化的覆盖率保证，但现有的 LLM-CP 方法仅适用于单个模型输出，无法处理具有自适应停止的多轮流水线。为了填补这一空白，我们提出了带有符合预测的多轮语言模型 (MiCP)，这是首个用于多轮推理的 CP 框架。MiCP 在不同轮次之间分配不同的误差预算，使模型能够在保持整体覆盖率保证的同时实现尽早停止。我们在自适应 RAG 和 ReAct 上演示了 MiCP，它在单跳和多跳问答基准测试中均达到了目标覆盖率，同时减少了轮次、推理成本和预测集大小。我们还引入了一种新指标，用于共同评估覆盖率有效性和回答效率。

## Abstract
Large Language Models (LLMs) increasingly rely on multi-turn reasoning and interaction, such as adaptive retrieval-augmented generation (RAG) and ReAct-style agents, to answer difficult questions. These methods improve accuracy by iteratively retrieving information, reasoning, or acting, but introduce a key challenge: \textbf{When should the model stop?} Existing approaches rely on heuristic stopping rules or fixed turn budgets and provide no formal guarantees that the final prediction still contains the correct answer. This limitation is particularly problematic in high-stakes domains such as finance and healthcare, where unnecessary turns increase cost and latency, while stopping too early risks incorrect decisions. Conformal prediction (CP) provides formal coverage guarantees, but existing LLM-CP methods only apply to a single model output and cannot handle multi-turn pipelines with adaptive stopping. To address this gap, we propose Multi-Turn Language Models with Conformal Prediction (MiCP), the first CP framework for multi-turn reasoning. MiCP allocates different error budgets across turns, enabling the model to stop early while maintaining an overall coverage guarantee. We demonstrate MiCP on adaptive RAG and ReAct, where it achieves the target coverage on both single-hop and multi-hop question answering benchmarks while reducing the number of turns, inference cost, and prediction set size. We further introduce a new metric that jointly evaluates coverage validity and answering efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：大语言模型多轮推理的自适应停止 (MiCP)

#### 1. 核心问题与整体含义
*   **研究动机**：现代 LLM 系统（如自适应 RAG 和 ReAct 智能体）通过多轮检索、推理和行动来解决复杂问题。然而，这些系统面临一个核心矛盾：**何时停止？** 停止过早会导致信息不足、回答错误；停止过晚则增加延迟、推理成本，甚至引入噪声。
*   **背景局限**：现有的停止准则多为启发式（如固定轮次或简单的置信度阈值），缺乏形式化的数学保证，无法确保最终输出包含正确答案。这在医疗、金融等高风险领域是不可接受的。
*   **核心目标**：将“符合预测”（Conformal Prediction, CP）引入多轮推理，在保证用户指定的错误率（如 95% 的准确率保证）的前提下，实现自适应的提前停止。

#### 2. 方法论：MiCP 框架
MiCP（Multi-Turn Language Models with Conformal Prediction）包含两个关键阶段：

*   **阶段 1：检索过滤（Retrieval Filtering）**
    *   在每一轮检索中，利用 CP 校准一个相关性阈值 $\hat{q}_{ret}$。
    *   仅保留相关性得分高于该阈值的文档，确保在过滤噪声的同时，以 $1-\alpha_{ret}$ 的概率保留金标文档。
*   **阶段 2：误差预算分配与自适应停止**
    *   **核心思想**：将总的错误预算 $\alpha$ 分配到不同的推理轮次中。简单问题在早期轮次消耗较少预算，复杂问题留给后期轮次。
    *   **不确定性度量**：使用采样答案分布的**归一化熵（Normalized Entropy, NE）**作为信号。
    *   **校准流程**：
        1.  在校准集上计算每一轮的 NE 分布。
        2.  通过网格搜索优化各轮次的误差分配比例，最小化联合评估指标。
        3.  计算每一轮的 NE 阈值 $\hat{q}_t$。
    *   **推理逻辑**：在测试时，若当前轮次的置信度超过该轮次的 CP 阈值，则立即停止并输出预测集；若达到最大轮次仍未达标，则输出包含“无法回答（Can't Answer）”标签的预测集。

#### 3. 实验设计
*   **数据集**：涵盖单跳和多跳问答，包括 Natural Questions (NQ), TriviaQA, HotpotQA, MuSiQue, 和 2WikiMulti-HopQA。
*   **应用场景**：
    *   **Adaptive RAG**：动态决定是否需要进一步检索。
    *   **ReAct**：模型在推理和外部工具行动之间交替。
*   **基准模型（Backbones）**：Gemma-2-9B-IT, Qwen3.5-9B, GPT-4o-mini。
*   **对比方法**：
    *   **w/o ES**：无提前停止（运行所有轮次）。
    *   **Avg Iter**：以最小化平均轮次为目标的优化变体。
    *   **Answer Rate**：以最大化可回答率为目标的优化变体。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练总时长。
*   **推理配置**：每轮抽取 $M=15$ 个随机样本，温度系数设为 1.4。检索使用基于 Elasticsearch 的维基百科索引。
*   **计算特性**：由于 MiCP 属于后处理（Post-hoc）校准框架，不需要对 LLM 进行重新训练或微调，主要的计算开销在于推理时的多样本采样。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流数据集上进行了测试，每个数据集划分为优化集（300）、校准集（300）和测试集（300）。
*   **覆盖范围**：测试了 5 个不同的目标错误率 $\alpha \in \{0.05, 0.10, 0.15, 0.20, 0.25\}$。
*   **公平性与客观性**：实验涵盖了开源（Gemma, Qwen）和闭源（GPT）模型，并对比了不同的优化目标。通过 3 个随机种子取平均值，结果显示 MiCP 在不同模型和任务下均能稳定维持覆盖率保证，实验设计较为充分且客观。

#### 6. 主要结论与发现
*   **可靠性**：MiCP 在所有测试场景下均成功达到了预设的覆盖率目标（即实际准确率 $\ge 1-\alpha$）。
*   **效率提升**：相比于不停止的基准，MiCP 显著减少了推理轮次（尤其在 2WikiMHQA 等任务上），降低了推理成本。
*   **预测集优化**：通过提前停止，MiCP 过滤了后期可能引入的噪声答案，从而减小了最终预测集的大小。
*   **模型差异**：更强大的模型（如 Qwen3.5, GPT-4o-mini）在区分“可回答”与“不可回答”问题上表现更好，从而在 MiCP 框架下获得更高的效率增益。

#### 7. 优点
*   **首创性**：提出了首个针对多轮 LLM 推理的符合预测框架。
*   **理论完备**：提供了检索覆盖率和预测集覆盖率的数学证明。
*   **通用性强**：不依赖模型内部 Logits（仅需采样输出），可无缝接入 RAG、ReAct 等多种多轮流水线。
*   **多维评估**：引入了联合衡量可靠性与效率的新指标，更符合实际应用需求。

#### 8. 不足与局限
*   **过覆盖问题（Overcoverage）**：实验观察到实际覆盖率有时远高于目标值（例如目标错误 0.1，实际仅 0.02），这主要是由于基于频率的非置信度得分具有离散性，导致预测集可能过于保守。
*   **采样成本**：虽然减少了轮次，但 CP 框架要求每轮进行多次采样（如 15 次），这在单次推理成本上仍高于单次生成的启发式方法。
*   **数据依赖**：需要一定规模的校准集来确定阈值，对于极度稀缺的数据场景可能受限。

（完）
