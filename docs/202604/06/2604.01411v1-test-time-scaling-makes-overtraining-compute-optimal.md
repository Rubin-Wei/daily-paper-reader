---
title: Test-Time Scaling Makes Overtraining Compute-Optimal
title_zh: 测试时缩放使过度训练达到计算最优
authors: "Nicholas Roberts, Sungjun Cho, Zhiqi Gao, Tzu-Heng Huang, Albert Wu, Gabriel Orlanski, Avi Trost, Kelly Buchanan, Aws Albarghouthi, Frederic Sala"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01411v1"
tags: ["query:ttt"]
score: 9.0
evidence: 预训练与测试时缩放的联合优化
tldr: 本研究针对现代大语言模型在推理阶段的缩放特性，指出传统的Chinchilla预训练缩放法则因忽略推理成本而存在局限。为此，作者提出了Train-to-Test ($T^2$) 缩放法则，旨在固定总预算下联合优化模型参数量、训练数据量及推理采样次数。研究发现，考虑推理成本后，最优预训练策略会显著向“过度训练”区间偏移。通过预训练验证及后训练阶段的测试，证明了该法则在提升模型性能和部署效率方面的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01411v1/fig-001.webp\", \"caption\": \"Figure 2: Optimal pretraining forecasts predicted by both T2 approaches, compared to Hoffmann et al. (2022). (Left) Optimal tokens per parameter (including the 20 tokens per parameter rule of thumb used by practitioners), (Middle) Optimal model sizes. (Right) Optimal training set sizes. Both T2 approaches forecast extreme overtraining.\", \"page\": 6, \"index\": 1, \"width\": 811, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2604.01411v1/fig-002.webp\", \"caption\": \"Figure 6: Approach 1 IsoFLOP profiles across different scaling budgets for all eight tasks.\", \"page\": 15, \"index\": 2, \"width\": 829, \"height\": 1223}, {\"url\": \"assets/figures/arxiv/2604.01411v1/fig-003.webp\", \"caption\": \"Figure 1: Our T2 scaling laws combine Chinchilla scaling for pretraining with pass@k modeling for test-time scaling via repeated sampling to obtain optimal pretraining allocations subject to a test-time scaling budget. T2 recommends overtraining compared to Chinchilla.\", \"page\": 2, \"index\": 3, \"width\": 826, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.01411v1/fig-004.webp\", \"caption\": \"Figure 3: T2 scaling across all of our evaluation tasks. Both approaches improve monotonically over Chinchilla scaling, while Chinchilla exhibits non-monotonic scaling in Ctrain.\", \"page\": 7, \"index\": 4, \"width\": 802, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2604.01411v1/fig-005.webp\", \"caption\": \"Figure 8: Overall checkpoint scaling grid. Each cell reports the number of tokens per parameter. Orange cells are overtrained checkpoints we created.\", \"page\": 17, \"index\": 5, \"width\": 829, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2604.01411v1/fig-006.webp\", \"caption\": \"Figure 5: T2 overtraining findings survive post-training. The optimal frontier is slightly subdued compared to base models, which is consistent with Springer et al. (2025).\", \"page\": 9, \"index\": 6, \"width\": 813, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2604.01411v1/fig-007.webp\", \"caption\": \"Figure 7: Approach 2 IsoFLOP profiles across different scaling budgets for all eight tasks.\", \"page\": 16, \"index\": 7, \"width\": 829, \"height\": 1223}]"
motivation: 传统的预训练缩放法则忽略了推理阶段的计算成本，导致在端到端总预算下无法实现性能最优。
method: "提出 $T^2$ 缩放法则，将推理侧的 pass@k 采样建模引入缩放公式，实现预训练与推理决策的联合优化。"
result: 实验表明，在考虑推理成本时，最优预训练策略会显著转向“过度训练”区间，即使用更多数据训练更小的模型。
conclusion: 推理侧缩放使得过度训练成为实现全局计算最优的关键，且该结论在现代大模型的后训练和实际部署中依然成立。
---

## 摘要
现代大语言模型（LLM）在测试时进行缩放（例如通过重复采样），此时推理成本随模型大小和采样数量而增加。这产生了一种权衡，而 Chinchilla 等预训练缩放法则并未解决这一问题。我们提出了“训练到测试”（$T^2$）缩放法则，在固定的端到端预算下，联合优化模型大小、训练 Token 数量和推理采样次数。$T^2$ 利用用于测试时缩放的 pass@k 建模对预训练缩放法则进行了现代化改进，随后联合优化预训练和测试时的决策。$T^2$ 的预测在不同的建模方法中均表现稳健：包括测量对任务损失的联合缩放效应，以及建模对任务准确率的影响。在八个下游任务中，我们发现当考虑到推理成本时，最优预训练决策会激进地转向过度训练区间，远超标准预训练缩放研究的范围。我们通过在 $T^2$ 缩放预测的最优区域内预训练深度过度训练的模型来验证了我们的结果，证实了其性能显著优于仅考虑预训练缩放的模型。最后，由于前沿 LLM 都会经过后训练，我们证明了我们的发现在后训练阶段依然成立，使得 $T^2$ 缩放在现代部署中具有实际意义。

## Abstract
Modern LLMs scale at test-time, e.g. via repeated sampling, where inference cost grows with model size and the number of samples. This creates a trade-off that pretraining scaling laws, such as Chinchilla, do not address. We present Train-to-Test ($T^2$) scaling laws that jointly optimize model size, training tokens, and number of inference samples under fixed end-to-end budgets. $T^2$ modernizes pretraining scaling laws with pass@$k$ modeling used for test-time scaling, then jointly optimizes pretraining and test-time decisions. Forecasts from $T^2$ are robust over distinct modeling approaches: measuring joint scaling effect on the task loss and modeling impact on task accuracy. Across eight downstream tasks, we find that when accounting for inference cost, optimal pretraining decisions shift radically into the overtraining regime, well-outside of the range of standard pretraining scaling suites. We validate our results by pretraining heavily overtrained models in the optimal region that $T^2$ scaling forecasts, confirming their substantially stronger performance compared to pretraining scaling alone. Finally, as frontier LLMs are post-trained, we show that our findings survive the post-training stage, making $T^2$ scaling meaningful in modern deployments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Test-Time Scaling Makes Overtraining Compute-Optimal》的结构化深度分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：传统的预训练缩放法则（如 Chinchilla）仅关注如何分配预训练算力（模型大小 $N$ 与数据量 $D$），而忽略了部署时的推理成本。
*   **研究动机**：现代 LLM 普遍采用测试时缩放（Test-Time Scaling），如通过重复采样（pass@k）来提升性能。小模型虽然单次推理弱，但推理成本低，可以在相同推理预算下进行更多次采样。论文旨在解决：**如果已知测试时的推理预算，预训练阶段的最优决策（$N$ 和 $D$ 的比例）是否应该改变？**

### 2. 方法论：Train-to-Test ($T^2$) 缩放法则
论文提出了两种互补的建模方法，将预训练与推理采样联合优化：
*   **核心思想**：在固定总预算（训练成本 $6ND$ + 推理成本 $2Nk$）下，同时优化参数量 $N$、训练 Token 数 $D$ 和采样次数 $k$。
*   **方法一：基于任务损失（NLL）的参数模型**
    *   将 Chinchilla 损失公式扩展，增加一个关于采样次数 $k$ 的幂律项：$\hat{L}(N, D, k) = E + AN^{-\alpha} + BD^{-\beta} + Gk^{-\gamma}$。
    *   该模型将重复采样的增益转化为负对数似然（NLL）形式，使其与预训练损失函数兼容。
*   **方法二：基于任务准确率（Accuracy）的参数模型**
    *   直接建模 pass@k 准确率。利用 Beta 分布来模拟题目难度的分布，通过 Sigmoid 函数将 Chinchilla 损失映射为 Beta 分布的参数，从而推导出 pass@k 的解析表达式。
*   **推理成本修正**：引入推理预算 $C_{inf} = 2Nk$，使得在对比不同模型时，小模型被分配更多的采样次数 $k$，从而在相同的“推理算力”下进行公平竞争。

### 3. 实验设计与 Benchmark
*   **数据集与任务**：涵盖 8 个下游任务，包括 4 个真实任务（LAMBADA, ARC-Easy, SciQ, OpenBookQA）和 4 个合成任务（知识召回、算术推理、因果推理、空间推理）。
*   **模型设置**：使用了 106 个不同配置的 Checkpoints，参数量从 5M 到 901M，训练 Token 从 50M 到 120B。
*   **对比基准**：
    *   **Chinchilla 方案**：遵循 $D/N \approx 20$ 的标准最优比例。
    *   **过度训练方案**：测试 $D/N$ 远超 20（甚至达到数百倍）的模型在 $T^2$ 法则下的表现。
*   **后训练验证**：对模型进行微调（FT）和指令微调（SFT），验证缩放法则在经过后处理后是否依然有效。

### 4. 资源与算力
*   **算力设备**：后训练实验明确提到使用了 **4 台 NVIDIA A10 GPU**。
*   **训练细节**：预训练使用了 OpenLM 框架，采用 AdamW 优化器，bfloat16 混合精度。
*   **算力规模**：实验覆盖了 3 个数量级的计算规模（训练 FLOPs 从 $10^{16}$ 到 $10^{19}$）。虽然未详细列出总 GPU 时数，但从 106 个 Checkpoints 的规模来看，实验计算量较大。

### 5. 实验数量与充分性
*   **实验规模**：论文构建了一个包含 106 个模型的密集网格，并额外专门训练了 21 个处于“过度训练区”的模型进行验证。
*   **充分性**：实验跨越了 8 个不同性质的任务，并对比了两种完全不同的建模路径（损失 vs 准确率），结果高度一致。
*   **客观性**：作者不仅在基础模型上做了实验，还考虑了后训练（SFT）的影响，这使得结论更贴近工业界实际部署场景，实验设计较为严谨。

### 6. 主要结论与发现
*   **过度训练是计算最优的**：一旦考虑推理成本，最优预训练决策会激进地向小模型倾斜。为了在推理时获得最佳 pass@k 性能，模型应该比 Chinchilla 建议的训练时间长得多（即更小的 $N$，更大的 $D$）。
*   **法则的稳健性**：无论建模 NLL 还是 Accuracy，$T^2$ 均预测出相同的过度训练趋势。
*   **后训练持久性**：尽管过度训练的模型微调起来更难，但 $T^2$ 预测的最优区间在 SFT 后依然保持领先。
*   **性能超越**：在相同推理预算下，经过 $T^2$ 优化的过度训练小模型，其性能显著超过了按 Chinchilla 比例训练的大模型。

### 7. 优点与亮点
*   **视角创新**：首次将预训练、过度训练和测试时采样（pass@k）整合进统一的数学框架。
*   **理论与实践结合**：不仅给出了公式，还通过从头训练新模型验证了预测的准确性，证明了法则的预测外推能力。
*   **解决实际痛点**：为模型开发者提供了明确的指导——如果目标是部署一个高性能的推理模型，应该“缩减模型规模，延长训练时间”。

### 8. 不足与局限
*   **模型规模限制**：实验主要在 1B 参数以下的小模型上进行，虽然跨越了多个数量级，但在千亿级（100B+）参数模型上的适用性仍需进一步验证。
*   **推理模式单一**：主要关注并行采样（pass@k），未深入探讨更复杂的测试时缩放技术（如搜索树 MCTS、思维链 CoT 迭代等）对缩放法则的影响。
*   **架构依赖**：实验基于标准 Transformer 架构，未考虑推理优化技术（如量化、推测采样、KV Cache 优化）可能带来的推理成本非线性变化。

（完）
