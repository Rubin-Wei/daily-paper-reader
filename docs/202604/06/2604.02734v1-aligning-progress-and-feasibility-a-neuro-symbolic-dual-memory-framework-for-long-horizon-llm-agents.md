---
title: "Aligning Progress and Feasibility: A Neuro-Symbolic Dual Memory Framework for Long-Horizon LLM Agents"
title_zh: 对齐进度与可行性：一种用于长程大语言模型智能体的神经符号双记忆框架
authors: "Bin Wen, Ruoxuan Zhang, Yang Chen, Hongxia Xie, Lan-Zhe Guo"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02734v1"
tags: ["query:am"]
score: 10.0
evidence: LLM智能体的神经符号双重记忆框架
tldr: 针对大语言模型智能体在长程任务中容易陷入循环或偏离目标的难题，本文提出一种神经符号双记忆框架。该框架将语义进度引导与逻辑可行性验证解耦，分别利用基于神经网络的“进度记忆”从成功经验中提取语义蓝图，以及基于符号逻辑的“可行性记忆”对失败尝试进行严格校验。实验证明该方法在多个基准测试中显著提升了任务成功率，并大幅降低了无效动作率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02734v1/fig-001.webp\", \"caption\": \"Table 5: Environment-wise statistics of rule verification and pruning. Positive transitions are used to eliminate false-positive rules, and negative transitions are used for greedy coverage-based selection. The final rule bank used at inference time is intentionally compact.\", \"page\": 15, \"index\": 1, \"width\": 827, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2604.02734v1/fig-002.webp\", \"caption\": \"Figure 1: Illustration of our neuro-symbolic dual-alignment framework. (a) The DualAlignment Challenge: Long-horizon agents often trap themselves in a reinforcing failure cycle caused by coupled progress drift and feasibility failures. (b) The Dual-Alignment Paradigm: Our approach shifts from error-prone unaligned execution to a stable, dually aligned reasoning loop. (c) Neuro-Symbolic Dual Memory: The agent concurrently aligns local actions via executable symbolic rules in Feasibility Memory and anchors global steps via retrieved procedural blueprints in Progress Memory.\", \"page\": 2, \"index\": 2, \"width\": 812, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.02734v1/fig-003.webp\", \"caption\": \"Table 1: Main Results. We compare our proposed dual-alignment framework against competitive baselines on ALFWorld, WebShop, and TextCraft. Our method achieves consistent and substantial improvements in both success rates and task scores across all domains.\", \"page\": 8, \"index\": 3, \"width\": 826, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2604.02734v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of our neuro-symbolic dual memory framework. The proposed system explicitly separates local feasibility alignment from global progress alignment according to the distinct reasoning demands of the two objectives. Top (Offline Phase): Failed interactions are compiled into executable symbolic verifier rules to construct the symbolic Feasibility Memory, while successful trajectories are distilled into stage-anchored procedural blueprints to form the neural Progress Memory. Bottom (Inference Phase): At inference time, Progress Memory provides stage-aware guidance for proposing progress-consistent actions, while Feasibility Memory performs symbolic feasibility verification and iterative refinement before execution.\", \"page\": 4, \"index\": 4, \"width\": 744, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2604.02734v1/fig-005.webp\", \"caption\": \"Table 2: Ablation study on the components of our dual-alignment framework. Evaluated on the full ALFWorld test set. Removing either Progress Memory or Feasibility Memory leads to performance degradation. The full system achieves the highest Success Rate (SR) and lowest Invalid Action Rate (IAR) alongside optimized Average Trajectory Length (ATL), demonstrating the complementary roles of the two memory modules.\", \"page\": 9, \"index\": 5, \"width\": 826, \"height\": 253}]"
motivation: 现有方法尝试用单一范式同时处理模糊的语义规划和严格的逻辑约束，导致智能体在复杂长程任务中频繁出现进度偏移和可行性违规。
method: 提出一种神经符号双记忆框架，通过神经进度记忆引导全局任务推进，并利用基于Python验证函数的符号可行性记忆执行动作逻辑校验。
result: 在ALFWorld、WebShop和TextCraft等任务中，该方法性能显著优于现有基线，同时大幅减少了无效动作数量和平均轨迹长度。
conclusion: 解耦语义进度与逻辑验证的神经符号双记忆机制是提升LLM智能体长程决策可靠性和执行效率的有效途径。
---

## 摘要
大语言模型（LLMs）在长程决策任务中展现出强大潜力，例如具身操控和网页交互。然而，在复杂环境中，智能体经常陷入无休止的试错循环，或偏离主要目标。我们将这些失败归因于两个根本性错误：全局进度漂移（Progress Drift）和局部可行性违背（Feasibility Violation）。现有方法通常尝试使用单一范式同时解决这两个问题。然而，这两项挑战在本质上是不同的：前者依赖于模糊语义规划，而后者则要求严格的逻辑约束和状态验证。这种单一范式方法的固有局限性，为现有模型处理长程任务带来了根本性挑战。受此启发，我们提出了一种神经符号双记忆框架，将语义进度引导与逻辑可行性验证显式解耦。具体而言，在推理阶段，该框架同步调用两种记忆机制：一方面，基于神经网络的进度记忆（Progress Memory）从成功轨迹中提取语义蓝图，以引导全局任务推进；另一方面，基于符号逻辑的可行性记忆（Feasibility Memory）利用从失败转移中合成的可执行 Python 验证函数，进行严格的逻辑校验。实验表明，该方法在 ALFWorld、WebShop 和 TextCraft 上的表现显著优于现有的竞争基线，同时大幅降低了无效动作率和平均轨迹长度。

## Abstract
Large language models (LLMs) have demonstrated strong potential in long-horizon decision-making tasks, such as embodied manipulation and web interaction. However, agents frequently struggle with endless trial-and-error loops or deviate from the main objective in complex environments. We attribute these failures to two fundamental errors: global Progress Drift and local Feasibility Violation. Existing methods typically attempt to address both issues simultaneously using a single paradigm. However, these two challenges are fundamentally distinct: the former relies on fuzzy semantic planning, while the latter demands strict logical constraints and state validation. The inherent limitations of such a single-paradigm approach pose a fundamental challenge for existing models in handling long-horizon tasks. Motivated by this insight, we propose a Neuro-Symbolic Dual Memory Framework that explicitly decouples semantic progress guidance from logical feasibility verification. Specifically, during the inference phase, the framework invokes both memory mechanisms synchronously: on one hand, a neural-network-based Progress Memory extracts semantic blueprints from successful trajectories to guide global task advancement; on the other hand, a symbolic-logic-based Feasibility Memory utilizes executable Python verification functions synthesized from failed transitions to perform strict logical validation. Experiments demonstrate that this method significantly outperforms existing competitive baselines on ALFWorld, WebShop, and TextCraft, while drastically reducing the invalid action rate and average trajectory length.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Neuro-Symbolic Dual Memory Framework**（神经符号双记忆框架）的系统，旨在解决大语言模型（LLM）智能体在处理长程（Long-Horizon）任务时的可靠性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：LLM 智能体在执行复杂、长步骤任务（如具身操控、网页购物）时，经常出现两种失败模式：
    1.  **全局进度漂移（Progress Drift）**：智能体忘记了当前处于任务的哪个阶段，或者偏离了最终目标。
    2.  **局部可行性违背（Feasibility Violation）**：智能体尝试执行环境逻辑不允许的动作（如在没打开微波炉时尝试放入食物）。
*   **研究动机**：作者认为现有的单一范式（纯神经网络或简单的文本反射）无法同时处理这两种本质不同的挑战。进度对齐需要**模糊的语义泛化**（适合神经机制），而可行性对齐需要**严格的逻辑验证**（适合符号机制）。

### 2. 方法论：核心思想与关键技术
该框架通过显式解耦“语义进度引导”与“逻辑可行性验证”，构建了双记忆系统：
*   **神经进度记忆（Progress Memory - Neural）**：
    *   **构建**：从成功的历史轨迹中提取“程序蓝图”（Procedural Blueprints），将任务分解为有序的阶段锚点（Anchors）。
    *   **作用**：在推理时，根据当前任务检索相似蓝图，引导智能体在正确的语义阶段推进，防止“进度漂移”。
*   **符号可行性记忆（Feasibility Memory - Symbolic）**：
    *   **构建**：从失败的尝试中提取经验，利用 LLM 诱导（Induction）并编译成**可执行的 Python 验证函数**。
    *   **作用**：在智能体提交动作前进行“硬拦截”。如果动作违反逻辑（如位置不可达、手部已占满），验证函数会返回错误信息并强制智能体修正动作，防止“可行性违背”。
*   **推理流程**：蓝图规划 -> 动作提议（神经） -> 符号验证与迭代修正（符号） -> 环境执行 -> 进度监控与状态转移。

### 3. 实验设计
*   **实验场景**：
    1.  **ALFWorld**：具身智能家庭环境任务。
    2.  **WebShop**：电子商务网站交互与购物任务。
    3.  **TextCraft**：类 Minecraft 的复杂合成与逻辑规划任务。
*   **对比基线**：包括 ReAct、Reflexion、ADaPT、StateAct、ExpeL、WALL-E 2.0 和 AWM 等 7 种代表性智能体框架。
*   **评估指标**：成功率（SR）、无效动作率（IAR）、平均轨迹长度（ATL）。

### 4. 资源与算力
*   **模型使用**：所有实验均采用 `gpt-4o-2024-11-20` 作为核心骨干模型。
*   **算力细节**：论文**未明确说明**具体的 GPU 硬件型号、数量或训练时长。由于该框架主要基于 Prompt Engineering 和离线规则诱导，其核心开销在于 LLM API 的调用，而非传统的深度学习模型训练。

### 5. 实验数量与充分性
*   **实验规模**：在 ALFWorld 上测试了 134 个任务，WebShop 和 TextCraft 各测试了 100 个任务。
*   **消融实验**：作者进行了多组详尽的消融实验，分别验证了：
    1.  双记忆模块的互补性（证明两个模块缺一不可）。
    2.  进度记忆中“结构化蓝图”与“检索机制”的贡献。
    3.  可行性约束的不同形式（对比了 Prompt 约束与可执行代码约束）。
*   **充分性评价**：实验设计覆盖了具身、网页、逻辑合成三大领域，对比方法均为近年来的 SOTA（先进水平），实验结果较为客观、公平且充分。

### 6. 主要结论与发现
*   **性能提升**：该框架在所有基准测试中均取得最高成功率（如 ALFWorld 达到 94.78%）。
*   **效率优化**：显著降低了无效动作率（IAR），并大幅缩短了完成任务所需的平均步数（ATL）。
*   **功能分工**：实验证明，进度记忆主要负责提升任务推进的效率，而可行性记忆主要负责减少无效的试错。

### 7. 优点
*   **解耦思想**：敏锐地捕捉到了长程任务中“模糊语义”与“硬逻辑”的矛盾，并给出了优雅的解耦方案。
*   **符号化纠错**：将失败经验转化为可执行代码，比纯文本提示词（Prompt）更严谨，且具有极高的可解释性。
*   **通用性**：框架不依赖特定环境的底层代码，通过观察轨迹即可自动生成验证规则。

### 8. 不足与局限
*   **冷启动依赖**：构建记忆库需要一定数量的初始轨迹（成功和失败的各约 50 条），在完全陌生的新环境中可能存在冷启动困难。
*   **规则诱导风险**：如果 LLM 在归纳失败原因时产生幻觉，生成的 Python 验证函数可能会误判正确的动作（尽管文中设计了过滤机制来缓解）。
*   **应用限制**：对于那些失败信号极其模糊、难以用逻辑规则描述的环境，该方法的优势可能会减弱。

（完）
