---
title: Adaptive Parallel Monte Carlo Tree Search for Efficient Test-time Compute Scaling
title_zh: 用于高效推理时计算扩展的自适应并行蒙特卡洛树搜索
authors: "Hongbeen Kim, Juhyun Lee, Sanghyeon Lee, Kwanghoon Choi, Jaehyuk Huh"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00510v1"
tags: ["query:ttt"]
score: 10.0
evidence: 用于提高推理性能的测试时计算缩放方法
tldr: 蒙特卡洛树搜索（MCTS）是提升大语言模型推理性能的有效手段，但其执行时间波动大，常导致严重的长尾延迟。本文提出了一种自适应并行MCTS框架，通过引入“负向早停”机制剪枝无效搜索路径，并结合“自适应增强”机制动态重新分配计算资源。该方法集成于vLLM框架，在保持推理准确率的同时，显著降低了p99端到端延迟并提升了系统吞吐量，实现了高效的推理侧计算扩展。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00510v1/fig-001.webp\", \"caption\": \"Figure 9. Node-reusing MCTS algorithm with four phases.\", \"page\": 11, \"index\": 1, \"width\": 1017, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.00510v1/fig-002.webp\", \"caption\": \"Figure 1. Two categories of the TTCS method: (a) sequential search and (b) parallel search. t denotes the time.\", \"page\": 2, \"index\": 2, \"width\": 486, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.00510v1/fig-003.webp\", \"caption\": \"Figure 7. Impact of request arrival rates on p50 (darker) and p99 (lighter) end-to-end latency across different search strategies.\", \"page\": 7, \"index\": 3, \"width\": 486, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2604.00510v1/fig-004.webp\", \"caption\": \"Figure 8. Request throughput across various model-task configurations. PE and NE consistently increase throughput. PE+NE+Boosting yields throughput gains across most cases.\", \"page\": 8, \"index\": 4, \"width\": 487, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2604.00510v1/fig-005.webp\", \"caption\": \"Figure 10. Beam Search algorithm with beam width 2 and 4 sampled candidates per beam.\", \"page\": 12, \"index\": 5, \"width\": 1017, \"height\": 200}]"
motivation: MCTS在提升模型推理能力时存在执行时间不确定和长尾延迟严重的问题，且现有优化难以处理搜索无进展的情况。
method: 引入负向早停机制剪枝低效路径，并采用自适应增强机制将回收的计算资源重新分配给并发搜索任务。
result: 在vLLM集成测试中，该方法在维持推理准确性的同时，显著降低了p99延迟并提升了系统整体吞吐量。
conclusion: 通过自适应资源调度和路径剪枝，可以有效解决MCTS在推理侧扩展时的效率瓶颈。
---

## 摘要
蒙特卡洛树搜索（MCTS）是一种有效的推理时计算扩展（TTCS）方法，用于提升大语言模型的推理性能，但其高度波动的执行时间在实践中导致了严重的长尾延迟。现有的优化方法（如正向提前退出）在理想情况下能降低延迟，但在搜索持续进行却无实质性进展时效果较差。我们引入了“负向提前退出”（negative early exit），用于剪枝无成效的 MCTS 轨迹，以及一种“自适应增强机制”（adaptive boosting mechanism），通过重新分配回收的计算资源来减少并发搜索间的资源竞争。这些技术已集成到 vLLM 中，在显著降低 p99 端到端延迟的同时，提升了吞吐量并保持了推理准确性。

## Abstract
Monte Carlo Tree Search (MCTS) is an effective test-time compute scaling (TTCS) method for improving the reasoning performance of large language models, but its highly variable execution time leads to severe long-tail latency in practice. Existing optimizations such as positive early exit, reduce latency in favorable cases but are less effective when search continues without meaningful progress. We introduce {\it negative early exit}, which prunes unproductive MCTS trajectories, and an {\it adaptive boosting mechanism} that reallocates reclaimed computation to reduce resource contention among concurrent searches. Integrated into vLLM, these techniques substantially reduce p99 end-to-end latency while improving throughput and maintaining reasoning accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：用于高效推理时计算扩展的自适应并行蒙特卡洛树搜索

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**推理时计算扩展（Test-time Compute Scaling, TTCS）**领域。虽然蒙特卡洛树搜索（MCTS）在提升大语言模型（LLM）逻辑推理能力方面优于传统的并行采样（如 Best-of-N），但其**串行执行本质**导致了严重的**长尾延迟**和**资源浪费**。
*   **动机**：现有的优化（如“正向早停”）仅在找到正确答案时停止，但对于那些难以解决或搜索无果的请求，系统仍会耗尽所有搜索预算，导致排队延迟。
*   **核心问题**：如何在不损失推理准确性的前提下，通过系统级的资源管理和搜索剪枝，降低 MCTS 的长尾延迟并提高服务吞吐量。

#### 2. 方法论
论文提出了一套自适应并行 MCTS 框架，核心包含三个关键技术：
*   **WU-PUCT（并行搜索策略）**：改进了传统的 WU-UCT 算法，将“未观测计数”（Unobserved Counts）引入 PUCT 公式。通过记录正在进行但尚未回传结果的路径，动态惩罚过度探索的节点，从而在并行化搜索时保持探索与利用的平衡。
*   **负向早停（Negative Early Exit, NE）**：
    *   **原理**：基于过程奖励模型（PRM）的特性，即路径最终得分受限于其叶子节点的得分。
    *   **机制**：当搜索树中所有叶子节点的得分都低于预设阈值（即判定为“徒劳”节点）时，立即终止该请求的搜索，避免无效计算。
    *   **优化**：引入“选择性徒劳检查”，利用第一步奖励与最终得分的相关性，排除低潜力分支。
*   **自适应增强与调度（Adaptive Boosting & Scheduling）**：
    *   **动态并行度控制**：根据任务的“等待时间”和“得分紧迫性”（是否接近正确答案）计算优先级分数。
    *   **资源重分配**：将“正向/负向早停”回收的计算资源（GPU 算力槽位）动态分配给更有希望成功的并发任务，通过提高其并行度（DOP）来加速完成。

#### 3. 实验设计
*   **数据集**：
    *   **Math500**：从 MATH 数据集中提取的 500 个数学问题，用于衡量步进准确率。
    *   **AMC23**：2023 年美国数学竞赛题目，用于测试模型对未见问题的泛化能力。
*   **基准模型（Benchmark）**：
    *   生成模型：Llama-3.1-8B-Instruct, Qwen2.5-14B-Instruct。
    *   奖励模型（Verifier）：Qwen2.5-Math-PRM-7B。
*   **对比方法**：
    1.  **Beam Search**（束搜索，带正向早停）。
    2.  **Vanilla MCTS**（原始串行 MCTS）。
    3.  **PE**（仅带正向早停的 MCTS）。
    4.  **PE+NE**（正向+负向早停）。
    5.  **PE+NE+Boosting**（本文完整系统）。

#### 4. 资源与算力
*   **硬件配置**：单节点配备 **4 张 NVIDIA H100-SXM 80GB GPU**，通过 NVLink 互联。
*   **算力分配**：2 张 GPU 用于生成模型推理，2 张 GPU 用于奖励模型（PRM）推理。
*   **软件集成**：该系统构建在 **vLLM** 推理框架之上。
*   **时长说明**：文中未明确给出总训练或总推理时长，但强调了在不同请求到达率（0.5 到 4 req/sec）下的实时表现。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种主流模型架构（Llama 和 Qwen）和两个难度不同的数据集。
*   **维度**：评估了 p50/p99 延迟、吞吐量（req/sec）以及推理准确率。
*   **充分性**：实验设计较为充分，包含了消融实验（逐步添加 PE、NE 和 Boosting）以验证各组件的贡献。通过改变请求到达率模拟了真实生产环境中的负载波动，增强了结果的说服力。

#### 6. 主要结论与发现
*   **延迟降低**：相比串行 MCTS，完整系统将 **p99 端到端延迟降低了高达 2.83倍**；相比仅使用正向早停的系统，延迟进一步降低了 1.46倍。
*   **吞吐量提升**：系统吞吐量最高提升了 **2.44倍**。
*   **准确率保持**：在大幅缩减计算开销的同时，推理准确率与原始 MCTS 基本持平（波动在随机误差范围内）。
*   **关键发现**：长尾延迟主要由那些“既无法快速解决、又无法产生高质量路径”的困难请求引起，负向早停是解决这一问题的关键。

#### 7. 优点
*   **系统级视角**：不只是优化算法本身，而是将 MCTS 视为一个需要主动资源管理的系统问题。
*   **资源利用率高**：通过自适应增强机制，实现了计算资源的“跨请求”动态平衡，这在多用户并发场景下极具实用价值。
*   **即插即用**：集成于 vLLM，展示了在工业级推理引擎中落地的可行性。

#### 8. 不足与局限
*   **任务领域局限**：实验主要集中在数学推理（Math），对于代码生成、创意写作等奖励信号不那么明确或 PRM 效果较差的领域，负向早停的有效性尚未验证。
*   **并行开销**：在某些简单任务（如 AMC23 配合 Qwen 模型）中，Boosting 带来的调度开销反而导致吞吐量略微下降，说明策略在极简单负载下可能存在过度干预。
*   **数值敏感性**：论文提到并行化带来的浮点运算顺序变化可能导致准确率微小波动，这在对确定性要求极高的场景下可能是一个潜在风险。

（完）
