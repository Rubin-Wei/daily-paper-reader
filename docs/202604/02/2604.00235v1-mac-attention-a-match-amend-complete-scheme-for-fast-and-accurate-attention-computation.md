---
title: "MAC-Attention: a Match-Amend-Complete Scheme for Fast and Accurate Attention Computation"
title_zh: MAC-Attention：一种用于快速且准确注意力计算的“匹配-修正-补全”方案
authors: "Jinghan Yao, Sam Adé Jacobs, Walid Krichene, Masahiro Tanaka, Dhabaleswar K Panda"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00235v1"
tags: ["query:lcp"]
score: 9.0
evidence: 长上下文解码的快速注意力计算
tldr: "针对大语言模型长文本解码中KV缓存带来的IO瓶颈，本文提出MAC-Attention。该方法通过“匹配-修正-完成”三阶段方案，复用语义相似查询的历史注意力计算结果，在保持全注意力精度的同时显著减少KV缓存访问。实验表明，在128K上下文下，该方法可降低99%的KV访问，提升端到端速度达2.6倍，且不损失生成质量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00235v1/fig-001.webp\", \"caption\": \"Figure 1. Accuracy vs. KV budget (the fraction of KV cache used in each decoding step) across long-context benchmarks. Top: LongBench v2 (up to 120K context) (Bai et al., 2024). Bottom left: LongGenBench (up to 16K continuous generation) (Wu et al., 2024). Bottom right: RULER (120K context) (Hsieh et al., 2024). MAC-Attention is highlighted (dark pink); Full attention shown as a gray dashed line.\", \"page\": 1, \"index\": 1, \"width\": 467, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-002.webp\", \"caption\": \"Figure 3. Decode micro-pipeline for MAC-Attention. (a): per-request rings L2 match with SplitQ design; (b): per-head work spans differ because the reuse point p and band size r vary, the schedule assigns more CTAs to longer band+tail spans as shown in green blocks; many heads do a cheap merge when reuse is strong; (c): Overview of MAC-Attention workflow.\", \"page\": 6, \"index\": 2, \"width\": 492, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-003.webp\", \"caption\": \"Table 8. Enabling MAC-Attention for both prefilling and decoding on LongBench v2. We sweep τ and band r; the window is set to inf. to permit global search across the entire prefix (ablation-only; not production). Hit% and Skip% are reported as prefill/decode. Baseline is full attention.\", \"page\": 15, \"index\": 3, \"width\": 814, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-004.webp\", \"caption\": \"Figure 10. Normalized KV token efficiency( Acc./KV-usage fraction ) across rectification band and search window. Stars denote accuracy ≥ θ of baseline.\", \"page\": 15, \"index\": 4, \"width\": 472, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-005.webp\", \"caption\": \"Table 1. Main accuracy results across models and benchmarks. Accuracy (%) on LongBench v2 (up to 120K context), LongGenBench (up to 16K continuous generation), and RULER (120k context). Rows compare Full attention, Quest variants (config / shows the budget settings for each benchmark; Quest only supports LLaMA models.), and our MAC-Attention. The KV ↓ % columns report average KV-budget reduction relative to full attention (higher is more savings); - denotes not applicable. LongGenBench is generation-heavy; most models cannot complete the required task; we therefore report Finish% (higher is better), and finish-weighted accuracy.\", \"page\": 7, \"index\": 5, \"width\": 1045, \"height\": 523}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-006.webp\", \"caption\": \"Figure 8. Kernel-level costs and load balancing. (a) L2 Match kernel latencies under different GQA settings and window lengths (using standard Attention for reference). (b) Effect of the load-balancing planner compared with a perfectly balanced oracle and an unbalanced baseline. (c–d) Per-step composition of MAC-Attention (Match, Plan, Attention) across contexts.\", \"page\": 10, \"index\": 6, \"width\": 480, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-007.webp\", \"caption\": \"Figure 6. Attention speedup vs. batch size and KV skip ratio across context lengths. Speedup is computed as the combined attention-phase latency of our scheme (512-token matching window, load-balanced planning, rectified-prefix reuse, and tail attention). The wide shared axes are split into four subgroups for contexts {32K, 64K, 128K, 256K}; within each subgroup, the x-axis places batch sizes {1, 2, 4, 8, 16, 32}, the y-axis (shown once, left) is log-scale with a 1× reference line. Each line denotes a KV skip ratio. For more thorough latency comparison with existing efficient attention methods, refer to §5.4.\", \"page\": 10, \"index\": 7, \"width\": 985, \"height\": 189}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-008.webp\", \"caption\": \"Figure 7. End-to-end decode latency breakdown and speedup. For a fixed batch size, stacked bars show phase-level latency (QKV+RoPE, attention, FFN, misc.) under full attention and MAC-Attention across contexts and skip ratios; speedup is computed end-to-end.\", \"page\": 10, \"index\": 8, \"width\": 477, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-009.webp\", \"caption\": \"Figure 4. Rectification error vs. reuse gap and band width. Layerwise heatmaps show the normalized output error as a function of reuse gap (∆) and rectification band width (r); representative layers are displayed.\", \"page\": 8, \"index\": 9, \"width\": 492, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-010.webp\", \"caption\": \"Figure 2. MAC-Attention: Match–Amend–Complete with async cache update. (a) Match: at position m, compare the pre-RoPE query Q̃m against a small ring of recent queries; if the nearest match p passes an L2 threshold, fetch its attention summary AS1:p−r (otherwise run full attention). (b) Complete (critical path): with the post-RoPE query qm, compute attention only on the band+tail [ p−r+1, m ] and log-domain merge AS1:p−r ⊕ ASp−r+1:m; the KV [1, p−r] is not accessed. (c) Amend (async): compute a rectification term ASr+1:m and update the cache via online subtraction to obtain ASm−r; insert q̃m,ASm−r into the rings. Symbols: p—match position, r—band width; ⊕—log-domain merge, ⊖—log-domain removal. Shaded regions denote KV segments not re-read under reuse.\", \"page\": 4, \"index\": 10, \"width\": 995, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-011.webp\", \"caption\": \"Figure 5. Layerwise reuse patterns. Acceptance rate (left) and skipped-prefix fraction (right) per layer. Top: threshold sweep at fixed window size. Bottom: window-size sweep at fixed threshold.\", \"page\": 9, \"index\": 11, \"width\": 481, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2604.00235v1/fig-012.webp\", \"caption\": \"Table 5. Fine-grained attention-path latency profile (µs) at fixed match window K = 1024. Full attention reports FlashInfer attention latency. MAC reports the Match kernel, load balancer, attention kernel, and overall attention-path latency.\", \"page\": 9, \"index\": 12, \"width\": 487, \"height\": 486}]"
motivation: 现有的长文本注意力加速技术如压缩或剔除往往会降低模型精度或限制访问范围，难以在提升速度的同时保持高保真度。
method: 提出一种包含匹配（Match）、修正（Amend）和完成（Complete）三个阶段的方案，通过复用历史计算并进行数值稳定的融合来加速推理。
result: "在多个长文本基准测试中，MAC-Attention将KV访问减少了高达99%，使128K长度下的生成延迟降低超过60%，且保持了全注意力级别的质量。"
conclusion: MAC-Attention是一种模型无关且高效的注意力计算方案，为长上下文推理提供了兼具高速度与高保真度的可行路径。
---

## 摘要
大语言模型（LLM）中的长上下文解码受限于 I/O：每个 token 都会重新读取不断增长的 KV 缓存。先前的加速方法通过压缩（会降低保真度）或选择/剔除（会限制可访问内容）来减少字节传输，这两者都可能损害延迟召回和长文本生成能力。我们提出了 MAC-Attention，这是一种保持保真度和访问能力的替代方案，通过重用语义相似的近期查询的先前注意力计算来加速解码。它包含三个阶段：匹配（match）阶段，在短局部窗口内执行 RoPE 前的 L2 匹配；修正（amend）阶段，通过重新计算匹配边界附近的一个窄带（small band）来校正重用的注意力；以及补全（complete）阶段，通过数值稳定的合并操作，将校正后的结果与在 KV 尾部计算的新注意力进行融合。在匹配命中时，无论上下文长度如何，计算和带宽复杂度均为常数。该方法与模型无关，且可与 I/O 感知算子、分页 KV 管理器以及 MQA/GQA 结合使用。在 LongBench v2 (120K)、RULER (120K) 和 LongGenBench (16K 连续生成) 测试中，与最新的 FlashInfer 库相比，MAC-Attention 减少了高达 99% 的 KV 访问，在 128K 长度下将 token 生成延迟降低了 60% 以上，并实现了超过 14.3 倍的注意力阶段加速（端到端加速高达 2.6 倍），同时保持了全注意力（full-attention）的质量。通过重用计算，MAC-Attention 实现了既快速又忠实的长上下文推理。代码开源地址：https://github.com/YJHMITWEB/MAC-Attention.git

## Abstract
Long-context decoding in LLMs is IO-bound: each token re-reads an ever-growing KV cache. Prior accelerations cut bytes via compression, which lowers fidelity, or selection/eviction, which restricts what remains accessible, and both can degrade delayed recall and long-form generation. We introduce MAC-Attention, a fidelity- and access-preserving alternative that accelerates decoding by reusing prior attention computations for semantically similar recent queries. It starts with a match stage that performs pre-RoPE L2 matching over a short local window; an amend stage rectifies the reused attention by recomputing a small band near the match boundary; and a complete stage fuses the rectified results with fresh attention computed on the KV tail through a numerically stable merge. On a match hit, the compute and bandwidth complexity is constant regardless of context length. The method is model-agnostic and composes with IO-aware kernels, paged-KV managers, and MQA/GQA. Across LongBench v2 (120K), RULER (120K), and LongGenBench (16K continuous generation), compared to the latest FlashInfer library, MAC-Attention reduces KV accesses by up to 99%, cuts token generation latency by over 60% at 128K, and achieves over 14.3x attention-phase speedups, up to 2.6x end-to-end, while maintaining full-attention quality. By reusing computation, MAC-Attention delivers long-context inference that is both fast and faithful. Code is available here: https://github.com/YJHMITWEB/MAC-Attention.git

---

## 论文详细总结（自动生成）

以下是对论文《MAC-Attention: a Match-Amend-Complete Scheme for Fast and Accurate Attention Computation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **背景**：大语言模型（LLM）在处理长上下文时，解码阶段面临严重的 **I/O 瓶颈**。由于注意力机制需要逐个 token 读取不断增长的 Key-Value (KV) 缓存，导致内存带宽成为限制推理速度的主因。
*   **研究动机**：现有的加速方案（如 KV 压缩或 Token 剔除）虽然减少了数据传输，但往往会降低模型精度（保真度损失）或限制模型访问历史信息的能力（召回率下降）。
*   **核心目标**：提出一种既能显著减少 KV 缓存读取（加速），又能保持与全量注意力（Full Attention）一致精度的方法。

### 2. 方法论：MAC-Attention 方案
MAC-Attention 采用“匹配-修正-补全”三阶段流程，核心思想是**利用查询（Query）向量在时间上的语义冗余性**。
*   **匹配 (Match)**：
    *   在应用旋转位置编码（RoPE）之前（**Pre-RoPE**）的空间内，计算当前查询与近期历史查询的 L2 距离。
    *   选择 Pre-RoPE 是为了消除位置信息的干扰，纯粹捕捉语义相似性，从而提高匹配命中率。
*   **修正 (Amend)**：
    *   直接复用历史计算会产生误差，尤其是靠近匹配点附近的 token 贡献了大部分注意力权重。
    *   MAC 通过重新计算匹配边界附近的一个短窄带（宽度为 $r$）来校正结果，从而以极小的常数代价消除主要的近似误差。
*   **补全 (Complete)**：
    *   将复用的历史注意力摘要（Attention Summary）与当前查询在 KV 尾部（新生成的 token）计算的结果进行融合。
    *   使用数值稳定的 **Log-domain Merge**（对数域合并）算法，确保合并过程不损失精度。
*   **系统实现**：维护两个短周期的环形缓冲区（Ring Buffer），分别存储历史查询和修正后的注意力摘要，支持异步更新，不干扰主计算路径。

### 3. 实验设计
*   **数据集与基准 (Benchmark)**：
    *   **LongBench v2**：评估高达 120K 上下文的深度理解与推理能力。
    *   **RULER**：测试 120K 长度下的长程检索与召回稳定性。
    *   **LongGenBench**：评估 16K 连续长文本生成的质量（使用 LLM-as-a-judge）。
*   **对比方法**：
    *   基准线：**Full Attention** (基于 FlashInfer 库)。
    *   高效注意力变体：**Quest**、**SnapKV**、**StreamingLLM**、**TOVA**、**RocketKV**、**Multipole** 等。
*   **测试模型**：LLaMA-3.1 (8B/70B)、Phi-4-Mini、GLM-4-32B、Qwen3-30B-A3B (MoE 模型)。

### 4. 资源与算力
*   **硬件平台**：实验主要在 **NVIDIA H100 SXM5 GPU** 上运行。
*   **软件环境**：CUDA 12.8.1，推理框架使用 **SGLang** (0.4.9) 和 **FlashInfer** (0.2.7)。
*   **训练时长**：该方法属于 **Training-free**（无需训练），直接应用于推理阶段，因此不涉及训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从轻量级（4B）到大规模（70B）的模型，并验证了在稠密（Dense）和混合专家（MoE）架构上的有效性。
*   **消融实验**：
    *   对比了 Pre-RoPE 与 Post-RoPE 匹配的效果。
    *   分析了 L2 距离与余弦相似度的差异。
    *   详细探讨了修正带宽 $r$ 和搜索窗口 $K$ 对精度与速度的权衡。
*   **客观性**：实验在统一的 SGLang 框架下进行，对比了多种最新的 SOTA 方法，并提供了端到端延迟的详细分解（QKV 投影、注意力、FFN 等），实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **极致加速**：在 128K 上下文下，MAC-Attention 将 KV 访问量减少了 **99%**，注意力阶段实现 **14.3x 至 46x** 的加速，端到端生成速度提升高达 **2.6x**。
*   **高保真度**：在所有长文本基准测试中，MAC-Attention 的精度几乎与 Full Attention 完全一致，显著优于其他通过剔除 token 实现加速的方法。
*   **复杂度解耦**：一旦匹配命中，单步解码的计算和带宽开销变为**常数级**，不再随上下文长度线性增长。

### 7. 优点
*   **精度无损**：通过“修正”机制巧妙解决了复用计算带来的精度漂移问题。
*   **即插即用**：无需重新训练模型，兼容现有的分页 KV 管理（PagedAttention）和 GQA 架构。
*   **低额外开销**：存储查询和摘要环形缓冲区的额外显存开销仅占 KV 缓存总量的约 **5%**（在 128K 长度下）。

### 8. 不足与局限
*   **Prefill 阶段限制**：在 Prefill（预填充）阶段启用时，若复用跨度过大，精度保持较难，目前主要推荐用于 Decode（解码）阶段。
*   **最差情况开销**：如果查询完全不具有语义重复性（匹配不命中），会回退到全量计算，此时会产生微小的匹配计算开销。
*   **参数依赖**：虽然默认参数表现良好，但在不同任务（如纯生成 vs. 纯检索）间，匹配阈值 $\tau$ 可能需要微调以达到最优平衡。

（完）
