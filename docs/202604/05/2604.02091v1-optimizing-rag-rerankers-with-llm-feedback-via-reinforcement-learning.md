---
title: Optimizing RAG Rerankers with LLM Feedback via Reinforcement Learning
title_zh: 通过强化学习利用大语言模型反馈优化 RAG 重排序器
authors: "Yuhang Wu, Xiangqing Shen, Fanfan Wang, Cangqi Zhou, Zhen Wu, Xinyu Dai, Rui Xia"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02091v1"
tags: ["query:rag"]
score: 9.0
evidence: 利用LLM反馈优化RAG重排序器
tldr: 针对检索增强生成（RAG）中重排序器与下游生成任务不匹配的问题，本文提出RRPO框架。该框架利用强化学习将重排序视为序列决策过程，直接以大语言模型（LLM）的生成质量作为反馈信号进行优化。RRPO无需昂贵的人工标注，通过引入参考锚定基准确保训练稳定，在多个知识密集型任务中显著提升了重排序效果和生成质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02091v1/fig-001.webp\", \"caption\": \"Table 1: Main experimental results on the HotpotQA and AmbigNQ datasets. * adapts from previous work DeepRAG (Guan et al., 2025).\", \"page\": 6, \"index\": 1, \"width\": 469, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.02091v1/fig-002.webp\", \"caption\": \"Table 9: Performance of RRPO with smaller LLM Readers.\", \"page\": 15, \"index\": 2, \"width\": 461, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.02091v1/fig-003.webp\", \"caption\": \"Figure 1: Comparison of previous methods with our proposed RRPO framework.\", \"page\": 2, \"index\": 3, \"width\": 946, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2604.02091v1/fig-004.webp\", \"caption\": \"Table 6: Generalization results on different prompt templates on HotpotQA.\", \"page\": 14, \"index\": 4, \"width\": 461, \"height\": 293}]"
motivation: 传统的重排序模型基于静态相关性标注，导致检索出的文档虽主题相关但对LLM生成答案的实际效用不足。
method: 提出RRPO框架，将重排序建模为序列决策过程，利用LLM的生成反馈作为奖励信号，并通过参考锚定基准优化模型。
result: 实验表明RRPO在多个基准测试上优于RankZephyr等强基线模型，且在不同阅读器和查询扩展模块下表现出良好的泛化性。
conclusion: 通过强化学习将重排序与生成质量直接对齐，是提升RAG系统整体性能的一种高效且无需人工标注的有效途径。
---

## 摘要
重排序器在优化检索增强生成（RAG）的检索结果中起着至关重要的作用。然而，目前的重排序模型通常是在静态的人工标注相关性标签上进行独立优化的，与下游的生成过程相脱节。这种脱节导致了根本性的不一致：被信息检索指标认定为主题相关的文档，往往无法提供大语言模型（LLM）生成精确答案所需的实际效用。为了弥补这一差距，我们提出了重排序偏好优化（RRPO），这是一种通过强化学习框架直接将重排序与 LLM 的生成质量对齐的方法。通过将重排序建模为序列决策过程，RRPO 利用 LLM 反馈来优化上下文效用，从而消除了对昂贵人工标注的需求。为了确保训练稳定性，我们进一步引入了一种参考锚定的确定性基准。在知识密集型基准测试上的广泛实验表明，RRPO 显著优于强基准模型，包括强大的列表式重排序器 RankZephyr。进一步的分析强调了该框架的多功能性：它可以无缝泛化到不同的阅读器（如 GPT-4o），与 Query2Doc 等查询扩展模块正交集成，并且在面对带有噪声的监督者训练时仍能保持鲁棒性。

## Abstract
Rerankers play a pivotal role in refining retrieval results for Retrieval-Augmented Generation. However, current reranking models are typically optimized on static human annotated relevance labels in isolation, decoupled from the downstream generation process. This isolation leads to a fundamental misalignment: documents identified as topically relevant by information retrieval metrics often fail to provide the actual utility required by the LLM for precise answer generation. To bridge this gap, we introduce ReRanking Preference Optimization (RRPO), a reinforcement learning framework that directly aligns reranking with the LLM's generation quality. By formulating reranking as a sequential decision-making process, RRPO optimizes for context utility using LLM feedback, thereby eliminating the need for expensive human annotations. To ensure training stability, we further introduce a reference-anchored deterministic baseline. Extensive experiments on knowledge-intensive benchmarks demonstrate that RRPO significantly outperforms strong baselines, including the powerful list-wise reranker RankZephyr. Further analysis highlights the versatility of our framework: it generalizes seamlessly to diverse readers (e.g., GPT-4o), integrates orthogonally with query expansion modules like Query2Doc, and remains robust even when trained with noisy supervisors.

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimizing RAG Rerankers with LLM Feedback via Reinforcement Learning》的结构化深入分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：在检索增强生成（RAG）系统中，重排序器（Reranker）与下游生成器（LLM Reader）之间存在**目标不一致（Misalignment）**。
*   **研究背景**：传统的重排序模型通常基于静态的人工标注（如主题相关性）进行独立训练，使用 NDCG 等信息检索指标。然而，对 LLM 生成答案最有用的文档并不一定是主题最相关的文档。这种脱节导致重排序器无法针对“生成质量”这一最终目标进行优化。

### 2. 方法论：重排序偏好优化 (RRPO)
*   **核心思想**：将文档重排序建模为一个**序列决策过程（MDP）**，直接利用下游 LLM 的生成质量作为奖励信号，通过强化学习（RL）实现检索与生成的端到端对齐。
*   **关键技术细节**：
    *   **MDP 建模**：状态（State）为剩余未选文档集；动作（Action）为从集合中选择一个文档；奖励（Reward）由 LLM 生成结果的评价指标（EM、F1、Hit）决定。
    *   **策略网络**：使用点对点（Pointwise）重排序模型作为 Agent，通过 Softmax 转化为选择概率。
    *   **参考锚定确定性基准（Reference-anchored Deterministic Baseline）**：这是本文的核心创新。为了解决 LLM 奖励信号稀疏且高方差的问题，作者放弃了训练复杂的 Critic 网络，转而使用一个固定的参考策略（Reference Policy）进行贪婪搜索得到的得分作为基准值（Baseline），从而稳定 PPO 算法的训练。
    *   **优化目标**：结合了 PPO-clip 机制和 KL 散度惩罚，确保模型更新不会偏离原始分布太远。

### 3. 实验设计
*   **数据集**：
    *   **HotpotQA**：多跳推理问答。
    *   **AmbigNQ**：歧义性自然语言问答。
    *   **2WikiMultiHopQA & MusiQue**：用于验证复杂场景下的泛化性。
*   **对比方法（Baselines）**：
    *   **基础检索**：Naive RAG (BM25)。
    *   **主流重排序器**：bge-reranker, jina-reranker, gte-reranker, qwen3-reranker。
    *   **SOTA 列表式重排序器**：RankZephyr (7B)。
    *   **先进 RAG 策略**：FLARE, DRAGIN。
*   **评估指标**：Exact Match (EM) 和 F1 Score。

### 4. 资源与算力
*   **硬件配置**：
    *   **训练**：使用了 **4 张 NVIDIA RTX 3090 GPU**。
    *   **推理（LLM Reader）**：使用了 **2 张 NVIDIA A6000 GPU**，并采用 vLLM 框架加速。
*   **训练细节**：在 HotpotQA 上训练 2 个 epoch，在 AmbigNQ 上训练 4 个 epoch。为了提高效率，采用了检索缓存（Retrieval Caching）和 LLM 响应重放（Response Replay）技术，减少了约 50% 的 LLM 调用。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括主实验、消融实验、跨模型泛化实验、提示词鲁棒性实验等，总实验组数较多。
*   **充分性与客观性**：
    *   **多模型验证**：验证了 RRPO 在不同架构（Encoder-only, Decoder-only）重排序器上的有效性。
    *   **跨 Reader 泛化**：证明了在 Qwen2.5 上训练的重排序器可以直接提升 GPT-4o、Claude-3.5 等闭源模型的表现。
    *   **统计显著性**：进行了配对 t 检验（p < 0.01），确保改进并非偶然。
    *   **公平性**：所有对比实验均在相同的检索候选池和 LLM Reader 配置下进行。

### 6. 主要结论与发现
*   **性能领先**：RRPO 显著优于所有监督学习的重排序基准，甚至超过了参数量更大的列表式重排序器 RankZephyr。
*   **效用优先**：实验证明，优化“上下文效用（Utility）”比单纯优化“语义相关性（Relevance）”对 RAG 系统更有利。
*   **强泛化性**：学习到的重排序策略具有普适性，不依赖于特定的 LLM Reader 或 Prompt 模板。
*   **正交性**：RRPO 可以与 Query2Doc 等查询扩展技术结合，产生叠加增益。

### 7. 优点与亮点
*   **无需人工标注**：完全依赖 LLM 反馈，解决了 RAG 领域高质量重排序标注数据匮乏的问题。
*   **训练稳定性**：提出的“参考锚定基准”巧妙避开了 RLHF 中 Critic 网络难训练的坑，降低了 RL 在 NLP 任务中的应用门槛。
*   **即插即用**：训练后的重排序器可以作为独立模块，提升任何下游 LLM 的 RAG 效果。

### 8. 不足与局限
*   **召回率依赖**：作为重排序模型，其性能上限受限于初始检索（如 BM25）的召回率。如果初始检索没能找回相关文档，RRPO 也无法发挥作用。
*   **训练开销**：尽管有加速策略，但 RL 训练过程仍需频繁调用 LLM 生成答案，相比传统的监督学习，计算成本和时间成本更高。
*   **任务局限**：目前主要在知识密集型问答任务上验证，对于长文本摘要或创意写作等 RAG 场景的适用性尚待进一步探索。

（完）
