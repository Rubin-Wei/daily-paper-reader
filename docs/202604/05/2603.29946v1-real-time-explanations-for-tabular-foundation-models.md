---
title: Real-Time Explanations for Tabular Foundation Models
title_zh: 表格基础模型的实时解释
authors: "Luan Borges Teodoro Reis Sena, Francisco Galuppo Azevedo"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29946v1"
tags: ["query:pretrain"]
score: 6.0
evidence: 集成回归功能的基座模型架构
tldr: 针对表格基础模型解释性差且现有SHAP方法计算开销大的问题，本文提出ShapPFN。该模型将Shapley值回归直接集成到架构中，通过单次前向传播同时生成预测和解释。在保持竞争力预测性能的同时，其解释速度比KernelSHAP快1000倍以上，且具有极高的保真度，为科学机器学习提供了实时交互的探索能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29946v1/fig-001.webp\", \"caption\": \"Table 1: ROC-AUC benchmark across tabular datasets. Foundation models are separated from classical baselines. Datasets are split into HPO and Eval-only subsets, with per-group averages. ShapPFN(+loss) is the proposed full method; ShapPFN(arch) is the same architecture with loss weight set to 0. Best score per row is in bold and highlighted in gold; second/third best are highlighted in silver/bronze. 4\", \"page\": 4, \"index\": 1, \"width\": 826, \"height\": 1284}, {\"url\": \"assets/figures/arxiv/2603.29946v1/fig-002.webp\", \"caption\": \"Table 2: SHAP values quality and computational efficiency. SHAP approximation quality and computation time across datasets. Quality metrics: R2, Cosine similarity, Spearman correlation (vs KernelSHAP). ShapPFN(+loss) is the full model with SHAP loss; ShapPFN(arch) uses the same architecture without SHAP loss. SHAP values are computed using a background set of 150 samples and explanations are generated for 50 test instances per dataset. Times in seconds; speedup uses geometric mean.\", \"page\": 5, \"index\": 2, \"width\": 830, \"height\": 675}, {\"url\": \"assets/figures/arxiv/2603.29946v1/fig-003.webp\", \"caption\": \"Table 3: Sequential hyperparameter optimization. Each block optimizes one parameter (highlighted in gray) while keeping others fixed. Best ROC AUC in each block shown in bold.\", \"page\": 7, \"index\": 3, \"width\": 518, \"height\": 361}]"
motivation: 现有的表格模型解释方法（如SHAP）计算成本极高，限制了科研中的实时交互式探索。
method: 提出ShapPFN模型，将Shapley值回归集成到基础模型架构中，实现预测与解释的同步输出。
result: ShapPFN在保持高预测精度的同时，解释速度比KernelSHAP快1000倍以上，且与真实SHAP值高度一致（R²=0.96）。
conclusion: ShapPFN通过高效的内置解释机制，成功解决了表格基础模型在实时应用和科学验证中的可解释性瓶颈。
---

## 摘要
可解释性对于科学机器学习至关重要，因为理解模型做出预测的原因能够促进假设的生成与验证。虽然表格基础模型表现出强大的性能，但现有的解释方法（如 SHAP）计算成本高昂，限制了交互式探索。我们引入了 ShapPFN，这是一种将 Shapley 值回归直接集成到其架构中的基础模型，能够在单次前向传播中同时产生预测和解释。在标准基准测试中，ShapPFN 在实现具有竞争力的性能的同时，生成了高保真度的解释（$R^2$=0.96，余弦相似度=0.99），其速度比 KernelSHAP 快 1000 倍以上（0.06秒对610秒）。我们的代码可在 https://github.com/kunumi/ShapPFN 获取。

## Abstract
Interpretability is central for scientific machine learning, as understanding \emph{why} models make predictions enables hypothesis generation and validation. While tabular foundation models show strong performance, existing explanation methods like SHAP are computationally expensive, limiting interactive exploration. We introduce ShapPFN, a foundation model that integrates Shapley value regression directly into its architecture, producing both predictions and explanations in a single forward pass. On standard benchmarks, ShapPFN achieves competitive performance while producing high-fidelity explanations ($R^2$=0.96, cosine=0.99) over 1000\times faster than KernelSHAP (0.06s vs 610s). Our code is available at https://github.com/kunumi/ShapPFN