---
title: "Oblivion: Self-Adaptive Agentic Memory Control through Decay-Driven Activation"
title_zh: Oblivion：通过衰减驱动激活实现的自适应智能体记忆控制
authors: "Ashish Rana, Chia-Chien Hung, Qumeng Sun, Julian Martin Kunkel, Carolin Lawrence"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00131v1"
tags: ["query:am"]
score: 10.0
evidence: 通过衰减驱动激活的自适应智能体记忆控制
tldr: 针对LLM智能体在长程交互中面临的内存干扰和高延迟问题，本文受人类记忆遗忘机制启发，提出Oblivion框架。该框架将遗忘视为衰减驱动的可访问性降低而非直接删除，通过解耦读写路径，根据智能体不确定性按需检索，并强化对生成响应有贡献的记忆。实验证明，Oblivion能有效平衡学习与遗忘，实现高效的层次化记忆管理，提升了智能体在复杂环境下的推理表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00131v1/fig-001.webp\", \"caption\": \"Figure 2: (a) Ebbinghaus forgetting curve showing decay patterns with reinforcement. (b) Retention distributions at time steps t ∈ {50, 100, 150} for temperature T ∈ {1, 3, 5, 10, 20, 50}. (c) Reinforcement fraction over time.\", \"page\": 6, \"index\": 1, \"width\": 887, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-002.webp\", \"caption\": \"Table 1: LONGMEMEVAL results in accuracy (%): Oracle (clean) vs. S (noisy). For close comparisons in EVERMEMOS (Hu et al., 2026), we report only GPT-4o-mini and GPT-4.1-mini.\", \"page\": 5, \"index\": 2, \"width\": 948, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-003.webp\", \"caption\": \"Table 2: Evaluation results of GOODAILTM as solved scenarios out of (11, 10, 11, 11) for given memory span settings respectively. † Selected models are instruct.\", \"page\": 5, \"index\": 3, \"width\": 454, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-004.webp\", \"caption\": \"Figure 1: OBLIVION facilitates memory-augmented agents by decay-driven activation over hierarchical memory traces. The Executor orchestrates the read path for uncertainty-gated retrieval (⋄); and the write path for feedback-driven updates (⋄), enabling dynamic control over memory activation.\", \"page\": 1, \"index\": 4, \"width\": 449, \"height\": 347}]"
motivation: 现有的LLM智能体依赖全量检索和扁平化存储，导致随着历史增长出现严重的干扰和延迟问题。
method: 提出一种解耦读写路径的内存控制框架，利用衰减机制降低记忆可访问性，并基于不确定性和贡献度动态触发检索与强化。
result: 在长程交互基准测试中，Oblivion成功实现了动态的内存访问与强化，在保持高层策略的同时有效管理了细节信息。
conclusion: 模拟人类的衰减驱动遗忘机制对于优化LLM智能体的内存管理和提升长程推理效能至关重要。
---

## 摘要
人类记忆通过选择性遗忘进行适应：经验随时间变得难以获取，但可以通过强化或上下文线索重新激活。相比之下，记忆增强型大语言模型（LLM）智能体依赖“始终开启”的检索和“扁平化”的记忆存储，随着历史记录的增长，这会导致严重的干扰和延迟。我们介绍了 Oblivion，这是一个记忆控制框架，它将遗忘视为衰减驱动的可访问性降低，而非显式的删除。Oblivion 将记忆控制解耦为读路径和写路径。读路径根据智能体的不确定性和记忆缓冲区的充足性来决定何时查阅记忆，从而避免冗余的始终开启式访问。写路径通过强化对生成响应有贡献的记忆，来决定需要加强的内容。两者结合实现了层级化的记忆组织，在保持持久的高层策略的同时，根据需要动态加载细节。我们在静态和动态的长时程交互基准测试上进行了评估。结果表明，Oblivion 能够动态调整记忆访问和强化，在不断变化的上下文中平衡学习与遗忘，强调了记忆控制对于有效的 LLM 智能体推理至关重要。源代码可在 https://github.com/nec-research/oblivion 获取。

## Abstract
Human memory adapts through selective forgetting: experiences become less accessible over time but can be reactivated by reinforcement or contextual cues. In contrast, memory-augmented LLM agents rely on "always-on" retrieval and "flat" memory storage, causing high interference and latency as histories grow. We introduce Oblivion, a memory control framework that casts forgetting as decay-driven reductions in accessibility, not explicit deletion. Oblivion decouples memory control into read and write paths. The read path decides when to consult memory, based on agent uncertainty and memory buffer sufficiency, avoiding redundant always-on access. The write path decides what to strengthen, by reinforcing memories contributing to forming the response. Together, this enables hierarchical memory organization that maintains persistent high-level strategies while dynamically loading details as needed. We evaluate on both static and dynamic long-horizon interaction benchmarks. Results show that Oblivion dynamically adapts memory access and reinforcement, balancing learning and forgetting under shifting contexts, highlighting that memory control is essential for effective LLM-agentic reasoning. The source code is available at https://github.com/nec-research/oblivion.

---

## 论文详细总结（自动生成）

### 论文总结：Oblivion —— 通过衰减驱动激活实现的自适应智能体记忆控制

#### 1. 核心问题与整体含义（研究动机和背景）
现有的记忆增强型大语言模型（LLM）智能体在处理长程交互时面临三大挑战：
*   **检索冗余（Always-on Retrieval）：** 无论是否需要，智能体在每一轮对话都会进行检索，导致高延迟。
*   **记忆扁平化（Flat Memory）：** 存储系统无法区分持久的高层策略与瞬时的底层细节，通常采用“硬删除”方式管理上下文，导致重要信息丢失。
*   **干扰与污染：** 随着历史记录增长，无关或过时的信息会干扰推理（即“大海捞针”难题）。

**研究动机：** 受人类认知心理学启发，论文认为“遗忘”不应是物理删除，而应是**可访问性的下降**。基于此，作者提出了 **Oblivion** 框架，将记忆管理视为一个自适应的控制问题。

#### 2. 方法论：核心思想与关键技术
Oblivion 的核心思想是将记忆控制解耦为**读路径（Read Path）**和**写路径（Write Path）**，并引入层级化记忆结构。

*   **层级化记忆表示：**
    *   **L1 (动态程序记忆)：** 元索引，包含集群摘要和处理特定领域问题的“策略指令”。
    *   **L2 (语义记忆)：** 离散的事实陈述，带有时间戳以解决“世界漂移”冲突。
    *   **L3 (抢占式情节记忆)：** 将交互片段转化为可执行的反馈条目。
*   **读路径（衰减驱动的激活）：**
    *   **Decayer（衰减器）：** 基于智能体的不确定性（LLM 评判 + 嵌入相似度）决定是否检索。利用 **Ebbinghaus 遗忘曲线公式** 计算保留分数 $R_t(c)$，频繁使用或高价值的记忆衰减慢，反之则逐渐进入潜伏状态。
    *   **Activator（激活器）：** 当不确定性高时触发，通过 **DAG（有向无环图）查询扩展** 进行多步检索，将相关记忆重新加载到缓冲区。
*   **写路径（反馈驱动的更新）：**
    *   **Recognizer（识别器）：** 进行“信用分配”，仅强化那些对生成当前响应有贡献的记忆。
    *   **Memory Manager（记忆管理器）：** 负责新记忆的持久化和效用元数据的更新。

#### 3. 实验设计
*   **数据集/场景：**
    1.  **LongMemEval：** 静态基准测试，包含约 100K 上下文的跨会话 QA，测试智能体在海量历史中提取证据的能力。
    2.  **GoodAI-LTM：** 动态交互基准测试，包含约 120K 上下文，评估智能体在长对话中保留、修正和更新信息的能力。
*   **对比方法：**
    *   **Direct（直接访问）：** 如 LME-RFT、Full Context（全上下文）。
    *   **Memory（记忆增强）：** 如 EverMemOS、Beyond Prompts（类似 MemGPT 的扁平检索架构）。
*   **Backbone 模型：** Phi-4-mini (4B), Qwen3-30B, GPT-4o-mini, GPT-4.1-mini。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件训练细节（如 GPU 型号或数量），因为该框架主要是一个**推理侧的控制循环**，而非从头训练模型。
*   **效率分析：** 论文提到了成本优化，在 120K 上下文设置下，Oblivion 相比全上下文方法减少了约 **73% 的 Token 消耗**。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 500 个静态 QA 问题和 33 组动态交互测试。
*   **消融实验：** 针对记忆层级（L1/L2/L3 的组合效果）和遗忘曲线参数（温度 $T$ 的影响）进行了详细分析。
*   **充分性：** 实验跨越了不同规模的模型（从 4B 到 30B 及闭源 API 模型），并在静态和动态两种完全不同的维度上验证了有效性，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先：** Oblivion 在所有基准测试中均优于直接访问和现有的记忆增强基准。
*   **成本效益：** 通过不确定性门控检索，显著降低了 Token 成本和延迟，同时避免了“迷失在中间”的现象。
*   **层级优势：** 证明了 L1/L2/L3 三层链接结构在推理准确性上优于任何单一层级。
*   **动态适应：** 衰减机制能有效过滤无关信息，使智能体在长程任务中保持策略的一致性。

#### 7. 优点
*   **认知启发：** 模拟人类遗忘机制，将“删除”改为“降低优先级”，更符合智能体持续学习的需求。
*   **读写解耦：** 避免了盲目检索，通过不确定性驱动激活，极大地提升了系统效率。
*   **多步推理：** 采用 DAG 查询扩展，比传统的单次 RAG 检索更能处理复杂的信息需求。

#### 8. 不足与局限
*   **模型依赖性：** 框架的效果高度依赖于 Backbone 模型评估自身“不确定性”的能力，较弱的模型可能导致检索失效。
*   **超参数敏感：** 遗忘曲线的温度参数 $T$ 需要针对不同任务进行调优，缺乏完全自动化的设置方法。
*   **覆盖范围：** 目前主要集中在文本交互，尚未在多模态或特定专业领域（如医疗、法律）进行广泛验证。

（完）
