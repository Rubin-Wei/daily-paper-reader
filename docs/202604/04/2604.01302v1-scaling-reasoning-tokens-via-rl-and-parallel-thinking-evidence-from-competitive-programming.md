---
title: "Scaling Reasoning Tokens via RL and Parallel Thinking: Evidence From Competitive Programming"
title_zh: 通过强化学习与并行思考扩展推理 Token：来自竞赛编程的证据
authors: "Qianfan Zhang, Tianyu Guo, Xuandi Ren, Jiale Chen, Ming Ding, Ran Xin, Xia Xiao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01302v1"
tags: ["query:ttt"]
score: 9.0
evidence: 通过测试时并行思考缩放推理Token
tldr: 本研究探讨了在竞赛编程中通过强化学习（RL）和测试时并行思考来扩展推理Token预算的方法。研究发现验证准确率与推理Token数量呈对数线性关系，并通过验证RL预热和随机裁剪优化了训练轨迹。此外，引入了多轮并行思考流水线，将Token预算分配到多个线程进行生成、验证和精炼。该系统在AetherCode硬题上超越了GPT-5-high，证明了大规模推理Token对复杂任务的显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01302v1/fig-001.webp\", \"caption\": \"Figure 5 The parallel thinking inference pipeline. The system spawns N independent threads. Each thread generates a candidate solution, then verifies it via V independent sampling calls, each producing a correctness judgment and reasoning. If all V verdicts unanimously deem the solution correct, the thread terminates early; otherwise, one negative verdict is randomly selected and the model refines the solution conditioned on the previous attempt and the selected reasoning. This verify-refine loop repeats for up to M rounds. After all threads complete, all solutions across threads and rounds are ranked by verification vote count, and the highest-scoring one is returned.\", \"page\": 6, \"index\": 1, \"width\": 638, \"height\": 656}, {\"url\": \"assets/figures/arxiv/2604.01302v1/fig-002.webp\", \"caption\": \"Figure 1 Log-linear trend: validation accuracy scales linearly with the logarithm of the average number of reasoning tokens during RL training. Each point corresponds to a successive RL training checkpoint.\", \"page\": 2, \"index\": 2, \"width\": 406, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2604.01302v1/fig-003.webp\", \"caption\": \"Figure 8 Oracle pass@k versus token budget for three training stages: SFT cold start, verification RL, and generation RL after verification RL. Generation RL dominates at every token budget. The gap persists even at pass@256, indicating that RL can expand solution coverage beyond the SFT frontier rather than merely concentrating probability on existing solutions.\", \"page\": 14, \"index\": 3, \"width\": 534, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2604.01302v1/fig-004.webp\", \"caption\": \"Figure 2 Randomized clipping replaces the hard reward cliff (left) with a smooth ramp (middle), producing a steeper log-linear scaling curve (right).\", \"page\": 3, \"index\": 4, \"width\": 975, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2604.01302v1/fig-005.webp\", \"caption\": \"Figure 7 Parallel scaling accuracy versus number of threads N for varying verdicts V ∈ {1, 2, 4, 8} (colored), with M=1, i.e., no refinement. More verdicts improve ranking quality, but the gap to the oracle pass@N (dashed) remains substantial, suggesting that verifier capability is the primary bottleneck.\", \"page\": 13, \"index\": 5, \"width\": 486, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2604.01302v1/fig-006.webp\", \"caption\": \"Figure 6 Left: Unified token budget axis spanning training-time RL scaling from 20K to 60K tokens and test-time parallel thinking with the end-to-end RL model from 60K to 7.6M tokens. Sequential refinement outperforms parallel generation at every token budget; combining both extends scaling beyond the sequential plateau and surpasses GPT-5high by a wide margin. Right: Effect of end-to-end RL on sequential-plus-parallel scaling. Separate training initially outperforms end-to-end RL with turn-grouped GRPO due to stronger single-round generation, but the latter catches up by ∼2M tokens and overtakes it at higher budgets. End-to-end RL with REINFORCE scales substantially worse.2\", \"page\": 8, \"index\": 6, \"width\": 967, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.01302v1/fig-007.webp\", \"caption\": \"Figure 4 Verification RL warmup. Left: recall remains high while precision and accuracy improve during verification RL. Right: initializing generation RL from the verification checkpoint shifts the log-linear scaling curve upward.\", \"page\": 5, \"index\": 7, \"width\": 868, \"height\": 285}]"
motivation: 旨在通过训练和推理阶段的策略优化，探索如何有效扩展推理Token预算以提升模型在复杂编程任务中的表现。
method: 结合了带有验证预热和随机裁剪的强化学习训练，以及支持多线程、多轮生成与验证的并行思考推理架构。
result: "基于Seed-OSS-36B的模型在每题消耗约760万Token的情况下，pass@1表现匹配了基准模型的oracle pass@16，并超越了GPT-5-high。"
conclusion: 通过RL训练与并行思考协同扩展推理Token，是提升大模型解决高难度逻辑问题能力的有效途径。
---

## 摘要
我们研究了如何通过两种互补的方法来扩展竞赛编程的推理 Token 预算：训练时的强化学习 (RL) 和测试时的并行思考。在 RL 训练过程中，我们观察到验证准确率与连续检查点生成的平均推理 Token 数量之间存在近似对数线性的关系，并展示了两种改变这一训练轨迹的方法：验证 RL 预热提高了起点，而随机裁剪在观察范围内产生了更陡峭的趋势。由于在全注意力机制下，RL 过程中扩展单次生成的推理成本迅速增加，我们引入了一种多轮并行思考流水线，将 Token 预算分配到生成、验证和优化的多个线程和轮次中。我们在此流水线上对模型进行端到端训练，使训练目标与测试时的结构相匹配。以 Seed-OSS-36B 为起点，该完整系统在每个问题平均使用 760 万个 Token 的情况下，通过 16 个线程、每个线程 16 轮的配置，在 pass@1 指标上达到了基础 RL 模型 oracle pass@16 的水平，并在来自 AetherCode 的 456 个高难度竞赛编程问题上超越了 GPT-5-high。

## Abstract
We study how to scale reasoning token budgets for competitive programming through two complementary approaches: training-time reinforcement learning (RL) and test-time parallel thinking. During RL training, we observe an approximately log-linear relationship between validation accuracy and the average number of generated reasoning tokens over successive checkpoints, and show two ways to shift this training trajectory: verification RL warmup raises the starting point, while randomized clipping produces a steeper trend in the observed regime. As scaling single-generation reasoning during RL quickly becomes expensive under full attention, we introduce a multi-round parallel thinking pipeline that distributes the token budget across threads and rounds of generation, verification, and refinement. We train the model end-to-end on this pipeline to match the training objective to the test-time structure. Starting from Seed-OSS-36B, the full system with 16 threads and 16 rounds per thread matches the underlying RL model's oracle pass@16 at pass@1 using 7.6 million tokens per problem on average, and surpasses GPT-5-high on 456 hard competitive programming problems from AetherCode.

---

## 论文详细总结（自动生成）

这篇论文由字节跳动 Seed 团队与普林斯顿、加州大学伯克利分校及斯坦福大学合作完成，探讨了在竞赛编程（Competitive Programming）领域如何通过强化学习（RL）和测试时计算（Test-time Compute）扩展推理 Token 预算，以提升大语言模型的逻辑推理能力。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在计算资源受限（尤其是注意力机制的平方复杂度）的情况下，通过增加推理 Token（Reasoning Tokens）来持续提升模型在复杂逻辑任务（如竞赛编程）中的表现。
*   **背景**：OpenAI o1 和 DeepSeek-R1 证明了强化学习能诱导模型产生长链思考（CoT）。然而，单次生成长度存在“计算墙”，且训练成本极高。论文旨在探索训练阶段的优化策略与测试阶段的并行化推理架构，以突破单次生成的瓶颈。

### 2. 方法论：核心思想与关键技术
论文提出了两阶段的扩展方案：

#### A. 训练阶段：优化 RL 轨迹
研究者发现验证准确率与推理 Token 数的对数呈线性关系（Log-linear trend）。为了优化这一趋势，提出了两种技术：
*   **验证 RL 预热（Verification RL Warmup）**：在生成 RL 之前，先训练模型进行“自我验证”（判断代码正误并解释原因）。这为生成任务提供了一个更高能力的起点。
*   **随机裁剪（Randomized Clipping, RC）**：针对硬性的 Token 长度限制会导致奖励函数出现“悬崖”的问题，RC 通过在训练中随机采样长度上限，将硬惩罚平滑化，从而诱导模型在更经济的 Token 预算下获得更高的准确率。

#### B. 测试阶段：并行思考（Parallel Thinking）架构
为了绕过单次生成过长导致的计算开销，设计了一个多线程、多轮次的流水线：
1.  **多线程生成**：同时开启 $N$ 个独立线程。
2.  **自我验证与精炼（Verify-Refine）**：每个线程内进行 $M$ 轮循环。模型先生成代码，再进行自我验证；若验证失败，则根据反馈进行自我修复（Refine）。
3.  **投票排序**：最后根据验证通过的票数对所有生成的解法进行排序，选出最优解。
*   **端到端训练**：使用 **Turn-grouped GRPO** 算法对上述多轮对话过程进行端到端强化学习，确保模型在测试时的多轮交互语境下保持分布一致。

### 3. 实验设计
*   **数据集/Benchmark**：
    *   **训练集**：约 10K 个来自 Codeforces 等平台的私有竞赛编程题目。
    *   **验证集**：**AetherCode**（包含 456 个来自 IOI、ICPC 等顶级赛事的难题）。
*   **对比方法**：
    *   基准模型：Seed-OSS-36B。
    *   对比对象：**GPT-5-high**（OpenAI 发布的具备高推理能力的模型版本）。
    *   消融实验：对比了纯并行生成、纯串行精炼、以及两者结合的效果；对比了不同 RL 算法（REINFORCE vs. Turn-grouped GRPO）。

### 4. 资源与算力
*   **硬件**：使用了 **256 或 512 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   最大上下文长度达到 90K（部分实验扩展至 120K）。
    *   在平均响应长度为 100K Token 时，单步 RL 训练在 256 张 A100 上需耗时约 4 小时。
    *   SFT 阶段使用了约 6K 条生成轨迹和 2K 条验证轨迹。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了从 SFT 到验证 RL、再到生成 RL 的完整演进曲线。针对并行思考的线程数（$N=1$ 到 $16$）和轮次（$M=1$ 到 $16$）做了详细的扩展性分析。
*   **充分性与客观性**：实验涵盖了从训练动态到测试时缩放的多个维度。通过 Oracle pass@k 曲线证明了 RL 确实扩展了模型的解空间，而非仅仅是概率坍缩。对比 GPT-5-high 提供了强有力的外部基准。

### 6. 主要结论与发现
*   **对数线性趋势**：在 RL 训练中，准确率随推理 Token 数的对数线性增长，这可以作为评估 RL 策略优劣的标尺。
*   **串行优于并行**：在相同 Token 预算下，**串行精炼（Sequential Refinement）比增加并行线程更有效**，因为前者利用了验证反馈。
*   **端到端训练的必要性**：端到端训练虽然在单轮表现上可能略逊于独立训练，但在大规模多轮推理中展现出更强的连贯性和最终上限。
*   **性能突破**：完整系统（16 线程 × 16 轮）在每题平均消耗 7.6M Token 的情况下，pass@1 达到了基础模型 oracle pass@16 的水平，并显著超越了 GPT-5-high。

### 7. 优点与亮点
*   **系统性强**：统一了训练时 RL 缩放和测试时搜索缩放的视角，将其置于同一个 Token 预算轴上进行比较。
*   **技术创新**：提出的“随机裁剪”和“Turn-grouped GRPO”解决了长序列 RL 训练中的奖励稀疏和优势归一化难题。
*   **实战导向**：在竞赛编程这一硬核领域取得了可验证的 SOTA 结果，证明了 Parallel Thinking 架构的实用性。

### 8. 不足与局限
*   **计算成本极高**：单题消耗 760 万 Token 的推理成本对于大多数实际应用场景来说过于昂贵。
*   **验证器瓶颈**：实验显示，验证器的准确性（能否识别错误代码）是限制性能进一步提升的主要瓶颈，而非采样数量。
*   **领域局限**：目前仅在编程领域验证，该模式是否能无缝迁移到证明数学题或通用逻辑推理尚待观察（编程有编译器提供客观反馈，而其他领域可能缺乏）。

（完）
