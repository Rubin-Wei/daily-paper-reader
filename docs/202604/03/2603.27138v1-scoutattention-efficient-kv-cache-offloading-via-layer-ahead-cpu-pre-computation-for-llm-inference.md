---
title: "ScoutAttention: Efficient KV Cache Offloading via Layer-Ahead CPU Pre-computation for LLM Inference"
title_zh: ScoutAttention：通过跨层提前 CPU 预计算实现大语言模型推理的高效 KV 缓存卸载
authors: "Qiuyang Zhang, Kai Zhou, Ding Tang, Kai Lu, Cheng Li, Zhenyu Yang, Peng Xu, Jiguang Wan"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27138v1"
tags: ["query:lcp"]
score: 9.0
evidence: 长上下文推理中的KV缓存卸载
tldr: 针对大语言模型长文本推理中KV缓存导致的显存瓶颈，本文提出ScoutAttention框架。该框架通过GPU-CPU协同的块状稀疏注意力机制减少计算负载，并引入创新的“提前一层”CPU预计算算法与异步周期性召回机制，有效解决了传统卸载方案中的I/O和计算延迟问题，在保持高精度的同时显著提升了推理速度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27138v1/fig-001.webp\", \"caption\": \"Figure 4: Overall architecture of ScoutAttention.\", \"page\": 3, \"index\": 1, \"width\": 502, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2603.27138v1/fig-002.webp\", \"caption\": \"Figure 5: Workflow of GPU-CPU collaborative block-wise sparse attention with layer-ahead CPU pre-computation.\", \"page\": 4, \"index\": 2, \"width\": 503, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2603.27138v1/fig-003.webp\", \"caption\": \"Table 1: Cosine similarity between predict query and real query.\", \"page\": 4, \"index\": 3, \"width\": 506, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2603.27138v1/fig-004.webp\", \"caption\": \"Figure 7: Results on LongBench of different methods.\", \"page\": 5, \"index\": 4, \"width\": 498, \"height\": 297}]"
motivation: 现有的KV缓存卸载技术受限于频繁的数据传输或沉重的CPU计算负担，导致GPU利用率低下。
method: 提出一种GPU-CPU协同的块状稀疏注意力机制，并结合提前一层的CPU预计算与异步周期性召回。
result: "在保持精度损失低于2.4%的前提下，相比现有卸载方法实现了2.1倍的推理加速。"
conclusion: ScoutAttention通过创新的层间预计算调度，成功打破了长文本推理中的显存与计算瓶颈。
---

## 摘要
大语言模型在长上下文推理过程中面临严重的 GPU 显存容量限制，其中 KV 缓存的内存消耗严重限制了解码批处理大小。虽然现有研究已经探索了将 KV 缓存卸载到 DRAM，但这些方法要么需要频繁的 GPU-CPU 数据传输，要么对 CPU 计算提出了极高要求，导致系统在等待 I/O 操作或 CPU 处理完成时 GPU 利用率低下。我们提出了 ScoutAttention，这是一种新型的 KV 缓存卸载框架，通过 GPU-CPU 协同注意力计算来加速大语言模型推理。为了防止 CPU 计算成为系统瓶颈，ScoutAttention 引入了 GPU-CPU 协同的分块稀疏注意力，显著降低了 CPU 负载。与传统的并行计算方法不同，我们的框架采用了一种新颖的跨层提前 CPU 预计算算法，使 CPU 能够提前一层启动注意力计算，并辅以异步周期性召回机制，以保持极低的 CPU 计算负载。实验结果表明，ScoutAttention 在保持准确率与基准线差距在 2.4% 以内的同时，与现有的卸载方法相比实现了 2.1 倍的加速。

## Abstract
Large language models encounter critical GPU memory capacity constraints during long-context inference, where KV cache memory consumption severely limits decode batch sizes. While existing research has explored offloading KV cache to DRAM, these approaches either demand frequent GPU-CPU data transfers or impose extensive CPU computation requirements, resulting in poor GPU utilization as the system waits for I/O operations or CPU processing to complete.   We propose ScoutAttention, a novel KV cache offloading framework that accelerates LLM inference through collaborative GPU-CPU attention computation. To prevent CPU computation from bottlenecking the system, ScoutAttention introduces GPU-CPU collaborative block-wise sparse attention that significantly reduces CPU load. Unlike conventional parallel computing approaches, our framework features a novel layer-ahead CPU pre-computation algorithm, enabling the CPU to initiate attention computation one layer in advance, complemented by asynchronous periodic recall mechanisms to maintain minimal CPU compute load. Experimental results demonstrate that ScoutAttention maintains accuracy within 2.4% of baseline while achieving 2.1x speedup compared to existing offloading methods.

---

## 论文详细总结（自动生成）

以下是对论文《ScoutAttention: Efficient KV Cache Offloading via Layer-Ahead CPU Pre-computation for LLM Inference》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大语言模型（LLM）的长文本推理中，**KV 缓存（KV Cache）** 消耗了大量 GPU 显存，限制了批处理大小（Batch Size）和推理吞吐量。
*   **研究背景**：
    *   现有的 **“召回式”卸载（Recall-based）**（如 InfiniGen）将 KV 缓存存放在内存（DRAM），需要时传回 GPU，但受限于 PCIe 带宽，导致 GPU 长期处于 I/O 等待状态（闲置率约 61%）。
    *   现有的 **“协同计算式”卸载（Co-attention）**（如 HGCA）在 CPU 上直接计算注意力，但由于 CPU 计算速度远慢于 GPU，导致 GPU 需等待 CPU 计算结果（闲置率约 57%）。
*   **动机**：设计一种既能消除 I/O 瓶颈，又能隐藏 CPU 计算延迟的高效卸载框架。

### 2. 方法论与核心技术
ScoutAttention 提出了 **GPU-CPU 协同的块状稀疏注意力机制**，包含以下三个关键技术：

*   **GPU-CPU 协同稀疏计算**：
    *   基于块状稀疏（Block-wise Sparsity）思想，仅在 GPU 显存中保留 KV 块的摘要（Digest）和一小部分关键块。
    *   计算时，GPU 识别出 Top-K 个重要块。若块在 GPU 中则直接计算；若不在，则由 CPU 在内存中完成计算。最后在 GPU 端合并结果。
*   **跨层提前 CPU 预计算（Layer-Ahead Pre-computation）**：
    *   **核心思想**：利用 Transformer 层间的残差连接特性，预测下一层的 Query。
    *   **流程**：当 GPU 正在计算第 $i$ 层时，利用第 $i$ 层的输入和第 $i+1$ 层的权重矩阵预估 $Q_{i+1}$，从而提前触发 CPU 开始计算第 $i+1$ 层中缺失的 Top-K 块。这使得 CPU 的计算时间窗口从单纯的注意力阶段扩展到了整个 Transformer 层的执行周期（增加了约 3 倍时间），有效隐藏了延迟。
*   **异步周期性 KV 缓存召回**：
    *   由于解码过程中重要块会发生偏移（Drift），导致 CPU 负担逐渐加重。
    *   ScoutAttention 会定期（如每隔 8-9 步）将最新的重要块异步从内存传回 GPU。这种异步设计利用了整个 Token 解码的时间窗口，确保 I/O 不在关键路径上。

### 3. 实验设计
*   **数据集/场景**：使用 **LongBench** 基准测试，涵盖单/多文档问答、摘要、信息检索等 8 个数据集，上下文长度最高达 64k。
*   **对比方法（Baselines）**：
    *   **FullKV**：标准的完整注意力机制。
    *   **InfiniGen**：基于预取的召回式卸载方法。
    *   **HGCA**：基于滑动窗口的 GPU-CPU 协同计算方法。
*   **测试模型**：Qwen 3 (8B/14B/32B)、Llama 3.1 (8B)、Mistral (7B) 等。

### 4. 资源与算力
*   **硬件环境**：实验使用了配备 **80GB HBM 显存的 GPU**（推测为 A100 或 H100）以及 **36 核 CPU**，两者通过 PCIe 4.0 x16 连接。
*   **软件框架**：基于 **SGLang** 推理引擎实现，CPU 端使用 Intel IPEX 库进行加速。
*   **训练/推理时长**：文中未提及模型训练时长（属于推理优化论文），主要关注解码阶段的吞吐量（tokens/s）和延迟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 个不同任务的数据集上进行了准确率验证。
    *   针对不同输入长度（8k 到 100k+）、不同批处理大小（16 到 64）进行了吞吐量测试。
    *   进行了详细的**消融实验**，分别验证了预计算（PC）和周期性召回（PR）的贡献。
*   **评价**：实验设计较为充分，涵盖了准确率、吞吐量、延迟分解等多个维度，对比基准均为该领域最新的代表性工作，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：在 64k 上下文下，ScoutAttention 的吞吐量是 FullKV 的 **5.1 倍**，是现有卸载方法（InfiniGen/HGCA）的 **2.1 倍**。
*   **精度保持**：相比 FullKV，在 2048 的稀疏预算下，平均准确率损失仅为 **2.1%**。
*   **延迟优化**：成功将 GPU 的闲置时间（Idle time）从 60% 左右降低至 **6%**。
*   **预测有效性**：实验证明预测 Query 与真实 Query 的余弦相似度高达 0.93-0.97，验证了跨层预计算的可行性。

### 7. 优点与亮点
*   **调度巧妙**：通过“提前一层”预测和异步召回，巧妙地利用了推理过程中的计算空隙，解决了异构计算中的同步等待问题。
*   **系统集成度高**：在 SGLang 等主流框架上实现，并针对 CPU 线程组进行了优化，具有较强的实用价值。
*   **平衡性好**：在显存节省、推理速度和模型精度之间找到了极佳的平衡点。

### 8. 不足与局限
*   **精度风险**：虽然平均损失小，但在极长文本或对精度极度敏感的任务中，预测 Query 带来的微小偏差可能累积。
*   **硬件依赖**：性能提升高度依赖于 CPU 的多核计算能力和 PCIe 的基础带宽，在 CPU 较弱的系统上效果可能打折扣。
*   **超参数敏感**：周期性召回的阈值 $\beta$ 和召回间隔需要针对不同模型进行离线分析（Profiling），缺乏完全自动化的动态调整机制。

（完）
