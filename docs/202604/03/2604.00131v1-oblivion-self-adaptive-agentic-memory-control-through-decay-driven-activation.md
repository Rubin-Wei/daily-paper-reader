---
title: "Oblivion: Self-Adaptive Agentic Memory Control through Decay-Driven Activation"
title_zh: Oblivion：通过衰减驱动激活实现的自适应智能体记忆控制
authors: "Ashish Rana, Chia-Chien Hung, Qumeng Sun, Julian Martin Kunkel, Carolin Lawrence"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00131v1"
tags: ["query:am"]
score: 10.0
evidence: 大模型智能体的衰减驱动记忆控制
tldr: 针对LLM智能体在长程交互中面临的内存干扰和高延迟问题，本文提出Oblivion框架。该框架借鉴人类记忆的衰减与激活机制，将遗忘视为可访问性的降低而非物理删除。通过解耦读写路径，Oblivion能根据不确定性自主决定检索时机，并强化对生成响应有贡献的记忆，实现了分层记忆管理，显著提升了智能体在复杂动态环境下的推理效率与性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00131v1/fig-001.webp\", \"caption\": \"Figure 2: (a) Ebbinghaus forgetting curve showing decay patterns with reinforcement. (b) Retention distributions at time steps t ∈ {50, 100, 150} for temperature T ∈ {1, 3, 5, 10, 20, 50}. (c) Reinforcement fraction over time.\", \"page\": 6, \"index\": 1, \"width\": 887, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-002.webp\", \"caption\": \"Table 1: LONGMEMEVAL results in accuracy (%): Oracle (clean) vs. S (noisy). For close comparisons in EVERMEMOS (Hu et al., 2026), we report only GPT-4o-mini and GPT-4.1-mini.\", \"page\": 5, \"index\": 2, \"width\": 948, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-003.webp\", \"caption\": \"Table 2: Evaluation results of GOODAILTM as solved scenarios out of (11, 10, 11, 11) for given memory span settings respectively. † Selected models are instruct.\", \"page\": 5, \"index\": 3, \"width\": 454, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.00131v1/fig-004.webp\", \"caption\": \"Figure 1: OBLIVION facilitates memory-augmented agents by decay-driven activation over hierarchical memory traces. The Executor orchestrates the read path for uncertainty-gated retrieval (⋄); and the write path for feedback-driven updates (⋄), enabling dynamic control over memory activation.\", \"page\": 1, \"index\": 4, \"width\": 449, \"height\": 347}]"
motivation: 传统LLM智能体采用全量检索和扁平化存储，导致随着历史增长出现严重的干扰和延迟问题。
method: 提出一种基于衰减驱动的记忆控制框架，通过解耦读写路径，依据智能体不确定性触发检索并动态强化关键记忆。
result: 在静态和动态长程交互基准测试中，该方法成功平衡了学习与遗忘，实现了高效的记忆访问与强化。
conclusion: 记忆控制是提升LLM智能体推理能力的关键，通过模拟人类的遗忘机制可有效优化长程任务表现。
---

## 摘要
人类记忆通过选择性遗忘进行适应：经验随时间推移变得难以获取，但可以通过强化或上下文线索重新激活。相比之下，记忆增强的大语言模型（LLM）智能体依赖“常驻式”检索和“扁平化”记忆存储，随着历史记录的增长，会导致严重的干扰和延迟。我们提出了 Oblivion，这是一个将遗忘视为衰减驱动的可访问性降低而非显式删除的记忆控制框架。Oblivion 将记忆控制解耦为读路径和写路径。读路径根据智能体的不确定性和记忆缓冲区的充足性来决定何时查阅记忆，从而避免冗余的常驻式访问；写路径则通过强化对生成响应有贡献的记忆，来决定需要加强的内容。两者结合实现了层级化的记忆组织，在保持持久的高层策略的同时，根据需要动态加载细节。我们在静态和动态的长程交互基准测试上进行了评估。结果表明，Oblivion 能够动态调整记忆访问和强化，在不断变化的上下文中平衡学习与遗忘，强调了记忆控制对于有效的 LLM 智能体推理至关重要。源代码可在 https://github.com/nec-research/oblivion 获取。

## Abstract
Human memory adapts through selective forgetting: experiences become less accessible over time but can be reactivated by reinforcement or contextual cues. In contrast, memory-augmented LLM agents rely on "always-on" retrieval and "flat" memory storage, causing high interference and latency as histories grow. We introduce Oblivion, a memory control framework that casts forgetting as decay-driven reductions in accessibility, not explicit deletion. Oblivion decouples memory control into read and write paths. The read path decides when to consult memory, based on agent uncertainty and memory buffer sufficiency, avoiding redundant always-on access. The write path decides what to strengthen, by reinforcing memories contributing to forming the response. Together, this enables hierarchical memory organization that maintains persistent high-level strategies while dynamically loading details as needed. We evaluate on both static and dynamic long-horizon interaction benchmarks. Results show that Oblivion dynamically adapts memory access and reinforcement, balancing learning and forgetting under shifting contexts, highlighting that memory control is essential for effective LLM-agentic reasoning. The source code is available at https://github.com/nec-research/oblivion.

---

## 论文详细总结（自动生成）

以下是对论文《Oblivion: Self-Adaptive Agentic Memory Control through Decay-Driven Activation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的记忆增强型大语言模型（LLM）智能体在处理长程交互时面临“大海捞针”难题。由于采用“常驻式（Always-on）”检索和“扁平化”存储，随着历史记录增长，无关信息的干扰（Prompt污染）和推理延迟显著增加。
*   **研究动机**：借鉴认知心理学中人类记忆的特性——遗忘并非物理删除，而是“可访问性”的降低。人类大脑会根据不确定性触发检索，并通过重复或上下文线索强化重要记忆。
*   **整体含义**：本文提出 **OBLIVION** 框架，将记忆管理视为一个自适应控制问题，通过模拟艾宾浩斯遗忘曲线实现动态的记忆激活与衰减，从而在长程任务中平衡学习与遗忘。

### 2. 方法论：核心思想与关键技术
OBLIVION 的核心是将记忆控制解耦为**读路径（Read Path）**和**写路径（Write Path）**，并采用层级化记忆结构。

*   **层级化记忆表示**：
    *   **L1（动态程序记忆）**：元索引，包含集群摘要、统计数据和处理特定领域查询的策略。
    *   **L2（语义记忆）**：带时间戳的事实性陈述。
    *   **L3（抢占式情节记忆）**：将交互片段转化为可执行的反馈条目。
*   **读路径（不确定性门控检索）**：
    *   **Decayer（衰减器）**：通过 LLM 评判和嵌入相似度估计“不确定性”。同时利用艾宾浩斯公式计算**保留分数（Retention Score）**，决定哪些记忆在当前是可访问的。
    *   **Activator（激活器）**：当不确定性高时触发。它将查询分解为有向无环图（DAG）形式的子查询，进行多步证据发现。
*   **写路径（反馈驱动更新）**：
    *   **Recognizer（识别器）**：进行“信用分配”，识别哪些记忆条目对生成当前响应有贡献。
    *   **Memory Manager（记忆管理器）**：仅强化被使用的记忆（提高其保留分数），未被使用的记忆则随时间自然衰减。
*   **Executor（执行器）**：作为中枢协调读/写循环，维持工作记忆（WM）状态。

### 3. 实验设计
*   **数据集/场景**：
    1.  **LONG MEM EVAL**：静态基准测试，包含约 100K 上下文的跨会话 QA，测试智能体在海量历史中定位证据的能力。
    2.  **GOOD AILTM**：动态交互基准测试，包含约 120K 上下文，测试智能体在长对话中保留、修订和更新信息的能力。
*   **对比方法**：
    *   **Direct（直接访问）**：如 LME-RFT、Full Context（全上下文输入）。
    *   **Memory（记忆增强基准）**：如 EverMemOS（自组织记忆系统）、Beyond Prompts（类似 MemGPT 的扁平检索架构）。
*   **模型后端**：使用了 Phi-4-mini (4B)、Qwen3-30B、GPT-4o-mini 和 GPT-4.1-mini。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件训练资源（如 GPU 型号或数量）。
*   **原因分析**：由于 OBLIVION 主要是一个**推理时（Inference-time）**的控制框架，不涉及大规模的模型预训练或微调，其开销主要体现在 API 调用次数和向量数据库检索上。文中提供了推理延迟（Latency）和吞吐量（TPS）的对比数据。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 500 个人工标注的 QA 实例（静态）和 33 个复杂的交互场景（动态）。
*   **消融实验**：针对 L1/L2/L3 记忆层级进行了组合测试，验证了全层级结构的优越性。
*   **充分性评估**：实验设计较为充分。它不仅对比了准确率，还深入分析了记忆衰减温度（T）的影响、Token 消耗成本以及推理延迟。通过在不同规模（从 4B 到 30B 及闭源模型）的模型上测试，验证了框架的通用性。

### 6. 主要结论与发现
*   **性能提升**：OBLIVION 在静态 QA 和动态交互任务中均一致优于直接输入和现有的记忆增强基准。
*   **成本优化**：在 120K 长程设置下，相比全上下文方法，OBLIVION 减少了约 **73%** 的 Token 成本。
*   **记忆动态性**：实验证明，通过衰减机制让不相关信息“隐身”而非删除，能有效缓解“迷失在中间（Lost-in-the-middle）”现象，同时保留了未来重新激活的可能性。
*   **层级优势**：L2（事实）与 L3（情节）的链接对于处理复杂的跨会话推理至关重要。

### 7. 优点与亮点
*   **认知启发**：将心理学中的遗忘曲线成功引入 LLM 智能体控制，设计理念新颖且符合直觉。
*   **读写解耦**：通过不确定性门控避免了“常驻式检索”带来的冗余计算和干扰。
*   **高效性**：在保证性能的同时显著降低了长文本处理的经济成本。
*   **即插即用**：作为模型无关的控制层，不需要对底层 LLM 进行微调。

### 8. 不足与局限
*   **模型依赖性**：框架中的不确定性估计和记忆提取高度依赖 LLM 自身的能力，较弱的小模型可能会产生低质量的记忆或错误的检索决策。
*   **超参数敏感**：衰减温度 $T$ 需要根据具体任务进行调优，不当的设置会导致过度遗忘或缓冲区饱和。
*   **评估范围**：虽然涵盖了静态和动态任务，但尚未在多模态、多语言或特定专业领域（如医疗、法律）的智能体场景中进行广泛验证。
*   **延迟开销**：虽然 Token 成本降低，但由于引入了多次 LLM 评判和迭代检索，其推理延迟略高于简单的全上下文方法。

（完）
