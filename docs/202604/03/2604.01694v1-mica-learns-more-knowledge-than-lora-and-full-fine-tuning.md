---
title: MiCA Learns More Knowledge Than LoRA and Full Fine-Tuning
title_zh: MiCA 比 LoRA 和全量微调学习到更多知识
authors: "Sten Rüdiger, Sebastian Raschka"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01694v1"
tags: ["query:pm"]
score: 9.0
evidence: 用于提升权重中知识获取的参数高效微调
tldr: "MiCA是一种新型的大语言模型参数高效微调方法，旨在通过利用模型表示中未被充分利用的次要子空间来提升性能。与关注主成分的LoRA不同，MiCA通过奇异值分解识别与最小奇异值相关的方向并限制参数更新。实验证明，MiCA在知识获取效率上最高可达LoRA的5.9倍，且参数量仅为LoRA的6%至60%，为模型集成新知识提供了更高效、稳定的机制。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01694v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of BLOGS-MC (new knowledge), TruthfulQA-MC1 and HellaSwag accuracies across different finetuning methods for the blog post training. For each accuracy value we averaged 8 runs per hyperparameter choice. Additional columns for optimal adapter rank (r), learning rate (LR), and training epochs are included for completeness. The last columns shows the number of parameters of the full model or the number of trainable parameters for the LoRA and MiCA runs, respectively.\", \"page\": 6, \"index\": 1, \"width\": 886, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2604.01694v1/fig-002.webp\", \"caption\": \"Figure 3: Retention of domain knowledge for the history book and for several fine-tuning methods for Llama-2.\", \"page\": 6, \"index\": 2, \"width\": 481, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.01694v1/fig-003.webp\", \"caption\": \"Figure 1: (a) Diagram of a LoRA-style low-rank adaptation module applied to a weight matrix. (b) Diagram of a MiCAstyle adaptation module illustrating its distinct update structure compared to LoRA (blue: constrained components, orange: fine-tuned components initialized as indicated in each trapezoid). U [:, −r :] means all rows, last r columns of the matrix U defined in the text.\", \"page\": 2, \"index\": 3, \"width\": 960, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2604.01694v1/fig-004.webp\", \"caption\": \"Table 5: LoRA and MiCA adapter configurations and trainings hyperparameters.\", \"page\": 14, \"index\": 4, \"width\": 456, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.01694v1/fig-005.webp\", \"caption\": \"Figure 2: Retention of domain knowledge from the BLOGS dataset as a function of number of training epochs for MiCA and LoRA. Error bars denote the standard error of 8 evaluation runs. The horizontal dashed line shows the baseline performance of the respective foundation model. The green dashed line in (b) shows results for a fixed B matrix initialized with random SVD components instead of the minor componenents and same hyperparameters as for the MiCA runs.\", \"page\": 5, \"index\": 5, \"width\": 439, \"height\": 728}]"
motivation: 传统微调方法如LoRA主要针对主成分子空间，忽略了模型中未被充分利用的次要子空间在知识获取中的潜力。
method: 利用奇异值分解（SVD）识别与最小奇异值相关的次要奇异向量方向，并将微调过程中的参数更新限制在这些子空间内。
result: "MiCA在知识获取能力上比LoRA提升高达5.9倍，且其参数占用量仅为LoRA的6%至60%。"
conclusion: 将模型适配限制在次要奇异方向是预训练语言模型集成新知识的一种比LoRA和全量微调更高效且稳定的机制。
---

## 摘要
次要分量自适应（Minor Component Adaptation, MiCA）是一种针对大语言模型的新型参数高效微调方法，其核心在于调整模型表示中未被充分利用的子空间。与针对主导子空间的低秩自适应（LoRA）等传统方法不同，MiCA 利用奇异值分解来识别与最小奇异值相关的次要奇异向量子空间，并将微调过程中的参数更新限制在这些方向上。在优化的训练超参数下，该策略使知识获取能力提升了高达 5.9 倍，且与 LoRA 相比，其参数占用仅为 6-60%。这些结果表明，将自适应限制在次要奇异方向上，为将新知识整合到预训练语言模型中提供了一种更高效且稳定的机制。

## Abstract
Minor Component Adaptation (MiCA) is a novel parameter-efficient fine-tuning method for large language models that focuses on adapting underutilized subspaces of model representations. Unlike conventional methods such as Low-Rank Adaptation (LoRA), which target dominant subspaces, MiCA leverages Singular Value Decomposition to identify subspaces related to minor singular vectors associated with the least significant singular values and constrains the update of parameters during fine-tuning to those directions. This strategy leads to up to 5.9x improvement in knowledge acquisition under optimized training hyperparameters and a minimal parameter footprint of 6-60% compared to LoRA. These results suggest that constraining adaptation to minor singular directions provides a more efficient and stable mechanism for integrating new knowledge into pre-trained language models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MiCA（Minor Component Adaptation，次要分量自适应）** 的新型参数高效微调（PeFT）方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在大模型（LLM）适配特定领域时，全量微调成本极高，而现有的 PeFT 方法（如 LoRA）主要针对模型权重中的“主导子空间”（即奇异值较大的方向）。
*   **核心假设**：作者认为模型的主导子空间已经饱和了通用的预训练知识，而**次要子空间（奇异值较小的方向）**未被充分利用，具有更高的“可塑性”，更适合用于注入新的特定领域知识。
*   **整体含义**：MiCA 通过锁定并更新权重的次要奇异向量方向，实现了比 LoRA 更高效、更稳定的知识获取能力。

### 2. 方法论
*   **核心思想**：利用奇异值分解（SVD）识别权重矩阵中贡献最小的方向，并将微调限制在这些方向上。
*   **关键技术细节**：
    1.  **SVD 分解**：对预训练权重 $W$ 进行分解：$W = U \Sigma V^\top$。
    2.  **子空间选择**：选择 $U$ 矩阵中对应最小奇异值的最后 $r$ 列，记为 $U_r$。
    3.  **参数初始化与冻结**：
        *   引入两个低秩矩阵 $A$ 和 $B$。
        *   令 $B = U_r$ 且在训练过程中**保持冻结**。
        *   令 $A = 0$ 且设为**可训练**。
    4.  **权重更新**：更新公式为 $\Delta W = \frac{\alpha}{r} BA$。最终权重为 $W_{final} = W + \Delta W$。
*   **指令增量合并**：为了保持指令遵循能力，作者先在基座模型上用 MiCA 学习新知识，然后将得到的权重增量与原模型的“指令微调增量”（Instruction Delta）进行线性合并。

### 3. 实验设计
*   **数据集/场景**：
    1.  **BLOGS**：2024年5-6月的 OpenAI 博客文章（模型知识截止日期之后的内容），包含 300 道合成的多选题（BLOGS-MC）。
    2.  **HISTORY**：一本 2024 年出版的 300 页德语历史书（约 10 万 token），包含 102 道多选题。
    3.  **通用基准**：TruthfulQA 和 HellaSwag（用于评估灾难性遗忘和通用推理能力）。
*   **对比方法（Benchmark）**：
    *   全量微调（Full Fine-Tuning）。
    *   LoRA（低秩自适应）。
    *   预训练基座模型（Llama-2-7B, Qwen-2.5-7B）。
    *   消融实验：随机分量初始化、主成分初始化（Major-r）。

### 4. 资源与算力
*   **算力设备**：论文提到在 AWS SageMaker 上运行，使用了 **8 × H100 80GB GPU** 节点进行分布式训练（针对 HISTORY 数据集）。
*   **训练细节**：使用了 `bfloat16` 精度、梯度检查点（Gradient Checkpointing）以及 AdamW 优化器相关的配置。
*   **时长**：未明确给出总训练时长，但提到微调通常在 1-8 个 Epoch 内完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种主流模型架构（Llama 和 Qwen）、两种语言（英语和德语）、以及不同规模的语料（短博文与长篇书籍）。
*   **充分性**：
    *   针对 BLOGS 数据集，每个配置都进行了 **8 次独立运行**并取平均值，提供了标准差，统计学意义较强。
    *   进行了详细的**消融实验**，对比了“次要分量”、“主要分量”和“随机分量”，证明了 MiCA 性能提升确实源于对次要方向的选择。
    *   **客观性**：在相同的超参数搜索空间下对 LoRA 和 MiCA 进行了公平对比。

### 6. 主要结论与发现
*   **知识获取更强**：MiCA 在新知识测试（BLOGS-MC, HISTORY-MC）中的表现显著优于 LoRA 和全量微调。在 Llama-2 上，MiCA 的准确率比 LoRA 高出约 10 个百分点。
*   **参数效率极高**：MiCA 仅需 LoRA **6% 到 60%** 的可训练参数量即可达到甚至超越 LoRA 的效果（因为 $B$ 矩阵被冻结）。
*   **稳定性与抗遗忘**：MiCA 在获取新知识的同时，在 TruthfulQA 和 HellaSwag 上的得分保持稳定，说明其对原有知识结构的干扰更小。
*   **可塑性验证**：实验证实，次要奇异方向确实比主导方向或随机方向更适合吸收新信息。

### 7. 优点
*   **理论新颖**：反其道而行之，挑战了“微调应针对主导子空间”的传统认知。
*   **极低开销**：大幅减少了需要优化的参数量，降低了存储和通信成本。
*   **即插即用**：可以方便地集成到现有的 Transformer 架构中，且与指令微调模型兼容性好。

### 8. 不足与局限
*   **任务局限性**：目前主要证明了其在“知识注入”（持续预训练）任务上的优越性，对于纯粹的“指令遵循”或“风格迁移”任务的效果尚待验证。
*   **预处理成本**：虽然训练快，但初始的 SVD 分解在大规模层上存在一次性的计算开销。
*   **模型规模覆盖**：实验主要集中在 7B 参数规模，尚未展示在 70B 或更大规模模型上的扩展性表现。

（完）
