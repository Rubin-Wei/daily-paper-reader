---
title: Test-Time Adaptation for Height Completion via Self-Supervised ViT Features and Monocular Foundation Models
title_zh: 基于自监督 ViT 特征与单目基础模型的测试时自适应高度补全
authors: "Osher Rafaeli, Tal Svoray, Ariel Nahlieli"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02009v1"
tags: ["query:ttt"]
score: 9.0
evidence: 地理空间任务中基础模型的测试时自适应
tldr: 针对数字表面模型（DSM）缺失或过时的问题，本文提出Prior2DSM框架，这是一种无需预训练的测试时自适应方法。该框架结合DINOv3的自监督ViT特征与单目深度基础模型，通过语义特征对应关系将不完整的度量高度先验传播至全局。利用LoRA和轻量级MLP预测空间变化的缩放与偏移参数，实现了从相对深度到绝对度量高度的精准转换，显著提升了DSM补全的结构保真度与泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02009v1/fig-001.webp\", \"caption\": \"Figure 3: The experimental dataset consists of two domains: (1) the Denver NAIP dataset and (2) the Worldview-3 Fresno dataset. Both datasets include high-resolution RGB imagery, ground-truth normalized DSM (nDSM), and three levels of prior incompleteness (25%, 50%, and 75%) for evaluation.\", \"page\": 6, \"index\": 1, \"width\": 1070, \"height\": 474}, {\"url\": \"assets/figures/arxiv/2604.02009v1/fig-002.webp\", \"caption\": \"Table 3: Mean metrics of WV3 dataset against GT for 25%, 50%, and 75% completion.\", \"page\": 7, \"index\": 2, \"width\": 1104, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2604.02009v1/fig-003.webp\", \"caption\": \"Table 2: Mean metrics of NAIP dataset against GT for 25%, 50%, and 75% completion.\", \"page\": 7, \"index\": 3, \"width\": 1104, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.02009v1/fig-004.webp\", \"caption\": \"Figure 6: Visual comparison of relative height predictions from three models, Depth Anything V2 (DAV2), Depth Pro, and MoGe-2. DAV2 provides a smoother representation that better preserves flat regions such as ground and roads, while Depth Pro and MoGe-2 produce sharper and more detailed predictions with enhanced structural boundaries. Height: black = low, yellow = high. Error: blue = low, red = high.\", \"page\": 10, \"index\": 4, \"width\": 520, \"height\": 785}, {\"url\": \"assets/figures/arxiv/2604.02009v1/fig-005.webp\", \"caption\": \"Figure 7: Coupled RGB–DSM editing using generative remote sensing models: Text2Earth generates modified RGB imagery from textual prompts describing new urban structures. The proposed framework then predicts the corresponding metric DSM for the edited scene. Red boxes indicate the regions where new buildings are introduced and reconstructed in the generated height map.\", \"page\": 10, \"index\": 5, \"width\": 525, \"height\": 607}, {\"url\": \"assets/figures/arxiv/2604.02009v1/fig-006.webp\", \"caption\": \"Table 1: Comparison of representative height completion approaches. ✓ indicates full support, × indicates no support, and ◦ indicates limited support.\", \"page\": 3, \"index\": 6, \"width\": 1043, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2604.02009v1/fig-007.webp\", \"caption\": \"Figure 4: Comparison of DSM completion methods. From left to right: RGB image, Bilinear interpolation, Global affine fitting, Locally Weighted Linear Regression (LWLR), Spatial kNN propagation, DINOv3 feature-space kNN, and the proposed Prior2DSM. Each block corresponds to different levels of missing prior data (25%, 50%, and 75%). For each case, the ground truth (GT) DSM and the degraded prior input are shown on the left, followed by the reconstruction results and the corresponding absolute error maps. While simple interpolation and global fitting produce oversmoothed surfaces and spatial artifacts, semantic feature-based approaches better preserve structural details. The proposed framework consistently achieves more accurate reconstructions with lower errors, particularly under severe missing data conditions. Height: black = low, yellow = high. Error: blue = low, red = high.\", \"page\": 8, \"index\": 7, \"width\": 1079, \"height\": 1089}, {\"url\": \"assets/figures/arxiv/2604.02009v1/fig-008.webp\", \"caption\": \"Figure 1: Overview of the proposed height completion framework. Given an RGB image, a monocular depth estimation (MDE) model produces a relative height map, while a self-supervised DINOv3 encoder extracts dense semantic features. Incomplete metric priors are used together with a change mask to identify regions that require completion. A lightweight MLP predicts spatially varying scale and shift parameters that transform relative height predictions into metric heights. During test-time adaptation (TTA), LoRA is applied to the DINOv3 attention layers and the MLP is optimized using the available metric priors, enabling local calibration of the relative height prediction. The final output is a refined metric DSM where missing regions are reconstructed while preserving structural consistency.\", \"page\": 4, \"index\": 8, \"width\": 1081, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2604.02009v1/fig-009.webp\", \"caption\": \"Figure 5: Prior-based DSM completion results: From left to right: RGB image, Marigold-DC, PriorDA, and the proposed framework. Each row corresponds to increasing levels of missing data (25%, 50%, and 75%). For each case, the ground truth (GT) DSM and the degraded prior input are shown on the left. The second row in each block shows the corresponding absolute error maps. The framework Prior2DSM produces sharper building structures and lower reconstruction errors compared to existing prior-based approaches, particularly under severe missing data conditions. Height: black = low, yellow = high. Error: blue = low, red = high.\", \"page\": 9, \"index\": 9, \"width\": 521, \"height\": 785}]"
motivation: 传统DSM补全方法依赖空间插值或特定领域的监督训练，难以处理复杂物体缺失且泛化性较差。
method: 利用自监督ViT特征与单目深度模型，通过测试时自适应（TTA）和LoRA技术将相对深度转换为度量高度。
result: "该方法显著优于传统插值和现有单目深度模型，在RMSE指标上相比线性拟合方法降低了高达46%。"
conclusion: Prior2DSM证明了利用基础模型和测试时自适应技术，无需大规模监督训练即可实现高质量、高保真度的度量级DSM补全。
---

## 摘要
准确的数字地表模型（DSM）对于城市监测、环境分析、基础设施管理和变化检测等许多地理空间应用至关重要。然而，由于采集限制、重建伪影或建成环境的变化，大规模 DSM 经常包含不完整或过时的区域。传统的高度补全方法主要依赖于空间插值，这些方法假设空间连续性，因此在物体缺失时会失效。最近的基于学习的方法提高了重建质量，但通常需要在特定传感器的数据集上进行监督训练，限制了它们在不同领域和传感条件下的泛化能力。我们提出了 Prior2DSM，这是一个无需训练的尺度 DSM 补全框架，通过利用基础模型完全在测试时运行。与以往需要特定任务训练的高度补全方法不同，该方法将来自 DINOv3 的自监督视觉 Transformer（ViT）特征与单目深度基础模型相结合，通过语义特征空间对应关系从不完整的高度先验中传播尺度信息。测试时自适应（TTA）是使用参数高效的低秩自适应（LoRA）结合轻量级多层感知机（MLP）执行的，该感知机预测随空间变化的缩放和平移参数，从而将相对深度估计转换为尺度高度。实验表明，与基于插值的方法、基于先验的缩放高度方法以及最先进的单目深度估计模型相比，该方法具有持续的改进。Prior2DSM 在保持结构保真度的同时降低了重建误差，与单目深度估计的线性拟合相比，RMSE 降低了高达 46%，并进一步实现了 DSM 更新和耦合的 RGB-DSM 生成。

## Abstract
Accurate digital surface models (DSMs) are essential for many geospatial applications, including urban monitoring, environmental analyses, infrastructure management, and change detection. However, large-scale DSMs frequently contain incomplete or outdated regions due to acquisition limitations, reconstruction artifacts, or changes in the built environment. Traditional height completion approaches primarily rely on spatial interpolation or which assume spatial continuity and therefore fail when objects are missing. Recent learning-based approaches improve reconstruction quality but typically require supervised training on sensor-specific datasets, limiting their generalization across domains and sensing conditions. We propose Prior2DSM, a training-free framework for metric DSM completion that operates entirely at test time by leveraging foundation models. Unlike previous height completion approaches that require task-specific training, the proposed method combines self-supervised Vision Transformer (ViT) features from DINOv3 with monocular depth foundation models to propagate metric information from incomplete height priors through semantic feature-space correspondence. Test-time adaptation (TTA) is performed using parameter-efficient low-rank adaptation (LoRA) together with a lightweight multilayer perceptron (MLP), which predicts spatially varying scale and shift parameters to convert relative depth estimates into metric heights. Experiments demonstrate consistent improvements over interpolation based methods, prior-based rescaling height approaches, and state-of-the-art monocular depth estimation models. Prior2DSM reduces reconstruction error while preserving structural fidelity, achieving up to a 46% reduction in RMSE compared to linear fitting of MDE, and further enables DSM updating and coupled RGB-DSM generation.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：Prior2DSM

#### 1. 核心问题与研究动机
*   **核心问题**：如何在高分辨率遥感图像中，利用不完整或过时的数字表面模型（DSM）先验，实现高精度的度量级高度补全（Height Completion）。
*   **研究背景**：
    *   传统的空间插值法（如克里金插值）假设地表连续，在建筑物等物体完全缺失时失效。
    *   现有的深度学习方法通常需要针对特定传感器进行监督训练，泛化性差且存在严重的尺度歧义（Scale Ambiguity）。
    *   遥感图像多为近正射投影，缺乏透视线索，导致单目高度估计（MHE）极度依赖阴影，易受光照影响。

#### 2. 方法论：Prior2DSM 框架
*   **核心思想**：将高度补全建模为一个“特征引导的校正”过程。利用自监督学习（SSL）特征空间中的语义相似性，将已知区域的度量高度信息传播到语义相似但空间分离的缺失区域。
*   **关键技术细节**：
    *   **特征提取**：使用 **DINOv3**（针对卫星图像预训练的 ViT）提取稠密语义特征。通过“步长重叠累加策略”（Strided Overlap）将空间采样密度提升 16 倍，解决 ViT 常见的块状伪影。
    *   **相对高度估计**：利用单目深度基础模型（如 Depth Anything V2, Depth Pro, MoGe-2）生成初始的相对高度图 $H_{rel}$。
    *   **测试时自适应（TTA）**：
        *   **LoRA 适配**：在 DINOv3 的注意力层注入低秩自适应（LoRA）参数，在推理阶段微调以适应当前场景。
        *   **尺度-偏移预测**：设计一个轻量级 MLP，输入 DINOv3 特征，为每个像素预测空间变化的缩放因子 $s$ 和偏移量 $b$。
    *   **转换公式**：最终度量高度 $\hat{z} = s \cdot H_{rel} + b$。在测试时，仅利用已知先验像素作为约束，通过反向传播优化 LoRA 和 MLP 参数。

#### 3. 实验设计
*   **数据集与场景**：
    *   **Denver NAIP**：高分辨率航空影像（30cm 分辨率），包含复杂城市建筑和工业区。
    *   **Fresno WorldView-3 (WV3)**：卫星影像（60cm 分辨率），以低矮住宅区为主。
*   **实验设置**：模拟了 25%、50%、75% 三种不同程度的先验缺失（基于 LULC 掩码随机移除建筑物或植被）。
*   **对比方法（Baselines）**：
    *   传统方法：双线性插值、全局线性缩放、局部加权线性回归（LWLR）。
    *   SOTA 方法：Spatial kNN、RS3DAda（纯单目模型）、Marigold-DC（基于扩散模型）、PriorDA（基于深度补全）。

#### 4. 资源与算力
*   **硬件设备**：实验使用了 **NVIDIA RTX 4000 GPU**。
*   **耗时情况**：
    *   该方法无需离线预训练。
    *   在测试时优化阶段，每平方公里（100 个优化步骤）大约需要 **8.2 分钟**。
    *   对于局部小范围更新（如单个建筑），仅需数秒。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同来源（航空 vs 卫星）、不同地形特征（高层建筑 vs 住宅区）的数据集上进行了验证。
    *   涵盖了三种缺失率梯度，验证了模型对极度稀疏先验的鲁棒性。
    *   **消融实验**：验证了 MDE 基础模型、LoRA 适配以及 MLP 架构的必要性。
    *   **应用实验**：展示了 2008 年旧 DSM 的真实更新场景，以及与 Text2Earth 生成模型结合的 RGB-DSM 同步编辑。
*   **评价**：实验设计客观、公平，对比了从传统到最前沿的多种路径，数据量足以支撑其结论。

#### 6. 主要结论与发现
*   **精度提升**：Prior2DSM 在所有缺失水平下均优于对比方法。在 Denver 数据集上，相比全局线性拟合，RMSE 降低了高达 **46%**。
*   **语义传播的有效性**：证明了 DINOv3 特征能有效识别语义一致的物体，从而实现比空间邻域插值更准确的高度传递。
*   **模型灵活性**：该框架是“模型无关”的，可以适配不同的单目深度后端（其中 MoGe-2 表现最佳）。

#### 7. 优点与亮点
*   **无需训练（Training-free）**：完全在测试时运行，避免了对大规模标注遥感数据集的依赖，解决了领域偏移问题。
*   **结构保真度高**：通过语义特征引导，补全后的建筑物边缘清晰，不会出现插值法常见的平滑模糊现象。
*   **参数高效**：利用 LoRA 技术，仅需极少量的参数更新即可实现场景级的精准校准。

#### 8. 不足与局限
*   **计算开销**：由于每个场景都需要进行 TTA 优化，在大规模区域（如全城级）实时部署时，计算成本高于直接推理模型。
*   **依赖后端质量**：最终精度受限于单目深度基础模型（MDE）捕捉相对几何关系的能力，若 MDE 初始预测存在严重拓扑错误，TTA 难以完全修复。
*   **先验依赖**：虽然支持高缺失率，但仍需一定比例的准确度量先验作为锚点，在完全缺乏先验的“盲测”场景下无法工作。

（完）
